---
name: 리서치
description: >-
  강의 주차의 콘텐츠 리서치와 실습 설계 검증을 수행해 sessions/N주차/자료/에 콘텐츠리서치 결과·
  개념 지식베이스(개념KB)·출처레지스트리·실습안 검증결과·결정요청사항 5파일을 기본 산출하는 팀
  워크플로 스킬이다. 공식 정의만 모으지 않고 실무 사례·대안 비교·한계와 반론까지 관점을 넓혀
  조사하며(G8), 커리큘럼 밖이라도 학생이 배운 직후 바로 물을 인접 개념(전제·후속·혼동)을
  게이트A 승인 아래 얕은 청크로 덧붙인다(G9). 사람 읽기용 종합 요약본과 리뷰 HTML은
  사용자가 요청할 때만 추가로 산출한다(기본
  산출 아님). 독립적인 조사 항목이 여럿이면 순차 대신 병렬로 조사해 소요 시간을 줄인다. 사용자가
  스킬 이름(/리서치 또는 "리서치 스킬")을 직접 지목한 명시 호출 전용이며, "자료 조사해줘" 같은
  보통명사 자연어 요청만으로는 자동 발동하지 않는다. 집필(초안 작성)은 콘텐츠 스킬, 덱 조립은
  vibecoding-deck, 검토는 검토 스킬이 맡으며 이 스킬은 그 영역을 다루지 않는다.
---

# Claude Code 어댑터

1. 이 어댑터가 호출되면 먼저 `../../../skills/리서치/SKILL.md`를 **처음부터 끝까지 완전히 읽는다**.
2. 그 파일이 있는 저장소 루트를 공통 스킬 루트로 삼고, 정본에 적힌 모든 상대 경로(`sessions/`·`skills/README.md` 등)를 저장소 루트 기준으로 해석한다. 어댑터 디렉터리를 기준으로 해석하지 않는다.
3. 정본의 §0 하드 게이트·8단계 워크플로·산출물 규격을 그대로 따라 조사 → 검증 → 산출 → 자기검증 흐름을 수행한다. 이 어댑터는 게이트 조건이나 워크플로 단계를 추가·변경하지 않는다.
4. Windows에서 Python 스크립트가 필요하면 저장소 루트에서 `python <경로> ...` 형식으로 실행한다.
5. 정본 4단계가 말하는 심층 조사 스킬은 Claude Code 번들 스킬이다. 가용성 판단과 (있을 때/없을 때) 폴백 절차는 정본 4단계를 그대로 따르며, 이 어댑터가 별도 규칙을 추가하지 않는다.
6. `../../../skills/리서치/SKILL.md`를 읽을 수 없으면 대체 규칙을 추측하지 말고 공통 정본을 찾을 수 없다고 보고한다.

# 워커 실행 통제 (Claude Code 전용)

정본 4·5단계가 **병렬 조사·검증**을 지시할 때, Claude Code에서는 워커를 아래 형태로만 띄운다.
이 절은 **실행 수단만** 고정한다 — 게이트 조건·워크플로 단계·산출물 규격은 정본 그대로이며 여기서 바꾸지 않는다.
Codex에는 `Explore`가 없으므로 이 절은 적용되지 않고, 정본 4단계의 내장 폴백을 그대로 쓴다.

```
Agent(subagent_type: "Explore", model: "sonnet", prompt: <청크 1개 분량 조사 지시>)
```

## 강제 수준을 혼동하지 마라

| 항목 | 값 | 강제 수준 |
|---|---|---|
| 서브에이전트 타입 | `Explore` | **구조적** — `Write`·`Edit`·`NotebookEdit`·`Agent`를 애초에 보유하지 않는다(중첩 차단 포함) |
| 모델 | `sonnet` | **구조적** — 호출 파라미터. 실측 해석값 `claude-sonnet-5`. `inherit` 금지 |
| 허용 도구 | `WebSearch` `WebFetch` `Read` `Grep` `Glob` + **`ToolSearch`(전제 조건)** | ⚠️ **자연어 지시 + 사후 감사** |
| 불허 도구 | 위 목록 외 **전부** — `Bash`·`PowerShell`·브라우저 자동화·`Skill`·알림/스케줄/메시지 계열 포함 | ⚠️ **자연어 지시 + 사후 감사** |
| 턴 상한 | **없음** | ❌ **강제 수단 없음.** `maxTurns`는 `.claude/agents/` frontmatter 전용이라 이 경로에서는 쓸 수 없다 |

`Explore`는 부모 세션의 도구에서 `Agent`·`Artifact`·`ExitPlanMode`·`Edit`·`Write`·`NotebookEdit`만 뺀 집합을 갖는다.
**`Bash`와 `PowerShell`이 남아 있으므로 `Write` 차단만으로는 파일 쓰기를 막지 못한다.**
근거와 실측 기록은 `_dev/설계기록/Phase2-워커실행통제-검증절차.md`.

⚠️ **`ToolSearch`는 조사 도구가 아니라 전제 조건이다.** 이 환경에서 `WebSearch`·`WebFetch`는
deferred(이름만 노출·스키마 미로드) 상태라 `ToolSearch`로 스키마를 먼저 로드해야 호출된다 —
Phase 7 실측에서 워커 **3/3**이 그랬다. 허용 범위는 **그 둘의 스키마 로드까지**이며,
`ToolSearch`로 다른 도구를 불러 실제로 쓰면 **그 도구 이름으로 감사에 잡힌다.**

## 워커 프롬프트에 반드시 넣을 문구

정본 4단계의 청크 템플릿·레지스트리 행 형식에 **더해** 아래를 그대로 넣는다.

- `조사에는 WebSearch, WebFetch, Read, Grep, Glob만 사용한다. 그 외 어떤 도구도 호출하지 마라 — Bash·PowerShell 포함. (WebSearch·WebFetch가 deferred 상태면 ToolSearch로 그 둘의 스키마만 로드해도 된다 — 다른 도구는 로드하지 마라.)`
- `파일을 생성·수정·삭제하지 마라. 기본 5파일은 리드만 쓴다.`
- `S-### 출처 ID를 스스로 할당하지 마라. URL과 서지 정보만 반환한다.`
- `접근 실패(403·429·JS 렌더링·PDF 초과·DNS 실패)는 숨기지 말고 URL과 사유를 그대로 보고하라.`

## 사후 감사 (생략 불가)

워커 실행이 끝나면 **트랜스크립트로** 확인한다. 워커 자기보고는 정본이 아니다(1.5-B 실측: 자기보고 11 vs 트랜스크립트 12).

```bash
python scripts/analyze_agent_usage.py --tool-audit --session <세션 ID>
```

| 종료코드 | 의미 | 처리 |
|---|---|---|
| `0` | 허용목록 외 도구 0건 · 전 워커 `claude-sonnet-5` | 통과 |
| `3` | 허용목록 외 도구 사용 **또는** 모델 불일치 | 해당 워커 산출물을 **채택하지 않는다.** Opus로 전환됐으면 계획상 **RED** |

감사 결과(에이전트 수·모델·턴·도구 호출)는 정본 8단계 완료 보고의 「병렬/순차 실행 여부」 항목에 함께 적는다.
