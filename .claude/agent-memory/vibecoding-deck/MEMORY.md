# vibecoding-deck 메모리 어댑터

공통 누적 규칙과 하드윈 버그의 정본은 다음 파일 하나다.

`../../../.agents/agent-memory/vibecoding-deck/MEMORY.md`

Claude Code 작업에서는 루트 `CLAUDE.md`가 `AGENTS.md`를 import하고, `AGENTS.md`가 위 정본을 작업 전 필수 읽기로 지정한다. 이 파일에는 규칙을 복제하지 않는다.
