interface:
  display_name: "검토 (산출물 검토 보고)"
  short_description: "강의 제작 파이프라인 산출물(자료·초안·덱) 읽기 전용 검토 보고"
  default_prompt: "Use $검토 to review this week's pipeline outputs (research materials, draft, or deck) and produce a read-only review report under sessions/N주차/."
