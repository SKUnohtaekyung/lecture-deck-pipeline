---
name: 콘텐츠
description: >-
  리서치 산출물을 근거로 슬라이드 글·비유·멘트를 집필해 sessions/N주차/초안.md
  (콘텐츠 초안 4열 표)를 산출하는 팀 워크플로 스킬. 대상은 40~50대 초보자이며,
  여기서 쓰는 문장이 곧 화면 최종 문장이다(하류가 재작성하지 않는다). 사용자가
  스킬 이름(/콘텐츠 또는 "콘텐츠 스킬")을 직접 지목한 명시 호출 전용이다 —
  "초안 써줘"·"글 다듬어줘" 같은 자연어 요청만으로는 자동 발동하지 않는다.
  자료 조사는 리서치 스킬, 덱 조립은 vibecoding-deck 담당 — 이 스킬은 그
  사이에서 리서치 결과를 슬라이드 문장으로 옮기는 역할만 한다.
---

# Codex 어댑터

1. 이 파일을 기준으로 `../../../skills/콘텐츠/SKILL.md`를 열고 처음부터 끝까지 완전히 읽는다.
2. 저장소 루트의 `skills/콘텐츠/SKILL.md`를 이 Skill의 공통 정본으로 적용한다.
3. 정본 본문에 나온 모든 상대경로는 이 어댑터 폴더가 아니라 정본이 있는 저장소 루트를 기준으로 해석한다.
4. 이후 read-path 탐색·게이트A/게이트B 확인·집필·산출·자기검증 흐름은 정본 `skills/콘텐츠/SKILL.md`의 지시를 그대로 따른다. 이 어댑터는 집필 규칙이나 형식 조건을 추가·변경하지 않는다.
