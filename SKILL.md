---
name: create-slides
description: >-
  강의덱·발표자료(예: 바이브코딩 — AI와 함께 코딩)를 1280×720 HTML 웹덱으로 조립하는 자립형 스킬.
  채워진 콘텐츠 초안(교시별 표·본문·강사 멘트)을 읽어(없으면 자료·브리프로 계획을 먼저 초안·확인)
  방향키 발표·PDF·base64 단일파일 덱과 강사용 발표자 노트 HTML을 만든다. 대상은 코딩 경험이
  없는 입문자 중심의 혼합군(연령·경험 수준 다양 · 큰 글씨·고대비·친숙한 비유). 정보 모양에 따라 코드 시각화·GUI·다이어그램·실제 캡처·설명 이미지를 고르고 실습 화면은
  주석 스크린샷으로 안내한다. "강의덱·세션·발표자료·교육 슬라이드 만들어줘", "다음 주차 덱",
  "이 초안/계획서/자료로 덱 조립", "PPT를 HTML로", "슬라이드에 비교·흐름·수치 차트 추가" 같은
  요청에 쓴다. "템플릿"·"스킬"을 명시하지 않아도, 강의·교육 슬라이드를 만들거나 초안·자료를
  웹덱으로 옮기는 맥락이면 이 스킬을 사용할 것.
  레이아웃은 정보 모양을 판단해 고르며 "좌 글/우 시각" 한 구도로 쏠리지 않는다.
---

# create-slides — Core Contract

채워진 **콘텐츠 초안**을 읽어 1280×720 단일 HTML 웹덱을 조립한다(방향키 발표 · `F` 전체화면 · `G` 발표 메뉴 · PDF 출력).
이 문서는 **계약과 게이트 지도만** 담는다. 단계별 상세 규칙은 `references/phases/0N`을 그 단계에서만 읽는다.

## ① 입력 계약

- **세션 폴더 규약**: `courses/<과목>/sessions/N주차/N주차_초안.md`를 콘텐츠 초안으로 읽는다(레거시 무접두어 `초안.md`는 폴백 인식). 산출물도 **같은 폴더**에 쓴다. 새 주차 골격은 `sessions/_template/` 복사, 규약은 `sessions/README.md`.
- **3가지 진입**: ⓐ 채워진 콘텐츠 초안(공식 입력 — 교시별 `#·슬라이드 제목·본문 문구·비유·멘트` 4열 표 + 아이콘 범례) ⓑ 폼 없이 브리프·원자료만 → **슬라이드 계획을 먼저 초안**하고 확인 ⓒ 한 장만·기존 덱에 추가 → 스캐폴딩 생략하고 2~5단계만.
- ⚠️ **ⓑ 경로의 근거 추적 의무(R-TRACE-01 · 2026-07-29 신설).** refs 주석·출처 추적표·게이트A는 전부 **초안 산출물에 붙어 있어서**, 초안 없이 진입하는 ⓑ에서는 근거 추적이 **구조적으로 0**이었다. 2026-07-28 4구간 9장이 실측으로 그 상태였다(refs 0건·집필노트 없음·개념KB `PPT 소재` 63건 미회수). 그래서 ⓑ로 진입하면 **`courses/<과목>/sessions/N주차/자료/N주차_콘텐츠_집필노트.md`를 함께 산출한다** — 출처 추적표(판단 5분류 R-JUDGE-01 — `D2`는 별도 행)와 「재료 처분표」(생략 `S`·필수 진술의 유일한 검사기)가 그 안에 있다. 형식 정본은 `skills/콘텐츠/SKILL.md` 「산출물 규격」이며, **작성 주체만 "create-slides(ⓑ 진입)"로 적는다.**
  - ⓑ에서는 초안이 없으므로 KB 회수 통로(refs 주석)도 없다. **개념KB 「청크 인덱스」 표에서 직접 회수**하고, 회수한 청크를 처분표에 남긴다.
- **조건부 KB 회수**: 초안 행에 `<!-- refs: C-… -->`가 있을 때만, `courses/<과목>/sessions/N주차/자료/N주차_개념KB.md`에서 **해당 청크만 grep 회수**한다. **개념KB를 통째로 Read 하지 않는다**(조립 전에 컨텍스트가 포화된다). 시각 자료 설계에만 쓰고 본문 문구 재작성에는 쓰지 않는다.
- **회수한 청크의 2등급 필드(R-KB-01 · 2026-07-29)**: `PPT 소재:`는 **형태 제안**이라 정보 모양 판단에 따라 자유롭게 대체한다(R-LAYOUT-02가 우선 — 대체 사유만 남긴다). `필수 진술:`은 **내용 보존 의무**라 표현·배치는 바꿔도 **뜻이 화면에 남아야 한다** — 빠지면 그 슬라이드는 미완성이다. 정본은 `skills/리서치/references/chunk-schema.md`.

## ② 출력 계약

- **정본은 조각이다**: 편집본은 `courses/<과목>/sessions/N주차/강의덱.초안/`에 `shell.html`(head·고정 슬라이드·`<!-- ::PARTS:: -->` 마커·JS) + PART마다 `part-NN.html`로 쓴다. kit CSS 링크는 `../../kit/styles/…`(세션 폴더 2단계).
- **생성물**: `python scripts/assemble_deck.py courses/<과목>/sessions/N주차/강의덱.초안` → `강의덱.html`(미리보기 통합본, `--watch`로 자동 재조립).
- **배포본**: `python scripts/build_release.py courses/<과목>/sessions/N주차/강의덱.초안` → `강의덱_배포.html`(단일 자립).
- **발표자 노트**: 초안에 `💬`/`👀`/`🗣`가 하나라도 있으면 `<덱이름>_발표자노트.html`을 같은 폴더에 별도 산출한다.

## ③ 정본 우선순위

```
현재 사용자 지시 → 채워진 콘텐츠 초안 → 원본 자료 → 과목 프로필 → 이 스킬의 판단 규칙
```

- **과목 프로필(`courses/<과목>/profile.md`)** — 강의명·대상·관통 문장·회차 구조·장수/밀도 기준선·표현 취향·브랜드·커리큘럼 정본 경로가 여기 있다. **이 스킬 본문에는 과목 고유 값을 두지 않는다**(스키마 `입력양식/과목프로필템플릿.md` · 검사 `python scripts/verify_subject_isolation.py`). 프로필이 없으면 오류가 아니라, 기준선을 요구하는 게이트가 WARN으로 내려가고 그 사실을 보고한다. **한 과목의 실측치를 다른 과목의 규칙으로 물려주지 않는다.**

- 기존 덱 리비전에서 사용자가 문구·구조·레이아웃 수정을 명시하면 **그 지시가 초안 소유권보다 우선**한다. 단 접근성·1280×720 오버플로·엔진 hook·브랜드 계약은 언제나 지킨다.
- **디자인 정본 4파일**: `kit/guide/디자인시스템.md`(R-COLOR-01~05·R-EMPH-01·R-TERM-01) · `kit/guide/토큰-치트시트.md`(R-TYPE-01~03) · `kit/guide/정보모양-taxonomy.md` · `kit/guide/카탈로그-규격.md`(R-LAYOUT-03).
- 교육 원칙은 `kit/guide/교육원칙-요약.md`를 읽는다. **커리큘럼 정본의 경로는 과목 프로필 §1이 가지고 있다** — 충돌하면 언제나 그 정본이 이긴다(정본이 바뀌면 요약본도 함께 고친다). 정본은 배포에서 제외되므로 킷 자립성을 위해 read-path 필수로 걸지 않는다. ⚠️ `kit/guide/교육원칙-요약.md`는 **문서 전체가 과목 종속 산출물**이라 격리 대상이 아니라 재분류 대상이다(미결 — 프로필 §8 참고).

## ③-B 통합 실행과 승인 게이트 (R-EXEC-01 · 2026-07-29)

집필과 조립은 **하나의 실행 단위**다. 정보 모양·구조 재구성은 화면을 알아야 판단할 수 있고, 문장은 그 구조에 맞물려 결정된다 — 둘을 갈라 왕복하면 판단이 끊긴다. 스킬 파일은 `skills/콘텐츠/SKILL.md`와 이 문서 둘 다 남는다(**사람이 초안을 쓰는 경로를 죽이지 않는다**). 달라지는 것은 실행 방식이지 계약이 아니다.

### 소유권은 없애지 않는다 — 작성 주체로 분기한다 (R-OWN-03)

판정 근거는 **초안 메타 헤더의 작성 주체 필드**다(`> 작성:` 또는 `> 작성 주체:` — 둘 다 인식).

| 작성 주체 | 문장 수정 권한 | 조건 |
|---|---|---|
| **사람** (또는 **필드 없음**) | **불가** — R-OWN-01 그대로 | 고칠 곳은 고치지 말고 **보고**한다(R-OWN-02) |
| **스킬**(콘텐츠 스킬 · create-slides ⓑ 진입) | **같은 실행 안에서만 가능** | 수정할 때마다 **변경 사유를 판단 기록에 남긴다**(R-JUDGE-01) |

- **필드가 없으면 사람으로 간주한다.** 보수적 기본값이다 — 잘못 추정해 남의 문장을 고치는 쪽이 되돌리기 어렵다.
- **"같은 실행 안에서만"이 핵심이다.** 다음 세션이 지난 실행의 산출물을 자기 것으로 여겨 고치면 소유권 경계가 사실상 사라진다. 실행이 끝나면 그 문장은 확정된다.

### 승인 게이트 2개 (고정)

| | 시점 | 제출물 | 통과 |
|---|---|---|---|
| **G1** | **조립 전** | 골격(장수·핵심 한마디) + **정보 모양** + **시각 후보**(레이아웃·element) + **슬라이드 결정표** | 아래 분기 |
| **G2** | **조립 후** | 렌더 결과(헤드리스 Chrome 실측 + 캡처) | 사용자 확인 |

**G1의 제출은 언제나 필수다. 대기 여부만 갈린다** — 이것이 3원 충돌의 해소다.

- `references/콘텐츠초안-입력형식.md` §7 "확인 없이 즉흥 조립하지 않는다"(강) → **결정표 작성·제출은 무조건**으로 살린다.
- `references/phases/03-레이아웃선택.md` R-NOTE-01a "자율 실행 권한이 명확하면 재확인하지 않는다"(약) → **대기하지 않고 진행**으로 살린다.
- 즉 **자율 실행 권한이 명확하면 제출 후 바로 진행**하고, 권한이 없거나 결과를 바꾸는 모호한 선택이 있으면 **답을 기다린다.** 어느 쪽이든 결정표는 남는다.

## ④ 절대 금지 10항

**충돌 시 우선순위**: 겹침·화면 이탈·잘림 0이 이미지 확대·장식·밀도·레이아웃 취향보다 먼저다. 1280×720 안에서 제목·본문·캡션·이미지·페이지 번호가 모두 보여야 한다. 정확한 콘텐츠와 학생이 직접 확인할 수 있는 기준이 시각적 화려함보다 먼저다.

1. **raw `#hex`·navy·그라데이션 금지** — 토큰만 쓴다(R-COLOR-01).
2. **민트·코랄 fill 위 흰 글자 금지** — `--on-mint`/`--on-coral`. 예외는 `.hl-mint-mark` 하나뿐(R-COLOR-02).
3. **흰색-온-흰색 표면 금지** — 흰 fill + `--line` 단독 보더는 verify FAIL(R-COLOR-04).
4. **본문 22px 미만 금지** — 예외는 주차 계약에 등재해야 유효(R-TYPE-01).
5. **콘텐츠 하단 666px 침범 금지** — 폰트를 줄여 해결하지 않는다(R-TYPE-03).
6. **초안 제목·본문 임의 재작성 금지** — 새 덱 조립에서 제목·본문은 콘텐츠팀 소유다. 한 장은 정보 묶음을 담고, 수용량은 자동 분할 명령이 아니라 오버플로 경고 기준이다(R-OWN-01). 비유-정의 순서 같은 **콘텐츠 계층 문제는 고치지 말고 보고한다**(R-OWN-02).
7. **아이콘 누출 금지** — `💬`·`👀`·`🗣`은 학생 덱에서 제거하고 발표자 노트에만 둔다. 학생 덱의 `.hint-reveal`·`강사 힌트`·`막힐 때만 보기`·`완성 예시 보기`와 이모지 문자는 verify FAIL(R-ICON-01).
8. **생성물 직접 편집 금지** — `강의덱.html`·`강의덱_배포.html`은 생성물이다. 조각을 고쳐 재조립·재빌드한다.
9. **`overflow:hidden`으로 은폐 금지** — 잘림·초과를 가려 통과시키지 않는다. `getBoundingClientRect()`로 실측한다. 이미지 확대와 겹침이 충돌하면 **언제나 겹침 0을 택한다**(R-IMG-04).
10. **정적 PASS만으로 완료 선언 금지** — 브라우저 전수 측정 전에는 "완성"이 아니다(R-VERIFY-01).
    - 레이아웃도 같은 원칙이다: **모든 레이아웃·element는 동급이고 정보 모양이 고른다**(no-default). **"좌 글/우 시각"은 기본값이 아니다.** 직전 콘텐츠 슬라이드와 같은 구도 패밀리를 피하고, **split(좌우분할)은 희소하게 쓰며 연속 금지**다(R-LAYOUT-02).

## ⑤ 9단계 게이트 지도

각 단계에 진입할 때 해당 `references/phases/` 문서를 읽는다. 전부 미리 읽지 않는다.

| 단계 | 하는 일 | 읽을 문서 | 통과 조건 |
|---|---|---|---|
| 1 입력 분석 | 3진입 판별·아이콘 라우팅·PART 매핑·조건부 KB 회수 | [phases/01-입력분석.md](references/phases/01-입력분석.md) · `references/콘텐츠초안-입력형식.md` | 슬라이드 목록과 소유권 경계가 확정됨 |
| 2 슬라이드 맵 | **정보 모양 분류(핵심 판단 게이트)** — 12 정준 모양 | [phases/02-슬라이드맵.md](references/phases/02-슬라이드맵.md) · `kit/guide/정보모양-taxonomy.md` | 모든 슬라이드에 정보 모양이 붙음 |
| 3 레이아웃 선택 | 역인덱스 후보 → **레이아웃과 element를 따로** 선택 → 결정표 | [phases/03-레이아웃선택.md](references/phases/03-레이아웃선택.md) · `kit/layouts/by-shape.md` | 결정표 완성 · 직전과 같은 패밀리 아님 · **G1 제출 완료**(③-B — 제출은 무조건, 대기는 자율 권한에 따라) |
| 4 조립 | 스타터 기반 마크업·엔진 hook·텍스트/박스 규칙 | [phases/04-조립.md](references/phases/04-조립.md) · `references/조립-리듬-불변요소.md` | 파트 수 = divider 수 · 안전영역 준수 |
| 5 시각화 | 카탈로그 코어·변형·독립 구도·환류·D3 정책 | [phases/05-시각화.md](references/phases/05-시각화.md) · `kit/charts/by-shape.md` | 정보 모양에 맞는 수단 선택됨 |
| 6 이미지 | 4상태 판정 → 재사용 검색 → 모드 → 생성/프롬프트 | [phases/06-이미지.md](references/phases/06-이미지.md) · `references/이미지-디렉션-프롬프트.md` | 판정·Asset ID가 `자료/이미지-에셋.json`에 기록됨 |
| 7 발표자 노트 | 멘트 라우팅·번호 정합 | [phases/07-발표자노트.md](references/phases/07-발표자노트.md) | `pn-no`가 덱 페이지 번호와 일치 |
| 8 검증 | 정적 스크립트 → **브라우저 전수 측정** | [phases/08-검증.md](references/phases/08-검증.md) | ⑥의 게이트 전부 통과 |
| 9 배포 | 단일 자립 파일 빌드 | [phases/09-배포.md](references/phases/09-배포.md) | fail-closed 게이트 통과 |
| 10 발표본 **(선택 · 명시 요청 전용)** | 완성된 배포본에 발표 런타임 주입 → `강의덱_발표.html` | [phases/10-발표자모드.md](references/phases/10-발표자모드.md) | 주입·전용 검증기 종료코드 0 · 슬라이드 불변 |

> **10단계는 자동으로 실행하지 않는다.** 사용자가 발표자 모드·발표본을 **명시적으로 요청했을 때만** 수행한다. 일반 조립 경로(`assemble_deck` · `build_release` · `inline_deck`)에는 발표자 코드 경로가 아예 없어, "자동 삽입 없음"이 플래그 기본값이 아니라 **구조로** 보장된다.

**이미지 판정 게이트(순서 고정)**: `정보 모양·시각 element 결정 → 이미지 목적 판정 → 중앙 레지스트리 재사용 검색 → generate/transform 수 산출 → 필요할 때만 IMAGE_MODE 확인`.
판정은 `NO_IMAGE | IMAGE_EXPLANATORY | IMAGE_MNEMONIC | IMAGE_DECORATIVE_OPTIONAL` 네 가지다.
`IMAGE_MODE = generate_now | prompt_only` 두 분기가 있으며, **신규 생성·변형이 0장이거나 자율 실행 권한이 명확하면 묻지 않는다.** 모드에 따라 산출물이 달라질 때만 한 번 묻고, 답 전에는 그 이미지 작업만 보류한다.
세부 계약(의미 브리프·프롬프트·재사용·투명 PNG QA)은 [`references/이미지-디렉션-프롬프트.md`](references/이미지-디렉션-프롬프트.md)가 정본이며, 이 문서나 플랫폼 어댑터에 복제하지 않는다.

**파트 계약**: 모든 파트는 `part-divider` 슬라이드로 시작한다(**PART 1 포함**). **파트 수 = divider 수.** 스타터 JS가 divider의 DOM 순서를 읽어 진행 도트·`PART n / N`·지오메트릭 큐브를 자동 생성하므로, 파트 SVG·도트·분모를 조각 파일에 **하드코딩하지 않는다**(R-PART-01·02).

## ⑥ 필수 검증 게이트

| 게이트 | 명령·수단 | 성격 |
|---|---|---|
| 정적 검증 | `python scripts/verify_deck.py <덱>.html --parts N` | FAIL 0 필수 |
| 주차 구조 계약 | `courses/<과목>/sessions/N주차/deck.contract.json` → `sessions/_contracts/` → 없으면 WARN | 계약 부재는 WARN(중단 아님) |
| 발표자 노트 정합 | `python scripts/verify_notes.py <덱> <노트>` | 불일치 목록 확인 |
| 이미지 배선 | manifest `ready` + 설명·기억 판정 자산이 덱에 배선됐는가 | FAIL(계약 등재분은 WARN) |
| **브라우저 실측** | 로컬 http 서버 + `scripts/measure_render.js` — 전 슬라이드 순회 | **정적 PASS의 대체가 아니다** |
| 킷 계약 | `python scripts/verify_kit.py` — 카탈로그·`patterns.css`를 건드렸으면 | FAIL 0 |
| 배포 자립성 | `build_release.py`의 `verify_distributable` | **fail-closed** — 외부 `href=http` 0 · 모든 `src`/`url()` `data:` · 임베드 `@font-face` 존재 · unresolved 설명·기억 슬롯 0 |
| 내용 밀도(WARN) | 초안: `verify_draft_quality.py <주차>` · 덱: `verify_deck_quality.py <덱>.html` | 저밀도 설명 슬라이드·근-빈 컨테이너·부록 강등 탐지(기본 WARN·exit 0, `--strict`로 FAIL) |

**밀도 하한은 슬라이드당 정보 단위 3~6개다(R-DENS-01, 정본 `skills/콘텐츠/SKILL.md` §0-6·상세 `references/phases/04-조립.md`)** — 장수를 시간예산으로 역산하지 않는다(2주차 저밀도 사고 원인, `.agents/agent-memory/create-slides/MEMORY.md` 참고).

## ⑦ 중단 조건

- 주차 구조 계약 파일이 없다 → **WARN으로 보고**하고 구조 검증 없이 진행(중단 아님).
- 검증 FAIL이 재작업 후에도 남는다 → 진행하지 말고 원인과 선택지를 보고한다.
- 초안 문장과 회수한 KB 청크가 충돌한다 → **덱이 고치지 말고 보고**한다(소유권 경계).
- 이미지 생성이 교정 1회·알파 재시도 1회를 넘긴다 → 설명·기억 이미지는 중단·보고, 장식 이미지는 반려 기록 후 생략.
- 사용자 지시와 엔진·브랜드·접근성 계약이 충돌한다 → 계약을 지키고 충돌 사실을 보고한다.

---

**킷 구조(자립 — 외부 import 0)**

```
kit/guide/       정보모양-taxonomy.md · 카탈로그-규격.md · 토큰-치트시트.md · 디자인시스템.md · 교육원칙-요약.md
kit/layouts/     by-shape.md(역인덱스) · families/*.md · catalog.html(코드 코어 12)
kit/charts/      by-shape.md · charts-*.md · diagrams-*.md · elements-code.md(총 23) · catalog.html(코어 8)
kit/styles/      deck.css · legibility.css · patterns.css      kit/images/ · kit/screenshots/
kit/starter/     deck-template.html · presenter-notes-template.html · logo.svg
references/      phases/01~09 · 조립-리듬-불변요소.md · 이미지-디렉션-프롬프트.md
                 이미지-스크린샷-배포.md · 콘텐츠초안-입력형식.md
scripts/         verify_deck.py · verify_notes.py · verify_kit.py · assemble_deck.py
                 build_release.py · inline_deck.py · verify_distributable.py · font_embed.py
                 report_draft_sync.py · measure_render.js        evals/ evals.json
```
