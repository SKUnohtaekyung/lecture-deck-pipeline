# 토큰 사용 감사 — 2026-08-06

> 대상: 이 저장소의 Claude Code 세션 전수(`~/.claude/projects/C--Users-miso-Desktop-template/`)
> 방법: 읽기 전용. 원시 트랜스크립트(JSONL)를 직접 집계. 분석 스크립트는 `tmp/token_spike.py`(임시).
> **전제: 현재의 품질·조건을 낮추는 절감안은 채택하지 않는다.** 아래 제안은 전부 「낭비 제거」이지 「깎기」가 아니다.

---

## 0. 먼저 — 계측기가 2.05배 과대 보고하고 있다

**모든 판단의 전제가 틀려 있었다.** `scripts/analyze_agent_usage.py`는 「1턴 = `message.usage`를 가진 assistant 레코드 1건」으로 세는데, 트랜스크립트는 **응답 1건을 콘텐츠 블록(thinking·text·tool_use)마다 별도 레코드로 쓰고 모든 레코드에 같은 `usage`를 복사한다.** 그래서 같은 호출이 2~4번 계수된다.

| 지표 | 계측기 보고 | 실제(`message.id` 중복제거) | 배수 |
|---|---:|---:|---:|
| 모델 호출 수 | 330 | **159** | 2.08× |
| 총 처리 토큰 | 102.3M | **50.0M** | 2.05× |

- 최대 4회까지 반복된 `message.id`가 있었다(중복 id 100개).
- **`.agents/agent-memory/…/MEMORY.md`와 `skills/하네스/SKILL.md`에 인용된 과거 실측치(2주차 196만 토큰 · 메인 83.1M · 턴당 평균 257K)도 같은 배수로 부풀려져 있다.** 비율(메인 대 워커)은 양쪽이 같은 배수로 부풀려졌다면 살아남지만, **절대값과 「턴당 평균」은 신뢰할 수 없다.**
- 고칠 지점: 집계 전에 `message.id`로 중복을 제거한다. 짝 파일로 `tests/test_analyze_agent_usage.py`를 함께 고쳐야 한다(안티패턴 #2).

---

## 1. 판정 기준 (이 감사에서 정한 것)

| ID | 기준 | 임계 | 근거 |
|---|---|---|---|
| **T1** | **단발 스파이크** — 한 호출의 캐시 재생성 | > 50k | 재생성은 읽기의 **12.5배** 단가다. 5만이면 읽기 62.5만 상당 |
| **T2** | **창 성장** — 평균 컨텍스트 | > 200k | `skills/하네스/SKILL.md` §4가 이미 「150k 넘으면 세션 분리」를 규정한다. 200k는 그 여유분 |
| **T3** | **무도구 턴 비중** — 도구를 부르지 않은 모델 호출의 비용 비중 | > 50% | 도구 없는 턴도 창 전체를 다시 청구한다. 이 비중이 높으면 「대화가 비싼」 상태 |
| **T4** | **캐시 만료** — 직전 호출과의 간격 | > 1시간 | 이 세션의 프롬프트 캐시 TTL이 1시간이다. 넘으면 창 전량 재생성 |
| **T5** | **워커 대 메인 비중** | 메인 > 90% | 하네스가 컨텍스트 보존 도구로 작동하지 않는다는 신호 |
| **T6** | **계측 신뢰** — 중복제거 전후 배수 | > 1.1 | 넘으면 그 계측 결과로 판단하지 않는다 |

---

## 2. 실측 — 어디서 나가는가

### 2.1 세션 비교 (계측기 원값 · 상대 비교용)

| 세션 | 레코드 | 총 처리 | 캐시읽기 비중 | 최종 창 |
|---|---:|---:|---:|---:|
| `760595f4` | 1,411 | **765.6M** | 96.3% | **966k** |
| `3451c918` | 875 | 408.1M | 96.7% | 299k |
| `71024492` | 782 | 287.5M | 96.5% | 521k |
| 이번 세션 | 330 | 102.3M | 97.7% | 466k |

**캐시읽기 96~98%는 전 세션 공통이다.** 이상 현상이 아니라 구조다.

### 2.2 이번 세션 비용 분해 (중복제거 · 입력단가=1 기준 · 출력 5배)

| 항목 | 비용 비중 | 판정 |
|---|---:|---|
| **캐시 읽기 = 창 재청구** | **70.8%** | ← 지배적 |
| 출력 | 17.1% | |
| 캐시 생성 | 12.0% | |
| 신규 입력 | 0.0% | |

### 2.3 T3 — 무도구 턴이 비용의 2/3다 (가장 큰 발견)

| 구분 | 호출 수 | 비용 비중 | 평균 창 |
|---|---:|---:|---:|
| 도구 호출 턴 | 54 | 32.3% | 338k |
| **텍스트만 턴** | **106** | **67.7%** | 295k |

**T3 위반(67.7% > 50%).** 일을 하는 턴보다 말을 하는 턴이 2배 많고 비용도 2배다. 도구를 부르지 않아도 창 전체가 다시 청구되기 때문이다.

### 2.4 T2 — 창이 4.5배로 자랐다

```
호출   1- 20     102k  ########
호출  41- 60     251k  ####################
호출  81-100     340k  ############################
호출 121-140     430k  ###################################
호출 141-160     462k  ######################################
```

**T2 위반(평균 309k > 200k).** 하네스 규정(150k 초과 시 세션 분리)을 **3배 초과**한 채로 끝까지 갔다.

### 2.5 T1·T4 — 단발 스파이크는 하나뿐이었고, 원인은 시간이다

| 호출 | 캐시 재생성 | 직전 간격 | 원인 |
|---:|---:|---:|---|
| 121 | **237,324** | **53,286초 (14.8시간)** | 1시간 캐시 TTL 만료 → 창 전량 재생성 |

**T1·T4 동시 위반.** 다만 비용 환산으로는 전체의 약 3%다 — **「갑자기 튀는 곳」은 실재하지만 지배적이지 않다.**

### 2.6 창을 채운 것 (문자 기준)

| 출처 | 문자 | 비중 |
|---|---:|---:|
| `Bash` tool_result | 132,221 | **35.5%** |
| 내 사고(thinking) | 88,193 | 23.7% |
| `Read` tool_result | 63,345 | 17.0% |
| 사용자·시스템 텍스트 | 42,893 | 11.5% |
| 내 텍스트 응답 | 25,993 | 7.0% |
| `Agent` 반환(워커 보고 11건) | 11,869 | **3.2%** |

### 2.7 T5 — 워커는 문제가 아니다

메인 **50.0M** 대 워커 합계 **약 0.88M** → 메인 **98.3%**.
**T5 위반이지만 방향이 중요하다: 워커를 덜 써서가 아니라 메인이 뚱뚱해서다.** 워커 11명 전체가 메인의 1.8%밖에 안 쓴다. **더 위임했어야 한다.**

### 2.8 한국어의 토큰 비용

창 증가분(약 406k 토큰) ÷ 내용 문자(372k) = **문자당 약 1.09 토큰**. 영어는 통상 0.25다 → **같은 내용에 4~5배**. 이 저장소는 문서·사고·대화가 전부 한국어라 이 배수가 전 항목에 곱해진다.

---

## 3. 원인 정리

| # | 원인 | 근거 | 성격 |
|---|---|---|---|
| **A** | **창이 무한 성장하고, 매 호출이 창 전체를 재청구한다** | 비용의 70.8% · 창 102k→462k | 구조 |
| **B** | **말하는 턴이 일하는 턴보다 2배 많다** | 106 대 54 · 비용 67.7% | 습관 |
| **C** | **Bash 출력을 통째로 창에 넣었다** | 내용의 35.5% | 습관 |
| **D** | **세션을 분리하지 않았다** | 하네스 규정 150k의 3배까지 방치 | 규정 미준수 |
| **E** | **장시간 중단 후 재개** | 14.8시간 갭 → 237k 재생성 | 운영 |
| **F** | **계측기가 2배 과대 보고해 문제를 잘못 보게 했다** | 중복제거 전후 2.05× | 계측 결함 |

---

## 4. 해결 — 품질을 낮추지 않는 것만

> **채택 기준: 「하는 일의 양·검증의 깊이·산출물의 질」을 건드리지 않을 것.** 아래는 전부 같은 일을 더 적은 토큰으로 하는 방법이다.

### 4.1 즉시 적용 가능 (품질 영향 0)

| # | 조치 | 절감 근거 | 품질 영향 |
|---|---|---|---|
| **S1** | **Bash 출력을 창에 넣지 않는다** — 종료코드만 필요하면 `>/dev/null 2>&1; echo $?`, 목록은 `\| tail -N`, 게이트 전문은 `\| tail -3` | 내용의 35.5%가 Bash 출력. 이번 세션만 해도 pre-commit 게이트 전문이 6회(약 7천 자) 들어왔는데 **필요한 정보는 종료코드 한 줄**이었다 | **없음** — 판정에 쓰는 신호는 그대로 |
| **S2** | **파일 읽기를 워커에 위임한다** — 메인이 직접 `Read`하면 이후 모든 호출에 실린다 | `Read` 결과가 내용의 17%. 워커 11명 전체가 메인의 1.8%다 | **없음** — 하네스 §6-1이 이미 규정한 것 |
| **S3** | **상태 보고 턴을 없앤다** — 「워커 대기 중」류는 결정을 담지 않는데 창 전체를 재청구한다. 다음 도구 호출과 같은 턴에 합치거나 생략 | 무도구 턴이 비용의 67.7% | **없음** — 사용자가 받는 결론은 동일 |
| **S4** | **계측기의 중복 계수를 고친다** — 집계 전 `message.id` 중복제거. 짝으로 `tests/test_analyze_agent_usage.py` 갱신 | 2.05배 과대 → 이후 모든 판단의 전제 | **개선** |

### 4.2 운영 규칙 (규정을 지키면 되는 것)

| # | 조치 | 절감 |
|---|---|---|
| **S5** | **창 250k에서 세션을 분리한다.** 하네스 §4는 이미 150k를 규정했다 — 지키지 않았을 뿐이다. 인계는 `MEMORY.md` `## 미해결` + `plans/<주제>/`로 이미 갖춰져 있다 | **−19.6%**(반사실 계산: 창이 250k에서 멈췄다면) |
| **S6** | **하루 이상 쉴 거면 새 세션으로 시작한다.** 재개하면 캐시 TTL 만료로 창 전량이 12.5배 단가로 재생성된다 | 단발 237k 재생성 회피 |
| **S7** | **`--enforce`가 아니라 관측으로 먼저 재듯이, 큰 팬아웃 전에 창 크기를 확인한다** | 예방 |

**S3+S5만 적용해도 이번 세션 기준 −33.9%~−45%다**(반사실: 텍스트만 턴 절반 −33.9%, 창 250k 캡 −19.6%, 둘은 부분 중복).

### 4.3 검토는 하되 지금 채택하지 않는 것

| # | 후보 | 왜 보류하는가 |
|---|---|---|
| N1 | 내부 사고(thinking)를 영어로 | 토큰은 1/4로 줄지만, 한국어 자료·한국어 산출물에 대한 판단 품질이 유지된다는 근거가 없다. **품질 무손실이 증명되지 않으면 채택하지 않는다** |
| N2 | 사고 깊이(effort) 하향 | **품질 저하 직결. 절대 채택하지 않는다** |
| N3 | 검증 단계 축소 | 같은 이유로 기각. 이 프로젝트의 사고 이력 대부분이 「검증을 건너뛴 대가」다 |
| N4 | 워커 수 축소 | 워커는 메인의 1.8%다. 줄일 대상이 아니라 **늘릴 대상** |

---

## 5. 실행 결과 (2026-08-06 · S1~S4 완료)

| 조치 | 산출물 | 검증 |
|---|---|---|
| **S4** 계측기 중복 계수 수정 | `scripts/analyze_agent_usage.py`에 `message.id` 중복제거 + 이상 징후 절에 제외 건수 보고 · 회귀 테스트 4개(`TestDuplicateUsageRecords`) + fixture `tests/fixtures/synthetic/sess-dup.jsonl` | 독립 계산과 **완전 일치**(호출 180 / 60,625,138 토큰 / 중복 185건) · **수정 전 코드에서 4개 전부 FAIL** 실증 · unittest 155→**159 OK** |
| **S1** Bash 출력 규율 | `scripts/audit_context_budget.py` 신설(T1~T4·TS·T6 판정기) + `AGENTS.md` 등재 | 종료코드 0/1/2 확인 · 현 세션에서 `TS`가 Bash 36.3% 검출 · 표준 라이브러리만 |
| **S2** 읽기 위임 규율 | `skills/하네스/SKILL.md` §6-1에 **300줄 판정선** + `AGENTS.md` 요약·포인터 | 두 문서 판정선 동일 · 충돌 0 · `test_rule_pointers` OK |
| **S3** 상태 턴 규율 | `AGENTS.md`에 「결정을 담지 않는 턴 금지」 + 캐시 TTL·창 250k 분리 | `T3`가 61.0% 검출 |

**검증 중 폐기한 기준 1건**: 초안의 `T6 = 원시 로그 중복 배수 > 1.1`은 **항상 발화하는 오탐**이었다(로그가 응답 1건을 블록마다 반복 기록하는 것은 정상이고, 감사기·계측기 모두 중복제거를 하므로 배수는 늘 2 안팎). T6을 **「계측기가 중복제거를 하는가」의 정적 확인**으로 바꾸고, 계측기를 되돌리면 T6이 뜨고 복원하면 사라지는 것을 실증했다. 대신 S1을 잴 `TS`(단일 출처 30% 초과)를 신설했다.

**비용**: 규칙 4개로 `AGENTS.md`가 +1,542B(13,491 → 15,033). Codex 결합 예산 56.1% → **60.8%**(한도 32KiB). 초안은 63.2%까지 갔으나 근거·실측치를 이 문서로 옮겨 압축했다 — **컨텍스트 절약 규칙이 상시 컨텍스트를 먹는 자기모순을 피하기 위해서다.**

**미적용**: S5(창 250k 세션 분리)·S6(긴 중단 뒤 새 세션)은 규칙으로 등재했으나 **기계 강제 수단이 없다.** 사람과 에이전트의 규율에 의존한다 — `audit_context_budget.py`가 사후에 위반을 드러낼 뿐이다.

---

## 6. 다음 행동

1. **S4(계측기 수정)를 먼저 한다.** 계측이 2배 틀린 상태로는 다른 조치의 효과를 잴 수 없다.
2. S1~S3을 작업 습관으로 굳힌다 — `AGENTS.md` 「공통 작업 방식」에 1줄씩 등재해야 다음 세션이 상속한다(「고쳤다」로 끝내면 이 세션에만 남는다).
3. S5를 하네스 정본에 **측정 가능한 형태로** 다시 쓴다(현재 「150k 넘으면 분리」는 지켜지지 않았다 — 강제 수단이 없기 때문이다).
4. 과거 문서에 인용된 토큰 수치에 **「계측기 2.05× 과대 · 재산정 필요」** 주석을 단다.
