# HIGH 빈칸 6종 — 카탈로그 스펙 초안 (author→auditor→revise 산출)

> 출처: 워크플로 `high-visual-specs`(18에이전트·1.35M토큰·0에러). 각 스펙은 적대적 감사(auditor)를 거쳐 수정됨.
> 상태: **보존용 초안.** E-code만 라이브 통합 완료(`kit/charts/elements-code.md`); 나머지 5종은 통합 대기(배치·균형 결정 후).
> ⚠️ 각 spec_markdown 안의 `balance_action`(예: L-td-bento 은퇴 제안)은 **미실행** — P3 접기 결정 사항이므로 그대로 적용 금지.
> 작성 2026-07-15.

## 요약 표

| id | 이름 | 종류 | 패밀리 | 구도 | 통합 |
|---|---|---|---|---|---|
| `E-code` | 주석 코드 블록 / Annotated Code Block | element | — | — | ✅ 라이브 |
| `D-gantt` | 비례 시간축·간트 / Proportional Timeline (Gantt) | element | — | — | ⏳ 대기 |
| `L-td-glossary` | 용어 정의 리스트 / Term Glossary List | layout | top-down | band-grid | ⏳ 대기 |
| `L-vf-case-acts` | 사례 3막 카드(상황·행동·결과) / Case Story in Acts | layout | vertical-flow | flow-vertical | ⏳ 대기 |
| `L-td-claim-rationale` | 주장·근거 밴드 / Claim–Rationale Band | layout | top-down | band-grid | ⏳ 대기 |
| `L-gm-pitfalls` | 함정·주의 세트 / Pitfall & Caution Set | layout | grid-mosaic | band-grid | ⏳ 대기 |

---

## E-code — 주석 코드 블록 / Annotated Code Block

- **종류**: element · **home_file(제안)**: `kit/charts/elements-code.md`
- **info_shapes**: [concept] · **composition_family**: — · **composition_shape**: —

### 스펙 마크다운

### E-code — 주석 코드 블록 / Annotated Code Block

- **id**: `E-code`
- **name**: 주석 코드 블록 / Annotated Code Block
- **kind**: `code`  *(차트/다이어그램이 아닌 신설 element 종. `chart|diagram` 스키마의 3번째 종으로, 소스코드 전시물을 가리킨다.)*
- **info_shapes**: `[concept]`
  - *(rationale: 정보모양-taxonomy 12개 중 이 element가 서비스하는 모양은 `concept`("A란 무엇인가"를 코드 한 조각 + 줄별 설명으로 전달). `screen-operation`의 **주석 문법**(번호·테두리로 특정 지점 지시)을 빌려오지만, `screen-operation`은 "실제 UI 화면 단계(버튼→메뉴→클릭)"라 스크린샷이 대상이고 코드가 아니다 — 그래서 인접일 뿐 값은 `concept`. 코드로 **절차**를 보이면 보조로 `flow`가 겹칠 수 있으나, "이 줄이 뭘 하나"의 주 모양은 개념 설명이다.)*
- **data_shape**: 짧은 소스코드 한 조각(함수·설정·프롬프트 응답·스니펫) + **특정 줄 몇 개**를 지목해 설명. 줄들 사이에 순서·인과가 핵심이 아니라 "이 줄의 역할"이 초점일 때. 판별: 순서 절차가 핵심이면 flow-v(단계 스택), UI 화면 조작이면 screen-operation(주석 스크린샷), 코드를 배경 분위기로만 깔면 full-bleed 코드(주석 없음), 값 비교면 차트.
- **when_to_use**: 실제 코드 한 조각을 띄우고 **특정 줄을 짚어** "이 줄이 무엇을 하는지" 초보에게 풀이할 때 — 바이브코딩 프롬프트가 만든 코드 읽기, 설정 파일 키 설명, 함수의 핵심 라인 해설. (소스: Material for MkDocs — `hl_lines`로 "Specific lines can be highlighted", code annotations는 "a comfortable and friendly way to attach arbitrary content to specific sections of code blocks by adding numeric markers".)
- **when_to_avoid**: 코드가 ~12줄을 넘겨 한 화면에 안 들어갈 때(→ 발췌·다중 슬라이드), 특정 줄을 지목하지 않고 통째로만 보일 때(→ 주석 없는 코드 패널/full-bleed), 대상이 코드가 아니라 UI 화면 단계일 때(→ `screen-operation` 주석 스크린샷), 터미널 명령·출력 로그일 때(→ 터미널 블록), 줄별 설명이 초보에게 과부하면(→ 개념 흐름 flow-v로 추상화). **어떤 element도 기본이 아니다** — data_shape가 "코드 + 지목할 줄"일 때만 고른다.
- **capacity**: 코드 **6~12줄**(줄당 ≤~50자, 초과 시 줄바꿈 위험) + **강조 줄 ≤4** + **여백 콜아웃 ≤4**(각 ≤2줄·≤18어, **풀이 텍스트 ≥17px** — 다이어그램 라벨/캡션 하한 준수). 선택적 상단 `.vc-bar`(파일명/언어) 1줄(≥17px). 강조가 5줄↑이면 "강조가 곧 평문"이 되어 무의미 → 캡. (산식: 코드 mono 20px×line-height 1.6 ≈ 32px/줄 → 세로예산 548px에서 패널 여백·`.vc-bar` 제하면 물리적으로 ≤12줄.)
- **content_slots**: `filename_bar`(선택 · 파일명/언어) · `code_lines[6..12]`(`.vc-line`, 라인번호 gutter 포함) · `highlight_lines[≤4]`(`.is-hl` 지목 줄) · `callouts[≤4]`(`.cn` 블루 번호 + 풀이 ≤2줄) · `lead_lines`(강조 줄↔콜아웃 잇는 블루 리더선)
- **element_vs_slide**: **(필수)** **fragment** = `<div class="viz-code" role="img" aria-label="…">` — 내부는 좌측 코드 패널(`.vc-gutter` 라인번호 + `.vc-line`들, 지목 줄에 `.is-hl`)과 우측 여백 콜아웃(`.vc-callout`, 각 `.cn` 블루 번호 + 짧은 풀이)을 `.vc-lead` 얇은 블루 리더선으로 잇는다. fragment 자체엔 아이브로우·슬라이드 제목을 넣지 않는다. **데모 host(whole-slide)** = 이 fragment를 얹는 슬라이드는 **실재 레이아웃** `L-dc-hero`(diagram-centric, `.s-full`+`.s-eyebrow` 무대 전체에 코드 fragment를 stage-figure로 얹고 하단 22px 캡션)로 별도 구성한다 — 다른 element가 실재 host를 참조하듯(D-radial→`L-dc-radial-hub`) 코드 fragment는 `L-dc-hero`를 host로 삼는다. 상단 타이틀 밴드가 필요하면 **top-down 패밀리 셸**(`.s-full`+`.canvas-fill`, 밴드 아래 코드 채움)에도 그대로 얹힌다(패밀리 셸이지 신규 ID 아님). **`L-dc-code`라는 레이아웃은 존재하지 않는다 — 명명 금지.** "레이아웃 ≠ element."
- **placement**: `[L-dc-hero]` (+ top-down 패밀리 셸 `.s-full`+`.canvas-fill`; 신규 레이아웃 신설 없음)
- **built_on**: `.s-full`(host), `--surface`(패널 배경) · `--ink`(코드 텍스트) · `--font-mono`(JetBrains Mono) · `--gray-400`(라인번호 gutter·비강조) · `--blue-soft`(강조 줄 배경) · `--blue`(콜아웃 번호 fill·리더선) · `--r-md`(패널 라운드) · `--shadow-md`(카드 그림자). **신규 CSS 요지**: `.viz-code{background:var(--surface);border-radius:var(--r-md);box-shadow:var(--shadow-md);font-family:var(--font-mono);color:var(--ink);position:relative;display:grid;grid-template-columns:1fr auto}` · 코드 열은 `.vc-line{display:flex}` + `.vc-num{color:var(--gray-400);width:2.4em;text-align:right}` · 강조 `.vc-line.is-hl{background:var(--blue-soft)}` · 여백 `.vc-callout{...}` 안 `.cn{background:var(--blue);color:var(--white);border-radius:var(--r-pill)}`(블루 원, 흰 숫자 = 어두운 fill 위 흰 글자라 대비 OK) · 리더선 `.vc-lead{height:1px;background:var(--blue)}`(또는 인라인 SVG `<line stroke="var(--blue)">`). 데모의 `.code-diagram`/`.mono`(`--font-mono` 게시 규칙)와 같은 코드-텍스트 원칙을 재사용.
- **a11y**: `role="img"` + `aria-label="주석 코드 블록(<언어> <파일명>, N줄): 강조 <n>번째 줄 = <풀이1>, <m>번째 줄 = <풀이2> …"`. 콜아웃 번호·리더선·라인번호 gutter는 장식이므로 개별 aria 없음, 전체를 하나의 img로. *(대안: 코드를 스크린리더가 실제로 읽어야 하면 코드 열은 실제 텍스트로 두고 콜아웃만 `aria-hidden`; 기본형은 카탈로그 관례대로 1 img.)*
- **sketch**:
```
┌──────────────────────────────────────────────────────────┐
│  app.py                                    (파일명 바·선택) │
│ ┌──────────────────────────────┐                          │
│ │ 1  import streamlit as st     │                          │
│ │ 2  st.title("내 앱")  ░░░░░░░░ │ ──①── 화면 제목을 그린다   │
│ │ 3  n = st.slider("값", 0, 100)│ ──②── 슬라이더 입력을 받음 │
│ │ 4  st.write(n * 2)  ░░░░░░░░░░ │ ──③── 입력을 2배로 출력    │
│ │ 5  # 끝                        │                          │
│ └──────────────────────────────┘   └ 얇은 블루 리더선       │
│   ░░ = 강조 줄(blue-soft 배경)   ①②③ = 블루 콜아웃 번호      │
└──────────────────────────────────────────────────────────┘
```
- **coded**: no
- **density_note**: 코드 텍스트는 **본문(22px 하한)이 아니라 별도 코드-디스플레이 종**이므로 mono **20px**가 하한(등폭·라인번호 정렬을 위해 서술 본문과 분리 관리 — 22px 하한 예외를 여기서 명시적으로 정당화). 콜아웃 풀이·`.vc-bar`는 다이어그램 라벨/캡션에 해당해 **≥17px** 하한을 못박는다(그 아래로 눌리지 않음). 산식 32px/줄 × ≤12줄 + `.vc-bar` 1줄이 세로예산 **548px** 안에 들어가 **스크롤 0**. 강조 줄·콜아웃은 최대(강조 ≤4, 콜아웃 ≤4)에서도 리더선 교차 없이 우측 여백에 수용. 40~50대 가독성 하한(코드 20px·라벨 17px)을 하회하지 않음.
- **source**: https://squidfunk.github.io/mkdocs-material/reference/code-blocks/ (verify pending — 본 실행에서 WebFetch 재확인 못 함; 라인번호 `linenums` · `hl_lines` 줄 강조 · code annotations 번호 마커 세 기능을 확증하는 안정 페이지로 알려짐, 원본 복제 아님·재표현만)
- **adaptation_note**: 소스는 라인번호 · `hl_lines` 줄 강조 · 번호 마커 주석 **세 기능을 확증**하는 영감일 뿐(원본 복제 아님). Material의 클릭-펼침 툴팁 주석 대신, 정적 슬라이드용으로 **여백 콜아웃 + 얇은 블루 리더선**(항상 펼쳐진 상태)으로 재표현 — 발표·인쇄에서 전부 보이게. 소스의 다색 구문강조(syntax highlight)는 **채택 안 함**: 40~50대 가독성·평면 토큰 시스템·"색=의미" 헌장에 맞춰 코드는 `--ink` 단색, 강조는 오직 강조 줄 배경(`--blue-soft`) + 콜아웃이 담당. 콜아웃 번호는 민트가 아니라 **블루**(구조), 세로예산 548px·mono 20px 산식으로 **≤12줄·강조 ≤4** 캡을 우리가 규정(no-default 아님, 물리적 상한).

### 저자 self-check (감사 반영)

> 감사 5개 이슈 전부 해소. (1) FABRICATED HOST: 존재하지 않던 `L-dc-code`를 제거하고 실재 레이아웃 `L-dc-hero`(diagram-centric stage-figure)로 host 교체 — placement=`[L-dc-hero]`. 타이틀 밴드가 필요하면 top-down 패밀리 셸(`.s-full`+`.canvas-fill`)에 얹힌다고 명시(신규 ID 신설 안 함 → §3 균형표 diagram-centric 목표 6과 무충돌). "`L-dc-code`는 존재하지 않음·명명 금지" 못박음. 라이브 파일(elements-code.md) 12행 fragment 표의 host도 `L-dc-*/L-td-*`(일반)→`L-dc-hero`+top-down 패밀리 셸로 함께 수정해 자기불일치 제거. (2) content_slots[] 신설: filename_bar(선택)·code_lines[6..12]·highlight_lines[≤4]·callouts[≤4]·lead_lines. (3) density_note 신설: 코드 mono 20px·콜아웃/바 ≥17px 하한 + 548px 예산 스크롤0 명시. (4) 콜아웃 폰트 못박음: 풀이 ≥17px(다이어그램 라벨/캡션 하한), 코드 mono 20px는 본문 22px가 아닌 별도 코드-디스플레이 종임을 density_note로 정당화. (5) source: (verified)→(verify pending)로 낮춤(본 실행 WebFetch 재확인 없음·원본 복제 아님). 색: 코드=--ink·배경=--surface·강조배경=--blue-soft·콜아웃번호/리더선=--blue, raw #hex·navy·그라데이션 0, 무지개 구문강조 미채택. 배지 split 준수: 콜아웃 번호 `.cn`=블루 fill(구조), 민트 fill 3종(.num-circle/.work-step .n/.pd-dot.is-active) 미사용. 흰 글자는 블루 fill 원 위에만, 민트/코랄 fill 위 흰 글자 없음. no-default 헌장 준수("어떤 element도 기본이 아니다"만 사용). a11y=role=img+비어있지 않은 aria-label. §5 필드 전부 채움. 중복 아님(concept/소스코드 게시 element 부재 gap 충족). 라이브 파일에도 동일 수정 반영 완료.

---

## D-gantt — 비례 시간축·간트 / Proportional Timeline (Gantt)

- **종류**: element · **home_file(제안)**: `kit/charts/diagrams-process.md`
- **info_shapes**: [flow, numeric] · **composition_family**: — · **composition_shape**: —

### 스펙 마크다운

### D-gantt — 비례 시간축·간트 / Proportional Timeline (Gantt)

- **id**: `D-gantt`
- **name**: 비례 시간축·간트 / Proportional Timeline (Gantt)
- **kind**: `diagram`
- **info_shapes**: `[flow, numeric]` — 주 모양은 **일정 흐름**(무엇이 언제·얼마나·병렬로 진행되나), 종속 모양은 **numeric**(막대 좌표·길이가 실제 시점·기간에 비례). **D-funnel과 일관 처리**: funnel이 "단계별 물량 감소"라는 비례 성분 때문에 `[flow, numeric]`을 다는 것과 같은 근거로, gantt도 막대가 실제 기간에 비례하므로 numeric을 병기한다. 단 **양 비교가 목적이 아니라** 기간·병렬 관계 전달이 목적이라 by-shape.md에는 **flow 절에만 등재**(funnel과 동형: numeric 절엔 괄호 각주로만). 정확한 양 비교가 목적이면 → `C-column`/`C-hbar`, 부분-전체 비율이면 → `C-pie`.
- **data_shape**: **공통 연속 시간축**(픽셀 위치 ∝ 실제 날짜/시점) 위에 놓인 (a) **기간 막대**(작업마다 시작~종료, 길이=소요) + (b) **마일스톤 점**(기간 0의 시점 사건). 여러 막대가 **세로로 겹쳐 병렬 진행**을 표현. 실제 시점 간격이 화면 간격에 그대로 비례하는 게 핵심 신호 — 3월과 4월 사이 폭 = 4월과 5월 사이 폭. **판별(중요): 축 없이 사건을 등간격으로 나열하고 기간이 없으면 그건 점-사건 타임라인 레이아웃(`L-vf-spine-timeline`)이지 간트가 아니다.** 되돌아 반복하면 cycle, 단계마다 물량이 줄면 funnel, 시점·기간 없이 순서만이면 flow-h/flow-v.
- **when_to_use**: 여러 작업의 **시작·종료·소요·겹침**을 하나의 비례 시간축에서 한눈에 — 프로젝트 일정, 강의 로드맵, 출시 계획, 병렬 워크스트림의 타이밍 관계. "설계와 개발이 언제부터 겹치나", "테스트가 며칠이나 걸리나"처럼 **기간과 병렬성**이 메시지일 때. (소스 재표현: 행=활동, 열=시간축이고 각 활동의 소요가 막대 길이로, 착수=막대 시작·완료=막대 끝으로 그려지며, 병렬로 도는 활동이 세로 겹침으로 드러난다.)
- **when_to_avoid**: 시점만 있고 기간·병렬이 없는 점-사건 나열이면(→ 레이아웃 `L-vf-spine-timeline` 또는 `D-flow-v`), 순서만 있고 실제 시점이 없으면(→ `D-flow-h`/`D-flow-v`), 반복 순환이면(→ `D-cycle`), 작업이 7행을 넘어 막대 행이 세로예산을 초과하면(→ 표로 축약하거나 분할), 시간 간격이 균일해 비례축의 이점이 없고 각 마디에 문장 설명이 길면(→ `L-vf-spine-timeline`), 정확한 양 비교가 목적이면(→ `C-column`/`C-hbar`).
- **capacity**: 작업(막대) 행 **5~6개 여유·7개 상한**. 세로 산식(형제 element와 정합): fragment `.viz-gantt` 높이는 세로예산 548px에서 **host 크롬(`.s-eyebrow` ~40px + 하단 캡션 ~40px = ~80px)을 뺀 ~468px**로 캡(형제 D-radial/D-cycle/D-network가 fragment를 ~500px로 캡하되 gantt는 host가 아이브로우·캡션을 fragment 주위에 얹으므로 그만큼 더 조인다). 그 ~468px에서 축 헤더(눈금 라벨) ~40px를 빼면 막대 플롯 ~428px → 행높이 ~56px(라벨 ≥17px 한 줄 + 막대 두께 ~20px + gap/패딩 ~16px)로 **7행이 물리 상한**(428/56≈7.6, 8행은 gap 붕괴·라벨 겹침). 선택 범례를 넣으면 ~28px를 더 먹어 6행이 상한. 마일스톤 점 ≤6(라벨 겹침 방지). 축 눈금 4~8개(작업 라벨 컬럼 ~220px를 뺀 플롯 ~900px에 등간격, 눈금당 ≥110px라 라벨 ≥17px 한 줄 수용). 작업 라벨 컬럼 폭 ~200~240px(라벨 ≤8자 전각). 막대가 너무 짧아 라벨이 안 들어가는 순간 사건은 마일스톤 점으로 대체.
- **content_slots[]**: `axis_scale`(공통 비례 시간축 — t0~t1, 눈금 4~8개) · `task_bars[]`(각 항목: `label`·`start`·`end`·`emphasis`(강조=blue / 비강조=periwinkle / 지연·리스크=coral)) · `milestones[]`(각: `label`·`date`, `◆` 점 ≤6) · `now_marker`(현재 시점 세로 파선 — 중립 구조 마커) · `caption`(fragment **외부**, host `L-dc-*`가 하단에 얹음). *(§5는 element = 레이아웃 스키마 §4 + 추가필드라 `content_slots[]`를 요구하므로 명시. 형제 diagrams-process 항목들이 `data_shape`로 슬롯을 갈음해 온 관행과 달리 여기선 축·막대·마일스톤·현재선이 뚜렷이 이름난 슬롯이라 명시가 더 정확하다.)*
- **element_vs_slide**: **fragment** = `<div class="viz-gantt" role="img" aria-label="…">` 안에 좌측 작업-라벨 컬럼 + 우측 플롯(공통 시간축). 각 막대는 `div`로 `left:%`(=`(start−t0)/span`)·`width:%`(=`(end−start)/span`)를 시간에 비례 배치, 마일스톤은 같은 좌표계 위 `◆` 점, 현재 시점은 세로 파선. **데모 host(별도 whole-slide)** = 이 fragment를 얹는 whole-slide는 `L-dc-*`(diagram-centric, `.s-full` + `.s-eyebrow` + 하단 캡션) 또는 `top-down`으로 **따로** 구성 — fragment 자체엔 헤더·아이브로우·제목을 넣지 않는다(레이아웃 ≠ 다이어그램, 규격 §5). host 크롬(아이브로우+캡션 ~80px)은 capacity 산식에서 이미 뺐다.
- **placement**: `[diagram-centric, top-down]`
- **built_on**: `.s-full`(host), 플롯 컨테이너(`position:relative`) + 막대 `div`(시간비례 `left`/`width`). **색 문법**: **강조 구간(현재/핵심 작업) = `--blue`**, **비강조 작업 = `--periwinkle`**, **지연·리스크·병목 구간 = `--coral-deep`**(주의 — 코랄은 오직 이 의미에만), 오류·심각 미달 = `--red`. 마일스톤 `◆` = `--blue`(구조 점 — 번호 배지 아님, 민트 fill 3종 규칙 무관), 축·격자선 = `--line`, 눈금·라벨 = `--ink`/`--gray-700`. **현재 시점 세로선 = `--ink` 파선(중립 구조 마커)** — 'today'는 경고가 아니라 위치 표시이므로 코랄을 쓰지 않는다(코랄 이중 의미 방지: 같은 화면의 '지연·리스크'와 색이 겹치면 색=의미가 무너짐). 현재선을 강조하고 싶으면 `--blue` 파선까지만 허용, 코랄은 불가. **신규 CSS 요지**: `.viz-gantt{display:grid;grid-template-columns:220px 1fr;height:468px}` + `.viz-gantt .plot{position:relative;height:100%}` + `.viz-gantt .bar{position:absolute;height:20px;border-radius:var(--r-pill);left:var(--x);width:var(--w)}` + 세로 격자 `.viz-gantt .tick{position:absolute;border-left:1px solid var(--line)}` + `.viz-gantt .ms`(마일스톤 `◆`, `color:var(--blue)`)·`.viz-gantt .now{border-left:2px dashed var(--ink)}`(현재선). 데모 `.code-chart`(막대 색·수치 텍스트)·`.timing`(**블루** 시점 배지) 규칙 재사용. **navy·그라데이션 금지, raw #hex 금지(흰 글자는 `var(--white)`).**
- **a11y**: `role="img"` + `aria-label="비례 시간축 간트: <t0>~<t1> 기간, 작업 N개 — <작업1> <시작1>~<종료1>, <작업2> <시작2>~<종료2>(<작업1>과 병렬), …; 마일스톤 <라벨> <시점>. 오늘 <today>."`. 막대·격자·`◆`·현재선은 장식이므로 개별 aria 없음 — 시작·종료·병렬 관계를 라벨 텍스트로 순서대로 담아 시각 정보를 대체, 전체를 하나의 img로.
- **sketch**:
```
┌────────────────────────────────────────────────────────────────┐
│ ▍EYEBROW · 제목                                (host: L-dc-*)   │  s-head+eyebrow는 host
│  작업 \ 축   1월    2월    3월    4월    5월    6월              │  비례 시간축(등간=실제 등간)
│              ├──────┼──────┼──────┼──────┼──────┼──────┤         │  --line 격자
│  기획       ████████                              ◆ 킥오프      │  ← --blue 강조 막대
│  설계            ▒▒▒▒▒▒▒▒▒▒                                     │  ← periwinkle(설계·개발 병렬)
│  개발                ▒▒▒▒▒▒▒▒▒▒▒▒▒▒                             │      세로 겹침=병렬 진행
│  테스트                        ▒▓▓▒▒▒▒▒   ┆                     │  ← ▓=coral 지연구간 · ┆=--ink 파선(오늘)
│  출시                                    ▒▒▒▒  ◆ 출시           │
│              └ 폭 = (종료−시작)/전체기간 · 좌표 = (시작−t0)/전체 ┘│  캡션은 host
└────────────────────────────────────────────────────────────────┘
   범례: --blue 강조 · periwinkle 비강조 · coral=지연/리스크 · ┆ --ink 파선=오늘(중립)
```
- **coded**: no
- **density_note**: 작업 라벨 ≥17px(다이어그램 라벨 하한)·축 눈금 ≥17px 한 줄(≤5자). 막대 두께 20px·행 gap ~16px로 **7행이면 ~428px(=468 fragment − 40 축헤더) 안에 수렴**, 6행이 여유(범례 넣으면 6행 상한). 8행은 gap 붕괴·라벨 겹침이라 금지 → 표 축약/분할로 넘긴다. 마일스톤 라벨은 점 우측 1줄 ≥17px, 겹치면 위/아래 교차 배치. host 크롬(아이브로우+캡션 ~80px)은 capacity 산식에서 이미 공제 — fragment는 ~468px를 넘지 않아 548px 안에서 스크롤 없이 성립. 세로 패딩 합이 플롯 높이의 1/3 넘지 않게(토큰 치트시트 세로 규칙). **강조는 `--blue` 1계열로 한정**(한 화면 파랑 도배 금지), 나머지 막대는 `--periwinkle`, 지연·리스크만 `--coral-deep`, 현재선은 `--ink` 파선(코랄 아님).
- **source**: https://datavizcatalogue.com/methods/gantt_chart.html — **verified** (이 실행에서 WebFetch로 실재 확인). 요지 재표현: 행=활동·열=시간축, 막대 길이=활동의 소요(막대 시작=착수·끝=완료), 병렬로 도는 활동이 드러남, 마일스톤은 심볼로 표기, 현재일은 세로선으로 표시. 원문 통구절 복제 아님 — 짧은 귀속 표현만.
- **adaptation_note**: 소스의 **비례 시간축 + 기간 막대 + 병렬 + 마일스톤 + 현재선**을 CSS 절대배치(시간→`left`/`width` 비례)로 재표현(이미지·간트 SW 스크린샷 아님, 시점·기간은 텍스트 라벨). **`L-vf-spine-timeline`과의 결정적 차이를 명문화**: 스파인 타임라인은 *점 사건을 등간격 지그재그로 나열*(축 비례·기간·병렬 없음)인 **레이아웃**, 이 항목은 *실제 간격이 비례하고 막대가 기간·병렬을 담는* **element**. 소스가 상한 미명시 → 가독성 하한(17px)·세로예산(548px − host 크롬 ~80px = fragment ~468px) 산식으로 **작업 ≤7·마일스톤 ≤6** 캡을 우리가 규정(no-default 아님, 물리적 상한). **색은 강조 구간만 `--blue`, 비강조 `--periwinkle`, 지연·병목만 `--coral-deep`, 현재선은 `--ink` 파선(중립)** — 코랄을 '오늘' 표시에 쓰지 않아 코랄=주의라는 문법을 한 화면 안에서 지킨다. navy·그라데이션·민트 fill 배지 미사용. 의존 화살표(소스의 activity 연결선)는 정적 슬라이드 가독성상 기본 생략, 필요 시 `--blue` 얇은 커넥터 1~2개만. **info_shapes는 D-funnel과 동형으로 `[flow, numeric]`** — 등재는 flow 절(비례축이 필요할 때), numeric 절엔 괄호 각주만(양 비교가 목적 아님).

### 저자 self-check (감사 반영)

> 색: 강조 구간=--blue, 비강조=--periwinkle, 지연/리스크/병목=--coral-deep, 오류=--red. [수정#1] 현재선을 --coral-deep 파선 → **--ink 파선(중립 구조 마커)**로 변경 — 코랄은 이제 '지연·리스크'에만 쓰여 한 화면 코랄 이중의미 제거(built_on·sketch·density_note·adaptation_note·a11y 5곳 반영). navy·그라데이션 0, raw #hex 0(흰색 var(--white)). 배지: 마일스톤 ◆는 구조 점이라 --blue(민트 fill 3종 .num-circle/.work-step .n/.pd-dot.is-active와 무관), 시점 배지 .timing 블루. [수정#2] content_slots[] 추가(axis_scale·task_bars[]·milestones[]·now_marker·caption) — §5(element=§4 스키마+추가필드) 필수 충족. [수정#3] capacity 세로예산 재산정: fragment ~468px(=548 − host 크롬 ~80px[아이브로우40+캡션40]), 축헤더 40 제하면 플롯 ~428px → 행 ~56px로 **7행 물리 상한**(형제 element 500px 캡과 정합, 8행 아님). capacity·density_note·when_to_avoid·adaptation_note 동시 수정. [수정#4] info_shapes [flow]→**[flow, numeric]**로 D-funnel과 일관, by-shape는 flow 절 등재·numeric 각주로 근거 1줄 명시. [수정#5] source 'verified'는 이 실행에서 WebFetch로 실제 확인함(재현 가능)·원문 통구절 복제 아닌 재표현·짧은 귀속. 가독성: 작업/축 라벨 ≥17px, 7행 산식 검산(전각폭 반영). 스키마: §5 element 필드 전부(id·name·kind·info_shapes∈taxonomy·data_shape·when_to_use·when_to_avoid·capacity·content_slots[]·element_vs_slide[fragment↔host 분리]·placement·built_on·a11y[role=img+aria-label]·sketch·coded:no·density_note·source·adaptation_note). no-default: '기본/최다/go-to' 서술 없음(물리 상한만 규정). 중복: LOW — charts/by-shape.md·layouts에 간트/비례축 element 없음, L-vf-spine-timeline(레이아웃·점사건·축비례 없음)과 data_shape·element_vs_slide·adaptation_note 3곳서 구별. 등재 제안: charts/by-shape.md flow 절 하단(funnel 뒤)에 D-gantt 추가 + numeric 절에 '(D-gantt도 numeric 겸: 비례 시간축)' 각주, split 1순위 금지 규칙과 무관.

---

## L-td-glossary — 용어 정의 리스트 / Term Glossary List

- **종류**: layout · **home_file(제안)**: `kit/layouts/families/top-down.md`
- **info_shapes**: [classification] · **composition_family**: top-down · **composition_shape**: band-grid

### 스펙 마크다운

### L-td-glossary — 용어 정의 리스트 / Term Glossary List

- **id**: `L-td-glossary`
- **name**: 용어 정의 리스트 / Term Glossary List
- **composition_family**: `top-down`
- **composition_shape**: `band-grid`
- **info_shapes**: `[classification]`
- **when_to_use**: 동급 용어 여러 개(6~7)를 각각 한 문장짜리 정의와 짝지어 한 화면에서 **병렬로 훑을** 때. 관객이 하는 일은 "이 사전이 정의하는 용어들을 쭉 살펴본다"이지 "A가 B를 정한다"가 아니다 — 그래서 mapping이 아니라 **classification**(동급 항목 3개↑ 나열)이다. 밴드가 이 용어들이 왜 한 묶음인지(무엇을 정의하는 사전인지) 한 줄로 걸고, 아래 2열 필드가 용어 → 정의를 정적으로 나열한다. 카드 격자로는 문장형 정의가 성기게 흩어지고 `table.t`(✓/✕·속성 격자)로는 문장이 셀에 안 들어갈 때.
- **when_to_avoid**: 용어가 **1개뿐**이라 하나를 크게 세워 못박아야 할 때(→ `L-ct-definition`, concept·centered). 좌·우가 문장형 정의가 아니라 **짧은 라벨끼리 방향성 대응**(≤6어, 좌→우 화살표로 "이 모델 → 이 지표")이면 회피(→ `L-td-mapping-rows`, info_shapes=[mapping], shape=flow-vertical). 동급 항목을 정의 없이 이미지/사실만으로 나열하면 grid-mosaic 카드가 맞다(→ `L-gm-cards-3x2`). 정의가 2문장 넘게 길어 22px에서 행당 2줄을 넘겨야 담기면 회피(용어 수를 4~5로 줄이거나 본문 서술형 top-down으로).
- **capacity**: 밴드 아래 term→def **6행**(정의가 전부 1줄일 때 최대 7행). 좌열 용어(term) ≤ 8자(고정폭 ≈ 260px, `--ink` 700 무채색 볼드), 우열 정의 ≤ 1줄(22px, 우열 ≈ 840px에서 keep-all ≈ 33 전각자). 어느 정의든 2줄이 필요하면 총 행수를 **4~5행**으로 줄인다(2줄 행은 ≈ 92px). 형제 항목과 같은 보수 기준: `L-td-mapping-rows` 5~6행·`L-td-table` 6~7행 라인에 맞춘 6~7행.
- **built_on**: `.s-full`, `.canvas-fill`, `.s-eyebrow`, `.s-title`(+`.hl`), `.accent-bar`, **행 컨테이너는 기존 `.card.surface` 재사용**(mapping-rows와 동일 프리미티브), 정의는 기존 `.s-body`(22px), 행 사이 `--line` 1px 구분선. 신규 유틸 **딱 1개** `.dl-term`(좌열 고정폭·`--ink` 700 무채색 볼드) — `.s-body strong`은 토큰 규칙상 자동으로 블루가 되어(치트시트 `strong`→블루 800) 좌열 전체가 파래지므로, "블루가 아닌 고정폭 용어 라벨"을 낼 기존 프리미티브가 없어 이 하나만 추가한다. 그 외 4개 신규 클래스(`.def-list`/`.dl-row`/`.dl-def`)는 폐기 — `.card.surface`+`.s-body`로 흡수. 토큰만 사용.
- **content_slots**: eyebrow, title, accent, entries[6..7]{term, definition}
- **sketch**:
```
┌──────────────────────────────────────────────┐
│ EYEBROW                                       │
│  이 용어들을 한 줄로 묶는 제목   ▂▂ accent     │
├───────────────┬──────────────────────────────┤
│ 용어 1 (ink볼드)│ 정의 문장 한 줄 (22px · ink) │
├───────────────┼──────────────────────────────┤
│ 용어 2        │ 정의 문장 …                   │
├───────────────┼──────────────────────────────┤
│ 용어 3        │ 정의 문장 …                   │
├───────────────┼──────────────────────────────┤
│ 용어 4        │ 정의 문장 …                   │
│ …6~7행        │ (좌열 고정폭·무채색 / 우열     │
│               │  keep-all · 커넥터 없음)      │
└───────────────┴──────────────────────────────┘
```
- **coded**: no
- **density_note**: 세로 예산 ≈ 548px에서 밴드(eyebrow+title+accent-bar) ≈ 140px 소비 → 정의 필드 ≈ 408px. 정의 본문 22px × 1.78 = 줄당 ≈ 39px. 1줄 행 = 39 + 세로 패딩 12~14(치트시트 "1~2줄 = 패딩 12~14px" 준수, 8px는 미달이라 상향) + 구분선 ≈ 2 ≈ **53~55px/행** → 408px에 **7행이 상한**(전부 1줄), **6행이 여백 있는 권장선**. 정의 2줄(≈ 78px)이 섞이면 행이 ≈ 92px로 커져 **4~5행**으로 컷. 좌열 용어는 고정폭(≈ 260px)·`word-break:keep-all`, 우열 정의도 keep-all로 단어 중간 절단 방지. **색(도배 방지)**: 좌열 용어 6~7개를 전부 블루로 칠하면 좌열 컬럼 전체가 파래져 MEMORY "한 화면 파란색 도배 금지"에 저촉된다(`table.t` th의 블루는 1행이지만 여기선 N행 컬럼 전체다). 그래서 **용어는 `--ink` 700 무채색 볼드**(라벨 위계를 무게로 표현, 색으로 아님), 정의 본문은 `--ink`. 블루는 밴드의 `.s-title .hl`(강조어 1곳) + `.accent-bar` **구조 강조 딱 2곳**에만 — 필드 본체엔 블루 0. 민트/코랄 fill 배지 없음(용어는 텍스트 라벨이지 진행 번호가 아니라 `.num-circle`/`.work-step .n`/`.pd-dot.is-active` 셋과 무관). 표가 아니라 정의 필드이므로 17px 표 하한이 아닌 **22px 서술 하한** 적용.
- **source**: https://developer.mozilla.org/en-US/docs/Web/HTML/Element/dl (verified — WebFetch 확인. `<dl>`이 dt/dd로 용어→정의 그룹을 짝짓는 description list이며, MDN이 명시 use case로 "glossaries(용어집)"·key-value 메타데이터를 든다. 원문 복제 없이 시맨틱만 근거.)
- **balance_action** (§3 균형 — issue 2 해소, "소유자 판단" 미루기 금지): 이 항목을 그냥 넣으면 top-down 8→9로 §3 캡(8)을 넘고, §2b에서 이미 최다(14)로 편향 표적인 **band-grid** 구도를 더 부풀린다. 따라서 **총량 중립 은퇴**로 admit한다 — **`L-td-bento`를 은퇴·`L-gm-bento`로 병합**한다(둘 다 "히어로 타일 + 소형 위성"의 비대칭 bento·classification이고 by-shape.md classification #4/#5로 인접 중복. `L-td-bento`의 밴드가 `L-gm-bento` 대비 더하는 값이 얕다). 결과: **top-down 8 유지**(glossary +1, bento −1), **band-grid 구도 수 net 0**(glossary +1 band-grid, bento −1 band-grid — 편향을 악화시키지 않음), **총 50 유지**. by-shape.md classification 섹션에서도 `L-td-bento` 자리를 `L-td-glossary`로 교체(문장형 term→def 필드는 카드 격자엔 없던 텍스트-정의 옵션이라 순증 가치가 있음).
- **adaptation_note**: HTML `<dl>`(description list)의 "용어(dt) → 정의(dd)" 쌍 시맨틱만 차용한다(MDN 명시 use case = 용어집). 브라우저 기본 들여쓰기·마진을 복제하지 않고 밴드=`.s-eyebrow`+`.s-title`(강조어만 `.hl` 블루)+`.accent-bar`, 각 항목=`.card.surface` 행(flex: 좌 `.dl-term` 무채색 고정폭 + 우 `.s-body` 22px 정의) + `--line` 구분선으로 재표현. **`L-td-mapping-rows`와 3중 분리**: (1) info_shape가 classification(동급 용어 훑기) vs mapping(A→B 대응), (2) composition_shape가 band-grid(정적 2열 필드) vs flow-vertical(방향성 `→` 커넥터 행), (3) 콘텐츠가 문장형 정의(≤33자) vs 짧은 라벨(≤6어). 그래서 by-shape.md에서도 glossary는 **classification 후보**, mapping-rows는 **mapping 후보**로 서로 다른 모양 섹션에 등재된다(중복 아님). **`L-ct-definition`(용어 1개·concept·centered)과도 분리** — 이쪽은 다수 용어집(classification)이다. **`table.t`가 아님** — 정의가 문장형이라 셀 격자보다 term→def 행이 맞고, 셀 17px 하한 대신 22px 서술 하한을 지킨다.


### 저자 self-check (감사 반영)

> issue별 해소 확인 — (1)[스키마] composition_shape 줄 본문에 명시(`band-grid`, §2b 8구도 ∈, 규격 line147 충족). (2)[§3 균형] 소유자 판단 미루기 제거 → balance_action에 총량중립 은퇴 명시(L-td-bento 은퇴·L-gm-bento 병합 → top-down 8 유지·band-grid net 0·총 50 유지·§2b band-grid 편향 악화 없음·by-shape classification 자리 교체). (3)[중복] mapping-rows와 3중 분리 명문화: info_shape(classification≠mapping)·composition_shape(band-grid≠flow-vertical, 커넥터 유무)·콘텐츠(문장형 정의≠≤6어 라벨). by-shape 등재 섹션 자체가 달라짐(classification vs mapping) → 후보 목록 충돌 해소. (4)[info_shapes] mapping→classification 재분류(taxonomy: mapping='A가 B를 정함', classification='동급 항목 3개↑ 병렬 훑기'). 용어→정의는 라벨/설명이지 결정이 아니므로 classification 적합, when_to_use에 근거 명시. classification ∈ taxonomy 12. (5)[capacity 과대] 행당 패딩 8→12~14px(치트시트 기준) 반영, 현실 행높이 53~55px → 6행 권장·7행 상한(8행 폐기), 2줄 정의는 4~5행. 형제(mapping-rows 5~6·table 6~7)와 정합. (6)[블루 도배] 좌열 전체 블루 폐기 → 용어=`--ink 700` 무채색 볼드, 블루는 밴드 `.hl`+`.accent-bar` 2곳만·필드 본체 블루 0 → MEMORY 도배 금지 준수. (7)[reuse] 신규 4클래스(.def-list/.dl-row/.dl-def) 폐기, 행=기존 `.card.surface`(mapping-rows와 동일)·정의=`.s-body` 재사용, 신규는 `.dl-term` 1개뿐(그마저 `.s-body strong` 자동블루 회피용으로 불가피 — built_on에 사유 명시). (8)[source] MDN dl WebFetch 실제 확인 완료 → (verify pending)→verified, 글로서리 use case 원문 근거·복제 없이 재표현(§8 충족). 공통 규칙: navy·그라데이션 미사용·raw #hex 없이 토큰만(--line/--ink/--blue). 민트/코랄 fill 위 흰 글자 사례 없음(fill 배지 자체가 없음 — 용어는 텍스트). no-default: "기본/최다/go-to" 표현 없이 when_to_avoid로 경계 명시. 가독성: 정의 22px/1.78·keep-all·전각폭 33자, 밴드 140px 후 필드 408px·6행×54px=324px로 세로예산 548px 내 스크롤 0.

---

## L-vf-case-acts — 사례 3막 카드(상황·행동·결과) / Case Story in Acts

- **종류**: layout · **home_file(제안)**: `C:\Users\miso\Desktop\template\kit\layouts\families\vertical-flow.md`
- **info_shapes**: [concept, flow] · **composition_family**: vertical-flow · **composition_shape**: flow-vertical

### 스펙 마크다운

### L-vf-case-acts — 사례 3막 카드(상황·행동·결과) / Case Story in Acts

- **id**: `L-vf-case-acts`
- **name**: 사례 3막 카드(상황·행동·결과) / Case Story in Acts
- **composition_family**: `vertical-flow`
- **composition_shape**: `flow-vertical`
- **info_shapes**: `[concept, flow]`
  - *rationale*: 이 슬라이드가 존재하는 이유(**주 모양 = `concept`**)는 "추상 주장·개념을 **단일 사례 하나**로 접지"하는 것이다 — 절차 안내도, 대안 우열도 아니다. 다만 그 한 사례를 **상황→행동→결과**의 시간 순서로 풀기 때문에 `flow` *형식*을 빌린다. taxonomy는 한 슬라이드에 모양이 둘 이상일 때 병기를 허용하고(선례: `L-vf-chevron-map`=`[mapping, flow]`), sketch·`↓` 커넥터·순서 구조가 flow 차원을 명백히 갖는데 이를 누락하면 by-shape 색인을 왜곡한다. 그래서 **주 `concept` · 보조 `flow`**로 정직하게 병기한다(정보 모양 ≠ 표현 형식 원칙: 왜 존재하나=concept, 어떻게 읽히나=flow).
  - *concept vs flow 경계(중복 방어)*: 같은 flow-vertical 구도인 `L-vf-numbered-stack`은 **주 모양이 flow**(누구나 따라 하는 반복 가능 절차 1→2→3)다. 이 항목은 **주 모양이 concept**(재현 불가능한 *한 사례*의 이야기로 추상을 접지)라 방향이 반대다.
- **when_to_use**: 추상 주장·개념 뒤에 **하나의 실제 사례**를 붙여 설득할 때, 그 사례를 본문 불릿이 아니라 **상황 → 행동 → 결과 3막**의 세로 밴드로 구조화해 "무슨 상황에서 → 무엇을 했고 → 어떻게 됐다"를 순서대로 읽히고 싶을 때. 각 막은 **역할 이름표**(상황/행동/결과 — 번호가 아니라 서사적 배역)를 단 **닫힌 장면 카드**(`.card.surface` 밴드)로 감싸 세 막을 이질적 장면으로 구분하고, 밴드 사이 파란 `↓`가 인과의 진행을 못박는다.
- **when_to_avoid**: 사례가 여러 개라 병렬로 훑어야 할 때(→ 그리드 `L-gm-*`). 두 대안·두 입장의 정식 견줌/대립일 때(→ `comparison-symmetric`, 이 레이아웃엔 우열 위계가 없다). **누구나 재현하는 반복 절차**(따라 하는 1→2→3 스텝)일 때는 `L-vf-numbered-stack`이 맞다 — 이 항목은 *한 사례의 이야기*이지 처방된 단계 목록이 아니다(구별 표식: 여기엔 번호·`.num-circle`이 없고 **이름 붙은 배역 pill**만, 각 막이 닫힌 장면 카드; numbered-stack은 번호 원 + 열린 스파인). 추상 접지가 목적이 아니라 용어 정의 한 도해면(→ centered `L-ct-definition`/`L-ct-figure`). 막이 4개↑로 늘어 서사 초점이 흐려지거나 각 막 본문이 3줄↑이면 세로 예산 초과.
- **capacity**: 막(act) **정확히 3개**(상황·행동·결과 서사 아크 — 가변 N 아님). 막당 역할 `.pill`(≤4어·헤드라인과 같은 줄) + 헤드라인 `.s-lead` ≤7어(1줄) + 본문 `.s-body` 22px **≤2줄**. 막 사이 `↓` 커넥터 1개씩. `eyebrow` 1 + (선택)`lead_in` 추상 주장 1줄. 번호 배지·`.num-circle` 미사용. 4막↑ 금지.
- **built_on**: `.s-head`, `.s-full`, `.s-eyebrow`, `.card.surface`(각 막 장면 밴드), `.pill`(역할 이름표: 상황/행동/결과 — **블루**, 구조 라벨), `.s-lead`(막 헤드라인, 27px), `.s-body`(22px 본문), 커넥터 `↓`(막 사이 인과, **블루**), `.hl-mint-text`(결과 막의 핵심어 절제 강조 · 민트딥 **글자**), (선택)`.s-title`/`lead_in` 추상 주장 1줄
- **content_slots**: `eyebrow`, `lead_in`(선택·1줄 추상 주장), `acts[]`(각: `role_label`(상황/행동/결과), `headline`, `body`), `connector`(`↓`)
- **sketch**:
```
┌────────────────────────────────────────────────────┐
│ ▍EYEBROW                                           │  s-head + s-eyebrow
│ (선택) 추상 주장 한 줄 …………………………                │  lead_in (1줄)
│ ┌────────────────────────────────────────────────┐ │
│ │ [상황]  헤드라인 …………                          │ │  act ①  card.surface
│ │  본문 22px ≤2줄 ……………………………                 │ │  pill=블루(배역·무번호)
│ └────────────────────────────────────────────────┘ │
│                     ↓  (인과)                       │  블루 커넥터
│ ┌────────────────────────────────────────────────┐ │
│ │ [행동]  헤드라인 …………                          │ │  act ②
│ │  본문 22px ≤2줄 ……………………………                 │ │
│ └────────────────────────────────────────────────┘ │
│                     ↓                               │
│ ┌────────────────────────────────────────────────┐ │
│ │ [결과]  헤드라인 …………  (핵심어 .hl-mint-text)    │ │  act ③  민트 글자 절제
│ │  본문 22px ≤2줄 ……………………………                 │ │
│ └────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────┘
   단일 사례 · 세로 3막(상황→행동→결과) · 좌우분할 아님
   번호 없음(배역 pill=블루) · 각 막 닫힌 장면 카드 ← numbered-stack과 구별
```
- **coded**: no
- **density_note**: 장면 밴드 높이 ≈ 세로패딩 28(14×2) + 헤드라인 `.s-lead` **35px**(27px×렌더 행높이 ≈1.3, *28px 아님 — 감사 정정*) + gap 6 + 본문 2줄(22×1.78×2 ≈ 78) ≈ **147px**. 3막 = 147×3 = 441 + `↓` 커넥터 2×24 = 48 → **489px**, + `eyebrow` 행 40 = **529px** < 세로 예산 548px(여유 ~19px). `lead_in`(선택·1줄 39px)을 쓰면 한 막의 본문을 ≤1줄로 낮춰 예산 유지. 역할 `.pill`은 19px 하한(eyebrow급) 준수, 헤드라인과 같은 줄. **민트 fill 배지 3종(`.num-circle`·`.work-step .n`·`.pd-dot.is-active`) 어느 것도 미사용** — 역할 라벨·`↓`는 블루(구조), 결과 막의 성공 뉘앙스는 `.hl-mint-text`(민트딥 글자) 또는 `.card.surface` + `--mint-deep` 보더로만(민트 fill 위 흰 글자 없음).
- **source**: https://www.themuse.com/advice/star-interview-method (verified 2026-07-15, WebFetch 실개봉 — STAR(Situation·Task·Action·Result) 4단 서사가 "잘 푼다" 같은 **추상 주장을 한 구체 사례의 순서 있는 이야기**로 전환함을 확인: Situation=장면 설정, Task=책임·목표, Action=취한 단계, Result=측정된 성과)
- **adaptation_note**: STAR은 인터뷰 답변용 프레임이라 원문을 복제하지 않고 **구조(추상 주장 → 순차 구체 서사)만** 차용한다. 4단(Situation·Task·Action·Result)을 덱 맥락의 **3막**으로 압축(상황=Situation+Task 통합 · 행동=Action · 결과=Result), 각 막을 `.card.surface` 세로 스택 + `↓` 인과 커넥터로 재표현. 원본의 인터뷰 화법·불릿 나열은 버리고 역할 `.pill`(무번호 배역)+헤드라인+`.s-body`로 납작하게. 색은 토큰만: 역할 라벨·`↓`=블루(구조), 결과 막만 `.hl-mint-text`/`--mint-deep`로 성공·접지를 절제 강조(fill·raw hex·`.num-circle` 금지).

> **카탈로그 통합(등재 필수 — 규격 §7 체크리스트 "by-shape 등재" · "패밀리 헤더 정합"). 이 항목 추가 시 아래 두 편집을 함께 수행:**
> - **패밀리 헤더 갱신** — `kit/layouts/families/vertical-flow.md` line 3 "주 대상 정보 모양: `flow`, `mapping`" → "주 대상 정보 모양: `flow`, `mapping` (+ 보조 `concept`: 단일 사례 서사가 flow 형식으로 개념을 접지하는 경우 — `L-vf-case-acts`)". 헤더가 concept를 인정해야 concept-병기 신규 항목과 정합.
> - **by-shape 역인덱스 등재** — `kit/layouts/by-shape.md`:
>   - *concept 섹션*(현 line 18–23, 주 모양이라 실 등재): 기존 4항목 뒤·split 후순위 앞에 삽입 → `5. L-vf-case-acts — 단일 사례를 상황·행동·결과 3막으로 풀어 추상 개념을 접지(세로 서사, split 아님)` (기존 5번 split 후순위는 6번으로 이동). split 아니므로 §6 "split 1순위 배제" 규칙과 무관.
>   - *flow 섹션*(현 line 41–48, 보조 모양이라 낮은 순위 등재): 기존 목록 끝(dc-hero 앞/뒤)에 삽입 → `L-vf-case-acts — 한 사례의 상황→행동→결과 서사(반복 절차 아님 · 주 모양은 concept)`. numbered-stack(반복 절차)과의 경계를 괄호로 명시.

- **numbered-stack 구별 근거(감사 이슈4 — 시각·구조·용도 3중 분리)**:
  1. **역할 축**: numbered-stack=**서수 번호**(1·2·3…N, 교체·확장 가능, `.num-circle` 민트 원). case-acts=**고정 배역 이름표**(상황≠행동≠결과, 각 막이 유일한 서사 기능, **번호 없음**, `.pill` 블루). "관객 눈"에 하나는 번호 목록, 하나는 배역 붙은 장면들 → "또 같은 거네" 실패.
  2. **카드 처리**: numbered-stack=**열린 스파인**의 맨몸 좌정렬 행. case-acts=**닫힌 `.card.surface` 장면 밴드**(테두리로 각 막을 장면으로 감쌈).
  3. **개수·용도**: numbered-stack=가변 N 스텝의 **반복 가능 절차**(flow 주). case-acts=**고정 3막**의 *한 사례* 서사로 **추상 접지**(concept 주). 구별이 분류 라벨에만 의존하지 않고 무번호 배역 pill + 장면 카드 enclosure로 시각적으로도 분리된다.

### 저자 self-check (감사 반영)

> 색: 토큰만(raw #hex 0). 역할 pill·↓ 커넥터=블루(구조), 결과 막 성공 뉘앙스는 .hl-mint-text(민트딥 글자) 또는 --mint-deep 보더로만 — 민트 fill 위 흰 글자·navy·그라데이션 없음. 배지 split: 민트 fill 3종(.num-circle/.work-step .n/.pd-dot.is-active) 모두 미사용, 역할 라벨=.pill 블루 — 번호 자체를 없애 num-circle 혼동을 원천 차단(numbered-stack과의 구별 표식 겸함). 가독성: 본문 22px/1.78, 헤드라인 .s-lead 27px(렌더 35px로 산식 정정 — 감사 이슈6 반영), pill 19px, 3막×2줄 산식 = 밴드 147×3=441 + 커넥터 48 + eyebrow 40 = 529px < 548 예산(여유 19px, 오버플로 0). 정직 병기: info_shapes=[concept, flow] — concept 주·flow 보조로 정직화(감사 이슈1), 선례 chevron-map=[mapping,flow]와 정합. 패밀리 정합: vertical-flow.md line3 헤더에 보조 concept 추가 지침 명시(감사 이슈2). by-shape 등재: concept 섹션(주·실등재 rank5, split은 6으로) + flow 섹션(보조·낮은 rank) 실제 삽입 문구 포함(감사 이슈3, 규격 §7). 중복: numbered-stack과 시각(무번호 배역 pill vs 민트 번호원)·구조(닫힌 장면카드 vs 열린 스파인)·용도(고정3막 concept접지 vs 가변N flow절차) 3중 분리 명시(감사 이슈4). 스코프: 2막 '개념↔예시' 변형 제거 — centered concept(L-ct-definition/figure) 중복 회피, 3막 단일 서사로 초점(감사 이슈5). no-default 헌장: '기본/가장 흔한/go-to' 서술 없음, split 미사용. 소스: themuse STAR WebFetch 실개봉(verified 2026-07-15) — 원본 복제 없이 구조만 재표현, adaptation_note 명시. 스키마 §4 레이아웃 필드 전부 채움 + composition_shape 포함.

---

## L-td-claim-rationale — 주장·근거 밴드 / Claim–Rationale Band

- **종류**: layout · **home_file(제안)**: `C:\Users\miso\Desktop\template\kit\layouts\families\top-down.md`
- **info_shapes**: [structure] · **composition_family**: top-down · **composition_shape**: band-grid

### 스펙 마크다운

### L-td-claim-rationale — 주장·근거 밴드 / Claim–Rationale Band

- **id**: `L-td-claim-rationale`
- **name**: 주장·근거 밴드 / Claim–Rationale Band
- **composition_family**: `top-down`
- **composition_shape**: `band-grid`
- **info_shapes**: `[structure]`
- **when_to_use**: 하나의 완결-문장 주장(테제)을 상단 밴드에 걸고, 그 아래 **종속 근거** 카드 ①②③로 "이 주장은 참 — 왜냐면"의 **2층 논증 위계**(부모=주장, 자식=근거)를 보일 때. 요소=주장+근거들, 관계=주장↓근거 종속(위계). 각 카드는 홀로 서는 동급 항목이 아니라 **주장에 매달린 이유**여서, 카드만 떼면 뜻이 성립하지 않는다 — 이 종속이 곧 structure(요소+관계·위계) 신호다. (복합 2구역이지만 상단 밴드가 테제를 지배하고 하단은 그에 종속된 근거로 남은 세로를 채우므로 top-down으로 분류.)
- **when_to_avoid**: 근거가 서로 대등해 주장 없이도 홀로 서는 **동급 나열**일 때(→ `L-td-swiss-columns`·`L-gm-cards-3x2`; 그쪽은 classification, 여기는 종속 위계) — 판별: 카드 하나만 떼도 뜻이 서면 avoid. 층위가 3단 이상인 진짜 계층 트리일 때(→ `L-ct-pyramid`·`D-tree`), 주장 없이 개념만 풀 때(→ `L-ct-definition`), 근거끼리 대립·비교라 위계가 아닐 때(→ comparison/contrast 패밀리). 근거가 4개를 넘으면 한 행 카드 밀도가 붕괴 → 회피.
- **capacity**: 주장 헤드라인 ≤ 16어(44px 800, ≤2줄) + 근거 카드 **2~4장**(①~④, 권장 3). `.grid-3` 기준 카드폭 ≈ 360px, 카드당 소제목 ≤ 6어(**22px/800**) + 본문 ≤ 3줄(22px/1.78). 4장은 `.grid-4` 폭 ≈ 264px로 좁아져 본문 ≤ 2줄로 컷.
- **built_on**: `.s-full`, `.canvas-fill`, `.s-eyebrow`, `.s-title`(+`.hl`), `.accent-bar`, `.grid-3`, `.card`(+`.surface`), `.s-body`, 관계 커넥터 `↓`(블루, 치트시트 "검증/아래" 기호), 근거 번호 마커 = `.timing`(블루 배지 프리미티브 — 치트시트 블루 노드 계열, 민트 fill 배지 3종 아님)
- **content_slots**: eyebrow, claim{title, hl}, connector_spine(`↓` 왜냐면), rationale_cards[2..4]{marker, subhead, body}
- **sketch**:
```
┌──────────────────────────────────────────────┐
│ EYEBROW                                       │
│  주장 한 문장(완결 테제)        ▂▂ accent     │ ← .s-title, 핵심어 1곳 .hl
│                ↓ 왜냐면                        │ ← 블루 커넥터 스파인(종속 명시)
│                │                              │   band→cards 물리적 연결
├───────────┬────┴──────┬───────────────────────┤
│ [1] 근거  │ [2] 근거  │ [3] 근거              │ ← .timing 블루 번호 마커
│ 소제목    │ 소제목    │ 소제목                │
│ 본문 2~3줄│ 본문 2~3줄│ 본문 2~3줄            │
└───────────┴───────────┴───────────────────────┘
```
- **coded**: no
- **density_note**: 세로 예산 548px. 밴드 = eyebrow(≈30px) + 주장 title 44px ≤2줄(≈104px) + accent-bar 5px(+여백 ≈19px) + `↓ 왜냐면` 커넥터 스파인 스트립(≈30px) ≈ **183px** 소비 → 근거 카드 행에 ≈ **365px** 남음. 카드 = 번호 마커(≈34px) + 소제목 행(22px 글자, 행 ≈28px) + 본문 3줄(3×39=117px) + 패딩·gap(≈32px) ≈ **211px** → 한 행에 여유(오버플로 0). 본문 ≥22px/keep-all, 마커·소제목·커넥터 라벨 ≥17px 하한. **swiss-columns와의 구별(중복 차단)**: swiss-columns는 대등 단(column)의 병렬 훑기(classification)라 밴드가 "한 줄 인사이트 제목"이고 커넥터·번호가 없다. 여기는 (1) 밴드가 **완결-문장 주장**으로 카드를 지배, (2) `↓ 왜냐면` **커넥터 스파인이 밴드를 카드 행에 물리적으로 잇는 종속 시각**, (3) 각 카드에 **블루 번호 마커**(=매달린 이유임을 표식)를 둔다 — 이 셋이 구조적 차별자다. **번호 마커는 구조 마커이므로 블루**(`.timing` 블루 배지; 민트 fill 배지 3종 `.num-circle`·`.work-step .n`·`.pd-dot.is-active` **아님** — 그 외 노드=블루 규칙 준수). **근거 카드는 서로 대등 → 색 차등·강조 순위 금지**: 전 카드 `--surface` 중립 + 블루 번호로 균일. 위계는 오직 밴드(주장)↓카드(근거) **배치·커넥터·번호**로 표현하고, 강조(`.hl`)는 주장 밴드 핵심어 **한 곳에만**. navy·그라데이션 없음, 민트/코랄 fill 위 흰 글자 없음, raw `#hex` 없이 토큰만.
- **source**: https://www.assertion-evidence.com/ (verified — 이 실행 WebFetch로 실재 확인: 구절 나열형 헤드라인+불릿 대신 "succinct message(assertion)를 헤드라인으로 걸고 그 아래 근거로 뒷받침" 원리 서술 확인. 논증 위계=주장→근거의 근거 소스. 원문 복제 없이 재표현)
- **adaptation_note**: Michael Alley의 assertion–evidence 원리(상단 "완결 문장 주장" + 하단 "이를 받치는 근거")만 차용하고, 원본의 단일-시각 evidence를 **병렬 근거 카드 ①②③**로 재표현. 밴드=`.s-eyebrow`+`.s-title`(핵심어만 `.hl` 블루)+`.accent-bar`, 종속 표시=치트시트 관계 커넥터 `↓`(블루)를 밴드-카드 사이 스파인 스트립으로, 근거=`.grid-3`+`.card.surface`+블루 번호(`.timing`). 원본이 명시 않는 근거 간 순위를 색으로 만들지 않도록 카드는 중립 통일. 원본의 bullet 회피 정신에 맞춰 카드 본문은 3줄 이내 완결 근거로.

### 저자 self-check (감사 반영)

> 6개 감사 지적 전부 해소. (1) composition_shape: 본문에 `- **composition_shape**: band-grid` 줄 추가 + 래퍼 필드도 유효값 `band-grid`(8구도 원형)로 교정 — 무효값 'top-down'(family명) 제거. (2) info_shapes [structure] 정합: 종속 2층 위계(주장↓근거, 카드가 홀로 못 섬)로 structure(요소+관계·위계) 신호를 명시해 '단일층 지지' 지적 반박; classification으로 내리면 swiss-columns와 직접 중복이라 structure 유지가 오히려 중복 회피에 유리. → **필수 후속: top-down.md 라인4 패밀리 헤더 '맞는 정보 모양'에 structure 추가 + by-shape.md structure 절에 후순위(예: 7위, top-down 옵션) 등재. 1순위는 기존 pyramid 유지(split 아님이라 랭크 제약 무관).** self_check가 헤더 갱신 누락했던 점 교정 완료. (3) swiss-columns 중복: 구조적 차별자 3종 명문화(완결-문장 주장 밴드가 카드 지배 · ↓ 커넥터 스파인이 밴드→카드 물리적 연결 · 블루 번호 마커=매달린 이유 표식) + when_to_avoid에 "카드 하나만 떼도 뜻 서면 avoid" 판별선으로 classification과 경계 분리. (4) built_on 실 프리미티브: 가짜 명 '블루 구조 배지' 제거 → 번호 마커=`.timing`(치트시트 블루 배지 프리미티브), 커넥터=치트시트 관계 커넥터 `↓`로 명명. (5) source: 이 저자 실행에서 WebFetch로 assertion-evidence.com 실재 확인 성공(assertion=헤드라인+evidence 원리 서술 확인) → verified 유지하되 "이 실행 WebFetch 확인" 증빙 병기(verify pending 불필요). (6) 수치 불일치: 소제목을 capacity·density 모두 22px/800로 통일, 행높이 28px는 '글자 22px+행여백'으로 별개 표기해 폰트/행배분 혼동 제거. 색: 구조=블루(주장 핵심어 .hl·커넥터·번호 .timing), 근거 카드 중립 --surface 통일(간 색차등·순위 금지). 배지 split: .timing 블루 명시 + 민트 fill 3종에서 명시적 제외(MEMORY 반복 위반 선제 차단). 가독성: 본문22·라벨17·548px, 밴드183+카드행211 실측 오버플로0, 4장은 grid-4 본문2줄 컷. no-default: '기본/최다/go-to' 표현 없음.

---

## L-gm-pitfalls — 함정·주의 세트 / Pitfall & Caution Set

- **종류**: layout · **home_file(제안)**: `kit/layouts/families/grid-mosaic.md`
- **info_shapes**: [checklist, classification] · **composition_family**: grid-mosaic · **composition_shape**: band-grid

### 스펙 마크다운

---

### L-gm-pitfalls — 함정·주의 세트 / Pitfall & Caution Set

- **id**: `L-gm-pitfalls`
- **name**: 함정·주의 세트 / Pitfall & Caution Set
- **composition_family**: `grid-mosaic`
- **composition_shape**: `band-grid`
- **info_shapes**: `[checklist, classification]`
- **when_to_use**: 안티패턴·함정 3~4개를 "위험 신호"로 묶어 각 항목을 **위험 + 왜 위험한가(이유) + 대안** 세 지문의 가로 밴드로 나란히 세워, "이건 피하고 대신 이렇게"를 한 화면에서 훑게 할 때(초보자 흔한 실수 모음, 코드 금기 목록, 프롬프트 함정 점검). 밴드는 **순서 없는 동급 병렬 카드**의 세로 스택이라 `grid-mosaic`(동급 격자)의 밴드형(`band-grid`)으로 잡는다 — 1→2→3 절차가 아니므로 `vertical-flow`가 아니고, 각 밴드가 셀 값이 아니라 서술 지문(이유·대안 문장)을 담으므로 표(`L-td-table`)도 아니다. *(taxonomy 근거: 피할 항목을 병렬로 훑는 "부정형 점검"이라 주 모양은 `checklist`, 항목이 동급 병렬이라 `classification`을 겸함. `L-gm-checklist`가 항목별 ✓/✗ **상태**만 찍는 것과 달리, 이쪽은 피할 항목마다 **이유+대안**을 실어 역할이 구분된다.)*
- **when_to_avoid**: 위험이 1~2개뿐일 때(단일 `.callout.coral` 하나가 정직 — 밴드 격자는 과함) · 항목이 순서·절차를 가질 때(`vertical-flow`) · 대안·이유 없이 단순 경고 나열이라 세 지문 중 둘이 빌 때(`L-gm-checklist` 2열 상태 격자) · 위험이 5개↑일 때(타이틀 블록까지 계상하면 548px 세로 예산을 넘김 — 아래 capacity 참조, 개수 축소 또는 지문이 라벨뿐이면 `L-gm-checklist`, 정형 셀이면 `L-td-table`).
- **capacity**: 3~4 위험 밴드(세로 1열 스택). 밴드당 세 지문(위험 제목 / 이유 / 대안)이 가로 3분할, 각 지문 폭 ~365px(1152/3−gap), 본문 22px/1.78·keep-all에서 **줄당 ~16자**. **세로 검산은 타이틀 블록을 반드시 포함**: `.s-eyebrow`(19px pill ~30px) + `.s-title`(44px ~52px) + 하단 gap ≈ **타이틀 블록 ~80px**(옵션 `.s-lead` 한 줄 추가 시 +~40px). 남는 밴드 예산 = 548 − 80 = **~468px**(lead 있으면 ~428px). — **3밴드**: 지문 2줄까지 여유(밴드 ~134px = 헤드라인 28 + 2줄 78 + 패딩 28). risk_title ≤5어 · reason/alternative 각 ≤~8어(≤2줄). 세로: 80 + 3×134 + gap 28 ≈ **510 ≤ 548** ✓. — **4밴드**: 각 지문 1줄로 압축(밴드 ~100px). reason/alternative 각 ≤~5어(라벨성, 1줄 ~16자 안). 세로: 80 + 4×100 + gap 42 ≈ **522 ≤ 548** ✓(이 경우 intro_lead 생략 권장). — **5밴드↑**: 500 + 타이틀 80 + gap 56 ≈ **636 > 548** → 예산 초과이므로 채택 금지, 4개로 축소하거나 `L-gm-checklist`/`L-td-table`로.
- **built_on**: `.s-full`(left:64 right:64 top:118) + card-grid(1열 세로 스택, gap 12~14) · 각 밴드 `.card` + **좌측 `border-left`(코랄) 또는 코랄 위험 표식** · 위험 표식 `.pill`(배경 `--coral-soft` · 글자 `--coral-deep`) · 이유/본문 `.s-body`(`--ink`) · 대안 앞 `--mint-deep` ✓ 글리프(텍스트, fill 배지 아님). 상단 `.s-eyebrow`/`.s-title`, 필요 시 `.s-lead` 한 줄(단 4밴드에선 생략). *(deck.css의 whole-slide `.risk-slide`/`.risk-card`(3단 세로 보드 + 일러스트, 보조문 15px)와는 별개 — 그건 split 계열 단일 슬라이드, 여기선 가독성 하한을 지키는 grid-mosaic N-밴드로 재구성.)*
- **content_slots**: title, (eyebrow), (intro_lead), band×3~4 { risk_label(⚠ 표식), risk_title, reason, alternative(✓ + 대안) }
- **sketch**:
```
┌──────────────────────────────────────────────────────┐
│ EYEBROW                                               │
│ 조심할 위험 신호 — 슬라이드 제목            ← 타이틀블록 │
│ ┏┃──────────────┬────────────────┬──────────────────┐ │  ┃ = 코랄 좌보더/표식
│ ┃┃ ⚠ 위험 ①     │ 이유: 왜 위험한가 │ ✓ 대안: 이렇게    │ │
│ ┗┃──────────────┴────────────────┴──────────────────┘ │
│ ┏┃──────────────┬────────────────┬──────────────────┐ │
│ ┃┃ ⚠ 위험 ②     │ 이유: …          │ ✓ 대안: …         │ │
│ ┗┃──────────────┴────────────────┴──────────────────┘ │
│ ┏┃──────────────┬────────────────┬──────────────────┐ │
│ ┃┃ ⚠ 위험 ③     │ 이유: …          │ ✓ 대안: …         │ │
│ ┗┃──────────────┴────────────────┴──────────────────┘ │
└──────────────────────────────────────────────────────┘
   (3밴드=각 지문 2줄 / 4밴드=각 지문 1줄·lead 생략)
```
- **coded**: no
- **density_note**: 색 문법이 밴드마다 한 줄로 읽힌다 — **코랄=주의는 위험 표식/좌보더에만**, 이유 본문은 `--ink`, **대안은 `--mint-deep` ✓**(개선 행동). 코랄 fill 위 흰 글자 금지(표식은 `--coral-soft` 배경 + `--coral-deep` 글자 — `--on-coral` 불요). 민트 ✓는 **텍스트 글리프**라 `.num-circle`·`.work-step .n`·`.pd-dot.is-active` 3종 외 민트 fill 배지 금지 규칙과 무관(fill 배지 아님). 블루는 제목 `.hl` 한 곳 정도로 절제(한 화면 파랑 도배 금지) — 밴드 안의 어떤 라벨·표식도 블루 fill 배지로 만들지 않는다. **세로 검산 요지(capacity와 동일 산식)**: 타이틀 블록 ~80px를 먼저 제하고 나머지 ~468px에 밴드를 채운다 → 3밴드는 2줄(134px)까지, 4밴드는 1줄(100px)로만 성립, 5밴드는 타이틀 포함 시 예산 초과. reason·alternative가 4밴드에서 1줄(~16자·~5어)을 넘기려 하면 밴드 3개로 줄이거나, 지문이 라벨뿐이면 `L-gm-checklist`, 정형 셀 값이면 `L-td-table`로.
- **source**: https://www.nngroup.com/articles/error-message-guidelines/ (verified — NN/g 에러·경고 메시지 원칙: "무엇이 문제인가 + 왜 그런가 + 어떻게 고치나"의 3부 구조를 위험+이유+대안 밴드로 차용. 원문의 "constructive advice(대안 제시)"·"positive framing(사용자 비난 회피)"을 대안 슬롯·코랄 절제로 재표현) · 보조 https://www.presentationgo.com/presentation/tag/mistakes/ (verify pending)
- **adaptation_note**: 원본(경고 메시지 UX 원칙)은 개별 인라인 경고다 — 슬라이드로 재표현하며 여러 위험을 동급 밴드 격자로 묶고, 원본의 붉은 경고색을 deck.css 토큰 `--coral`(표식·좌보더)으로만 국한해 카드 본문 가독성을 지킨다. "고치는 법"은 `--mint-deep` 체크로 옮겨 색이 곧 의미(코랄=피하라 / 민트=이렇게)가 되게 하고, 3D 아이콘·붉은 배경 블록 등 장식은 제거한다.


### 저자 self-check (감사 반영)

> 스키마: 레이아웃 §4 필수필드 전부 채움 — 이번에 누락됐던 `composition_shape: band-grid`를 composition_family 바로 아래에 추가(형제 8항목과 동일 위치·값). id·name·family·shape·info_shapes·when_to_use·when_to_avoid·capacity·built_on·content_slots·sketch·coded:no·density_note·source·adaptation_note 모두 존재. | 세로예산(감사 #2): 타이틀 블록(s-eyebrow~30 + s-title~52 = ~80px, lead +40) 명시 포함해 재검산 → 3밴드 2줄(510) / 4밴드 1줄(522) 만 성립, 5밴드는 636으로 초과라 삭제·상한을 3~4로 하향. capacity·density_note·when_to_avoid 세 곳에 동일 산식 반영. | 모순(감사 #3): 5밴드 케이스 제거로 '5밴드×1줄 vs ≤10어' 모순 해소. 지문 상한을 폭 기준으로 재산정 — 3밴드=≤~8어(2줄), 4밴드=≤~5어(1줄 ~16자). | by-shape(감사 #4): kit/layouts/by-shape.md의 classification 섹션과 checklist 섹션 양쪽에 L-gm-pitfalls 등재 완료, checklist에서는 L-gm-checklist(상태 ✓/✗)와의 역할 구분 문구('피할 항목마다 이유+대안') 부기. | source(감사 #5): NN/g error-message-guidelines를 이 실행에서 WebFetch로 실개봉·확인(무엇+왜+어떻게 3부 구조 확認) → (verified)로 전환, §8 채택조건(열리는 신뢰 소스 ≥1) 충족. 보조 presentationgo는 (verify pending) 유지하되 정본 아님. 원문 복제 없이 재표현. | family/shape 적합성(감사 #6): grid-mosaic band-grid인 이유(순서 없는 동급 병렬 카드 → vertical-flow 아님, 서술 지문이라 표 아님)를 when_to_use에 한 줄 명시. | 색 v2: 코랄=주의(표식·좌보더에만), 이유=--ink, 대안=--mint-deep ✓, 블루=제목 .hl 절제. navy·그라데이션 없음, raw #hex 없음(토큰만), 코랄 fill 위 흰 글자 없음(coral-soft 배경+coral-deep 글자). | 배지 split: 대안 ✓는 텍스트 글리프라 민트 fill 배지 3종 규칙과 무관, 밴드 내 라벨·표식을 블루 fill 배지로 만들지 않음 — 위반 없음. | 가독성: 서술 본문 22px/1.78, 표 없음, 지문 폭 ~365px 기준 밴드 3~4개를 548px 안에서 타이틀 포함 검산. | no-default: '기본/최다/go-to' 서술 없음. | 중복: grid-mosaic 8항목·L-gm-checklist(상태표식)·단일 .callout.coral과 구조 구별, by-shape 등재 시 역할 구분 문구 부기로 혼동 방지.

---
