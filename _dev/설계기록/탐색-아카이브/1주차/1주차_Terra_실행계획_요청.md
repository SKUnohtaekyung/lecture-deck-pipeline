# Terra 세션 실행계획 요청서 — 1주차 강의덱

아래 조건으로 1주차 강의덱 구현 계획을 먼저 제시하고, 사용자가 승인하면 구현을 시작하세요. HTML 조립을 즉시 시작하지 말고, 계획·입력 동기화·캡처 결손을 먼저 검증하세요.

## 세션 목표

`sessions/1주차/`의 기존 58장 초안과 결정표를 최신 총괄 결정에 맞춰 **61장 이상**으로 동기화하고, 이후 HTML 웹덱·발표자 노트·검증 가능한 배포본을 만드는 실행 계획을 수립하세요.

상세 정본은 반드시 `sessions/1주차/자료/1주차_Terra_인수인계.md`를 먼저 읽으세요. 이 요청서보다 인수인계의 확정값과 금지 사항이 우선합니다.

## 시작 절차

1. `git status --short --branch`로 dirty 상태를 확인합니다.
2. `AGENTS.md`, `.agents/agent-memory/vibecoding-deck/MEMORY.md`의 `## 미해결`, Codex 어댑터와 루트 `SKILL.md`를 모두 읽습니다.
3. 루트 스킬의 조립 전 read-path를 순서대로 읽습니다.
4. 1주차 초안·콘텐츠 결정표·집필노트·덱 결정표·결정요청사항·사용자 피드백·Terra 인수인계를 대조합니다.
5. 기존 사용자 변경, 특히 `_dev/설계기록/HIGH-6종-스펙_2026-07-15.md`와 `scripts/verify_deck.py`를 수정·되돌림·정리하지 않습니다.

## 확정 조건

- PART 6개 순서는 유지합니다.
- 58장 고정은 해제했습니다. 최소 61장으로 계획합니다.
- `C:\VibeCoding\Codex_연습`, `C:\VibeCoding\내_자기소개`, `C:\VibeCoding\내_자기소개\index.html`을 모든 관련 화면·문서·요청문에 통일합니다.
- PART 2에 `내 PC→C:`, `C:\VibeCoding` 생성, 파일 확장명 표시를 독립 조작 단계로 추가합니다.
- 한 슬라이드에는 조작 한 단계만 둡니다. 조작 화면의 주석 지점은 1~3개까지 허용합니다.
- 공통 요청문은 한국어·초보자 문장·단일 `index.html` 결과를 지향합니다. 최종 전문 후보 1개와 빈 폴더 반복 실측 계획을 제시하고 사용자 승인을 받습니다.
- 역할 비교는 사실 설명의 중심, 비유는 **“작업실→작업지시서→제작→검수” 고정 체계**로 보조합니다.
- 필수 수정 한 가지와 학생 자유 수정 한 가지를 모두 지원합니다. 공통 필수 수정이 소개 문구·배경색·글자 크기/강조 중 무엇인지 승인받기 전에는 임의로 고정하지 않습니다.
- 대표 오류·레퍼런스는 각각 2~3개, 강제 오류 실습은 하지 않습니다.
- 장식 아이콘·이모지는 학생 화면 0개입니다. `💬`·`👀`·`🗣` 라우팅을 보존합니다.

## 캡처·시각화 규칙

- Windows·Codex 조작은 실제 사용자 캡처만 사용합니다. 같은 캡처를 서로 다른 실습에 재사용하지 않습니다.
- `sessions/1주차/screenshot/`의 현재 파일은 인수인계 문서의 자산 판정을 따릅니다. 참고용 파일을 학생 덱에 넣지 않습니다.
- 브라우저 열기→결과→한 가지 수정→새로고침→변경 확인은 실제 브라우저 캡처 대신 HTML 기반 정적 시각화로 설계합니다.
- 생성 이미지 12장은 투명 PNG가 목표지만, `IMAGE_MODE = generate_now | prompt_only`의 정확한 사용자 답을 받기 전에는 생성하거나 프롬프트 시트를 확정하지 않습니다. `prompt_only`면 expected 슬롯·프롬프트/에셋 문서까지만 만들고 배포본은 보류합니다.

## 계획 응답에 반드시 포함할 내용

1. 최종 슬라이드 수와 PART별 슬라이드 수
2. 기존 58장에서 추가·이동·교체되는 슬라이드의 번호 매핑
3. 각 화면 조작의 실제 캡처 파일명, 사용 가능 여부, 결손 캡처
4. HTML 시각화로 구현할 브라우저 흐름의 슬라이드 목록과 구조
5. 정보 모양·레이아웃·element의 다양성 검토 방법
6. 이미지 12장의 목적별 목록과 IMAGE_MODE 게이트
7. 파일별 수정 순서, 검증 명령, 사용자 확인이 필요한 마지막 항목(D2 최종 전문·D5 공통 수정 1개·IMAGE_MODE 포함)

## 구현 시작 허용 조건

계획이 위 조건을 모두 충족하고, 캡처 결손과 IMAGE_MODE 상태를 명확히 보고한 뒤에만 구현을 시작하세요. 이후에도 콘텐츠 문구를 임의로 미화하거나, 보호 파일을 건드리거나, 기존 변경을 되돌리거나, 커밋하지 마세요.
