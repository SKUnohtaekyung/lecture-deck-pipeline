# 새 세션 시작 가이드 — vibecoding-deck

> ⚠️ **아카이브(2026-07-16) — 인수인계용으로 읽지 말 것(스테일).** 이 문서는 3세션 릴레이 빌드(`../세션프롬프트_3세션.md`)의 인수인계 바통이었고, 릴레이 종료 후 갱신되지 않았다. 예: "첫 실전 강의덱 미조립"(실제 2026-07-15 완료), "SKILL.md 8단계"(실제 9단계). **현재 상태·다음 할 일 정본: `.agents/agent-memory/vibecoding-deck/MEMORY.md`의 `## 미해결`.**

> 처음 보는 에이전트 인수인계. 새 대화 첫 지시로 "이 파일부터 읽고 이어서" 하도록.
> **마지막 갱신 시점 = 색 시스템 v2 + v2.1 템플릿 + 규칙 문서화·자동강제 완료.**

## 30초 요약
- **무엇**: 채워진 슬라이드 계획서 → 1280×720 HTML 웹덱(40~50대) 조립 자립형 스킬. 정보 모양 판단으로 레이아웃/차트 선택(좌우분할 쏠림 금지, 코드 시각화 우선).
- **상태**: 작동·검증 완료. **색 시스템 v2(블루·민트·코랄) + v2.1 템플릿 리프레시 + 규칙 문서화·자동강제 완료.** 데모 덱 2종·선택기 HTML 다수.
- **남은 블로커/결정**: ✅ 로고 확정(2026-07-13, V-마크) · 첫 실전 강의덱 미조립. (표지=**라이트 지오메트리 확정**, 2026-07-13 · 아젠다/파트전환=**노드-타임라인·아이소 큐브 클러스터 확정**, 2026-07-14.)

## 먼저 읽을 것 (순서)
1. `CLAUDE.md` — 3층 구조·테스트·컨벤션(프로젝트 매뉴얼)
2. `.claude/agent-memory/vibecoding-deck/MEMORY.md` ★ — 하드윈 버그 + **색 v2 + v2.1 규칙**(배지 split·navy금지·그라데금지·eyebrow태그·헤더 파트진행·페이지네이션). **안 읽으면 규칙 위반.**
3. `_dev/설계기록/색시스템-v2-명세.md` ★ — 색·폼·템플릿 규칙 **정본**(토큰값·배지 split·접근성 대비).
4. `kit/guide/토큰-치트시트.md` — 조립 시 쓰는 토큰/프리미티브(v2 반영됨).
5. `SKILL.md` — 8단계 판단 게이트.
(옛 배경은 `_dev/설계기록/구조-및-빌드-기록.md` P0~P6, 필요할 때만.)

## 지금 완료된 것
- **색 시스템 v2**: 코발트 파생 → `--blue #1D4ED8` / `--mint #14B8A6` / `--coral #F97360` / `--red #DC2626` **전면 개명**. deck.css·patterns.css·전 카탈로그·데모 반영. `var(--navy)` 사용 0.
- **v2.1 템플릿**: 번호·스텝 배지=민트(`.num-circle`/`.work-step .n`/`.pd-dot.is-active`만; 다이어그램/플로우 노드는 블루) · 소프트 벤토 폼(`--r-*`/`--shadow-*`) · eyebrow=태그(pill) · accent-bar 슬림(64×5) · work-step 조임 · **헤더 파트진행(`.s-part`) + 하단 페이지네이션(`.s-pageno`)** (JS 자동주입, divider 기준) · 그라데 금지.
- **규칙 문서화 + 자동강제**: 11규칙을 조립 read-path 문서에 명문화 + `verify_deck.py` **8개 신규검사**(kit·덱 그라데 · `var(--navy)` · 토큰값 · 민트배지 3종 · 블루노드 8종 · 주입존재) + evals 어서션. 이제 규칙 위반 시 verify가 FAIL.
- **데모 덱**: `데모_제작규칙.html`(규칙 설명 덱) · `데모_색시스템v2.html`(색 v2 설명 덱) — 둘 다 v2 색 렌더, verify FAIL 0.
- **선택기/결정 기록 HTML(`_dev/설계기록/탐색-아카이브/`, 2026-07-14 루트에서 이동)**: `바이브코딩_PPT_컬러…10종` · `디자인시스템_선택안` · 표지·아젠다·파트전환 초안들 · 고아 인덱스 `PPT_레이아웃_템플릿_스킬_가이드.html`.
- **스킬 관리**: `.claude/skills/` 구조 + `ui-ux-pro-max` 설치(디자인 근거 DB — 조립·리뷰 시 `python .claude/skills/ui-ux-pro-max/scripts/search.py`).
- 기존: 판단축·카탈로그(레이아웃50/element23)·코드코어·스크립트·회귀 evals.

## 다음 작업 (우선순위)
1. ✅ **로고 확정(2026-07-13)** — V-마크(민트·블루·잉크·26°) 인라인 `<svg class="s-logo">`를 헤더에 반영(patterns.css·deck.css·스타터). 표준자산 `kit/starter/logo.svg`. 데모·catalog×2·표지 1주차 실전/최종·references 스니펫까지 전 파일 스윕 완료(2026-07-14).
2. ✅ **표지 최종 확정(2026-07-13)** — F1 다크네이비 폐기 → **라이트 지오메트리**: 브랜드-화이트 `#F4F8FB` + `>_` 리드(Pretendard) + 92px 제목 + 민트 캐럿 + **아이소메트릭 3단 큐브 스택 + 코랄 점**(모노·결과물 없음). `deck.css` §11 + 스타터 반영, **verify FAIL 0·WARN 0**. 전체 이미지 무드=**"클린 라이트 + 지오메트리 액센트"**. 1주차 실물: `_dev/설계기록/탐색-아카이브/표지_1주차_최종.html`. 상세: MEMORY "표지 v4" · 명세 §6.
3. ✅ **아젠다·파트전환 지오메트리 확정(2026-07-14)** — 아젠다(`s03-slide`)=**노드-타임라인**(팀명 제거, 좌 제목+인트로/우 세로트랙+4노드 블루fill) · 파트전환(`part-divider`)=**아이소 큐브 클러스터**(헤더 전체 제거, 표지와 동일 토큰 9면 큐브+코랄점+중앙 타이틀). `deck.css` §13·§14 + 스타터 반영. `.pd-dot.is-active` 셀렉터명 유지(verify 강제). 외부 CSS 글래스 오탐은 2026-07-16에 `verify_deck.py`가 링크 CSS를 읽도록 수정해 해소. 상세: MEMORY "아젠다·파트전환 v2" · 명세 §6b.
4. **첫 실전 덱 조립** — `_dev/강의자료/` 1주차 자료로 진짜 강의덱 → `verify_deck.py --parts N`. (데모 2종은 메타 문서지 실전 덱 아님)
5. **catalog.html 확장**(코드코어 14 → 71) · **차트 스펙 잔여 대비 팔로업**(밝은 램프 위 텍스트 — 피라미드처럼).
6. (WSL/맥) description 자동최적화(`run_loop.py`).

## 작업 규칙 (v2/v2.1 — MEMORY·명세 상세, verify_deck가 강제)
- 조립 전 **정보모양 분류 → 역인덱스 → 레이아웃+element 따로**. 직전과 같은 구도 회피, split 희소·비연속.
- **오버플로 0**: 세로예산 548px · 본문 22px×1.78≈39px/줄(2줄=78px) · 한글 전각폭=글자수×폰트px.
- **색 문법 v2**: 블루=구조/강조1 · 민트=행동·성공·진행 · 코랄=주의 · 레드=오류 · **민트·코랄 fill 위 흰글자 금지**(`--on-mint`/`--on-coral`). **navy·그라데이션 금지.**
- ⚠️ **배지 split**(가장 흔한 오적용): 민트 배지 = `.num-circle`·`.work-step .n`·`.pd-dot.is-active` **셋만**. 다이어그램/플로우 노드(`.cyc-node .cn`·`.vt-root`·`.rad-hub`·`.rad-sat .tag`·`.flow-step .n`·`.rev-no`·`.quad-card .qn`·`.lc-n`)·`.timing` = **블루**.
- **토큰만**(raw #hex 금지, 흰색 `var(--white)`). 코드 시각화 우선(이미지 아님).
- **변경 후 반드시**: `verify_deck.py`(색·토큰·배지까지 강제) + 브라우저(로컬 http 서버, `file://` 패널 불가). ⚠️ **브라우저는 deck.css를 캐시함** → 링크 `href`에 `?v=`를 붙여 강제 재요청해야 새 CSS 반영. **스크린샷은 폰트 CDN 때문에 타임아웃 잦음 → JS 측정(scrollHeight/computed color) 우선.**

## 멀티에이전트
Workflow/Agent 툴 — 빌드=`general-purpose`(Edit), 검증=`Explore`(읽기전용), effort `high`. `args`는 JSON 값으로(문자열 X). **규칙 대량 갱신 같은 완결성 작업은 2역할(반영 author → 적대적 감사 auditor) 루프**가 효과적(전례: 이번 규칙 강제 패스 — author 3병렬 + auditor 1 + 수정 1). 커스텀 `.claude/agents`는 없음(인라인 프롬프트).
