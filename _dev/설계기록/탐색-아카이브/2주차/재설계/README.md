# 2주차 재설계 15장 — 역할을 다한 중간 산출물 (2026-08-03 아카이브)

> ⚠️ **이 폴더는 더 이상 작업 대상이 아니다. 여기서 시작하지 마라.**
> 2026-07-29 세션이 만든 4구간 9장 + 5구간 6장(총 15장)의 확정본이고, **그 15장은 이미 2주차 덱에 편입됐다.**
> 원래 위치는 `courses/바이브코딩/sessions/2주차/재설계/`였고 2026-08-03에 여기로 옮겼다.
>
> **현재 정본은 `courses/바이브코딩/sessions/2주차/강의덱.초안/`의 shard다.** 이 폴더의 HTML을 고쳐도 덱에 반영되지 않는다.
> 이 폴더의 가치는 «왜 이렇게 만들었나»의 근거(특히 아래 A/B 판정)뿐이다.

## 무엇이 들어 있나

```
재설계/
  4구간/  s01~s09  + samples.css + index.html     AI에게 큰 작업을 맡기는 시스템
  5구간/  s01~s06  + samples.css + index.html     설계한 기준으로 화면 만들기
```

- **한눈에 보기**: 로컬 서버를 띄우고 각 구간의 `index.html`을 연다.
  ```powershell
  python -m http.server
  ```
  → `http://localhost:8000/_dev/설계기록/탐색-아카이브/2주차/재설계/4구간/index.html`
  (아카이브 이동 후에도 kit CSS 상대경로는 6단계 그대로 유효하다 — 2026-08-03 브라우저 실측으로 확인)

- ⚠️ **`file://`로 열지 않는다.** kit CSS가 상대경로라 서버로 열어야 한다.

## 5구간은 A/B 판정 결과를 반영한 것이다

같은 6장을 「개념KB의 `PPT 소재`를 가린 채」(A)와 「회수해서」(B) 두 번 만들어 사용자가 판정했다. **B 5승 · A 1승.**

| 슬라이드 | 채택 | 왜 |
|---|---|---|
| 1 정보 구조 | **B** | 빙산 도해가 「보이는 것 ⊂ 결정한 것」을 즉시 전달 |
| 2 화면 순서 | **B** | 두 장 비교 + 시선 경로로 «왜»를 눈으로 증명 |
| 3 화면 문구 | **A** | 정보 모양이 mapping — **1:1 대응 표가 카드 병렬보다 낫다** |
| 4 가독성·접근성 | **B** | 색각 시뮬레이션이 수치보다 설득력 있다 |
| 5 DESIGN.md | **B** | 전/후 비교 + 메모지가 구체적 |
| 6 디자인 토큰 | **B** | 값→이름→재사용 3단이 방사보다 과정을 보여준다 |

**5구간 CSS는 병합본이다** — B 전체 + A의 `w5-map` 블록. 수정할 때 `w5b-*`와 `w5-map` 두 계열이 함께 있다는 점을 유의한다. 판정 근거 전문: `_dev/설계기록/품질감사-2026-07/P7_ab_criteria.md`.

## ~~다음 세션이 가장 먼저 할 일~~ — 아래 3항 전부 처리됨(2026-08-03)

### 1. 필수 진술 보류 3건 — 2026-07-30 해소 완료

| 문장 | 슬라이드 | 반영 위치 |
|---|---|---|
| 이 구성 목록은 확정된 표준이 아닙니다 — 사람마다 묶는 방식이 다릅니다 | 4-3 | 하단 회색 한 줄 `.pp-std` |
| 두 회사 모두 "단순한 쪽을 먼저 써라"고 권고합니다 — 에이전트가 상위 호환이 아닙니다 | 4-1 | 하단 코랄 띠 `.ax-warn`(기존 문구 교체 — 사유는 집필노트 §2) |
| 여럿에게 나눠 맡기는 구조는 출처마다 분류가 다릅니다 — 하나의 표준 구조가 있는 것이 아닙니다 | 4-8 | 하단 회색 한 줄 `.mt-std` |

`verify_judgement_log.py 2` FAIL 0 · 렌더 실측(1280×720, scale=1) 경계 위반 0 확인. 덱 편입 후 `verify_required_statements.py 2 --deck <덱>.html`로 재검한다.

### 2. ~~덱 편입은 아직 하지 않았다~~ → **편입 완료(2026-08-03 실측 확인)**

이 항목은 2026-07-30 시점 서술이라 낡았다. 그 뒤 전체 재구성·PART4↔5 스왑·수정요청 v2를 거치며 **15장이 모두 덱에 들어갔다.** 「덱·CSS 동결」도 더 이상 없다.

**편입하면서 5구간 클래스 이름이 주차 표기로 통일됐다** — 옛 이름으로 덱을 찾으면 «없다»고 오판한다:

| 원본(이 폴더) | 덱에서 쓰는 이름 | 슬라이드 |
|---|---|---|
| `w5b-berg` | `w2-ia` | `C5-1` 정보 구조 |
| `w5b-cmp` | `w2-cmp` | `C5-2` 화면 순서·정보 우선순위 |
| `w5-map` | `w2-map` | `C5-3` 화면 문구 *(A/B에서 A가 이긴 장)* |
| `w5b-a11y` | `w2-a11y` | `C5-4` 기본 가독성·접근성 |
| `w5b-memo` | `w2-memo` | `C5-5` 화면 기준을 담은 design.md |
| `w5b-chain` | `w2-chain` | `C5-6` 디자인 시스템 |

4구간은 이름 그대로 들어갔다(`w2-axis`·`w2-pipe`·`w2-figure`·`w2-quad`·`w2-dialog`). 나머지 3종(`w2-gantt`·`w2-branch`·`w2-metric`)은 **이후 수정요청에서 그 슬라이드가 삭제·재구현되며 사라졌고**, 계약에 남아 있던 죽은 등재 11종은 2026-08-03에 정리했다(44종 → 33종).

### 3. 근거는 집필노트에 있다

`courses/바이브코딩/sessions/2주차/자료/2주차_콘텐츠_집필노트.md` — 출처 추적표(판단 5분류)와 재료 처분표. **이 15장은 초안을 거치지 않은 ⓑ 경로 산출물**이라 이 파일이 유일한 근거 기록이다.

## 지켜야 할 것

- ~~**1주차는 수정 금지**(동결)~~ — 1주차 동결은 2026-07-26 사용자 결정으로 해제됐다. 2026-08-03에 1주차 kit CSS 404도 고쳤다(`b8119b0`).
- 슬라이드를 고치면 **반드시 브라우저로 렌더해 눈으로 본다.** 정적 통과는 완료가 아니다.
- 강조색(노랑 등)은 **덱 전체의 2% 이내**에서만, `palette-x` 표식과 함께(R-COLOR-06). 현재 5-5 메모지 1장이 그 예산을 쓰고 있다.
- 새 슬라이드를 만들 때는 **결정표를 먼저 채운다**(정보 모양 → 역인덱스 → 레이아웃·element 분리). 각 구간 폴더의 원본 결정표는 `_dev/설계기록/품질감사-2026-07/samples_v5A|v5B/DECISION.md`에 있다.
