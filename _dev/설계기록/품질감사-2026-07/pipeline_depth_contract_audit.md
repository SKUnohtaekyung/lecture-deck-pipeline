> 이 문서는 2026-07-27 감사 에이전트의 반환 내용을 오케스트레이터가 복원한 것이다. 원 에이전트가 파일을 쓰지 않아 대화에만 존재하던 결과를 보존한 것이며, 인용은 표본 검증됐다.

## §A 깊이 손실 추적 (12개 고영향 행)
| 교육 소재 항목 | 리서치 산출 | 콘텐츠 소비 | 검토 확인 | 근거(file:line) | 구멍 유형 |
|---|---|---|---|---|---|
| 왜 필요한가(동기) | 아니오 | 아니오(R-COMP-01 요소 목록에 없음) | 아니오 | `skills/리서치/references/chunk-schema.md:52-68`(필드 없음), `skills/콘텐츠/SKILL.md:40` | **산출 의무 없음** — 리서치 청크에 field 자체가 없다 |
| 흔한 오해→교정 | 예(≥2, 코어 필수) | 예(grep 회수) | 부분(자료검토엔 명시 없음) | `chunk-schema.md:64-65,86`, `skills/콘텐츠/SKILL.md:55` | 검토 확인 약함 |
| 나쁜 예(안티패턴) | 아니오(워크드예시=정답만) | 아니오 | 아니오 | `chunk-schema.md:59` | **산출 의무 없음** |
| 좋은 예(워크드예시) | 예(≥1) | 예 | 예(청크↔초안 인용대조) | `chunk-schema.md:59,86`, `skills/검토/SKILL.md:105` | 완비 |
| 비교 대상 | 조건부(G8 3필드 중 2/3만 P0 의무) | 예 | 예(G8) | `skills/리서치/SKILL.md:94`, `검토/SKILL.md:69` | 개념별 보장 아님 |
| 생활 비유 | 예(≥2) | 예 | 예(R-ACC-01 한계표시) | `chunk-schema.md:57-58,86`, `콘텐츠/SKILL.md:41` | 완비 |
| 실제 사례(+가상 사례) | 조건부(G8 택2, 주차 바닥선 ≥3청크뿐) | 예 | 예(G8 바닥선) | `리서치/SKILL.md:95`, `chunk-schema.md:61` | 개념별 보장 아님, 주차 총량만 |
| 적용 판단 기준(언제 쓰나) | 아니오 | 아니오 | 아니오 | `콘텐츠/SKILL.md:40`(요소 목록에 부재) | **산출 의무 없음** |
| 예상 질문(핵심개념 FAQ) | G9는 인접 개념만 — 코어 개념 자체 FAQ는 대상 아님 | G9만 멘트 라우팅 | G9만 검사 | `리서치/SKILL.md:100-115`, `검토/SKILL.md:71,106` | 코어 FAQ는 **범위 밖** |
| 한계·주의점 | 조건부(G8 축D, 택2 중 하나일 수 있음) | 예 | 예(G8) | `리서치/SKILL.md:92` | 개념별 보장 아님 |
| 강사 시연 스크립트 | 아니오 | 아이콘 `👀` 라우팅만 | 아니오 | `콘텐츠/SKILL.md:96` | 라우팅만, **시연 구성요소(준비물·기대결과·실패시 대응) 미정의** |
| 실습 완료 기준 | 예(파일4 ⑥) | 반영 | 간접(R-QC-09 사람검토 강등) | `chunk-schema.md:102` | 완비(실습 한정) |

## §B 개념 묶음 단위 부재 — 검증과 계약 스케치
검증: `verify_draft_quality.py`·`verify_deck_quality.py`·`verify_deck.py` 어디에도 cluster/묶음 그룹핑 로직 0건(grep). 유일한 다중 슬라이드 단위 `data-series`(`verify_deck.py:1347-1400`)는 레이아웃 family 반복 예외용이며 콘텐츠·초안 단계엔 없다. 커리큘럼 기준안 원칙 4(`sessions/바이브코딩_커리큘럼_기준안.md:162-200`)가 개념별 깊이 등급을 이미 정의하나, `kit/guide/교육원칙-요약.md:79-81`이 이를 "과소비 금지" 방향으로만 요약했고 확장 방향이 없다. 그 요약본은 create-slides read-path에만 있고(`SKILL.md:41`) 리서치·콘텐츠 read-path엔 없다 — 골격을 짜는 단계에 깊이 판단 재료가 도달하지 않는다.
계약 스케치(최소 변경): 리서치 = 청크 메타에 `깊이:클러스터|단일` 선택 필드, 클러스터 지정 청크만 G8 P0 3필드 전량 요구 / 콘텐츠 = 3단계 골격설계(`SKILL.md:81-86`)에 클러스터 체크 추가, **그룹핑 키는 새 문법 없이 기존 `<!-- refs: C-… -->` 재사용**(연속 행이 같은 C-슬러그를 참조하면 그것이 클러스터) / create-slides = 같은 클러스터에 `data-series="C-슬러그"` 부여(기존 메커니즘 재사용, 새 검증 코드 불필요) / 검토 = "클러스터 역할 슬롯 실현 대조" 신설(`검토/SKILL.md:108` PPT소재 대조와 같은 패턴).

## §C 시간 예산 부재
교육 시간 필드는 실습(파일4 ⑦ `chunk-schema.md:101`) 하나뿐. 개념설명·시연 시간, 교시 합계 대조는 전무(`콘텐츠/SKILL.md`·`검토/SKILL.md`·verify 3종 grep 0건). R-PLAN-01은 "역산 금지"만 규정하고 상향 검증이 없다.
최소 추가안: 콘텐츠 3단계 골격 표에 선택 열 `예상 소요(분)`(범위 표기 허용), 검토가 교시별 합계를 배정 시간 대비 **WARN**으로 보고. `verify_draft_quality.py` R-QD-08 후보이나 **이번 범위에서 구현 착수 금지** — 숫자가 목표로 오독될 위험이 커 사람 검토 항목으로 먼저 시작할 것.

## §D 스킬별 판정
| 스킬 | 현재 책임 | 결손 | 판정 | 최소 변경안 | 위험 |
|---|---|---|---|---|---|
| 리서치 | G8·G9 청크 생산, 최소깊이 강제 | 왜필요한가·적용기준·나쁜예 필드 없음, 코어 FAQ 미대상, G8 P0가 3중2만 | **수정** | 클러스터 지정 청크만 G8 전량화 + `깊이:` 메타 | §0.5 예산 브레이크와 충돌 — P0·클러스터 한정으로 좁힐 것 |
| 콘텐츠 | 슬라이드 단위 밀도·R-DENS-02·R-ANNEX-01·R-COMP-01 | 슬라이드가 유일 단위, 클러스터 계획 단계 없음, 원칙4 read-path 밖 | **수정** | 3단계에 클러스터 체크, 원칙4를 read-path에 추가 | §0-1 "임의 분할 금지"와 표현 충돌 우려 |
| create-slides | 초안 충실 렌더링, R-OWN-01/02, family 다양성 | 클러스터 시각 응집 신호 없음 | **이미 해결됨(재사용만)** | `data-series` 안내 1줄 | 낮음 — R-OWN 유지 |
| 검토 | G8/G9 자료검토, PPT소재 실현대조, 화면단독완결성 | 클러스터 역할 슬롯·시간예산·시연 내용 미확인 | **수정** | "클러스터 역할 슬롯 실현 대조" 신설 | 읽기전용이므로 결손 재확인 루프 필요 |

## §E 새 목표와 충돌하는 규칙
| 규칙 | 위치 | 충돌 | 조치안 |
|---|---|---|---|
| deck.contract.json 슬라이드 수 정확 일치 | `sessions/2주차/deck.contract.json:6`(⚠️ 정정: 원 인용 `:5`는 `"강의덱": {` 행이며, `"slides": 107` 필드는 6행이다), `verify_deck.py:1090-1094` | 확장마다 계약 수정 마찰 | 코드 변경 불필요 — "계약 수치는 목표가 아니라 현재값 로그"임을 명문화 |
| 임의 분할 금지 | `skills/콘텐츠/SKILL.md:25-26` | 물리한도 분할과 계획된 다중슬라이드가 문면상 구분 안 됨 | 후자는 게이트B 승인 대상이라 "임의"가 아님을 명시 |
| R-QD-04 교시당 8~11장 | `verify_draft_quality.py:343-358`, `콘텐츠/SKILL.md:83` | 8~11이 깊이설계와 무관한 옛 관례치 | 8~11을 기본값으로 재정의, `깊이설계:예` 선언 시 상한 스킵. FAIL 조건을 "노트 파일 존재"에서 "해당 교시를 언급하는 근거 문단 존재"로 강화 |
| CORE/FLEX/REC 애드혹 분류 | `2주차_최종수정계획.md:13,16`(스킬 정본 아님) | 시간예산에 맞춰 필수 깊이를 얕게 만드는 패턴 제도화 | 향후 주차에 이식 금지를 인수인계에 명기 |
| tests/test_quality_gates.py 냉동 fixture | `tests/test_quality_gates.py:20-22`(⚠️ 정정: 원 인용 `:18-19`는 `FIXTURES` 경로 상수 선언과 공백 줄이다. "동결"·"스냅샷" 주석이 실제로 있는 곳은 20~22행의 `W1_DECK`/`W2_FROZEN_DECK`/`W2_FROZEN_ROOT` 정의다) | **충돌 아님** — 냉동 스냅샷 사용 | 조치 불필요 |

## §F 유지할 것
R-PLAN-01/02 · R-DENS-02 · R-ANNEX-01 · R-COMP-01 · R-ACC-01(`콘텐츠/SKILL.md:36-41`) 전부 새 목표와 정합하며 클러스터 계약의 토대. G8·G9 유지. "PPT 소재 처분표"와 검토의 "PPT 소재 실현 대조"·"노트 없이 화면만으로 이해 가능한가" 유지. `data-series` 예외 메커니즘 유지.

## §G 하지 말 것 (감사자 경고)
1. 개념당 고정 슬라이드 수 하한 신설 금지 — R-PLAN-01이 막은 안티패턴의 클러스터 레벨 반복. 대신 "역할 슬롯 완비 여부"로 판정.
2. 원칙4·G8 필드를 전 청크로 일괄 확대 금지 — §0.5 실행예산과 정면 충돌. 클러스터 지정 청크로 범위 한정.
3. 이 감사를 시각 품질 remediation과 같은 게이트로 병합 금지 — 시각 밀도와 교육 깊이는 다른 축. 같은 슬라이드가 "화면은 빽빽한데 개념은 얕을" 수 있어 합치면 둘 다 못 잰다.
