# AGENTS.md — create-slides 공통 작업 매뉴얼

> 구 명칭 `/vibecoding-deck`은 2026-07에 `/create-slides`로 개명됐다(DEC-01: 호환 alias 없음).

이 저장소는 **`create-slides` 스킬을 개발·보관**하는 프로젝트다. 루트 `SKILL.md`와 `kit/`·`references/`·`scripts/`가 플랫폼 공통 정본이고, `.agents/`·`.claude/`는 그 정본을 로드하는 얇은 어댑터다. Codex는 이 파일을 직접 읽고, Claude Code는 `CLAUDE.md`의 `@AGENTS.md` import로 같은 규칙을 읽는다.

**이 파일에 두지 않는 것** — 각각 정본이 따로 있다:

| 성격 | 정본 |
|---|---|
| 변하는 현재 상태·다음 할 일 | `.agents/agent-memory/create-slides/MEMORY.md`의 `## 미해결` |
| 덱·kit 검증 명령과 사고 이력 | `references/검증-명령-지도.md` |
| 진행 중인 작업 계획 | `plans/<주제>/` |
| 작업 기록·변경 로그 | git |

계속 유효한 것만 남긴다. 관련 작업을 마칠 때 해결된 항목은 **삭제한다**(취소선·완료 표시로 보존하지 않는다).

## 완료의 정의

1. 요청한 결과가 구현됐다.
2. 관련 검증을 실행했고 **종료코드를 확인**했다.
3. `git status --short`에 요청 범위 밖 변경이 없다.
4. 보존 조건(동결 산출물·기존 계약)을 확인했다.
5. 보고에 **실행한 명령과 실제 결과**, 그리고 **실행하지 못한 검증과 그 이유**를 적었다.

- 확인하지 않은 명령·경로·결과를 사실처럼 쓰지 않는다. 통과하지 않은 것을 통과로 보고하지 않는다.
- 기존 사용자 변경을 보존하고, 무관한 파일을 되돌리거나 정리하지 않는다.

## 구조 (①②③=스킬 3층 · ④=작업물 · ⑤=팀 스킬)

| 층 | 무엇 | 배포 | 규약 정본 |
|---|---|---|---|
| ① 포터블 스킬 | 루트 `SKILL.md` · `kit/` · `references/` · `scripts/` · `입력양식/` · `outputs/` · `데모_제작규칙.html` | **대상** | 루트 `SKILL.md` |
| ② 개발자료 | `_dev/설계기록/`(빌드·결정 기록 · `탐색-아카이브/` 미채택본 · `품질감사-2026-07/` 품질 게이트 임계값의 도출 근거) | 제외 | 그 폴더 README |
| ③ 플랫폼 어댑터 | `.agents/skills/`(Codex) · `.claude/skills/`(Claude) — 규칙을 복제하지 않고 정본만 로드 | 제외 | `.agents/README.md` |
| ④ 주차별 작업물 | `courses/<과목>/sessions/N주차/` — `N주차_초안.md` · `강의덱.html` · `강의덱_발표자노트.html` · `자료/` | 제외 | `sessions/README.md` |
| ⑤ 팀 워크플로 스킬 | `skills/` — 역할 2종(리서치·검토) + 횡단 프로토콜(하네스) | 제외 | `skills/README.md` |

- **누적 규칙·버그 정본은 `.agents/agent-memory/create-slides/MEMORY.md` 하나**이며 양 플랫폼이 함께 읽는다(`.claude/agent-memory/`는 포인터). `ui-ux-pro-max`만 각 플랫폼 위치에 실행 자원과 함께 둔다.
- **1주차 현행 산출물은 2026-07-26 사용자 결정으로 동결** — 수정·재조립하지 않는다. **동결 대상은 산출물(덱·배포본·발표본·노트·자료)이며, `deck.contract.json`은 검증 메타데이터로 동결에서 제외한다**(2026-08-17 사용자 결정 — waiver 등재·정합 목적의 편집만 허용). 구경로 `sessions/N주차`도 `scripts/_course_paths.py`가 계속 인식한다(동결 자료가 참조하므로 **폴백 제거 금지**). 새 주차는 `sessions/_template/` 복사.
- `_dev/강의자료/`와 `콘텐츠` 스킬은 폐기됐다. **다만 사람이 초안을 써서 넘기는 경로와 스키마(`입력양식/콘텐츠초안템플릿.md`·`references/콘텐츠초안-입력형식.md`)는 살아 있다** — 폐기 경위는 `skills/README.md`.
- git으로 관리한다(기본 브랜치 `main`). 의미 있는 변경은 verify 통과 후 커밋해 복구 지점을 만든다. 대규모 정리·대량 갱신은 먼저 영향 범위를 확인한다.

## 작업 전 필수 읽기

1. `.agents/agent-memory/create-slides/MEMORY.md`의 `## 미해결` — **무조건**. 나머지 절(누적 규칙·하드윈 버그·색 시스템)은 덱 조립·규칙 변경 작업일 때. 작업을 끝내기 전에 같은 파일을 다시 확인해, 해결된 항목과 더는 유효하지 않은 내용을 **삭제**한다.
2. `skills/README.md` — 팀 스킬(`/리서치`·`/검토`·`/하네스`)을 **호출할 때** 계약표·파이프라인 지도·명시 호출 규약.
3. 커리큘럼 — `courses/<과목>/커리큘럼_기준안.md`(전 주차 상위 기준)와, 있으면 `courses/<과목>/sessions/N주차/N주차_강의안설계.md`(주차 상세). **해당 주차 파일을 열거나 쓰기 전에 그 주차 것만** 읽는다. 주차 상세가 없는 주차가 있다(현재 1주차만 존재) — 없으면 커리큘럼 개요에서 역산하고 그것이 '제안'임을 밝힌다.
4. 루트 `SKILL.md` — 덱 조립·리뷰 작업일 때. 그 안의 read-path를 순서대로 따른다.
5. `references/검증-명령-지도.md` — 덱·kit 작업의 실행·검증 명령과 사고 이력.
6. `sessions/README.md` — 새 주차 폴더를 만들 때.
7. `_dev/설계기록/구조-및-빌드-기록.md` — 배경이 필요할 때.

## 무엇이 기계로 강제되는가

**자연어는 설명, 설정은 강제, git은 최종 증거.** 아래는 문서가 아니라 코드가 막는 것들이다. 「규칙 파일에 적었으니 지켜지겠지」로 넘기지 마라 — 2026-07-28에 규칙 파일에 다 있던 7항 점검이 통째로 건너뛰어졌고, 그래서 이 층이 생겼다.

| 규칙 | Claude Code | Codex | git(공통) |
|---|---|---|---|
| 슬라이드 사전 점검 7항 주입 | PreToolUse `checklist` | **경로 키 없음 — 이식 불가**(어댑터 필요) | — |
| 과목 지침 주입 | PreToolUse `course` | **경로 키 없음 — 이식 불가**(어댑터 필요) | — |
| R-QC-14 CSS 회귀 | PostToolUse `css-lint` | **경로 키 없음 — 이식 불가**(어댑터 필요) | **pre-commit 차단** |
| 생성물(`강의덱.html`) 직접 편집 | PreToolUse `generated-guard` **(관측)** | **경로 키 없음 — 이식 불가**(`apply_patch` 헤더 파싱 필요) | **pre-commit 차단**(shard 동반 확인) |
| 저장소 밖 쓰기 | PreToolUse `tmp-guard` **(관측)** | **경로 키 없음 — 이식 불가**(셸 문자열 파싱 필요) | — |
| kit 정합 · 선언↔집행 | — | — | **pre-commit 차단** |
| 스킬 어댑터 정합 | — | — | **pre-commit 차단** |
| 발표자노트 회귀 | — | — | **pre-commit 차단** |
| 리서치 워커 도구·모델 | `.claude/agents/*.md`(tools·model·maxTurns) | `.codex/agents/*.toml`(`sandbox_mode="read-only"`) | — |

- **(관측)** 은 아직 차단하지 않고 경고만 낸다는 뜻이다. 오탐률을 먼저 재고 0에 수렴할 때 `--enforce`로 승격한다 — 검사 범위를 넓힐 때 오탐률을 함께 재는 것은 이 프로젝트의 규율이다.
- **오탐만 재면 반쪽이다 — 미탐(false negative)도 함께 잰다**(2026-08-23 신설). 「몇 건을 잘못 잡았나」와 함께 **「몇 건을 보지도 못했나」**를 세어 판정 문구에 찍는다. 오탐은 사람이 시끄러워서 발견하지만 미탐은 **PASS로 위장하므로 아무도 발견하지 못한다.** 실측: `verify_deck`의 폰트 22px 하한 린트가 셀렉터 이름(`.s-body`·`.s-lead`)에 의존해 **저장소의 덱 4개 전부에서 판정 0건**이었고(미판정 32·360·51·50), 그동안의 PASS는 «위반 없음»이 아니라 «아무것도 안 봄»이었다. 판정 범위를 넓힐 수 없을 때는 **못 본 개수를 밝히는 것**이 최소 의무다.
- 설치: `python scripts/install_hooks.py` (`core.hooksPath=.githooks`). 상세는 `.githooks/README.md`.
- 탈출구를 숨기지 않는다: `git commit --no-verify` · `SKIP_DECK_GATES=1` · `DECK_EMERGENCY_EDIT=1`. 목표는 방탄이 아니라 잊어버림 방지다. 빠져나갔으면 같은 내용을 shard·정본에 반영한다.
- **Codex 열은 Gate 0 실측(2026-08-18)으로 채웠다.** 훅 자체는 발화한다(`SessionStart`·`PreToolUse`·`PostToolUse` 실측 확인). 막는 것은 배선이 아니라 **페이로드 스키마**다 — Codex의 `PreToolUse`에는 Claude Code의 `tool_input.file_path`에 대응하는 **파일 경로 키가 없고**, 경로가 `Bash`의 셸 문자열이나 `apply_patch`의 패치 헤더 **안에 문자열로 묻혀 있다.** 그래서 경로 기반 훅 4종은 파싱 어댑터 없이는 이식되지 않는다. 그때까지 Codex 세션의 강제는 **git 층뿐**이다 — 「동등」이 아니라 **「구조적 비대칭」**이다(종전 서술 「지연된 동등」은 실측으로 폐기). 실측 상세는 `MEMORY.md` 「지침 생태계 리팩터링」, 계획은 `plans/instruction-refactor/`.

## 플랫폼별 Skill 호출

- **Codex**: `.agents/skills/create-slides/SKILL.md`가 발견된다. `$create-slides` 또는 PPT·강의덱·HTML 웹덱 제작 요청으로 자동 호출.
- **Claude Code**: `.claude/skills/create-slides/SKILL.md`가 발견된다. `/create-slides` 또는 같은 자연어 요청으로 자동 호출.
- 두 어댑터는 반드시 루트 `SKILL.md`를 완전히 읽고, 그 안의 상대 경로를 **저장소 루트 기준**으로 해석한다. 어댑터에 디자인 규칙이나 워크플로를 복제하지 않는다.
- 팀 스킬 2종(`/리서치`·`/검토`)은 **명시 호출 전용**이다(정의는 `skills/README.md`가 정본). create-slides만 자동 발동을 유지한다. 파이프라인: `/리서치` → `/create-slides`(집필+조립 · 초안이 있으면 ⓐ, 없으면 ⓑ) → `/검토`(횡단).

## 스킬의 역할

채워진 콘텐츠 초안(교시별 표·본문·강사 멘트)을 1280×720 HTML 웹덱으로 조립한다. 대상은 **코딩 경험이 없는 입문자 중심의 혼합군**이다. 정보 모양을 먼저 판단해 레이아웃과 차트를 고르고 좌우분할 한 구도로 쏠리지 않게 하며, 이미지보다 코드 시각화를 우선한다. **초안의 제목과 본문을 존중하며 무단 재작성·분할하지 않는다.** 강사 멘트가 있으면 발표자 노트 HTML도 별도로 만든다.

## 불변 규칙과 파일 지도

**값을 여기 복제하지 않는다** — 요약본이 정본과 어긋나는 것이 이 프로젝트의 반복 사고였다. 정본을 직접 열어라.

| 영역 | 정본 | 규칙 ID |
|---|---|---|
| 색·폼 | `kit/guide/디자인시스템.md` (배경: `_dev/설계기록/색시스템-v2-명세.md`) | R-COLOR-01~05 · R-EMPH-01 · R-TERM-01 |
| 타이포·토큰·계산식 | `kit/guide/토큰-치트시트.md` | R-TYPE-01~03 |
| 레이아웃 선택 순서 | 루트 `SKILL.md` | R-LAYOUT-01·02 |
| 정보 모양 판단축 | `kit/guide/정보모양-taxonomy.md` | — |
| 크기·간격·자리 | `kit/guide/한장-참조표.md` | — |
| 카탈로그 규칙 | `kit/guide/카탈로그-규격.md` | — |
| 조립 리듬(배지+텍스트 정렬 등) | `references/조립-리듬-불변요소.md` | — |
| 누적 규칙·반복 버그 | `.agents/agent-memory/create-slides/MEMORY.md` | — |
| 레이아웃 후보 | `kit/layouts/` — `families/` 8파일에 50개 항목 + `by-shape.md` + `catalog.html` | — |
| 차트·다이어그램 element | `kit/charts/` — 21개 element + `by-shape.md` + `catalog.html` | — |
| 코드 코어 · 스타터 | `kit/styles/{patterns,deck}.css` · `kit/starter/` | — |

- 어긋나면 정본을 따른다. 정본을 고쳤으면 **집행부(`kit` CSS·verify 스크립트)도 고쳤는지** 확인한다 — `python scripts/verify_declared_vs_enforced.py`.
- **불일치를 없애려고 문서 쪽 값을 낮추지 마라.** 규칙을 현실에 맞춰 깎는 것이다.

## 실행과 검증

**명령 전체와 사고 이력은 `references/검증-명령-지도.md`가 정본이다.** 여기에 복제하지 않는다 — 덱·kit 작업을 시작하기 전에 그 파일을 연다. (조립 §1 · 덱 검증과 러너 §2 · 브라우저 렌더 감사 §3 · 스킬·킷 §4 · 파이프라인 문서 §5 · 회귀 10모듈 §6 · 배포·이미지 §7 · 사고 이력 §8)

넘어가기 쉬운 세 가지만 여기 남긴다:

- ⚠️ **종료코드가 통과했다고 화면이 멀쩡한 것이 아니다.** 조립 후에는 브라우저에서 전수 측정한다(지도 §3).
- ⚠️ **회귀는 전 모듈을 다 돌린다**(목록·개수 정본은 지도 §6). 일부만 돌리면 나머지 게이트가 검증되지 않는다.
- ⚠️ **덱을 고쳤으면 `run_deck_checks.py <주차>`를 돌린다.** 렌더 증거가 없거나 낡았으면 exit 1이다.

## 공통 작업 방식

- 텍스트와 파일 탐색은 `rg`/`rg --files`를 우선한다. Windows에서는 `python`을 쓴다.
- **임시·중간 파일은 저장소 안 `tmp/`에만 만든다.** 시스템 임시 폴더(`%TEMP%`·`AppData`·플랫폼 스크래치패드)에 두지 않는다. `tmp/`는 이미 `.gitignore`에 있다.
  - **서브에이전트에게도 이 제약을 프롬프트에 명시해 전달한다.** Claude Code의 built-in `Explore`·`Plan`은 **`CLAUDE.md`를 읽지 않으므로**(공식 동작), 적어 보내지 않으면 그 워커에게 이 규칙은 존재하지 않는다. 2026-07-30에 워커 4개가 전부 시스템 임시 폴더에 썼다.
  - 작업 종료 시 밖에 남은 파일이 **0개인지 세어 확인한다.** 옮기는 것은 정리가 아니다.
- 구현 요청은 수정 후 정적 검증과 가능한 범위의 브라우저 측정을 수행한다.
- **컨텍스트 예산 — 창에 넣은 것은 이후 모든 호출에 다시 청구된다**(비용의 70% 이상). 하는 일을 줄이는 규칙이 아니라 **같은 일을 더 적은 토큰으로 하는 규칙**이다. 실측 근거·반사실 계산은 `_dev/설계기록/토큰감사-2026-08-06.md`.
  - **Bash 출력을 통째로 받지 않는다** — 종료코드만 필요하면 `>/dev/null 2>&1; echo $?`, 목록·로그는 `| tail -N`, 검사기는 판정 줄만 `| grep -E "RESULT|FAIL"`.
  - **한 파일에서 300줄 넘게 읽어야 하면 워커에게 맡긴다** — 「무엇을 판단하려는지」를 주고 3천 자 이내 반환을 요구한다. 예외는 사용자가 전문 정독을 지시한 문서와 지금 편집할 파일이며, 예외를 쓰면 보고에 적는다(상세 `skills/하네스/SKILL.md` §6-1).
  - **결정을 담지 않는 턴을 만들지 않는다** — 「대기 중」류 상태만 담은 응답 대신 다음 작업을 진행한다. **줄이는 대상은 「내용 없는 말」이지 「보고」가 아니다** — 판정·근거·수치·다음 행동은 그대로 낸다.
  - **긴 중단 뒤에는 새 세션으로 시작한다** — 캐시 TTL(1시간) 만료 후 재개하면 창 전량이 12.5배 단가로 재생성된다. 창이 250k를 넘으면 같은 이유로 분리하고 인계는 `MEMORY.md` `## 미해결` + `plans/<주제>/`로 한다.
  - 검사: `python scripts/audit_context_budget.py <세션ID>` (T1~T4·TS·T6 · 위반 시 종료코드 1 · `--list`로 목록).
- **하네스 — 규모에 따라 고른다.** 단일 파일·사소한 편집·조회, 그리고 **판단이 무겁고 토큰이 가벼운 문서 정밀 편집은 solo**로 한다. 다파일 동시 변경·독립 구간이 큰 작업·대규모(계약 재설계, 40장+ 덱, 여러 파일에 걸친 규칙 변경)일 때 하네스를 건다. **의심스러우면 한 단계 낮춘다.**
  - ⚠️ **비용 절감 도구가 아니다** — 품질·컨텍스트 보존 도구다. 워커는 메인과 컨텍스트를 공유하지 않아 같은 파일을 각자 다시 읽고 총 토큰이 늘 수 있다(실측: 비용의 72%가 워커가 아니라 뚱뚱한 메인 창의 재독이었다).
  - 스케일 사다리·역할 계약·게이트 기준·워커 규율·안티패턴 정본: `skills/하네스/SKILL.md`(`/하네스`·`$하네스`).
- 웹 리서치가 필요한 사실은 검색 후 원문 출처를 확인한다. 외부 쓰기·게시·전송은 사용자 요청 범위를 넘겨 실행하지 않는다.

## 과목 프로필

- **스킬 본문에는 과목 고유 값을 두지 않는다.** 강의명·대상·관통 문장·회차 구조·장수/밀도 기준선·표현 취향·브랜드·커리큘럼 정본 경로는 전부 `courses/<과목>/profile.md`에 있다(스키마 `입력양식/과목프로필템플릿.md`). 검사: `python scripts/verify_subject_isolation.py`.
- **한 과목의 실측치를 다른 과목의 규칙으로 물려주지 않는다.** 기준선이 없는 새 과목은 FAIL이 아니라 **WARN으로 시작**한다.
- 사고 이력·실측치는 규칙의 **근거**이므로 스킬에 남긴다 — 격리 대상은 운영 파라미터뿐이다.
