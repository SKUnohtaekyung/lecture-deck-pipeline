# 렌더 비교 — 2차시 새 덱 vs 원본 2주차 덱 (오케스트레이터 실측 · 2026-09-11)

> 판정 원칙은 `렌더비교_1차시.md`와 같다. 짝짓기는 **ID 대응**으로만 한다(`렌더기준_원본2·3주차.md` 규칙 4 — 건수로 짝짓지 않는다). 2차시 새 덱은 보존 슬라이드의 `data-slide`에 원본 ID를 그대로 쓴다.

## 측정 조건

| 항목 | 새 2차시 | 원본 2주차(기준선) |
|---|---|---|
| 경로 | `courses/AI_코딩_에이전트_입문_3차시/sessions/2주차/강의덱.html`(sha256 앞 8자 `37e2bf9c`) | `courses/바이브코딩/sessions/2주차/강의덱.html` |
| 조건 | 서버 `ac3-render-audit` · 1280×720 · scale 1 · 77장 · 깨진 이미지 0 | 같은 조건 · 112장 · 현행 kit(`렌더기준_원본2·3주차.md`) |
| 증거 | 수신기 → `courses/…3차시/sessions/_verify/2주차/deck-audit.json`(slideCount 77) | 저장 없음 |
| 화면 DOM | 디바이더 `PART 1/8`~`8/8` 정상 · 렌더 본문 «2주차»·«교시»·Codex·ChatGPT·GPT **0** | — |

## 총계와 분류

| 결함 | 새 77 | 원본 112 | 새 덱 슬라이드 | 분류 |
|---|---:|---:|---|---|
| below 바닥선초과 | 14 | 0 | `N-14` 8 · `N-10` 3 · `A3F2` 3(`TH/TD@694` 비교표) | **A** 전부 |
| off 슬라이드이탈 | 4 | 0 | `N-14`(`P@이탈750`) | **A** |
| lap 겹침 | 6 | 2 | `C1-1` 4(`c11-tag×IMG`) · `C4-H1` 2(`s-title×IMG`·`hm-lead×IMG`) | `C1-1` **A** · `C4-H1` **B**(원본 `C4-H1` lap 2 · 원본 계약 `render_lap`) |
| wb 어절잘림 | 1 | 0 | `M1`(«GENT.m/d 준비») | **A** |
| wideEmpty | 2 | 4 | `C4-12`(`ns-foot@100w`) · `C4-N2`(`c4n2-row×3@100w`) | **B**(원본 같은 ID · 원본 계약 `render_wideEmpty`) |
| stretchX | 2 | 6 | `C4-12` · `C4-13`(`rt-sym×4@63w`) | **B**(원본 같은 ID · 원본 계약 `render_stretchX`) |
| lowFill | 1 | 1 | `N-04`(`w2-prompt terminal-dark`) | **A**(신규 슬라이드 · 원본 lowFill은 `C5-4`) |
| ovf·slots·occl·stretchY·hollow | 0 | 0 | — | — |
| 폰트하한 위반(WARN) | 91 | 182 | — | 래칫 등재 대상 |

- **B 확정 조건**: 원본 계약 `courses/바이브코딩/sessions/2주차/deck.contract.json`의 `known_violations`가 같은 ID를 이미 적시한다(`매핑_2차시.md` §⑧). 다만 규칙 4에 따라 **수정 담당이 소스에서 해당 요소(`ns-foot`·`c4n2-row`·`rt-sym`·`s-title`/`hm-lead`)가 원본과 같은 모양(문구 길이·요소 수)인지** 대조한 뒤 등재한다. 문구가 바뀌어 모양이 달라졌으면 A로 돌린다.

## 러너 `run_deck_checks 2주차`(exit 1 · 10단계 FAIL)

- 렌더 결함 7종 FAIL(위 표) · 구조 WARN 래칫 0→1 · 품질 WARN 4 미등재 · 렌더 WARN 97(폰트하한 91 + 자간 5 + 정렬 1) 미등재.
- PASS: 정적 게이트 · 내용 품질(기본 모드) · 노트 · 제목 생존 · 초안 동기화 · 보고 신선도 등.

## 품질 `--strict` FAIL 4 — 원본 동일 확인(오케스트레이터 실행)

| 규칙 | 새 2차시 슬라이드 | 원본 2주차에서도 걸리는 같은 ID |
|---|---|---|
| R-QC-03 근-빈 컨테이너 | `C1-14` · `C4-1` · `C4-3` | 전부 포함(원본은 +`C1-6`·`C5-4`) |
| R-QC-15 하단 결론 콜아웃 비율 | 30.6%(11/36) | 원본 19.7%(13/66) — 비율 지표(슬라이드 수가 줄어 상승) |
| R-QC-16 박스 기반 비율 | `S02B`·`C1-12`·`C4-5`·`C3-N4`·`C4-15`·`C4-N4` | `S02B`·`C1-12`·`C4-N4`·`C3-N4`·`C4-5` 포함(+외 1건) |
| R-QC-17 실습 완결성 | `C4-11` · `C6-N4` · `C6-N3` | 전부 포함 |

→ 러너 기본 모드에서는 WARN(상시 FAIL 규칙 아님) → **품질 WARN 래칫에 분해와 함께 등재**. 신규 22장은 해당 없음.

## 조립 보고의 결정요청에 대한 오케스트레이터 판정

| 항목 | 판정 |
|---|---|
| 부록 5장 헤더를 엔진이 «PART 8 / 8»로 덮어씀(D-13) | **부록에는 PART 번호를 붙이지 않는다.** 원본의 부록 구획 방식(`w2-annex` 클래스 · 원본 `part-04.html:760` 선례)을 확인해 엔진이 건너뛰게 하고, 헤더는 «부록»으로. |
| `A5F2-P` «48쪽에서 본 항목»이 뒤 장을 가리킴 · 실습 번호 PART 5 ⑧⑩ / PART 6 ①②③ 혼재 | **M4 상호참조 수정** — 새 덱의 실제 순서·번호로 맞춘다. |
| MAP1 인계 3장(`S3LM`·`S3CE`·`18`) 미포함 | 승인(매핑 v3 미배치 · `S3LM` 수치는 «Lost in the Middle 수치 금지»와 충돌). |
| `P2`·`P5` 간지 제목의 « — » 뒤를 설명 줄로 내림 · 표지 «3주 아카데미»→«3차시 과정» | 승인(한 줄 폭 초과 방지 · M1). 두 줄이 된 부제가 바닥선 결함을 만들면 A로 본다. |
| 초안 품질 R-QD-03·04·05(러너 밖) | 기록만. R-QD-05 리뷰 HTML 파일명 불일치는 1차시와 같은 사유. |

## 계수

- 새 덱 offender 6 + boxFill 4 · 원본 기준선 offender 1 + boxFill 8.
- 분류: A 6슬라이드(`N-14`·`N-10`·`A3F2`·`C1-1`·`M1`·`N-04`) · B 4슬라이드(`C4-H1`·`C4-12`·`C4-N2`·`C4-13`, 소스 모양 확인 조건부).
- 미판정: 구조 WARN 1건의 내용(수정 담당이 확인).
