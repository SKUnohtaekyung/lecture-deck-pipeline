# 렌더 기준선 — 원본 바이브코딩 2주차·3주차 덱 (오케스트레이터 실측 · 2026-09-10 17:0x)

> 용도: 2·3차시 새 덱의 렌더 결함을 **원본에서 물려받은 것(B · 증거 첨부 등재)**과 **이번 조립이 만든 것(A · 수정)**으로 가르는 기준. 방법은 `렌더비교_1차시.md`와 같다.
> 측정: 서버 `ac3-render-audit`(8799 · 저장소 루트) · 뷰포트 1280×720 · `--scale` 1 · 폰트·이미지 로드 대기 후 `audit_all.js` · **파일 저장 없음**(브라우저 JS 반환만 · 수신기 미사용 · 동결 증거 `sessions/_verify/2·3주차/` 무접촉).
> 두 원본은 `../../../../kit/`로 **현행 kit**을 참조한다 — 새 덱과 kit이 같아 1주차 비교보다 조건이 정확하다.

## 원본 2주차 — `courses/바이브코딩/sessions/2주차/강의덱.html`

- 112장 · 깨진 이미지 0 · 폰트하한 위반 182(원본 계약 `warn_baseline.render` 182와 일치)

| 결함 | 수 | 슬라이드 |
|---|---:|---|
| below | 0 | — |
| lap | 2 | `C4-H1` ×2 |
| ovf · slots · occl · off | 0 | — |
| stretchX | 6 | `C3-2` 1 · `C5-3` 2 · `C5-4` 1 · `C4-12` 1 · `C4-13` 1 |
| stretchY | 0 | — |
| wideEmpty | 4 | `C5-21` · `C4-12` · `C4-N2` · `C6-START` |
| lowFill | 1 | `C5-4` |
| hollow(껍데기 상자) | 20 | `C2-7` 5 · `C5-19` 5 · `C5-20` 8 (+기타) |

→ 원본 계약 `known_violations`(`render_lap` C4-H1 · `render_wideEmpty` C4-12·C4-N2 · `render_stretchX` C4-12·C4-13)가 **같은 ID에서 재현**됨. `매핑_2차시.md` §⑧ 「계약 waiver 승계」 목록과 일치.

## 원본 3주차 — `courses/바이브코딩/sessions/3주차/강의덱.html`

- 81장 · 깨진 이미지 0 · 폰트하한 위반 0

| 결함 | 수 | 슬라이드 |
|---|---:|---|
| below · lap · ovf · slots · occl · off | 0 | — |
| stretchX | 6 | `V1-10` · `V1-05` · `V3-11` · `V4-04` · `V5-02` · `V5-09` |
| stretchY | 3 | `V4-06` · `V5-04` · `V6-05` |
| wideEmpty | 2 | `V3-13` · `V5-02` |
| lowFill | 0 | — |
| hollow | 13 | `V1-12` 2 · `V2-07` 3 · `V5-09` 2 · `V6-03` 3 (+기타) |

## 사용 규칙

1. 새 덱 결함이 위 표의 **같은 원본 ID(새 덱의 `data-slide`가 원본 ID를 유지하는 경우)·같은 유형**이면 B. 그 외 전부 A.
2. 원본 ID가 새 덱에서 바뀐 경우(재번호·M5 통합)는 `보존매트릭스_N차시.csv`로 대응을 확인한 뒤 판정한다. 대응이 불명확하면 A로 본다.
3. 신규 M6 슬라이드의 결함은 항상 A.
4. ⚠️ **건수로 짝짓지 마라**(2026-09-11 1차시 사고): 재번호된 슬라이드(특히 디바이더)는 «같은 유형 결함 N건 ↔ N건»으로 맞추면 안 된다. 1차시에서 새 `P2`를 원본 `P3`와 짝지었으나 실제 원본은 `P6`(부제 한 줄)이었고, 새 덱이 부제를 두 줄로 만든 **새 결함을 원본 동일로 등재**할 뻔했다(R-BUILD1 치명). 반드시 `보존매트릭스_N차시.csv`의 `원본ID → 새순서` 대응으로 확인하고, 원본 쪽 요소가 **같은 모양(줄 수·크기)**인지 소스로 대조한다.
