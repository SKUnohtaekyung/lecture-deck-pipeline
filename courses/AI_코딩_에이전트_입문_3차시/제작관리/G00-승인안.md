# G00 승인안 — 모델·설정·경로 해결안의 수정 전 승인

> 작성 2026-09-08 · 오케스트레이터 Fable 5.1(세션 `671720eb-7ad2-410d-b288-2f021c67b84e`) · 상태 **BLOCKED(사용자 승인 대기)**
> 근거 문서(모두 `제작관리/`): `시작기준선.json`(T00) · `모델지원증거.md`(T01) · `모델계약호환성.md` v3(T02) · `경로스키마호환성.md` v3(T03) · `상태장부.md`
> 이 문서는 승인안이지 승인이 아니다. 아래 «결정 목록»에 답이 오기 전까지 공유 파일은 한 글자도 바꾸지 않았고, 강의자료 제작·콘텐츠 리서치·`/리서치`·`/하네스`는 시작하지 않았다.

## 1. 완료한 Task와 검증 결과

| Task | 결과 | 증거 |
|---|---|---|
| T00 기준선 | **PASS** | 루트 일치 · HEAD `ff48036` · origin/main 차이 0/0 · 추적 790+미추적 49=839파일 SHA-256 · 수정/staged 0 · 명령 10개 exit 0 |
| T01 환경 진단 | **PASS(사실 확인)** — 판단 2건은 §5로 | 2.1.260 데스크톱 세션 · `opus`→`claude-opus-5` 스모크 · effort 5값 · 내장 타입 기본 모델 스모크 2회 |
| T02 모델 계약 분석 | **FAIL** (v3 · 독립 검토 3회 연속 FAIL · 남은 결함은 좁음) | 검토자 3명이 인용 44/46/74건 대조 · 최종 미해소 1건 = ⑥ C-2 영향 서술(아래 §3-3) |
| T03 경로·스키마 분석 | **PASS** (v3 · R-T03-03 치명 0 · 비치명 8 — T04 적용 시 흡수) | 검토자 인용 78건 대조(일치 72) · `_verify` 참조 독립 재계수 17곳/9파일 일치 · A9 해시 5개 = 기준선 · diff 7블록 적용·컴파일 재현 |
| 회귀 기준선(변경 전) | 12모듈 **287건 OK** exit 0 | `tmp/AI_코딩_에이전트_입문_3차시/T01/regression-baseline.log` |
| 범위 보호 | 새 과정 폴더·`tmp/` 외 변경 0 · 기존 `sessions/_verify` 5파일 해시 불변 · 저장소 밖 쓰기 0 | `git status --short` · 기준선 대조 |

T02가 3회 FAIL한 이유는 **권고 내용이 흔들려서가 아니라** 큰 문서(1168행)의 계수·인용·승인표 정합 결함이 검토마다 새로 잡혔기 때문이다. 권고 골자(§3)는 3회 검토에서 한 번도 뒤집히지 않았다. 계획 §11.3-5에 따라 네 번째 시도는 사용자 결정 사항이다(§5 D9).

## 2. 확인된 모델 ID·effort와 적용 방법 (T01 · 전부 실측)

| 항목 | 값 | 근거 |
|---|---|---|
| 실행 바이너리 | Claude Code **2.1.260**(데스크톱 앱 번들). npm 2.1.207은 이 세션과 무관 | 트랜스크립트 `version` · 프로세스 경로 |
| 오케스트레이터 실효 | **`claude-fable-5-1`** · effort `xhigh` | 세션 JSONL 42/34건 |
| `opus` 별칭 | **`claude-opus-5`** | 스모크 `agent-aae8ea7cec24c60d9.jsonl` `"model":"claude-opus-5"` · 공식 표 |
| `fable` 별칭 | `claude-fable-5-1`(2.1.255+) | 공식 표 · 세션 실측 |
| effort 지원값 | `low < medium < high < xhigh < max` (5개 · Fable 5.1/Opus 5/Sonnet 5 공통 · 기본 `high`) | 2.1.260 `--help` · 공식 표 |
| 메인 모델·effort 지정 | `/model` · `--model` · `ANTHROPIC_MODEL` · 설정 `model` / `/effort` · `--effort` · `CLAUDE_CODE_EFFORT_LEVEL` · `effortLevel`·`modelSettings` | 공식 문서 |
| 서브에이전트 모델 | ① Agent 호출 `model` 인자(별칭 enum만) → ② 정의 frontmatter `model` → ③ `CLAUDE_CODE_SUBAGENT_MODEL` → ④ 메인 상속. **①이 frontmatter `model: sonnet`을 이김·`tools:`는 유지**(실측) | 공식 우선순위 · 스모크 |
| 서브에이전트 effort | **정의 frontmatter `effort:`만** 가능. Agent 호출 인자에 effort **없음**. 없으면 메인 상속(`xhigh` 실측) | 도구 스키마 · 스모크 |
| 내장 타입을 `model` 인자 없이 | `general-purpose` → **`claude-fable-5-1`**(정책 위반 경로) · `Explore` → `claude-opus-5` · `Plan` 미실측 | 스모크 15:15:29/31 |
| 감사 검출 | `--expect-model claude-opus-5`가 위 Fable 상속 로그를 실제로 검출(exit 3). 정본 명령(`--projects-dir` 생략)은 옛 폴더를 봐서 **«0개·exit 0» 거짓 PASS** | 재현 3회 |

**Astra 배정 정책(E0~E4) 대입안**: E0=`low` · E1=**`high`**(대안 `medium`) · E2=`xhigh` · E3=**`xhigh`**(대안 `max`) · E4=`max`. E1·E3는 규칙 문구가 두 갈래로 읽혀 사용자/Astra 확정 대상(§5 D3). 적용 수단은 §3의 과정 전용 정의 파일뿐이므로 **G00 전 실행분(T02·T03·검토 전부)은 요청 E3/E4 · 실효 `xhigh`** 로 장부에 기록했다.

## 3. 과정 격리 / 공유 설정 변경안 (T02 v3 권고 · 검토 3회에서 골자 불변)

**권고**: 「**B**(과정 전용 서브에이전트 정의 신설 · 기존 파일 무수정) + **C-1**(리서치 어댑터 문서 예외·정정) + **C-2/C-3**(감사기 결함 2건 · C-3는 (d) 종료코드 4) + **/리서치 전용 세션** + **내장 타입 사용 규율**」, effort 폴백은 **B′(Workflow)**. 해결안 A(무수정 실행 재정의)는 모델은 되지만 effort 정책을 0% 구현하므로 단독 채택 불가.

### 3-1. 신설 파일(공유 위치이지만 기존 파일은 무수정 · 복구 = 삭제)

| 파일 | model / effort / tools / maxTurns | 용도 |
|---|---|---|
| `.claude/agents/ac3-research-worker.md` | opus / (E2) / 기존 `research-worker`와 **동일**(Read·Grep·Glob·WebSearch·WebFetch·ToolSearch) / 30 | /리서치 청크 워커 |
| `.claude/agents/ac3-reviewer.md` | opus / (E4=`max`) / Read·Glob·Grep(Bash·Write·Edit·Agent 없음) | 모든 R-검토·G-게이트 |
| `.claude/agents/ac3-writer.md` | opus / (E3) / 쓰기 포함 | 집필·HTML·분석 |
| `.claude/agents/ac3-clerk.md` | opus / (E0=`low`) / Read·Grep·Glob·Bash(**Bash 쓰기는 자연어 강제뿐** — 훅 matcher가 Write\|Edit뿐) | 목록화·집계 |
| (조건부) `.claude/agents/ac3-research-lead.md` | opus / (E3) / Agent·Skill 보유 | 중첩 호출이 실측되면 |

다른 과정에는 «보이지만 호출되지 않는다»(opt-in). `verify_skill_setup.py`·pre-commit·라우팅 eval 어디에도 걸리지 않음(실측 exit 0 · PASS 68).

### 3-2. 공유 파일 수정(정확한 diff는 T02 v3 §4 · 전부 `git apply --check` exit 0 · C-2→C-3 순차 적용 exit 0)

| ID | 파일 | 내용 | 영향 | 복구 |
|---|---|---|---|---|
| C-1 | `.claude/skills/리서치/SKILL.md` (2 hunk) | 「G00 승인된 과정 전용 워커」 예외 1문단 · 내장 타입 금지 2줄 · 사실 오류 2줄 정정 · 종료코드 4 행 | pre-commit `check_skill` → `verify_skill_setup.py` 재실행. 정본·Codex 어댑터 무수정. **⑥(C-3) 동반 필수**(4가 있어야 함) | diff 역적용 |
| C-2 | `scripts/analyze_agent_usage.py` (5 hunk) | 낡은 기본 폴더(`…-Desktop-template`) → 현 프로젝트 · `DEFAULT_SESSION`·`DEFAULT_WORKFLOWS`를 `LEGACY_*`로 분리 · `--session` 필수화 | **⚠️ §3-3 보정 필요** | hunk 역적용 |
| C-3 | 같은 파일 (6 hunk · docstring 포함) | 0워커 «감사할 것 없음 → exit 0»을 **exit 4(미판정)** 로. 위반 3과 분리(AGENTS.md:72 래칫 규율) | 도움말·지도·html·`_dev/…Phase2…md:67` 등 «0 통과/3 위반» 문구 4곳 갱신 필요 | hunk 역적용 |

### 3-3. R-T02-03이 남긴 미해소 1건(오케스트레이터가 재현 · 승인 전에 알아야 할 사실)

- 메인 로그는 있고 워커가 0인 세션에서 정본 명령 `analyze_agent_usage.py --session <ID>`(`--no-baseline` 없음)는 `section_baseline`의 `statistics.mean([])`로 **트레이스백**한다. 이 버그는 **원본에도 있는 잠복 버그**다(원본 `--projects-dir` 명시 실행에서 동일 재현). C-2 이전에는 정본 명령이 옛 폴더를 봐서 «집계 대상 없음·exit 1»로 깨끗하게 끝났고, C-2 이후에는 세션이 잡히면서 이 버그가 **드러난다.**
- 따라서 T02 v3의 U8 «C-2 후 정본 명령은 죽지 않고 exit 0»는 `--no-baseline`을 붙인 다른 명령의 실측이었다(검토 지적 타당). **보정안**: C-2 diff에 「`ws`가 비면 baseline 절을 건너뛰고 미판정으로 표기」 가드 3~5줄 추가 + ⑥ 영향 열에 이 사실 등재. T04가 적용할 때 포함하면 되며, 방향 결정은 §5 D9와 함께.

### 3-4. 운영 규율(파일 수정 없음)

- **⑧ /리서치 전용 세션**: 감사기는 세션 폴더의 모든 `agent-*.jsonl`을 워커로 세고 `meta.json`의 `agentType`을 읽지 않는다(코드 확인). 이 세션은 이미 15개 로그(스모크·분석·검토)로 오염돼 있어 /리서치는 **새 세션**에서만 돌린다.
- **⑫ 내장 타입 규율**: 이 과정 Task는 승인된 `ac3-*`만 `subagent_type`으로 쓴다. 부득이 내장 타입을 쓰면 `model:"opus"` 명시 + 실행 후 `meta.json`·`.jsonl` 대조. `Plan`은 스모크 전 사용 금지.
- **⑭ 별칭 드리프트 검출**: /리서치 세션 종료 시 `--expect-model claude-opus-5` 감사 필수(범위는 **/리서치 세션 한정** — R-T02-03 비치명 1 반영). T04가 frontmatter 풀 ID 수용을 스모크로 확인하면 별칭 대신 풀 ID로 고정.
- **⑬ B′(Workflow) 폴백**: frontmatter `effort:`가 2.1.260에서 실제 수용되지 않을 때(T04 스모크 ㉠로 판정)만. **사용자 명시 opt-in 필수**(`skills/하네스/SKILL.md:209`). 근거 문서가 저장소 안에 없어 «저장소 내 검증 불가»로 등재.
- **⑦ T04 스모크 5종**(모델 호출 각 1회): ㉠ `ac3-reviewer` effort:max 실효 확인 ㉡ `ac3-research-worker` 쓰기 실패 확인 ㉢ 중첩 1회(depth 2 로그 경로) ㉣ `ac3-clerk` 셸 리다이렉션 쓰기 실측 ㉤ `Plan` 기본 모델.
- **⑪ 기각 확인**: 기존 `research-worker.md`·`instruction-reviewer.md`의 `model` flip(C-5)·하네스 4파일 수정(C-6)은 하지 않는다. `/하네스`를 호출하지 않으므로 하네스 문서의 Sonnet 전제 6건은 발화하지 않는다.

## 4. 렌더 증거 경로 격리안 (T03 v3 · B03)

**문제(재현)**: `CREATE_SLIDES_COURSE`는 **입력만** 격리한다 — 세션 경로는 `courses/C/sessions/1주차`로 가지만 `run_deck_checks.py:192`의 verify_dir과 `receive_audit.py:41`의 저장 경로는 루트 `sessions/_verify/1주차` 그대로이고, 수신기는 `wb`로 **덮어쓴다**. 게다가 `inject_presenter.py:476`은 `--meta` 사이드카(`강의덱_발표.meta.json` · 동결 5파일 중 2개)를 존재 검사·force 없이 쓴다.

**권고 ⓐ**: 라우팅은 «과목이 `courses/<과목>/sessions/_verify/`를 선언(마커)했는가»로, 파괴 방지는 «**덮어쓸 증거가 누구 것인가**»(기존 `deck-audit.json`의 `url`이 전부 `/courses/바이브코딩/…` · 사이드카는 `deckId`)로 판정한다. 마커를 만들지 않은 과목은 동작이 바뀌지 않는다. 설계 함정(«과목이면 무조건 신경로» → 바이브코딩 증거 «없음» FAIL / «신경로 있으면 신경로» → 구경로가 모든 과목에 존재해 충돌 잔존)은 문서 §2.0에 반증과 함께 기록.

| ID | 파일 | 변경 | 동작 변경 |
|---|---|---|---|
| A1 | `scripts/_course_paths.py` | `verify_root`·`verify_dir`·`verify_dir_or_legacy`(모호성 시 구경로+경고)·`verify_is_course_local`·`evidence_owner` 등 신설만 | 없음(호출부 전까지) |
| A2 | `scripts/run_deck_checks.py` | 192행 과목 인식 + `render_evidence` 진입부 namespace 출력·**소유 대조 게이트**(오귀속 시 렌더 단계 FAIL·exit 1) | 마커 없는 과목 불변 |
| A3 | `scripts/receive_audit.py` | 경로 산출 + **다른 과목 소유 증거 덮어쓰기만 exit 2로 거부** | 바이브코딩 불변 · **likelionSKU의 파괴 경로만 차단(의도된 변경 · 개별 승인)** |
| A4 | `scripts/compare_baseline_panel.py` | `verify_dir_or_legacy` 사용(종료코드 «항상 0» 계약 유지) | 경고 1줄 |
| A5 | `tests/test_course_paths.py`·`tests/test_deck_pipeline.py` | 테스트 9건(양성·음성) · 증거 스캔 0건이면 실패 | 회귀 2모듈 |
| A6 | 문서·문자열 16곳/10파일(`receive_audit.py:8,36` help 포함) | 경로 문구를 `<증거루트>`로. 규칙 값·임계 불변 | 없음 |
| A7 | `courses/AI_코딩_에이전트_입문_3차시/sessions/_verify/README.md`(마커) · `제작관리/verify_mobile.py`(선택) | C 안 신설 | C 전용 |
| A8 | (별건) `scripts/verify_session_docs.py` 주차 인자 정규화 | 전 과목 영향 → 분리 판단 | — |
| A9 | `.githooks/_gate.py` | 동결 5파일 sha256 pre-commit 게이트(값 = `시작기준선.json`) · `SKIP_DECK_GATES=1` 우회 존중 | 5파일 그대로면 항상 PASS |
| A10 | `scripts/inject_presenter.py` | `_guard_meta_target()` — `--meta` 대상이 존재하고 `deckId`가 다르면 **`--force`로도 거부**(exit 1) | 사이드카 파괴 차단 |

diff 7블록(A1~A5·A9·A10) 전부 `git apply --check` exit 0 · 일괄 적용 후 7파일 컴파일 0(오케스트레이터 재현). A1~A6은 한 묶음(A1만 승인하고 A3을 거절하면 파괴적 쓰기가 남는다). 공유 수정을 전부 거절하면 ⓒ(C 전용 래퍼)로 가되 **공유 수신기의 파괴 경로는 그대로 열려 B03 근본 위험이 미해결**임을 명시한다.

**B03 해제 증거(5조건)**: ① 동결 5파일 해시 불변 + A9 기계 강제(사후 탐지) ② C 증거는 `courses/C/sessions/_verify/N주차/`에만, 루트 `sessions/_verify/`에 새 파일 0 ③ 수신기 `[저장]` 경로 = 러너 `=== 렌더 증거 경로 ===` 경로, 소유 표기 «과목 전용 namespace» ④ 음성 시험 10종(러너 오귀속·과목 미지정·동결 훼손·사이드카 덮어쓰기 포함) + 12모듈 회귀 exit 0 + 판정/미판정 계수 ⑤ 기존 과목 시나리오 실측(바이브코딩 러너 1·2·3주차 exit 0·WARN 래칫 불변, MEMORY.md:321 기준선 대조).

**G30 BLOCKED 후보(리서치 스킬 전제 ↔ 새 커리큘럼)**: B-C1 `verify_session_docs.py:211~213`의 «6구간 미만 FAIL» + 출처레지스트리 6열의 «구간» 열 — 새 과정은 차시당 4블록이라 통과 불가 · B-C2 G8 주차 바닥선(≥3/≥3/≥1)이 작은 갭 집합과 양립 불가 · B-C3 차시별 강의안설계 부재(G7 재대조·/검토 커버리지 원문 없음) · B-C4 차시 간 중복 개념 귀속. 각각의 결정 질문 초안은 T03 v3 §3.1.

## 5. 정확히 무엇에 대한 사용자 승인이 필요한가 (결정 목록)

| ID | 결정 | 권고 |
|---|---|---|
| **D1** | 「오케스트레이터 Fable 5」를 실효 **Fable 5.1**(`claude-fable-5-1`)로 인정하는가 | 인정(2.1.260의 `fable` 별칭이 5.1이며 계획서가 «ID는 T01 전 미확인»이라 적음). 불인정이면 여기서 중단 |
| **D2** | 「모든 하위 에이전트 Opus 5」가 **이 과정 한정**인가 **저장소 전역**인가 | 과정 한정(전역이면 C-5로 방향 전환·타 과정 영향) |
| **D3** | effort 대입 2칸: E1 = `high` vs `medium` / E3 = `xhigh` vs `max` | `high` / `xhigh` |
| **D4** | §3-1 정의 4종 신설(+조건부 리드 1종) 승인 · 파일별 allowlist 그대로 | 승인 |
| **D5** | C-1(리서치 어댑터 2 hunk) · C-2(+§3-3 빈 ws 가드 보정) · C-3((d) exit 4) · 파급 문서 4곳 승인 | 승인(C-1은 C-3 동반) |
| **D6** | 운영 규율 ⑧ 전용 세션 · ⑫ 내장 타입 · ⑭ 감사 필수(범위 /리서치 세션) · ⑦ 스모크 5종 승인 | 승인 |
| **D7** | ⑬ B′(Workflow) 폴백 — **명시 opt-in** 여부 | U1 스모크 결과가 나올 때까지 보류 가능 |
| **D8** | T03 A1~A7·A9·A10 승인(A3의 likelionSKU 차단은 «의도된 변경»으로 개별 확인) · A8은 별건 | 승인 · A8 보류 |
| **D9** | T02 attempt-04(§3-3 보정·비치명 8건 반영 · 새 수정자+새 검토자 ≈ 40만 토큰) **vs** T04가 보정을 흡수하고 R-T04가 검증 | **후자** — 권고 골자는 3회 검토에서 불변이며 남은 결함은 T04 적용 diff에 흡수 가능 |
| **D10** | A9 해시 갱신 권한(동결 해제 시 누가 `.githooks/_gate.py`를 고치는가) · A9의 worktree 미탐(`_gate.py:48~50`이 훅 파일 위치 기준이라 `.claude/worktrees/*` 커밋은 메인 파일을 해싱) 처리 — `git rev-parse --show-toplevel` 또는 staged blob 대조로 보강할지 | 동결 해제는 사용자 결정 시 T04류 통합 담당 · worktree 보강은 T04 diff에 포함 권고 |
| **D11** | G30 BLOCKED 후보 B-C1~B-C4의 방향(각 ⓐ/ⓑ/ⓒ) — 지금 답하면 T30 진입이 빨라짐 | B-C1 ⓑ(미판정 선언) · B-C2 ⓑ(WARN 시작 — AGENTS.md 신규 과목 규율) · B-C3 대체 선언 · B-C4 G21에서 |
| **D12** | 총 호출·수정 시도 상한(계획 §5.2) — Phase 0에서 이미 서브에이전트 17회·약 3.3M 토큰 사용 | Phase 1 이후 Task당 시도 2회·검토 포함 상한을 사용자가 지정 |

## 6. 남은 BLOCKED

- **B01**(모델·설정): 사실은 전부 확인됐고 **D1~D7 답변 대기**. 미지원 모델 없음(대체 필요 없음).
- **B02**(참고 PPTX·PDF 현물): **손대지 않음** — G11 단계 · T10 인벤토리 후 제기.
- **B03**(렌더 증거): 해결안·해제 증거 정의 완료, **D8 답변 대기**. T03 v3의 최종 독립 판정은 R-T03-03 결과로 갱신.
- **G30 후보** B-C1~B-C4: D11.
- T02 산출물 자체: FAIL 상태(D9).

## 7. 실행하지 못한 검증과 이유

- frontmatter `effort:` 실제 수용(U1) · 중첩 서브에이전트 로그 경로(U2) · `Plan` 기본 모델 · `ac3-clerk` 셸 쓰기 — 정의 파일 신설이 G00 승인 대상이라 **T04 스모크로 이관**.
- A1~A10 적용 후 12모듈 회귀·음성 시험 10종·기존 과목 시나리오 실측 — 공유 파일 수정 금지라 **적용 전**. 변경 전 기준선(287 OK)만 확보.
- 모바일 판정 임계 — 브라우저 실측 전 숫자를 만들지 않음(T03 §5).
- SendMessage 도구가 이 빌드에 없어 수정 재위임은 매번 새 인스턴스(컨텍스트 재독 비용 발생).

## 8. 계수

- 서브에이전트 호출 17회(스모크 3 · T02 계열 7 · T03 계열 7) · 서브에이전트 토큰 약 3.3M · 429 중단 2회(16:17 · 재개 완료).
- 이 세션 `subagents/` 로그 15개(전부 `claude-opus-5` 또는 스모크 목적의 `claude-fable-5-1` 1건) · 요청/실효 모델 불일치 0.
- 저장소 밖 임시 파일 0 · `tmp/AI_코딩_에이전트_입문_3차시/{T01,T02,T03}/`만 사용 · 커밋·푸시·게시 0.
