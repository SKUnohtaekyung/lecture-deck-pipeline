# 렌더 비교 — 1차시 새 덱 vs 원본 정본 덱 (오케스트레이터 실측 · 2026-09-10 16:5x)

> 목적: `run_deck_checks 1주차`의 결함형 렌더 FAIL이 **원본에서 물려받은 것**인지 **이번 조립이 만든 것**인지 가른다.
> 판정 원칙: 새 결함은 shard를 고쳐 없앤다. 원본과 **같은 슬라이드 ID·같은 결함 유형**으로 재현된 결함만 이 문서를 증거로 `deck.contract.json` `known_violations`에 등재한다(미리 이식 금지 · 계획서 §13).

## 측정 조건

| 항목 | 새 덱 | 원본 정본 덱 |
|---|---|---|
| 경로 | `courses/AI_코딩_에이전트_입문_3차시/sessions/1주차/강의덱.html` | `.claude/worktrees/week-1-content-addition-8e2d72/sessions/1주차/강의덱.html` |
| 서버 | `ac3-render-audit`(8799 · 저장소 루트) | 동일 |
| 뷰포트 · `--scale` | 1280×720 · 1 | 1280×720 · 1 |
| 장수 · 깨진 이미지 | 73 · 0 | 85 · 0 |
| kit | 현행 저장소 `kit/` | worktree 자체 `kit/`(구버전 — 차이 원인이 kit일 수 있음을 유보) |
| 증거 저장 | 수신기 → `courses/…3차시/sessions/_verify/1주차/deck-audit.json` | **저장 안 함**(브라우저 JS 반환만 · 수신기 미사용 — 새 namespace 덮어쓰기 방지) |

## 총계

| 결함 | 새 73 | 원본 85 |
|---|---:|---:|
| 바닥선초과 below | 35 | 32 |
| 겹침 lap | 2 | 2 |
| 오버플로 ovf | 1 | 2 |
| 빈슬롯 slots | 2 | 2 |
| 가려짐 occl | 1 | 1 |
| 가로늘림 stretchX | 5 | 7 |
| 세로늘림 stretchY | 5 | 4 |
| 가로꽉채우고빔 wideEmpty | 1 | 5 |
| 채움률미달 lowFill | 9 | 7 |
| 껍데기 상자 hollow | 7(M6-05 ×4 포함) | 9 |

## 슬라이드별 분류

### A. 새로 생긴 결함 — **수정 대상**

| 슬라이드 | 결함 | 성격 |
|---|---|---|
| `M6-04` | below 3 | 신규 슬라이드 |
| `M6-05` | below 3 · 껍데기 상자 4 | 신규 슬라이드 |
| `M6-03` | stretchX · stretchY · lowFill (`card surface×2@49w/37x/45y`) | 신규 슬라이드 |
| `M6-07` | stretchX · stretchY · lowFill (`cc-side ai×2@46w/39x/52y`) | 신규 슬라이드 |
| `15` | lap 1 | 수정 슬라이드(원본 offender 아님) |
| `32` | below 1 | 수정 슬라이드(타자 게임 요청문으로 본문 길어짐 — 원본 offender 아님) |
| `S3PE` | lowFill +1(`eng-bad@26w/26x/36y/22f`) | 원본은 stretchY만 · lowFill은 새로 생김 |
| ovf 1 | 오버플로 1건(슬라이드 미특정) | 원본 2건과 동일 여부 미판정 → 수정 담당이 특정 |
| `S58` | **내용 모순**(렌더 결함 아님): 캡처 `win05_vibecoding-index-root.png`는 `C:\VibeCoding` 바로 아래 index, 본문은 `C:\VibeCoding\typing-game\` | M2 소재 치환의 부작용 |

### B. 원본과 동일 — **증거 첨부 후 등재 대상**

| 슬라이드(새) | 원본 대응 | 결함 |
|---|---|---|
| `S01` | `S01` | below 1(`cover-presenter@674`) |
| `S01A` | `S01A` | below 2 · stretchX(`intro-ai-note@43w/37x/5y`) |
| `S01B` | `S01B` | below 1 |
| `P1`·`P4` | `P1`·`P3`(디바이더 재번호) | below 1씩(`pd-sub@679`) |
| ~~`P2`~~ | ~~`P3`~~ → **정정: 새 `P2`의 원본은 `P6`(부제 한 줄 · 원본 `part-05.html:29`)** | **A로 재분류** — 이번 M1 문안이 부제를 `<br>` 두 줄로 만들어 생긴 새 결함(R-BUILD1 치명 1 · 2026-09-11). 건수만 맞춘 대응이었다. attempt-03에서 한 줄로 수정 → `render_below` 26→25 |
| `07` | `07` | below 2 |
| `S10C` | `S10C` | below 1 |
| `S10D` | `S10D` | lowFill(`index-name-token`) |
| `11` | `11` | below 2 |
| `12` | `12` | below 3 |
| `14` | `14` | below 2 |
| `S3DO` | `S3DO` | below 1 |
| `S3PE` | `S3PE` | slots 1 · stretchY(`eng-concept@26w/5x/67y`) |
| `S3PRV` | `S3PRV` | lap 1 · occl 1 · slots 1 · wideEmpty · lowFill(`privacy-principle@100w/83x/2y`) |
| `S28A` | `S28A` | stretchY · lowFill(`terminal-dark@60w`) |
| `31` | `31` | below 1 · stretchX · lowFill(`DIV×4@47w/56x`) |
| `S36` | `S36` | stretchX · lowFill ×2(`breadcrumb-track`·`workspace-path-label`) |
| `S39` | `S39` | below 3 |
| `S42` | `S42` | below 1 · stretchY(`cc-side ai×2@35w`) |
| `S58` | `S58` | below 3 |
| `S59` | `S59` | below 2 |

### C. 원본에만 있던 결함(이번 덱에 없음)

`S00` below+lap · `21`·`23`·`24` below 1씩 · 제외 슬라이드(`S05A`·`T01`·`T02`·`W05`·`X01`·`X03`·`X04`)의 wideEmpty·stretch·hollow.

### D. 정정 — CSS 무효화 복구 뒤 S00은 C가 아니라 B (2026-09-11 · BUILD1-CSSFIX)

- 원인: 조립 잔해인 선택자 없는 선언 조각 5개가 최상위에 `}`를 남겼다. 그 결과 뒤따르는 규칙 `.s00-hero`·`.intro-ai-copy`·`.slide .work-step`·`.privacy-lanes`가 무효화됐다(`tmp/…/CSSORPHAN/dropped.py`, 원본 worktree 0건). 이 때문에 새 덱 S00 표지가 원본과 **다르게** 렌더됐고, 위 C절의 «S00 원본 전용 결함»은 결함이 사라진 것이 아니라 **보존이 실패한 것**이었다. 렌더 검사기는 규칙 무효화를 결함으로 세지 않으므로 러너 PASS가 이를 가렸다(미탐).
- 조치: 조각 5개 삭제(가리키던 대상은 전부 새 덱 미사용 · `cover-week`는 `cover-track`으로 개명돼 같은 선언이 이미 존재) → 재조립 73장 `8f6bc2e4`.
- 재측정 r6(1280×720 · scale 1 · 깨진 이미지 0 · `.s00-hero` 1100px · `.work-step` 22px/1.45 적용 확인): below 25→**26** · lap 1→**2**. 늘어난 것은 **S00 `IMG@697`·`IMG×ko`** 한 장뿐이고 나머지 offender는 이전과 같다.
- B 근거 3중: ① 원본 worktree 85장을 같은 조건으로 재측정(JS 반환만 · 저장 없음)하니 S00 `IMG@697`·`IMG×ko`로 같은 요소·같은 좌표 ② 동결 증거 `sessions/_verify/1주차/deck-audit.json`(1600×1000) S00도 같은 값 ③ 원본 계약 `render_lap` 사유 «S00 도입 장 IMG×.ko»와 `render_below` 분해 ③항에 S00 포함. 소스 모양: S00 마크업 차이는 M1 문구뿐이다(제목 2줄·`.hl` 1개 유지 · 표지 이미지 동일 · 3칸 클래스 개명).
- `.work-step`(M6-04·S45·S53·S56) 복원으로 새 offender는 생기지 않았다.

## 러너의 나머지 FAIL (렌더 결함 아님)

- 품질(`verify_deck_quality`) 상시 FAIL 2건 — 규칙 R-META-01·R-QC-08·R-QC-18 중. 원본 85장 덱은 FAIL 9.
- 보고 신선도 — `조립_보고.md`에 «**N장**» + «sha256 `앞8자`» 식별자 없음.
- 래칫 — 구조 WARN 0→2 · `warn_baseline.quality`(5)·`render`(62) 미등재.
- 정보 모양 `data-shape` 기록 0/64장 — 「모양 대비 시각자료」 신호 측정 불가(WARN).

## 계수

- 대조한 슬라이드: 새 덱 offender 23 + boxFill 10 · 원본 offender 23 + boxFill 15.
- 분류: A 새 결함 9항(슬라이드 8 + ovf 1) · B 동일 22슬라이드 · C 원본 전용 11슬라이드.
- 미판정 2: ovf 1건의 슬라이드 · kit 버전 차이가 B 분류에 미친 영향.
- 화면 캡처는 창이 가려져 스크린샷이 시간 초과 — 확인은 DOM 조회로 대체(디바이더 `PART 1/5`~`5/5` 정상 · 렌더 본문 옛 표기 0).

## 보강 증거 — 본체 1주차(75장) 원본 계약과 동결 렌더 증거 (2026-09-10 17:0x 추가)

- 동결 증거 `sessions/_verify/1주차/deck-audit.json`(75장 · 뷰포트 1600×1000): below 25 · lap 1 · ovf 1 · stretchX 3 · stretchY 2 · wideEmpty 4 · lowFill 6.
- 원본 계약 `courses/바이브코딩/sessions/1주차/deck.contract.json`의 `known_violations`에 **같은 계열이 사유·분해와 함께 이미 등재**돼 있다: `render_below` 25(«kit 고정 컴포넌트 유래 — `pd-sub@679` ×3(P1·P3·P7) · cover-presenter …») · `render_lap` 1(S00) · `render_ovf` 2(슬라이드 14·32) · `render_wideEmpty` 4 · `render_stretchX` 3(S01A 등) · `render_stretchY` 2(S42 `cc-side`) · `render_lowFill` 6(S10D `index-name-token` 등) · `body_font_below_22`(의도된 20px 위계).
- 동결 증거 offender ID(`S01`·`S01A`·`S01B`·`P1`·`07`·`S10C`·`11`·`14`·`S3DO`·`31`·`S39`·`S42`)가 B 분류와 겹친다 → B는 «정본 worktree에서 재현»에 더해 «원본 계보의 기존 등재 결함»으로도 확인된다.
- 참고: 원본 계약의 `render_ovf`가 슬라이드 `32`(자기소개 프롬프트 장)를 적시한다 — 새 덱의 ovf 1건이 `32`라면 원본 계보 결함이지만, `32`는 타자 게임 요청문으로 **본문이 바뀐 수정 슬라이드**이므로 A로 두고 수정 시도한다.
