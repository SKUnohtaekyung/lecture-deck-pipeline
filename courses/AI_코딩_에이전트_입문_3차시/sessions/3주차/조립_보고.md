# 조립 보고 — 3차시 (단일 라이터 · 절차 B)

> 기준 덱: `강의덱.html` «**75장**»(본편 65 + 부록 10) · sha256 `8b6a3ff9` · PART 전환 6장 (attempt-06 재조립 · 직전 판 `f9e6d4a7` · 그 전 `0a9b802b` · `798639b0` · `419ddac9` · `10c32268`)
> 작성: 3차시 조립 담당 · 2026-09-11 · 커밋하지 않음 · 서브에이전트·웹 검색·브라우저 사용 없음 · attempt-02 이어받기 담당이 §0-B·§5·§6·§7·§8·§10·§11을, attempt-03 담당(독립 검토 R-BUILD3 지적 수정)이 §0·§4·§5·§6-A·§8·§9·§10·§11을, attempt-04 담당(r4 렌더 FAIL·N-24 요청문)이 §0-A·§6-0·§8·§10·§11을, attempt-05 담당(독립 검토 R-BUILD3-02 경미 2건)이 §0-C·§0-1·§6-D·§10·§11을, attempt-06 담당(보존 수정 3건 · D-08 부록 10장)이 §0-D·§1·§4·§6-E·§8·§10·§11을 갱신
> 정본 입력: `제작관리/제작브리프.md` · `보존매트릭스_3차시.csv` v3(81행) · `매핑_3차시.md` v3(§2·§3·§4·§6-2·§11) · 오케스트레이터 확정 판정 · `실습예제/CSV/`(지표기준값.json 외) · `커리큘럼_기준안.md` · `슬라이드지침.md` · `profile.md` · `자료/원문/01·02` · attempt-03: `kit/charts/by-shape.md` · `kit/charts/charts-basic.md` · `kit/charts/charts-ratio.md`(C-lollipop) · `kit/layouts/by-shape.md`
> 모든 명령은 `CREATE_SLIDES_COURSE=AI_코딩_에이전트_입문_3차시` 아래에서 실행했다. 아래 수치는 최종 산출물(`8b6a3ff9`)에 대한 마지막 재측정값이다.

## 0-D. attempt-06 변경 이력 — 사유가 약한 수정 3건 되돌림 · 부록 10장 수록(D-08) (2026-09-11)

> 입력: 보존 전수 대조 R-PRESERVE-3(PASS)이 지적한 «사유가 약한 수정» 3건 + 부록 미수록 1건. 오케스트레이터가 네 건 모두 수정 판정(부록은 매핑 권고안 «덱 뒤 부록 · 시간표 계수 밖» 채택).
> 방식: attempt-04·05와 같은 빌더 경로(`build.py`→`assemble_deck.py`→`notes_build.py`→`draft_build.py`→`note_build.py`). 편집 전 사본 `tmp/…/BUILD3/a06_backup/`(31파일)을 남기고, 무변경 재실행으로 shard 7 · 덱(`f9e6d4a7`) · 노트 · 초안 · json 3이 **바이트 동일**함을 확인한 뒤 소스를 고쳤다.

| # | 지적 | 원본 대조 | 조치(전 → 후) | 바뀐 산출물 |
|---|---|---|---|---|
| 1 | V5-04 세 번째 라벨 | 원본 «승인 범위 초과»(도구 무관) | «실행 승인» → **«승인 범위 초과»**(원본). 노트 큐 «세 번째 칸 «실행 승인»은 경고색이다…» → «…«승인 범위 초과»는 경고색이다…». `revision.css` 312~314행 `.w3-stateslide[data-slide="V5-04"] .w3-states{align-content:center}`는 **삭제하지 않았다** — 라벨 때문이 아니라 1단 본문(31→46자)·callout이 길어져 넣은 늘림 보정(§0-B-1)이고, 그 두 문구는 그대로다. 이 규칙을 참조하는 다른 선택자 0 | part-04 1줄 · 노트 1줄 · 초안 1행 |
| 2 | V3-06 제목·정의문 | 원본 제목 «한 파일은 한 담당만 씁니다» · 정의문 «같은 수정 단계에서 … 담당은 하나뿐이라는 규칙» | 제목 «한 파일을 쓰는 역할은 하나뿐입니다» → **«한 파일은 한 역할만 씁니다»**(원본 어순 · «담당»→«역할» 한 어절). 정의문 «같은 단계에서» → **«같은 수정 단계에서»**(뺀 사유 기록 없음 → 복원). 접미 «— 원본 CSV는 그 «하나»도 없습니다»는 **유지**(매트릭스 `변경내용` 등재 · attempt-03 도해 «분석대상.csv» 교체의 전제 · 노트 큐가 기댐) | part-04 2줄 · 노트 제목 1줄 · 초안 1행 |
| 3 | S03 ↔ V2-01 일 이름 | 원본 S03 «HTML·CSS·JS·QA»(4) · V2-01 «HTML·CSS·JS»(3) — V2-01 세 칸 = S03 앞 세 칸, 넷째 QA = 검토 | **S03에 맞췄다**: V2-01 좌·우 도해 «대조» → **«제작»**(2회). S03 «정리·계산·제작·검토»는 무변경. 근거: 원본의 대응 구조 · 커리큘럼 3차시 개념 «제작자와 검토자 역할 분리» · 매핑 D-01(제작/검토 2역할). 3차시의 «대조»(실습⑧)는 검토 역할의 일이라 S03 넷째 칸 «검토»에 속한다. 항목 수 S03 4 · V2-01 3 유지 · aria-label은 일 이름이 없어 무변경 | part-06 2줄 · 초안 1행 |
| 4 | D-08 부록 미수록 | 매트릭스 `새처리상태=부록` 10장 A1~A10 | §0-D-1 | shell +322줄(부록) · shell 엔진 2→8줄 · 노트 +10장분 · 초안 부록 절 · 집필노트 머리 1줄 · §1 10행 · §6 3항 |

### 0-D-1. 부록 10장 — 수록 방식과 원본 대조

- **과목 지침 대조**: `슬라이드지침.md`·`profile.md` 전문에 MCP·플러그인·커밋·GitHub를 금지하는 문장 **0건** → 10장 전부 수록. (관측: `커리큘럼_기준안.md` 23행 «Git·GitHub 제외» — 지시의 판정 범위(지침 2파일) 밖이라 제외 근거로 쓰지 않고 §11-15에 올렸다.)
- **위치**: THANK YOU(65쪽) 뒤 66~75쪽. 2차시 선례(부록 = THX 뒤 · PART 구획 밖 · divider 없음)와 같다. 3차시 shell은 `::PARTS::` 표식이 END 앞이라 part 파일로는 THX 뒤에 둘 수 없어 `build.py`가 shell의 THX 뒤에 넣는다. END 64쪽 · THX 65쪽 · 시간표 1–63쪽 · PART 전환 6은 **불변**.
- **헤더**: 원본 `<div class="s-team"></div>` → `<div class="s-team">3차시<br>부록</div>`(2차시 «2차시<br>부록»과 같은 표식 · section 클래스 주입 없음). shell 발표 엔진에 2차시와 같은 분기(`/부록/.test(team.textContent)` → `<span class="lbl">부록</span>`)를 넣었다. `data-slide`는 원본 ID 그대로.
- **M1 표기**: 10장 화면·헤더·노트에 «3주차»·«3주 아카데미»·WEEK **0건** → 문구 치환 0(빌더·노트 빌더가 잔존 시 중단하도록 가드).
- **원본 대비 차이**(`tmp/…/BUILD3/a06_annex_diff.py`): 10장 모두 텍스트 차이 = `+3차시` `+부록`(헤더 표식)뿐, 마크업 토큰 차이 = `+3차시` `+<br>` `+부록`뿐. 섹션 안 HTML 주석 9개(V2-02 1 · V2-03 1 · V2-04 2 · V2-06 2 · V5-02 1 · V5-12 1 · V6-02 1)는 본편 보존 섹션과 같은 `strip_comments`로 걷었다(화면 무관).
- **CSS·자산**: 원본 3주차 part `<style>` 0개 · 원본 `revision.css` 없음 → 부록 CSS는 전부 원본 shell `<style>`에 있다. 3차시 shell `<style>`은 원본과 규칙 420/420이 같다(부록 클래스별 규칙 수 차이 0 · `data-slide` 한정 규칙 V2-02 2 · V2-04 2 · V2-06 1 · V5-12 3 = 8건 동일). 3차시 `revision.css`에는 부록 클래스를 건드리는 비한정 규칙이 0이다. **옮긴 CSS 규칙 0 · 이미지 자산 0**(10장 `src`/`href` 0).
- **노트**: 원본 노트 10장분 11항목(원본 pn-no 17·22~26·54·55·71·74)을 문구 그대로 66~75쪽으로 옮겼다(문구 차이 0장). 노트 제목은 덱 h2를 쓰므로 V5-12만 «MCP» → «MCP Model Context Protocol».
- **1차 실행 사고(수정 완료)**: 처음에는 부록을 `scrub()` 앞에 넣었다. shell HTML 주석 속 경로 글롭 `families/*.md`의 `/*`(종전 1020행)가 JS 주석 끝 `*/`(1242행)과 짝지어져 슬라이드 구간 전체가 «주석» 하나로 읽혔고, 그 안에 부록의 «플러그인»·«MCP»가 들어가자 구간이 통째로 지워졌다(`::PARTS::`·S05·END·THX·부록 소실 → 조립기 FAIL). 부록 삽입을 scrub 뒤로 옮겨 해소했다. 본편 scrub 거동은 종전과 같다(part-01·02·03·05 바이트 동일).
- **게이트 단어**: 본편 0(종전과 같음) · 부록 8건(V5-02 «플러그인» 3 · «MCP» 3 · V5-12 «MCP» 2 — 원본 그대로).
- **집필노트 드리프트(§0-C 관측) 해소**: `note_build.py` §5 N-24 행을 현행 출력 문장으로 맞춘 뒤 전체 재실행했다. 백업 대비 diff는 머리 1줄 · §1 부록 10행 · §6 3항뿐이고 §5·§3 기존 기록은 그대로다.

## 0-C. attempt-05 변경 이력 — 독립 검토(R-BUILD3-02) 경미 2건 수정 (2026-09-11)

> 입력: 독립 검토 R-BUILD3-02가 PASS(경미 3)를 냈고, 이 중 2건(N-24 한 장 변경 예 둘 · N-20 롤리팝 정렬 예외 미문서화)을 고친다. 재조립 파이프라인은 attempt-04와 완전히 같은 빌더 경로(`build.py`→`assemble_deck.py`→`notes_build.py`→`draft_build.py`)를 따랐다.
> 방식: 편집 전에 소스를 attempt-04 최종 상태로 두고 `build.py`·`assemble_deck.py`·`notes_build.py`·`draft_build.py`를 무변경 재실행해 덱 sha256이 `0a9b802b`로 재현됨을 먼저 확인했다(재현성). 사본은 `tmp/…/BUILD3/a05_backup/`(산출물 7 + 소스 10 + m6 9)에 있다.

| # | 지적 | 조치 | 바뀐 산출물 |
|---|---|---|---|
| 1 | **N-24** — 한 장에 변경 예가 둘(요청문 «꺾은선그래프로»와 «입력» 칸 예 «가장 중요한 KPI를 위에 크게»가 중복) | `m6/m6_p5b.html`의 «입력» 칸을 «예: 월별 매출 차트 모양»으로 바꿨다(줄 수 유지). 노트 «학생에게 줄 프롬프트»의 따옴표 안에 있던 «(다음 요청 예: 가장 중요한 KPI를 상단에 크게 보여줘.)»를 따옴표 밖 `<p class="aside">`(기존 `.pn-hint .aside` CSS 클래스 · 1주차 노트에 이미 있는 패턴)로 뺐다 — 프롬프트 문자열은 «월별 매출을 꺾은선그래프로 바꿔줘. 다른 차트와 숫자는 그대로 둬.»만 남는다 | `m6/m6_p5b.html` → part-05 1줄 · 덱 1줄 / `notes_build.py`(N-24 항목 3-튜플화 + T 렌더링에 aside 분기 추가 + JSON 저장용 2-튜플 정규화) → 노트 1줄 추가 / 초안 `N-24` 행(파생 · 소스 미수정) |
| 2 | **N-20** — 롤리팝 «값 정렬 필수»(kit `charts-ratio.md`) 예외 미문서화. 마진율 점은 매출 막대와 행 순서를 공유해 값 내림차순이 아니다 | **화면·차트는 바꾸지 않았다.** 예외 사유를 `자료/3주차_콘텐츠_집필노트.md` §3(표 뒤)과 이 보고 §0-1(끝)에 문서화했다 | 집필노트 §3 1줄 추가 · 이 보고 §0-1 1줄 추가. `note_build.py` §3 표 생성부 뒤에도 같은 문장을 추가(소스·출력 정합) |

- **관측(충돌 아님 · 보고)**: `note_build.py`(tmp 빌더 소스)를 무편집 상태로 재실행해 보니 §5 재료 처분표의 «자연어 수정 요청 예 3문장» 행이 attempt-04 문서(§11 결정요청 #9)가 «미반영·결정요청»으로 남겨둔 것과 달리 **이미 현재 `집필노트.md`에 반영돼 있었다**(«…«월별 매출을 막대그래프로 바꿔줘»는 … «꺾은선그래프로 바꿔줘…»로 교체(2026-09-11 attempt-04)» 문장). 그런데 `note_build.py` 소스의 §5 표 데이터는 이 문장 없이 원래 짧은 문장만 만든다 — **소스가 출력과 어긋나 있다**(누가 언제 소스 밖에서 이 문장을 넣었는지는 이 담당이 확인할 수 없다). 이 때문에 attempt-05는 §3 추가분을 `note_build.py` 전체 재실행이 아니라 **출력 파일에 직접 대상 문장만 추가**했다(§5 등 다른 절은 건드리지 않음 · `note_build.py`는 §3 생성부만 고쳐 다음 재실행부터 정합). §11 결정요청 #9는 사실상 처리된 것으로 보이니 오케스트레이터 확인 요청.

## 0-A. attempt-04 변경 이력 — r4 렌더 FAIL(`N-19`) · `N-24` 요청문 (2026-09-11)

> 입력: 오케스트레이터 실측 r4(`798639b0`) — `N-19` 바닥선초과 2 · 이탈 2(`ag3-lval is-key@이탈1391` · `ag3-lval is-low@이탈1391`) + `N-24` 요청문 결함. 브라우저 금지라 아래 원인은 **정적 추론**이며 재측정으로만 확정된다.
> 방식: attempt-03과 같은 빌더 경로. 편집 전 사본 `tmp/…/BUILD3/a04_backup/`(31파일)을 남기고 무변경 재실행으로 shard 7 · 덱 · 노트 · 초안 · `claims`/`notes_items`/`order.json`이 **바이트 동일**함을 확인한 뒤 고쳤다.

### 0-A-1. `N-19` — 원인 특정

1. **1391은 x가 아니라 y다.** `scripts/audit_render.js` L425 `by = (r.bottom - sr.top) / K` · L453 `by > H ? '@이탈' : '@'` — 라벨 **아랫변 y**가 720을 넘었다는 표기다(«x≈1391 > 1280»은 오독).
2. **현행 CSS로는 기준 혼동도 viewBox 스케일 누락도 없다.** 선(`.ag3-lsvg` `inset:0;width:100%;height:100%` · `viewBox="0 -100 100 100"` · `preserveAspectRatio="none"`)과 점·라벨(`left:x%;bottom:y%`)이 같은 `.ag3-lplot` 상자를 0~100 기준으로 공유한다(x=(i+0.5)/12 · y=값/최댓값×80).
3. **두 라벨의 y가 똑같은 것은 절대배치로는 나올 수 없다.** `bottom` 80% ↔ 57.03%, `translateY` −14px ↔ +(100%+14px)이 달라, 같은 컨테이닝 블록에서 아랫변이 겹치려면 높이가 음수여야 한다. 같은 y는 **두 라벨이 인라인 흐름의 한 줄**에 있을 때만 나온다(인라인은 `transform`을 무시). 즉 렌더 시점에 `.ag3-lval`·`.ag3-lsvg` 규칙이 **적용되지 않았다**. 그러면 크기 속성 없는 `<svg viewBox>`가 폭 100% 정사각형(약 1.1천 px)으로 흐름에 들어가고 라벨이 그 아래 줄로 밀린다 — 1391과 크기가 맞는다.
4. **규칙이 빠진 이유 = 링크된 `revision.css`의 옛 판본.** `.ag3-l*` 규칙은 attempt-03에서 `revision.css`(09:34)에 처음 생겼는데 링크 URL은 `강의덱.초안/revision.css` 그대로였다. r3(09:00 · `419ddac9`)가 읽은 판본(`a03_backup/draft/revision.css`)에는 `ag3-lval`이 0건이다. CSS는 덱 해시 밖이라 식별자도 이를 못 잡는다. 문서화된 함정이다(MEMORY «브라우저 캐시를 피하려면 CSS 링크 버전을 갱신한다» · 지도 «`deck.css`를 캐시하므로 `?v=`»). **방증**: r3 → r4 «행간 normal» 392 → **394(+2)** — 사라진 `.ag3-val` 21개는 `line-height:1.3`이 있어 계수 밖이고, 늘어난 2는 규칙 없이 `normal`을 물려받은 라벨 2개와 맞는다. 배제한 것: 파일 자체(괄호·문자열·주석·at-rule·주석 밖 비ASCII 0) · kit 3종과 shell 인라인의 `svg`/`span` 덮어쓰기 규칙 0 · 덱 JS(`--scale`만) · 디스크 파일 낡음(`revision.css` 09:34 < r4 로그 09:47~09:49).
5. `N-21`이 안 걸린 이유: 같은 클래스지만 선 영역 안에 글자 없는 점뿐이고 영역 높이가 고정이라 아래 축·건수 줄은 밀리지 않는다. 또 `isGraphic`은 `tagName === 'SVG'`로 비교해 인라인 SVG(`tagName`이 소문자 `svg`)를 보지 못한다 — r4에서 넘친 svg 자체는 보고되지 않았다(§11-10).

### 0-A-2. 조치

| # | 조치 | 바뀐 산출물 |
|---|---|---|
| 1 | `build.py`가 revision 링크에 **내용 해시** `?v=<sha256 앞8자>`를 붙인다(현행 `?v=b1f2cd19`). CSS가 바뀌면 URL이 함께 바뀌어 옛 CSS와 새 마크업이 짝지어질 수 없다. 선례: 원본 1주차 shell `revision.css?v=…` · `verify_deck.py` L1286은 `?`를 떼고 읽는다 | `shell.html` 1줄 → 덱 19행 |
| 2 | 차트 좌표·라벨·CSS는 **바꾸지 않았다**(원인이 좌표가 아니다). 라벨 22px · y 0 기준 · aria-label 12개 값 그대로 · `revision.css` 무변경 | — |
| 3 | `check_numbers.py` ③ 참조 무결성이 `?v=`를 경로로 읽어 «없음 1»이 되는 것을 `?`·`#`를 뗀 뒤 존재 확인하도록 고쳤다(판정 대상 5건 그대로 · 건너뛰기 아님) | 검사기(tmp) |
| 4 | **`N-24`** «월별 매출을 **막대그래프로** 바꿔줘.» → «월별 매출을 **꺾은선그래프로** 바꿔줘.» `dashboard.html`의 `c-month`는 이미 `groupedBars`(총매출·마진 막대 + 주문 수 선)라 원문 예시대로면 화면이 바뀌지 않는다. 바꾸면 총매출 막대가 선이 되는 한 곳이 보인다. 6자 → 7자(1자 증가 · 2줄 유지 추정). «다른 차트와 숫자는 그대로 둬.»는 유지 | `m6/m6_p5b.html` → part-05 1줄 · 덱 2507행 / `notes_build.py` → 노트 651행 / 초안 `N-24` 행(`draft_build.py`) |
| 5 | 노트 «다음 요청 예: 가장 중요한 KPI를 상단에 크게 보여줘.»와 화면 «입력» 예는 **유지** — 대시보드의 KPI 4장은 머리글·필터·«요소 1» 절 아래에 같은 크기로 있어 요청하면 한 곳이 눈에 띄게 바뀐다 | — |

- **라벨 좌표 추정**(현행 CSS · 1280×720 · 머리 블록 높이 추정 ±20px): 선 영역 y≈306~526. 최고월 라벨 x≈918~1034 · y≈307~336(점 윗변 ≈341 · 영역 윗변보다 ≈1px 안 · 단위 줄과 ≈7px 간격). 최저월 라벨 x≈342~458 · y≈414~443. **x 최대 ≈1034 · y 최대 ≈443.** 그림 칸 여유 ≈81px라 선 영역(220px)은 줄지 않는다고 본다(줄면 최고월 라벨이 단위 줄 쪽으로 올라간다 — §8).
- **반증 조건**: `?v=b1f2cd19`로 재측정해도 두 라벨이 **같은 y**로 이탈하면 캐시 가설은 틀렸다.
- 지시대로 `N-16` «③ 월별 매출 막대» · `N-33` «월별 매출 막대 → …» · `N-19` 노트 «대시보드는 막대로…»는 두었다(덱 «월별 매출 막대» 2건).

## 0. attempt-03 변경 이력 — 독립 검토(R-BUILD3) 지적 수정 (2026-09-11)

> 입력: 오케스트레이터 판정 5건. 브라우저 금지라 화면 판정(글자 폭·겹침·세로 예산)은 **정적 추정**이며 렌더 재측정 전까지 확정이 아니다.
> 방식: 이전 담당과 같은 빌더 경로를 썼다. 편집 전에 `build.py`·`notes_build.py`·`draft_build.py`·`note_build.py`를 그대로 다시 돌려 shard 7·노트·초안·집필노트·`claims.json`이 **바이트 동일**함을 확인했다(재현성). 그다음 빌더 소스(`tmp/…/BUILD3/m6/m6_p2·p5a·p5b.html` · `build.py` · `reps.py` · `notes_build.py` · `note_build.py` · `check_numbers.py`의 kit ID 표)와 `revision.css`(손 편집)를 고쳐 재생성했다. 편집 전 산출물 사본은 `tmp/…/BUILD3/a03_backup/`에 있다.

| # | 지적 | 조치 | 바뀐 산출물 |
|---|---|---|---|
| 1 | **[치명]** `N-18` 캡션이 사실과 다름 | 캡션을 «`dashboard.html` 화면에도 같은 이름 **«총매출»**로 나옵니다»로 바꿨다. 삭제하지 않고 사실만 남겨 장의 세로 배치를 유지했다. `dashboard.html`에 «총매출(취소·대기 제외, 반품 미차감)» 라벨이 실제로 있는지 grep으로 확인했다. 노트 `N-18` 👀 항목도 «화면에도 같은 이름 «총매출(…)»으로 같은 숫자가 나온다»로 고쳤다. JSON 키 이름 설명은 넣지 않았다 — 노트 항목은 초안 멘트 칸으로 그대로 옮겨지기 때문이다 | part-05 1줄 · 노트 1줄 · 초안 `N-18` 행 · 집필노트 §2 `N-18` 행·§6-1 · 이 보고 §9-1·§11 |
| 2 | `V3-06` 그림이 원본에 쓰는 장면 | SVG `<text>` «원본 CSV» → **«분석대상.csv»**. 치환 규칙이 2회(왼쪽 «둘이 동시에 고치면» 덱 2026행 · 오른쪽 «쓰는 건 한 역할입니다» 덱 2042행)라 **두 그림이 함께** 바뀌었다. 왼쪽도 A·B가 원본에 쓰는 그림이라 같은 충돌이다. rect·circle·path 좌표, stroke-dasharray, aria-label은 그대로다. 폭은 21px/800 한글 4자 약 84 + «.csv» 약 38 → **약 122 / rect 안쪽 약 154**로 추정했다 | part-04 2줄 · 매트릭스 `V3-06` 변경내용 |
| 3 | 간지 부제 6건이 보고되지 않음 | 내용은 유지했다. 아래 표를 매트릭스 P1~P6 `변경내용`에 «부제(.pd-sub) 전 → 후»로 올렸고, 같은 칸의 [생존] «3 중 2 유지 = 67%»를 «3 중 1 유지 = 33%(부제 교체 반영)»로 고쳤다 | 매트릭스 6행 |
| 4 | M6 차트가 kit 규격 밖 | §0-1 | part-05(`N-19`·`N-20`·`N-21`) · part-02(`N-09` 막대 영역 1줄) · `revision.css` · 노트 `N-19` · 집필노트 §2·§3 |
| 5 | `V4-07` 판정 어휘 | 보존 화면은 바꾸지 않았다. 노트에 💬 항목 1개(2문장)를 넣었다: «앞 대조 단계의 «일치 · 불일치 · 확인 불가»는 대시보드 숫자가 원본에서 다시 센 값과 맞는지 본 결과다. 이 장의 «채택 · 부분 채택 · 기각»은 그 결과를 근거로 검토 의견을 반영할지 사람이 정하는 다음 단계다(«확인 불가»는 두 단계에 모두 쓴다).» | 노트 항목 104 → 105 · 초안 `V4-07` 멘트 칸 |

간지 부제(원본 `courses/바이브코딩/sessions/3주차/강의덱.초안/part-0N.html` ↔ 새 shard):

| 간지 | 전 | 후 |
|---|---|---|
| P1 | 손대기 전에 지키고, 화면에 생기를 더합니다. | 코드만이 아닙니다. 오늘 다루는 것은 데이터입니다. |
| P2 | AI를 쓰는 방식은 하나가 아닙니다. | 깨끗해 보이는 표에도 셋이 숨어 있습니다. |
| P3 | 내 프로젝트를 넷으로 나눕니다. | 무엇을 물을 수 있는지부터 정합니다. |
| P4 | 팀을 돌리고, 엇갈리면 사람이 판정합니다. | 시키기 전에 계획을 보고, 승인 지점을 정합니다. |
| P5 | 도구를 붙이고, 기록을 남깁니다. | 계산한 숫자를 한 화면으로 모읍니다. |
| P6 | 주소를 만들고, 열리는지 확인하고, 세상에 내놓습니다. | 숫자를 원본과 맞춰 보고, 문서로 남기고, 다른 형태로 넓힙니다. |

### 0-1. M6 차트 — 형태 결정과 근거

| 장 | 전 | 후 | 근거(kit) |
|---|---|---|---|
| `N-19` 월별 매출 12개월 | C-column 막대 12 · 값·축 16px | **C-line**: 1계열 선 + 점 12. 값 라벨(22px)은 **최고월 `20,064,500`(파랑 큰 점) · 최저월 `14,302,250`** 두 곳, 축 12개월은 20px. 12개 값 전체는 `figure role="img"` aria-label에 있다 | `charts-basic` C-column «상한 7» · «연속 시계열 추세 → C-line» / C-line «x 시점 4~12 · 값 라벨은 끝점·변곡점만 선택» / `by-shape` numeric 3번 |
| `N-21` 배송 기간 9구간 | C-column 막대 9 | **C-line**: 점 9. 건수(22px)는 축(20px) 아래 한 줄 | C-column 상한 7 초과 · C-hbar «상한 8» 초과 · C-lollipop은 «값 정렬 필수»라 1~9일 순서가 깨짐 · 구간을 묶으면 JSON에 없는 합계를 새로 만들어야 함 → JSON 구간값 9개를 그대로 쓰는 규격형은 C-line(x = 연속 구간)뿐이다. 매핑 §4 `N-21` 후보 «C-hbar / C-line»과도 맞다 |
| `N-21` 평점 5점 | C-column 막대 5 | C-column 유지 · 막대 영역 110 → 120px(22px 값 라벨) | 3~7개 안 · 순서형 축 |
| `N-20` 상품군 6 | C-hbar + C-lollipop · 값 16px | 형태 유지 · 막대 값·마진율 값·«전체» 라벨 16 → 19px · 머리 18 → 19px | C-hbar «값 ≥19px» · C-lollipop «점/값 ≥19px» |
| `N-09` 이상값 5 | C-column · 값 16 · 축 18px | 형태 유지 · 값 22 · 축 19px · 막대 영역 250 → 236px | `.ag3-val`·`.ag3-axis` 공유 규칙이라 함께 바뀜. 긴 축 이름(«배송일 30 이상»)이 두 줄로 접힐 수 있어 영역을 줄여 상쇄했다 |

- **숫자**: 화면 숫자는 전부 `{{V:키}}` 자리표시로 채워 JSON 표시값 그대로다. 새로 계산해 화면에 낸 숫자는 0이다. 좌표(x = 칸 가운데 `(i+0.5)/n`, y = 값/최댓값 × 80%(`N-19`)·86%(`N-21`))와 `N-20` 점 척도(45~62 → **45~63%**)는 `style` 속성 안의 배치값이라 화면에 나오지 않는다. 척도를 넓힌 이유: 19px «59.0%» 라벨 끝이 마진율 칸(약 411px)의 403px 부근까지 가서(추정) 387px 부근으로 여유를 뒀다.
- **y축 0 기준**: 축 눈금 숫자를 새로 만들지 않으므로, 확대한 축은 크기 차이를 과장한다(kit C-line adaptation_note «오해를 부를 값이면 0 기준»). `N-19` 최고월 점은 선 영역 220px의 80% 높이라 22px 라벨이 영역 안에 든다(추정).
- **최저월 라벨을 둔 이유**: 12개 값을 모두 22px로 달면 «17,611,700» 약 113px가 칸 약 96px를 넘어 겹친다. 제목 주장(최고월)만 두면 크기를 가늠할 수 없어, JSON에 키가 있는 최저월(`MONTH-06_최소월`)을 점 아래쪽에 하나 더 뒀다. 리드 «오르내릴 뿐»의 폭이 보인다.
- **구현**: 선은 `svg viewBox="0 -100 100 100" preserveAspectRatio="none"`에 `vector-effect:non-scaling-stroke`(굵기 4px 고정)로 그렸다. 점과 값 라벨은 HTML 절대 배치라 글자 크기가 화면 px 그대로다. 레이아웃 호스트(`L-td-column-series`·`L-cs-side-by-side`)와 섹션 클래스가 그대로라 `deck.contract.json`은 바꾸지 않았다. 새 클래스는 `ag3-linechart`·`ag3-lplot`·`ag3-lsvg`·`ag3-lseries`·`ag3-ldot`·`ag3-lval`·`ag3-laxis`·`ag3-lcount`, 범례 `is-lline`·`is-ldot`이고 figure 태그는 `viz-line`이다.
- **7항 점검(차트를 바꾼 장)**: 정보 모양 numeric 유지 · layouts/charts `by-shape` 둘 다 열람 · 호스트 레이아웃과 element를 따로 판단 · 강조 `hl-mint-text` 1개 유지 · 직전 장과의 구도 관계 불변 · 이미지 없음.
- **N-20 정렬 예외**(attempt-05 · R-BUILD3-02 경미-2 · 화면·차트 무변경): 마진율 점은 같은 행의 매출 막대와 행 순서를 공유하므로 값 정렬하지 않는다. 점 색은 과목 토큰 `var(--mint-deep)`. `N-21`에서 롤리팝을 쓰지 않은 근거(일수 순서가 깨짐)와 달리, `N-20`은 행 대응을 우선한다. 같은 문장을 `자료/3주차_콘텐츠_집필노트.md` §3에도 등재했다.

### 0-2. 판정 요청 (attempt-03)

1. `N-16` 구조도 «③ 월별 매출 막대», `N-33` «월별 매출 막대 → …», `N-24` 요청문 «월별 매출을 막대그래프로 바꿔줘»는 `dashboard.html`(막대)을 가리키는 문구라 그대로 뒀다. `N-19`만 선으로 그려 «대시보드는 막대, 슬라이드는 선»이 된다. 노트 `N-19`에 «대시보드는 막대로, 이 장은 흐름이 보이게 선으로 그렸을 뿐 값은 같다» 큐를 넣었다. 화면 문구도 맞출지 결정이 필요하다.
2. 매트릭스 `V3-06` `보존유보` 칸의 «도해 라벨은 「원본 CSV」로 줄이고»는 허용 칸(`변경내용`) 밖이라 고치지 않았다. 이제는 낡은 서술이다.
3. `V3-06` 왼쪽 그림까지 «분석대상.csv»로 바뀐 것(같은 치환 2회)을 받아들일지.

## 0-B. attempt-02 변경 이력 — 렌더 실측 수정 (2026-09-11)

> 입력 명세: `제작관리/렌더비교_3차시.md`(측정 대상 `10c32268`). 이전 담당이 세션 한도로 중단한 작업을 이어받았다. 브라우저 금지라 **아래 «반영» 판정은 정적 분석(`scripts/audit_render.js` 판정식과 대조한 추정)이다. 렌더 재측정 전까지는 확정이 아니다.**

### 0-B-1. 이어받기 시점 상태 점검

| 명세 항목 | 상태 | 확인 근거 |
|---|---|---|
| A `N-16` 가로 늘림 | 반영 | `revision.css` `.ag3-wire` 1.3fr→1fr · `.ag3-wf-z` 가운데 정렬. 벌 5칸 중 `is-wide` 칸의 좌측 여백이 15%를 넘어 «한쪽 몰림» 전제(leftAligned)가 성립하지 않는다(정적 추정) |
| A `N-11` 세로 늘림 2 | 반영 | `.ag3-qa{align-content:center}` — 행 높이를 긴 칸 내용이 정한다. 두 칸은 벌 키가 달라(`DIV`·`DIV.is-no`) 따로 판정되지만, 실측 dY가 둘 다 43%라 내용 높이가 같아 둘 다 여백 ≈0 |
| A `N-22` 세로 늘림 | 반영 | `.ag3-three{align-content:center}` · 3칸 한 벌 |
| A 껍데기 상자 6장 37건 | shard 반영 · 덱 미반영 | 막대 몸통(`ag3-seg`·`ag3-fill`·`ag3-cat-fill`)을 «배경만 칠한 빈 span/div»에서 `svg rect`로 교체. m6 조각(`m6_p2` N-08·N-09 · `m6_p5a` N-17)과 빌더 생성 블록(N-19·N-20·N-21). 누적 막대 틀(`ag3-stack-bar`)은 안에 svg가 생겨 그래픽 포함 상자가 된다. 색·폭·높이·순서 불변 |
| A 폰트하한 2 | 반영 | `N-19` `.ag3-month .ag3-unit` 16→22px(막대 영역 230→222px로 늘어난 줄 높이 상쇄) · `END` `.concept-bottom`·`small` 22px(1차시 `revision.css` 908–911행과 같은 값임을 확인) |
| B `V1-10` stretchX | 등재 · 조건 충족 | 원본 `part-01` 대조: 두 칸(h3 1 + p 3) 글자 동일 · callout 동일(바뀐 것은 칸 밖 제목·리드). 실측 dX 37 = 원본 계약 dX37 |
| B `V4-06` stretchY | 등재 · 조건 충족 | 원본 `part-04` 대조: 카드 4장(b·p) 글자 동일 · callout 동일 · 칸 밖 배너 1문장만 변경. 실측 dY 63 = 원본 계약 dY63 |
| B 후보 `V5-04` → A | A로 수정 · 반영 | 1단 본문 31→46자 · 세 라벨 전부 변경 · callout에 1절 추가 → 모양이 다름. 실측 dY 49 ≠ 원본 61. `.w3-stateslide[data-slide="V5-04"] .w3-states{align-content:center}` — 특이도 0,3,0이라 뒤에 오는 shell `kit-additions`의 `.w3-states`(0,1,0)에 덮이지 않는다 |
| 재조립 · 보고 갱신 | 미반영 | 덱 sha256 `10c32268` 그대로였음 |

- shard 일관성: 이어받기 직후 shard를 `tmp/…/BUILD3/a02_resume_backup/`에 복사하고 `build.py`를 재실행했다. shard 7개 sha256이 **전후 동일**하다(손 편집 흔적 0 · 조각·빌더와 일치).

### 0-B-2. 이번 조치

- 명세 항목이 전부 shard에 반영돼 있어 **shard를 추가로 고치지 않았다**(이전 편집 되돌림 0).
- 재조립 → `419ddac9`. 덱 안 svg 막대 44 · 배경 span/div 막대 0 · 「기본 경화 5종」 1회.
- 문구·숫자·클래스 의미가 그대로라 `3주차_초안.md`·발표자 노트·집필노트는 고칠 곳이 없다(관련 표기 검색 0건).
- `deck.contract.json`은 이전 담당의 `known_violations` 2키를 그대로 두었다(§7). `warn_baseline`은 추가하지 않았다.

### 0-B-3. 판정 요청

- 껍데기 상자 수정은 «막대 몸통을 그래픽으로 그린다»이다. 차트 막대는 내용이 들어갈 상자가 아니라 그림이라 hollow 대상에서 빠지는 것이 판정식 취지와 맞다고 봤다. 검출기 회피로 볼지는 렌더 재측정 때 확인해 달라. kit 차트의 막대 구현과 대조하지는 않았다.

## 1. 최종 장수 · part 구성 — 75장 (본편 65 = 원본 유래 33 + M6 신규 32 · 부록 10 = 원본 그대로 · attempt-06)

| 파일 | PART | 장수 | 슬라이드(`data-slide`) |
|---|---|---:|---|
| `shell.html` 앞 | 오프닝 | 4 | S01 S02 **N-32** S05 |
| `part-01.html` | 1 · Agent가 다루는 파일과 CSV 구조 | 7 | P1 M1 **N-01** V1-01 **N-02** **N-03** **N-04** |
| `part-02.html` | 2 · 중복 · 결측 · 이상값 | 8 | P2 M2 **N-05** V1-03 **N-07** **N-08** **N-09** **N-10** |
| `part-03.html` | 3 · 답할 수 있는 질문과 지표 정의 | 5 | P3 M3 **N-11** **N-12** **N-13** |
| `part-04.html` | 4 · 계획 검토 · 승인 · 원본 보존 | 9 | P4 M4 V1-10 V5-04 **N-14** **N-15** V1-05 V3-06 V1-06 |
| `part-05.html` | 5 · HTML 데이터 대시보드 | 12 | P5 M5 **N-16**~**N-23** **N-33** **N-24** |
| `part-06.html` | 6 · 대조 · 정리 · 확장 | 18 | P6 M6 S03 V2-01 V4-09 V5-11 **N-25** V4-02 **N-26** V4-06 V4-07 V6-08 **N-27**~**N-31** · V6-01은 N-30 뒤 |
| `shell.html` 뒤 | 마무리 | 2 | END(V6-10 통합) THX |
| `shell.html` THX 뒤 | 부록(PART 구획 밖 · divider 없음 · 시간표 밖 · attempt-06) | 10 | V1-11 V2-02 V2-03 V2-04 V2-05 V2-06 V5-02 V5-12 V6-02 V6-05 (66~75쪽) |

`data-slide`는 원본 ID를 그대로 쓰고(재번호 0), 신규는 매핑 ID(N-01~N-33, N-06 없음)다. 순서는 매핑 §2-2와 1:1이다(빌더가 순서표로 조립 · 계약 `sequences`와 일치).

## 2. 시간표 — 앞 50 + 뒤 50 = 100 (휴식 10 · 정리·질의 10 별도)

| 블록 | 구간 | 쪽 | 분 |
|---|---|---|---:|
| 앞 50 | ① 오프닝 + 파일 범위·CSV 구조 | 1–11 | 10.0 |
| 앞 50 | ② 중복·결측·이상값 + 실습② | 12–19 | 15.0 |
| 앞 50 | ③ 답할 수 있는 질문·지표 정의 + 실습③ | 20–24 | 10.0 |
| 앞 50 | ④ 계획 검토·승인·원본 보존 + 실습④⑤ | 25–33 | 12.5 |
| 앞 50 | ⑤ 여유 | — | 2.5 |
| 뒤 50 | ⑥ 대시보드 + 실습⑥⑦ + 복구 안내 | 34–45 | 20.5 |
| 뒤 50 | ⑦ 독립 숫자 대조 + 실습⑧ | 46–57 | 15.0 |
| 뒤 50 | ⑧ 분석 Markdown + 실습⑨ | 58–60 | 7.0 |
| 뒤 50 | ⑨ 문서·발표 확장 + 정적 배포 소개 | 61–63 | 3.0 |
| 뒤 50 | ⑩ 여유 | — | 4.5 |
| **계** | | 1–63 | **100.0** |
| 정리·질의 | 회고 · THANK YOU · 질의 | 64–65 | (10, 계수 밖) |

## 3. M1~M6 적용 계수 — CSV 대비 누락 0

| 항목 | 값 |
|---|---|
| 덱에 오는 원본 행 | 33 (본편 31 + 실습안내 2) + 통합 1(V6-10 → END) |
| 변경유형(CSV) | M1 2 · M2 2 · M4 29 · M5 1 — 치환표 적용 **33섹션 / 누락 0** |
| 치환 규칙 | **216건 / 216건 적용** — 빌더가 규칙마다 «정확히 1회(또는 명시 횟수)» 일치를 강제, 어긋나면 쓰기 없이 중단 |
| 문서 수준 표기(§11-1) | shell `<title>` · 노트 `<title>` · 노트 `.pn-kicker` 3곳 적용 + **노트 `.pn-title`(h1 «바이브코딩 — 3주차 발표자 노트») 1곳 추가 발견·적용** |
| M5 | V6-10 callout 문장을 END 하단 kit 슬롯 `<small>`로 흡수(타임라인 SVG는 매핑 D-11대로 미이전) |
| M6 | 32장 — 레이아웃·element ID와 근거는 `자료/3주차_콘텐츠_집필노트.md` §2·§3 |
| 원본 무변경 | `courses/바이브코딩/**`은 읽기만 했다(빌더 입력 경로 전용) |

## 4. 매핑 §11 조립 지시 7건

| # | 지시 | 결과 |
|---|---|---|
| 1 | 문서 수준 표기 3곳 | 적용(+ `.pn-title` 1곳). 덱·노트 옛 표기 게이트 0건(§6) |
| 2 | V4-02·V4-07 제목 CSV 기준 | 「검토 요청에 넣을 것 셋」·「결과 비교하고 판정하기」 원문 유지 |
| 3 | S03 → part-06(48쪽) | rect 4·좌표·fill 변수 유지, SVG `<text>` 4건 + aria 1건만 교체. 본문 장이 되어 헤더에 빈 `.s-team` 훅 1개 추가(§9) |
| 4 | V1-01 삽화 복사 | `자료/images/s81_harness-horse-metaphor-v1-transparent.png` 복사 · sha256 앞 16자 원본과 동일(`04e37831c0317741`) |
| 5 | 폭 실측 3건 | 브라우저 금지로 **미실측** — §8 1순위 |
| 6 | M6 kit ID 실재 | 채택 **24종**(32장) **실재 24 · 미실재 0**(attempt-03: `N-19`·`N-21`에 `C-line` 추가). 그중 `kit/layouts/catalog.html` 코드 코어가 있는 레이아웃은 5종, 나머지는 families 스펙 + kit 프리미티브로 조립 |
| 7 | V5-04 3단 유지 | 3단·`is-wait`/`is-over` 클래스 불변. 3번째 칸은 attempt-06에서 원본 «승인 범위 초과»로 되돌렸다(경고 톤과 일치 · 노트 읽는 법 큐는 라벨 이름만 갱신) |

## 5. 숫자 대조 — 판정 253 · 불일치 0

- 원칙: M6·치환 문구의 숫자는 전부 `{{V:키}}`(지표기준값.json) 또는 `{{K:이름}}`(확정값 레지스트리 14종 — 출처 문구가 파일에 실제로 있는지 검사) 자리표시로 쓰고 빌더가 채웠다. 손으로 적은 데이터 숫자 0.
- `BUILD3/check_numbers.py`(독립 점검 · `798639b0` 기준): 선언 266건 재해석 **불일치 0** · 덱 화면 텍스트(aria-label·alt 포함) 숫자 토큰 **판정 253 · 일치 253 · 불일치 0** · 미판정 124(구조 번호 96 · 차시·PART·쪽 표기 27 · 도해 번호 1) · `{{` 잔존 덱 0 / 노트 0.
- attempt-02(`419ddac9`) 대비 선언 276 → 266 · 판정 263 → 253: `N-19` 화면 값 라벨이 12개에서 2개(최고월·최저월)로 줄어든 10건이다. aria-label의 12개 값은 그대로 선언·판정된다. `N-21` 배송 건수 9개는 막대 위에서 축 아래 줄로 옮겼을 뿐 같다.
- 매출 라벨: «총매출(취소·대기 제외, 반품 미차감)» + 반품 금액(`REV-08`) 별도 표시. 원본 2,424행 · 고유 `order_id` 2,400 · 분석 2,203을 N-07·N-17에서 구분. 결측 2층(무응답 64 / 해당 없음 2,320)은 N-08.
- 숫자 출처표(슬라이드 → 키 → 표시값, 169행): 집필노트 §4.

## 6. 검증 명령 · 종료코드 · 결과

### 6-E. attempt-06 재측정 (`8b6a3ff9` · 이 보고의 현행 수치)

모든 명령은 `CREATE_SLIDES_COURSE=AI_코딩_에이전트_입문_3차시`에서 실행했다. 출력 원문은 `tmp/…/BUILD3/a06_*.out`에 있다.

| 명령 | exit | 결과 |
|---|---|---|
| 편집 전 재현성: `build.py`·`assemble_deck.py`·`notes_build.py`·`draft_build.py` 무변경 재실행 | 0 · 0 · 0 · 0 | shard 7 · 덱(`f9e6d4a7`) · 노트 · 초안 · `claims`/`order`/`notes_items.json`이 백업과 **바이트 동일** |
| `build.py` → `assemble_deck.py`(1차) | 0 → **1** | `shell.html must contain exactly one '<!-- ::PARTS:: -->' marker (found 0)` — §0-D-1 사고. 덱은 기록되지 않았고, `notes_build`·`draft_build`는 쓰기 전 중단(1 · 1), `note_build`가 틀린 claims로 쓴 집필노트는 2차에서 재생성했다 |
| `python tmp/…/BUILD3/build.py`(수정 후) | **0** | 파일 7 · 슬라이드 75(본편 65 + 부록 10) · M6 32 · 치환 섹션 33 · 치환 **215/215**(V5-04 라벨 규칙 1 삭제) · 부록 헤더 표식 10/10 · 자리표시 선언 266 · 게이트 단어 본편 0 · 부록 8 · `?v=b1f2cd19` 그대로 |
| `python scripts/assemble_deck.py "…/3주차/강의덱.초안"` | **0** | 섹션 75 · part-divider 6 · 덱 sha256 `8b6a3ff9` |
| `notes_build.py` · `draft_build.py` · `note_build.py` | 0 · 0 · 0 | 노트 72장분 · 항목 116(105 + 부록 11) · 🗣 9 · [선택 시연] 9 / 초안 표 행 75 · 구간 9 / 집필노트 추적 81행 · 숫자 출처 169행 |
| `python scripts/verify_deck.py …/강의덱.html --parts 6` | **1** | FAIL 3 · WARN 0 · PASS 61 · 미판정 1. FAIL 3은 전부 계약 키다: «슬라이드 75장 ≠ 계약 65장» · «마지막 슬라이드(V6-05)에 'THANK YOU' 없음»(`closing_text`) · «덱에 있는데 계약에 없음: V1-11 … V6-05»(`coverage_deck_only`). `deck.contract.json`은 편집 금지라 고치지 않았다(§11-13) |
| `python scripts/verify_deck_quality.py …/강의덱.html`(기본 모드) | **0** | FAIL 0 · WARN **3** · PASS 8 — R-QC-19 V1-06·V4-07(종전) + **R-QC-02 V2-06**(의미 블록 3 · 원본 3주차 덱에서도 같은 WARN: `a06_vq_original.out` V1-12·V2-06·V5-13·V6-03) + **R-QC-11** «종결 슬라이드 뒤 부록 10장 중 V1-11(예시 마커) 본류 강등 의심»(부록 배치로 새로 생김). 계약 `warn_baseline.quality` 1과 어긋난다(§11-13) |
| `python scripts/verify_notes.py …/강의덱.html …/강의덱_발표자노트.html` | **0** | MISMATCH 0 · OK 72/72 · 판정 72 · 미판정 0 |
| `python scripts/check_title_survival.py 3주차 --strict` | **0** | 제목 강등 0 · 대응 슬라이드 없음 0 / 초안 75행 |
| `python scripts/report_draft_sync.py 3 --strict` | **0** | 일치 66 · 제목불일치 9(표지·간지·마무리 접두 — 종전과 같음) · 초안에만 0 · 덱에만 0 |
| `python scripts/verify_contract_waivers.py` | **0** | 계약 11건 중 위반 0 |
| `python tmp/…/BUILD3/check_numbers.py` | **0** | 선언 266 불일치 0 · 덱 숫자 판정 253 · 일치 253 · 불일치 0 · 미판정 134(구조 번호 96 · 차시·쪽 표기 37 — +10은 부록 헤더 «3차시» · 도해 번호 1) · `{{` 0/0 · `@@` 0 · 참조 로컬 5 · 없음 0 · 외부 2 · kit ID 24/24 |
| `python tmp/…/CSSORPHAN/dropped.py "…/3주차/강의덱.초안" "courses/바이브코딩/sessions/3주차/강의덱.초안"` | **0** | 새 덱 합계 **0** · 원본 합계 0 |
| `python scripts/verify_draft_quality.py 3 --strict` | 1 | FAIL 2(R-QD-04 · R-QD-05 — 기존 두 건 · **새 FAIL 0**) · PASS 8 |
| `python tmp/…/BUILD3/a06_annex_diff.py` | 0 | END 64 · THX 65 · 부록 66~75 · 엔진 분기 1 · 표식 10 · 부록 텍스트 차이 헤더 표식뿐 · 노트 문구 차이 0장 |
| 백업 대비 diff | — | part-01·02·03·05 · `revision.css` 바이트 동일 · part-04 3줄 · part-06 2줄 · shell +322(부록) · 엔진 2→8줄(`<style>` 무변경) · 노트 V5-04 1줄 · V3-06 제목 1줄 · 부록 +104줄 · 초안 머리 3줄 · 3행 · 부록 절 15줄 · 집필노트 머리 1 · §1 10행 · §6 +3 |

### 6-D. attempt-05 재측정 (`f9e6d4a7` · 참고)

모든 명령은 `CREATE_SLIDES_COURSE=AI_코딩_에이전트_입문_3차시`에서 실행했다. 출력 원문은 `tmp/…/BUILD3/a05_*.out`에 있다.

| 명령 | exit | 결과 |
|---|---|---|
| 편집 전 재현성: `build.py`·`assemble_deck.py`·`notes_build.py`·`draft_build.py` 무변경 재실행 | 0 · 0 · 0 · 0 | 덱 sha256 `0a9b802b`(attempt-04 값) **재현** · 노트·초안 attempt-04 산출물과 **바이트 동일**(`cmp`) |
| `python tmp/…/BUILD3/build.py`(`m6_p5b.html` N-24 입력 칸 수정 반영) | **0** | 파일 7 · 슬라이드 65 · M6 32 · 치환 216/216 · 게이트 단어 잔존 0 · 내용이 바뀐 shard는 part-05(1줄)뿐 · `revision.css` 무변경(`?v=b1f2cd19` 그대로) |
| `python scripts/assemble_deck.py "…/3주차/강의덱.초안"` | **0** | 섹션 65 · 전환 6 · 덱 sha256 `f9e6d4a7` |
| `python tmp/…/BUILD3/notes_build.py`(N-24 aside 분기 반영) · `draft_build.py` | 0 · 0 | 노트 62장분 · 항목 105 · 🗣 9 · [선택 시연] 9(diff: N-24 노트 1줄 추가) / 초안 표 행 65(diff: N-24 행 1줄) |
| 집필노트 §3 N-20 예외 문장: `note_build.py`에 §3 생성부 뒤 문장 추가(소스 반영) · 실제 파일은 전체 재실행 대신 **동일 문장을 직접 추가**(§0-C 관측 참고 — 소스·출력 기존 드리프트 보존) | — | `자료/3주차_콘텐츠_집필노트.md` §3 뒤 1줄 추가 · 그 외 diff 0 |
| `python scripts/verify_deck.py …/강의덱.html --parts 6` | **0** | FAIL 0 · WARN 0 · PASS 64 · 미판정 1(`dark_terminal_slides`) |
| `python scripts/verify_deck_quality.py …/강의덱.html`(기본 모드) | **0** | FAIL 0 · WARN 1(R-QC-19 V1-06·V4-07 6건 — 이전과 같음) · PASS 10 |
| `python scripts/verify_notes.py …/강의덱.html …/강의덱_발표자노트.html` | **0** | 판정 62 · 미판정 0 · 불일치 없음(새 `<p class="aside">` 구조에도 항목 판정 영향 없음) |
| `python scripts/check_title_survival.py 3주차 --strict` | **0** | 제목 강등 0 · 대응 슬라이드 없음 0 / 초안 65행 |
| `python scripts/report_draft_sync.py 3 --strict` | **0** | 일치 56 · 제목불일치 9 · 초안에만 0 · 덱에만 0(attempt-04와 동일 수치) |
| `python scripts/verify_contract_waivers.py` | **0** | 계약 11건 중 위반 0 |
| `python tmp/…/BUILD3/check_numbers.py` | **0** | 선언 266 불일치 0 · 덱 숫자 판정 253 일치 253 불일치 0 · 미판정 124 · `{{` 덱 0/노트 0 · 참조 로컬 5 존재 5 없음 0 외부 2 · kit ID 24/24 실재(attempt-04와 동일 수치) |
| `python scripts/verify_report_freshness.py 3주차 --strict` | **0** | 현행 덱 65장 · `f9e6d4a7` · 이 보고 «기준 덱 일치» PASS(식별자 갱신 후 재실행) |
| `python scripts/verify_draft_quality.py 3 --strict` | 1 | FAIL 2(R-QD-04 · R-QD-05 — 기존 두 건 · 새 FAIL 0) · PASS 8 |
| `grep -c` 덱 / 노트 / 초안 | — | «가장 중요한 KPI를 위에 크게» 0/0/0 · «월별 매출 차트 모양»(덱) 1 · «꺾은선그래프로 바꿔» 1/1/1 · 노트 `class="prompt"` 줄 안 «다음 요청 예» 0건 |

- 숫자: attempt-05는 화면 숫자를 바꾸지 않았다(§5 수치 그대로).
- `verify_deck_quality --strict` · 브라우저 렌더 · `receive_audit` · `run_deck_checks`는 지시 목록 밖·지시로 금지라 실행하지 않았다(attempt-04와 동일 사유).

### 6-0. attempt-04 재측정 (`0a9b802b` · 참고)

모든 명령은 `CREATE_SLIDES_COURSE=AI_코딩_에이전트_입문_3차시`에서 실행했다. 출력 원문은 `tmp/…/BUILD3/a04_*.out`에 있다.

| 명령 | exit | 결과 |
|---|---|---|
| 편집 전 재현성: `build.py` · `assemble_deck.py` · `notes_build.py` · `draft_build.py` 무변경 재실행 | 0 · 0 · 0 · 0 | shard 7 · 덱 · 노트 · 초안 · json 3종 **백업과 바이트 동일**(`cmp`) |
| `python tmp/…/BUILD3/build.py` | **0** | 파일 7 · 슬라이드 65 · M6 32 · 치환 216/216 · 게이트 단어 잔존 0 · `revision.css 링크 버전 ?v=b1f2cd19` · 내용이 바뀐 shard는 shell(링크 1줄) · part-05(`N-24` 1줄)뿐 · `revision.css` 무변경 |
| `python scripts/assemble_deck.py "…/3주차/강의덱.초안"` | **0** | 섹션 65 · 전환 6 · 덱 diff 2줄(19행 링크 · 2507행 요청문) |
| `python tmp/…/BUILD3/notes_build.py` · `draft_build.py` | 0 · 0 | 노트 62장분 · 항목 105 · 🗣 9 · [선택 시연] 9(diff 1줄) / 초안 표 행 65(diff 1줄) |
| 집필노트 드리프트: `note_build.py`의 출력 경로만 `tmp/…/a04_note_check.md`로 바꾼 사본 실행 | 0 | 현행 `자료/3주차_콘텐츠_집필노트.md`와 **바이트 동일** → 갱신 불필요(`자료/`는 쓰지 않음) |
| `python scripts/verify_deck.py …/강의덱.html --parts 6` | **0** | FAIL 0 · WARN 0 · PASS 64 · 구도 다양성 17종/본문 54장 · 미판정 1(`dark_terminal_slides`) |
| `python scripts/verify_deck_quality.py …/강의덱.html`(기본 모드) | **0** | FAIL 0 · WARN 1(R-QC-19 V1-06·V4-07 6건 — 이전과 같음) · PASS 10 |
| `python scripts/verify_notes.py …/강의덱.html …/강의덱_발표자노트.html` | **0** | 판정 62 · 미판정 0 · 불일치 없음 · 항목 0개 슬라이드 없음 |
| `python scripts/check_title_survival.py 3주차 --strict` | **0** | 제목 강등 0 · 대응 슬라이드 없음 0 / 초안 65행 |
| `python scripts/report_draft_sync.py 3 --strict` | **0** | 일치 56 · 제목불일치 9 · 초안에만 0 · 덱에만 0 |
| `python scripts/verify_contract_waivers.py` | **0** | 계약 11건 중 위반 0 |
| `python tmp/…/BUILD3/check_numbers.py` | **0** | 선언 266 불일치 0 · 덱 숫자 판정 253 일치 253 불일치 0 · 미판정 124 · `{{` 덱 0/노트 0 · `@@` 0 · 참조 로컬 5 존재 5 없음 0 외부 2 · kit ID 24/24 실재 |
| `python scripts/verify_report_freshness.py 3주차 --strict` | **0** | 현행 덱 65장 · `0a9b802b` · 이 보고 «기준 덱 일치» PASS |
| `python scripts/verify_draft_quality.py 3 --strict` | 1 | FAIL 2(R-QD-04 · R-QD-05 — 기존 두 건 · 새 FAIL 0) · PASS 8 |
| `grep -c` 덱 / 노트 / 초안 | — | «순매출» 0/0/0 · `{{` 0/0/0 · «막대그래프로 바꿔» **0/0/0** · «꺾은선그래프로 바꿔» 1/1/1 · 게이트 단어(덱/노트) 0/0 · 큐 표식(덱) 0 · 덱 «월별 매출 막대» 2(`N-16`·`N-33` 유지) |

- 숫자: attempt-04는 화면 숫자를 바꾸지 않았다(§5 수치 그대로 · 선언 266 · 판정 253).
- `verify_deck_quality --strict`는 attempt-03과 같은 이유(지시 목록 밖)로 실행하지 않았다.

### 6-A. attempt-03 재측정 (`798639b0` · 참고)

모든 명령은 `CREATE_SLIDES_COURSE=AI_코딩_에이전트_입문_3차시`에서 실행했다. 출력 원문은 `tmp/…/BUILD3/a03_{build,assemble,notes,draft,note,cn,vd,vq,vn,ts,rs,waivers,dq,matrix}.out`에 있다.

| 명령 | exit | 결과 |
|---|---|---|
| 편집 전 재현성: `build.py`·`notes_build.py`·`draft_build.py`·`note_build.py` 무변경 재실행 | 0 · 0 · 0 · 0 | shard 7 · 노트 · 초안 · 집필노트 · `claims.json` **백업과 바이트 동일**(`cmp`) |
| `python tmp/…/BUILD3/build.py` | 0 | 파일 7 · 슬라이드 65 · M6 32 · 치환 216/216 · 자리표시 266 · 게이트 단어 잔존 0 · 내용이 바뀐 shard는 part-02·04·05뿐(나머지 4개 `cmp` 동일) |
| `python scripts/assemble_deck.py "…/3주차/강의덱.초안"` | 0 | 섹션 65 · 전환 6 · 「기본 경화 5종」 1회 |
| `python tmp/…/BUILD3/notes_build.py` · `draft_build.py` · `note_build.py` | 0 · 0 · 0 | 노트 62장분 · 항목 105 · 원본 재사용 표시 30장 / 초안 표 행 65 / 집필노트 추적 81행 · M6 32행 · 숫자 출처 169행 |
| `python scripts/verify_deck.py …/강의덱.html --parts 6` | **0** | FAIL 0 · WARN 0 · PASS 64 · 구도 다양성 17종/본문 54장 · 미판정 1(계약 키 `dark_terminal_slides` 없음) |
| `python scripts/verify_deck_quality.py …/강의덱.html`(기본 모드) | **0** | FAIL 0 · WARN 1(R-QC-19 V1-06·V4-07 6건 — 이전과 같음) · PASS 10 |
| `python scripts/verify_notes.py …/강의덱.html …/강의덱_발표자노트.html` | **0** | 판정 62 · 미판정 0 · 불일치 0 |
| `python scripts/check_title_survival.py 3주차 --strict` | **0** | 제목 강등 0 · 대응 슬라이드 없음 0 / 초안 65행 |
| `python scripts/report_draft_sync.py 3 --strict` | **0** | 일치 56 · 제목불일치 9 · 초안에만 0 · 덱에만 0 |
| `python scripts/verify_contract_waivers.py` | **0** | 계약 11건 중 위반 0 |
| `python tmp/…/BUILD3/check_numbers.py` | **0** | 선언 266 불일치 0 · 덱 숫자 판정 253 일치 253 불일치 0 · 미판정 124 · `{{` 덱 0/노트 0 · `@@` 0 · 참조 로컬 5 없음 0 외부 2 · kit ID 24/24 실재 |
| `grep -c '순매출'` 덱 / 노트 / 초안 | — | **0 / 0 / 0**(노트 키 이름 설명도 두지 않음) |
| `grep -ciE '3주차\|3주 아카데미\|github\|netlify\|mcp\|플러그인'` 덱 / 노트 · `grep -cE 'hint-reveal\|💬\|👀\|🗣'` 덱 | — | 0 / 0 · 0 |
| `python scripts/verify_draft_quality.py 3 --strict` | 1 | FAIL 2(R-QD-04 · R-QD-05 — attempt-01과 같은 두 건 · 새 FAIL 0) · PASS 8 |
| `python tmp/…/BUILD3/a03_matrix_edit.py`(쓰기 + 자체 검증) | **0** | 82줄(데이터 81행) · 16열 아닌 행 0 · 백업 대비 바뀐 행 7(P1~P6 · V3-06) · 바뀐 열 = `변경내용`뿐 |

- `verify_deck_quality --strict`는 지시 목록 밖이라 실행하지 않았다(R-QC-19 WARN 1이 그대로라 strict면 이전처럼 exit 1일 것으로 보지만 **미확인**).

### 6-B. attempt-02 기록 (`419ddac9` · 참고)

| 명령 | exit | 결과 |
|---|---|---|
| `python tmp/…/BUILD3/build.py` | 0 | 파일 7 · 슬라이드 65 · M6 32 · 치환 216/216 · 자리표시 276 · 게이트 단어 잔존 0 · shard sha256 재실행 전후 동일 |
| `python scripts/assemble_deck.py "…/3주차/강의덱.초안"` | 0 | 섹션 65 · 전환 6 · 「기본 경화 5종」 1회 |
| `python scripts/verify_deck.py …/강의덱.html --parts 6` | **0** | FAIL 0 · WARN 0 · PASS 64 · 구도 다양성 17종/본문 54장 · 미판정 1(계약 키 `dark_terminal_slides` 없음) |
| `python scripts/verify_deck_quality.py …/강의덱.html`(기본 모드) | **0** | FAIL 0 · WARN 1(R-QC-19 V1-06·V4-07 6건) · PASS 10 |
| `python scripts/verify_notes.py …/강의덱.html …/강의덱_발표자노트.html` | **0** | 판정 62 · 미판정 0 · 불일치 0 |
| `python scripts/check_title_survival.py 3주차 --strict` | **0** | 제목 강등 0 · 대응 슬라이드 없음 0 / 초안 65행 |
| `python scripts/report_draft_sync.py 3 --strict` | **0** | 일치 56 · 제목불일치 9 · 초안에만 0 · 덱에만 0 |
| `python scripts/verify_contract_waivers.py` | **0** | 계약 11건 중 위반 0(이 덱 계약 PASS) |
| `rg -i -c '3주차\|3주 아카데미\|github\|netlify\|mcp\|플러그인'` 덱 / 노트 | 1 / 1 | **0건 / 0건** |
| `rg -c 'hint-reveal\|💬\|👀\|🗣' 강의덱.html` | 1 | 0건 |
| `python tmp/…/BUILD3/check_numbers.py` | **0** | 선언 276 불일치 0 · 덱 숫자 판정 263 일치 263 불일치 0 · 미판정 124 · `{{` 덱 0/노트 0 · `@@` 0 · 참조 로컬 5 없음 0 외부 2 · kit ID 23/23 실재 |

### 6-C. attempt-01 기록 (`10c32268` · 참고)

| 명령 | exit | 결과 |
|---|---|---|
| `python tmp/…/BUILD3/build.py` | 0 | 파일 7 · 슬라이드 65 · M6 32 · 치환 216/216 · 게이트 단어 잔존 0 · 경화 5종 블록 템플릿과 동일 |
| `python scripts/assemble_deck.py "…/3주차/강의덱.초안"` | 0 | 섹션 65 · 전환 6 · 산출 덱에 「기본 경화 5종」 1회 |
| `python scripts/verify_deck.py …/강의덱.html --parts 6` | **0** | FAIL 0 · WARN 0 · PASS 64 · 구도 다양성 17종/본문 54장 · 미판정 1(계약 키 `dark_terminal_slides` 없음) |
| `python scripts/verify_deck_quality.py …/강의덱.html --strict` | **1** | FAIL 1(R-QC-19) · PASS 10 — R-QC-08 89.2%(33/37) · R-QC-16 5.4%(N-18·V4-06) · R-QC-17 0/10 · R-QC-15 0% |
| `python scripts/verify_notes.py …/강의덱.html …/강의덱_발표자노트.html` | **0** | 판정 62 · 미판정 0 · 불일치 0 |
| `python scripts/check_title_survival.py 3주차 --strict` | **0** | 제목자리 64 · 제목교체 1 · 강등 0 |
| `python scripts/report_draft_sync.py 3 --strict` | **0** | 일치 56 · 제목불일치 9(`(표지)`·`(간지)`·`(마무리)` 접두) · 초안에만 0 · 덱에만 0 |
| `python scripts/verify_draft_quality.py 3주차 --strict` | 1 | R-QD-00 «초안 없음: `sessions\3주차주차\초안.md`» — 인자 정규화 결함(검증명령.md §2와 같은 유형). 숫자로 재실행 ↓ |
| `python scripts/verify_draft_quality.py 3 --strict` | **1** | FAIL 2(R-QD-04 · R-QD-05) · PASS 8 · 0판정가드 판정 1 |
| `rg -i -c '3주차\|3주 아카데미\|github\|netlify\|mcp\|플러그인'` 덱 / 노트 | 1 / 1 | **0건 / 0건**(원문 소개 장에도 해당 단어 없음) |
| `rg -c 'hint-reveal\|💬\|👀\|🗣' 강의덱.html` | 1 | 0건 |
| `python tmp/…/BUILD3/check_numbers.py` | **0** | §5 + 참조 무결성 로컬 5 · 없음 0 · 외부 2 · 자료/images 1개 전부 덱 참조 |

## 7. 남은 FAIL · WARN과 사유 (`known_violations` 2키 · `warn_baseline` 추가 0)

| 규칙 | 값 | 사유 |
|---|---|---|
| `render_stretchX` count 1 | V1-10 | **원본 계승(B)** — 요소 모양 동일 · 실측 dX 37 = 원본 계약 dX37 (§0-B-1) |
| `render_stretchY` count 1 | V4-06 | **원본 계승(B)** — 요소 모양 동일 · 실측 dY 63 = 원본 계약 dY63 (§0-B-1). 같은 후보였던 V5-04는 모양이 달라 등재하지 않고 고쳤다 |
| R-QC-19 | V1-06 · V4-07 (6건) | **원본 계승(B)** — 같은 명령을 원본 3주차 덱에 돌리면 V1-06·V4-07 포함 40건/14장이 나온다. 보존본 `.desc` 안의 `<strong>` 중첩을 검사기 정규식이 맨 텍스트로 읽는 경우다. 보존본 구조를 바꾸지 않았다 |
| `verify_deck` 계약 3키(attempt-06) | `slides` 65 · `closing_text` · 부록 10 ID 미등재 | 부록 10장 수록의 결과 — 계약 갱신은 오케스트레이터(§11-13) |
| R-QC-02(attempt-06) | V2-06(의미 블록 3) | 부록 · **원본 계승(B)** — 원본 3주차 덱에서도 같은 WARN |
| R-QC-11(attempt-06) | V1-11(예시 마커) | 부록을 THANK YOU 뒤에 둔 배치의 구조 WARN(2차시와 같은 배치) — §11-13 |
| R-QD-04 | PART 1(7) · 3(5) · 5(12) · 6(18) | `profile.md` §2 «회차당 장수 범위» 미정 → 기본값 8~11로 판정. 정본은 분 단위 시간표. 사유는 집필노트 §7 — **결정요청** |
| R-QD-05 | `3주차_콘텐츠_리뷰.html` 없음 | 허용 목록 밖이라 만들지 않았다(폴더의 `콘텐츠_리뷰.html`은 템플릿 사본) — **결정요청** |
| 사람검토 | R-COPY-01 10 · R-COPY-02 1 · R-COPY-03 6 | 프로필 §3-G 임계 없음 → 판정 강등. 대부분 원문(V1-01 등) 또는 CSV 확정 문구(M2 rm-lead · V5-04 callout)의 «— » 잇기 |
| 미판정 | verify_deck `dark_terminal_slides` | 원본 3주차 계약에도 없던 키. 실습 장 터미널은 `.terminal-copy` 형태가 아니다 |

## 8. 렌더 실측 우선 확인 목록 (정적 PASS ≠ 화면 안전)

**attempt-06 재측정(`8b6a3ff9` · `revision.css?v=b1f2cd19` 그대로)** — 이번 판에서 먼저 볼 것:
- 부록 66~75쪽 헤더가 «PART 6 / 6»이 아니라 «부록»으로 뜨는지(엔진 분기 · 표식 «3차시<br>부록» 10). 본편 쪽 번호가 «N / 75»로 바뀐 것(2차시와 같은 거동).
- 부록 10장이 원본 3주차 덱과 같은 모양인지 — 원본 CSS는 전부 shell `<style>`에 있고(원본 part `<style>` 0 · 원본 `revision.css` 없음 · 3차시 `revision.css`에 부록 클래스를 건드리는 규칙 0) shell `<style>`은 무변경이다. 원본 렌더 증거와 대조해 hollow·폰트하한·늘림이 새로 생기지 않는지.
- `V5-04` 3번째 라벨 «승인 범위 초과»(5→7자)가 칸 안 한 줄인지 · 세로 늘림 보정(`revision.css` 312~314행)이 여전히 맞는지.
- `V3-06` 제목 «한 파일은 한 역할만 씁니다»(19→15자) · 정의문 «수정» 복원 뒤 한 줄인지(정적 추정 약 930px / 칸 1152px).
- `V2-01` 좌·우 도해 «제작»(«대조»와 같은 2자).

**attempt-04 재측정(`0a9b802b` · 링크 `revision.css?v=b1f2cd19`)** — 이번 판에서 먼저 볼 것:
- 브라우저가 실제로 `?v=b1f2cd19` URL을 읽었는지(네트워크 또는 `document.styleSheets` href). 캐시를 피하는 조치이므로 이것이 증거의 전제다.
- `N-19` 두 라벨이 **서로 다른 y**인지, 최고월 라벨 윗변이 선 영역 윗변(추정 ≈306) 아래이고 단위 줄과 겹치지 않는지(추정 간격 ≈7px로 빠듯함), 최저월 라벨이 선과 겹치지 않는지. 추정 x 최대 ≈1034 · y 최대 ≈443.
- r4는 옛 `revision.css`로 렌더됐을 가능성이 커서 **attempt-03의 CSS 변경 전부**(아래 0번 목록: `N-19`·`N-21` 선 차트 · `N-20` 19px 라벨 · `N-09` 축 19px·막대 영역 236px · `N-21` 평점 120px)가 r4에서 측정되지 않았을 수 있다 — 0번 목록을 다시 확인한다.
- `N-24` 요청문 1자 증가(«꺾은선그래프로») 후에도 터미널 요청문이 2줄이고 오른쪽 4행 명세와 높이가 맞는지.

0. **attempt-03 재측정(`798639b0`)**: `N-19` 꺾은선 — 최고월 값 라벨이 선 영역(220px) 위로 넘치지 않는지 · 최저월 라벨이 선·축과 겹치지 않는지 · 점 12개가 축 칸 가운데와 맞는지 · 축 «2025-01»(20px) 12개가 한 줄인지 · 세로 예산(선 영역 220 + 단위 22px 줄 + 범례 + 캡션). `N-21` — 배송 선 100px + 축 두 줄(20px 일 · 22px 건수) · 평점 막대 120px(22px 값) · 두 칸 세로 예산. `N-20` — 19px 라벨 폭(«59.0%»가 마진율 칸 안인지 · «전체 53.2%»와 Fashion 53.3% 겹침). `N-09` — 축 19px 두 줄 접힘 · 막대 영역 236px 안의 22px 값 라벨. `V3-06` — «분석대상.csv»가 좌·우 두 rect(160px) 안인지. `N-18` 캡션 1줄. 새 점(12px·18px 원)과 선이 hollow·장식 판정에 걸리지 않는지.
1. attempt-02 항목(`419ddac9` 기준 · 그대로 유효): `N-16`·`N-11`·`N-22`·`V5-04` 늘림 해소 · 신규 6장 hollow 0 · 폰트하한 0 · 늘림이 `V1-10` stretchX 1과 `V4-06` stretchY 1만 남는지 · `N-16` 구역 이름(좁아진 칸) 줄바꿈 · `END` 하단 두 줄 22px의 세로 예산 · `N-09` A7 막대(`is-hold`)의 흰 몸통 + 파랑 테두리(`stroke` + `non-scaling-stroke`)가 원래 3px 안쪽선처럼 보이는지 · 막대 둥근 모서리(`clip-path`). 렌더 WARN 중 자간 22건은 명세 밖이라 손대지 않았다.
2. §11-5: `V5-11` 412px 행 «총매출 — 대시보드와 같음»(17px) · `N-18` 총매출 9자리(44px, 1.3fr 칸). (`V3-06`은 위 0)
3. §11-7: `V5-04` 3번째 칸 — attempt-06에서 원본 «승인 범위 초과»로 복원(위 attempt-06 목록).
4. `P1` 간지 제목 «Agent가 다루는 파일과 CSV 구조» — 56px·1080px 상자 추정 폭이 한계치에 가깝다.
5. `S05` 아젠다 `an-h` 6개 — 원본보다 긴 라벨이라 2줄 접힘 시 세로 예산 초과 가능.
6. `V1-05` 도해 박스 «mock_ecommerce_orders.csv»(26px) 가로 넘침.
7. 세로 예산이 빠듯한 M6: `N-07` · `N-17` · 실습 8장(요청문 + 4행). (`N-19`·`N-20`·`N-21`은 위 0)
8. 차트 라벨(19~22px)이 audit_typography 티어에서 어떻게 분류되는지.
9. `S01` 표지 제목 줄바꿈 위치 변경 · `N-16` 구조도 · `N-31` SVG · `N-03` 칩 줄바꿈.

## 9. 판정된 예외 · 해석 (투명 보고)

1. **매출 라벨 우선순위** — CSV의 `V4-07` 행 라벨·`V5-11` SVG 텍스트는 «순매출»이었으나 오케스트레이터 확정 라벨 «총매출»로 썼다. `dashboard.html` 화면 라벨도 «총매출»이다. attempt-02까지 이 칸에 적었던 «대시보드 화면 표기는 «순매출»»과 그에 기댄 `N-18` 캡션·노트는 사실과 달라 attempt-03에서 정정했다(§0 #1). «순매출»은 이제 JSON 키 이름(`REV-01_순매출_krw` 등)에만 남는다.
2. **구조 예외(보존본)** — `S03` 헤더 빈 `.s-team` 훅 1개 추가(이동 결과 · 본문 장 PART 진행 표시) · `V1-10` 제목 `<code>` 제거 + 리드 파일명 `<code>` 1→2 · `S01` `<br>`을 «직접» 뒤로(조사 앞 줄바꿈 WARN 해소) · END `<small>` 1개(M5). 그 밖은 텍스트 노드·`aria-label`만.
3. **주석** — 보존 섹션 안 HTML 주석(원본 수정 이력)은 전부 걷었다. shell CSS/HTML 주석은 옛 과정 표기가 든 것만 지웠다(경화 5종 블록 제외).
4. **M1 로드맵 순서** — CSV 문구(1단계 «하네스 복습»)와 §2-2 순서(N-01 7쪽 → V1-01 8쪽)가 어긋나 둘 다 따르고 노트 큐로 보완했다.
5. **실습 장 형식** — M6 실습 8장은 요청문 터미널 + 슬라이드지침 «목적·입력·출력·검증 기준» 4행(kit `.eval-steps`)이다. 첫 판의 `<dl>` 명세가 R-QC-17(단계 요소 <2)에 걸려 바꿨다.
6. **노트 접기** — S01·END·THX는 화면 번호가 없어 verify_notes가 항목을 거부하므로 그 큐를 2쪽·63쪽 노트에 넣었다. 노트 62장분 · 항목 105(attempt-03 +1) · 원본 재사용 30장 · 신규 32장 · 🗣 9 · [선택 시연] 9.
7. **R-QD-03 1차 FAIL 해소** — 노트의 «선택 시연» 상투 문장이 길어 노트:본문 비율 3행이 0.85를 넘었다. 문장을 줄이고 초안 멘트 칸에서 출처 표지를 뺐다(출처는 집필노트 §1).
8. **간지 부제(attempt-03 공개)** — 6개 간지의 `.pd-sub`는 attempt-01 조립 때 옛 소재를 걷어 내며 바뀌었으나 CSV·보고에 없었다. 내용은 그대로 두고 매트릭스와 §0에 전 → 후를 등재했다.

## 10. 허용 밖 변경 · 충돌

- `git status --short`: 세션 시작 스냅숏과 **같은 줄 집합**이다(새 줄 0). 과정 폴더 전체가 untracked라 파일 단위 저작 구분은 경로로 했다 — 이 담당이 쓴 것은 §산출물 목록뿐이고, 같은 폴더의 템플릿 파일(`README.md` · `강의덱.초안/README.md` · `자료/개념KB.md` · `리서치_리뷰.html` · `이미지-에셋.json` · `이미지-프롬프트.md` · `콘텐츠_리뷰.html`)과 `실습예제/**`는 쓰지 않았다.
- 템플릿 `part-01.html`·`shell.html`·`3주차_초안.md`·`deck.contract.json`은 덮어쓰기 전에 `tmp/…/BUILD3/template_*.orig.*`로 사본을 남겼다.
- 임시 파일은 `tmp/AI_코딩_에이전트_입문_3차시/BUILD3/`에만 있다. 같은 폴더의 `src_notes.html`·`src_sections.html`은 **이전 담당이 남긴 파일**이라 건드리지 않았다. 플랫폼 스크래치패드 파일 0개.
- 다른 주차 폴더·`제작관리/`·kit·scripts와의 충돌 관측 없음.
- **attempt-02**: `git status --short` 29줄로 세션 시작 스냅숏과 같은 줄 집합이다(새 줄 0). 이번에 쓴 것은 `강의덱.초안/` shard 7개(`build.py` 재실행 · 내용 동일) · `강의덱.html`(조립기) · `조립_보고.md` · `tmp/…/BUILD3/`의 로그·백업(`a02_*`)뿐이다. `실습예제/**`·원본·kit·scripts·`_verify/**`·다른 주차는 쓰지 않았다. 플랫폼 스크래치패드 파일 0개.
- **attempt-03**: `git status --short` 29줄로 attempt-02 기록(`a02_gitstatus.txt`)과 같은 줄 집합이다(새 줄 0). 편집 전 표식 이후 바뀐 파일(저장소 전체 · `tmp`·`.git` 제외) 중 이 담당 것은 `3주차/강의덱.초안/` shard 7개(`build.py` 재생성 · 내용이 바뀐 것은 part-02·04·05) · `revision.css`(손 편집) · `강의덱.html`(조립기) · `강의덱_발표자노트.html`·`3주차_초안.md`·`자료/3주차_콘텐츠_집필노트.md`(빌더) · `조립_보고.md` · `제작관리/보존매트릭스_3차시.csv`(7행 `변경내용`)뿐이다. `deck.contract.json`·`실습예제/**`·원본·kit·scripts·`_verify/**`·다른 주차는 쓰지 않았다. 임시 파일은 `tmp/…/BUILD3/`(`a03_*` 로그·백업·`a03_matrix_edit.py`, 빌더 소스 편집)에만 있고 플랫폼 스크래치패드 파일은 0개다.
- **attempt-03 관측(이 담당이 쓰지 않음)**: 같은 기간에 `sessions/2주차/{강의덱.html, 강의덱.초안/revision.css, 조립_보고.md}`(다른 담당)와 `제작관리/상태장부.md`가 바뀌었다. 이 담당은 `상태장부.md`를 열거나 쓰지 않았다 — 다른 담당이나 오케스트레이터의 기록으로 보이며, 충돌로 보고한다.
- **attempt-04**: `git status --short` 29줄 · attempt-02 기록(`a02_gitstatus.txt`)과 같은 줄 집합(새 줄 0 · `a04_gitstatus.txt`). 이 담당이 쓴 것은 `3주차/강의덱.초안/` shard 7개(`build.py` 재기록 · 내용이 바뀐 것은 `shell.html`·`part-05.html` 각 1줄) · `강의덱.html`(조립기) · `강의덱_발표자노트.html`·`3주차_초안.md`(빌더 · 각 1줄) · `조립_보고.md` · `tmp/…/BUILD3/`(빌더 소스 `build.py`·`m6/m6_p5b.html`·`notes_build.py`·`check_numbers.py` 편집 · `a04_*` 로그 · `a04_backup/` · `a04_note_check*`)뿐이다. `revision.css`·`deck.contract.json`·`제작관리/**`·`실습예제/**`·`자료/**`·원본·kit·scripts·`_verify/**`·1·2주차는 쓰지 않았다. 플랫폼 스크래치패드 파일 0개.
- **attempt-05**: `git status --short` 29줄 · attempt-02 기록(`a02_gitstatus.txt`)과 같은 줄 집합(새 줄 0). 이 담당이 쓴 것은 `3주차/강의덱.초안/part-05.html`(빌더 재기록 · 1줄) · `강의덱.html`(조립기) · `강의덱_발표자노트.html`·`3주차_초안.md`(빌더 · 각 1줄) · `자료/3주차_콘텐츠_집필노트.md`(§3 뒤 1줄 · §5 등 다른 절 무변경) · `조립_보고.md` · `tmp/…/BUILD3/`(빌더 소스 `m6/m6_p5b.html`·`notes_build.py`·`note_build.py` 편집 · `a05_*` 로그 · `a05_backup/`)뿐이다. `revision.css`·`deck.contract.json`·`제작관리/**`·`실습예제/**`·원본·kit·scripts·`_verify/**`·1·2주차는 쓰지 않았다. `run_deck_checks`·`receive_audit`·브라우저는 지시로 금지라 실행하지 않았다. 플랫폼 스크래치패드 파일 0개.
- **attempt-05 관측(충돌 후보)**: §0-C에 적었듯 `note_build.py`(tmp 소스)의 §5 처분표 생성 코드가 현재 `자료/3주차_콘텐츠_집필노트.md`의 §5 실제 문장과 어긋나 있었다(소스를 그대로 돌리면 attempt-04가 넣었다고 §11-9가 «미결»로 적은 문장이 사라진다). 이 담당은 §5를 건드리지 않고 §3만 추가해 기존 내용을 보존했다 — 이 드리프트의 원인(누가 언제 `자료/`를 직접 고쳤는지)은 확인할 수 없어 오케스트레이터 확인이 필요하다.

- **attempt-06**: `git status --short` 29줄 · attempt-02 기록(`a02_gitstatus.txt`)과 같은 줄 집합(새 줄 0 · `a06_gitstatus.txt`). 이 담당이 쓴 것은 `3주차/강의덱.초안/` shard 7개(빌더 재기록 · 내용이 바뀐 것은 `shell.html`·`part-04.html`·`part-06.html`) · `강의덱.html`(조립기) · `강의덱_발표자노트.html`·`3주차_초안.md`·`자료/3주차_콘텐츠_집필노트.md`(빌더) · `조립_보고.md` · `tmp/…/BUILD3/`(빌더 소스 `reps.py`·`build.py`·`notes_build.py`·`draft_build.py`·`note_build.py` 편집 · `a06_*` 로그 · `a06_backup/` · `a06_drift/` · `a06_annex_diff.py`)뿐이다. `revision.css`·`deck.contract.json`·`제작관리/**`·`실습예제/**`·`자료/images`·원본·kit·scripts·`_verify/**`·1·2주차는 쓰지 않았다. `run_deck_checks`·`receive_audit`·브라우저는 지시로 금지라 실행하지 않았다. 플랫폼 스크래치패드 파일 0개.

## 11. 결정요청

1. **R-QD-05** — `3주차_콘텐츠_리뷰.html` 생성 여부(허용 목록 밖).
2. **R-QD-04 / `profile.md` §2·§3 기준선** — 이 덱을 첫 완성 덱 실측으로 기록할지(R-COPY 3건 미판정도 같은 원인).
3. **`자료/이미지-에셋.json`** — 템플릿 그대로(`session: N주차`, slides 0)라 V1-01 삽화가 매니페스트에 없다. 허용 목록 밖이라 등재하지 않았다.
4. **`제작관리/매핑_3차시.md` §4 `N-24` 행 «월별 매출을 막대로»** — 덱·노트·초안은 attempt-04에서 «꺾은선그래프로»가 됐다. 매핑은 편집 금지라 고치지 않았다(`N-16`·`N-33`·`N-19` 노트의 «막대»는 지시대로 유지 — attempt-03 §0-2-1 요청은 이것으로 닫힌다).
5. **매트릭스 `V3-06` `보존유보` 칸의 낡은 «원본 CSV» 서술** — 허용 칸 밖(§0-2-2). `V3-06` 왼쪽 그림 동반 교체 승인(§0-2-3).
6. **R-QC-19 원본 계승 2장** — waiver 등재 또는 보존본 `.desc` 강조 조정 중 선택.
7. 매핑에서 넘어온 미결: D-01(M4 해석 범위) · D-02(S02 뒤 두 칸) · D-03(DOCX/PPTX 시연 도구) · D-04(정적 배포 방식 · V6-01 «강사 시연 주소») · D-05(Antigravity UI 명칭) · D-08(부록 10장) · D-09(END 계보 문구 — 1·2차시와 같아야 함) · D-11(V6-10 타임라인 소실).
8. **껍데기 상자 수정 방식** — 막대 몸통을 svg로 그린 것이 판정식 취지에 맞는지(§0-B-3).
9. **집필노트 §5 처분표 «자연어 수정 요청 예 3문장 · 사용(2)»** — (attempt-05 확인) 현재 `자료/3주차_콘텐츠_집필노트.md` §5에는 이미 «…«막대그래프로»에서 «꺾은선그래프로»로 교체(2026-09-11 attempt-04)» 사유 문장이 들어 있다. 다만 `note_build.py`(tmp 소스)의 §5 표 데이터는 이 문장을 만들지 않아 **소스와 출력이 어긋난 상태**다(§0-C 관측). 이 항목은 사실상 처리된 것으로 보이나, 소스 정합은 별도 확인이 필요하다.
10. **감사기 미탐 — `audit_render.js` `isGraphic`** — `tagName === 'SVG'`는 인라인 `<svg>`(`tagName` 소문자)를 잡지 못한다. 2026-08-03에 넣은 그래픽 분기가 인라인 SVG에는 작동하지 않아, r4에서 넘친 `N-19`·`N-21` svg가 보고되지 않았다. scripts는 허용 밖.
11. **r4 증거의 범위** — r4가 옛 `revision.css`로 렌더됐다면 attempt-03 CSS 변경(`N-20`·`N-09`·`N-21` 평점 등)의 PASS도 측정되지 않은 것이다. `?v=b1f2cd19` 판으로 재측정해야 증거가 된다(§8).
12. **캐시버스터 유지 조건** — 해시는 `build.py`를 돌릴 때만 갱신된다. `revision.css`를 손으로 고친 뒤 `build.py` → 조립을 건너뛰면 URL이 그대로라 같은 사고가 난다. 이 절차를 조립 규약에 둘지.
13. **(attempt-06) 계약 갱신 — 부록 10장** — `deck.contract.json` `slides` 65→75 · `closing_text`(마지막 장이 V6-05 — 2차시는 이 키를 두지 않고 미판정으로 둠) · 부록 10 ID 등재(시퀀스 추가 또는 waiver `coverage_deck_only`) · `warn_baseline.quality` 1→3(R-QC-02 V2-06 원본 계승 · R-QC-11 부록 배치). 편집 금지라 손대지 않았다 — `verify_deck` exit 1의 원인은 이것뿐이다.
14. **(attempt-06) 매트릭스 반영** — `V5-04` `변경내용` 3단 «읽기 허용 → 계획 확인 → 승인 범위 초과» · `보존유보`의 «「실행 승인」 라벨» 서술 삭제 · [생존] 10 중 4(라벨 1 복원 · 추정). `V3-06` `변경내용` 제목 «한 파일은 한 역할만 씁니다» · w3-def «같은 수정 단계에서 …». `V2-01` `변경내용` 파일명 6건 → «정리 · 계산 · 제작». 부록 10행: 새순서 A1~A10 = 66~75쪽 · `변경내용` «원본 그대로 수록 · 헤더 «3차시<br>부록» 표식만 · 섹션 안 HTML 주석 제거».
15. **(attempt-06) 부록 문구의 원본 맥락** — 변경유형 «없음»이라 손대지 않았다. 화면: V1-11 «오늘 이 프로젝트는 커밋되고, 공개됩니다»(커리큘럼 23행 «Git·GitHub 제외»와 어긋남) · V6-02 «오늘 세 번 한 점검 중 이번이 제일 중요합니다» · V6-05 «그대로 다음 실습으로 갑니다» · V2-04 aria·V2-06 «HTML·CSS·JS·QA 담당». 노트: V1-11 «오늘 세 번 하는 점검의 첫 번째» · V2-04 «오늘 오후 내내 쓰는 방식» · V2-05 «왜인지는 다음 장» · V6-02 «PART 1·5에서 두 번 했다». 둘지·고칠지·뺄지.
16. **(attempt-06) 본편 쪽 번호 분모** — 부록이 덱 안에 들어가 본편 화면의 «N / 65»가 «N / 75»가 된다(2차시와 같은 거동). 받아들일지.

(9번은 attempt-06에서 `note_build.py` §5를 출력 문장에 맞춰 닫았다.)

(attempt-02까지의 «대시보드 라벨 통일 여부» 요청은 전제(대시보드 화면이 «순매출»)가 사실이 아니어서 삭제했다 — §9-1.)

## 실행하지 않은 검증과 이유

- 브라우저 렌더 실측 · `receive_audit` · `run_deck_checks` — 지시로 금지. 렌더 증거가 없으므로 첫 러너는 FAIL이 정상이다.
- `build_release.py`(배포본) · `verify_required_statements`(개념KB 없음) · `verify_judgement_log` — 이번 지시 범위 밖.
- attempt-02: 브라우저 렌더 재측정 · `receive_audit` · `run_deck_checks`는 지시로 금지라 늘림·hollow·폰트하한 해소는 **정적 추정**이다. `verify_deck_quality --strict`·`verify_draft_quality`는 지시 목록 밖이라 실행하지 않았다(§6-B).
- attempt-03: 브라우저 렌더 · `receive_audit` · `run_deck_checks`는 지시로 금지라 `N-19`·`N-21` 선 차트 배치, 19~22px 라벨 폭, «분석대상.csv» 글자 폭은 **정적 추정**이다(§8-0). `verify_deck_quality --strict`는 지시 목록 밖이라 실행하지 않았다.
- attempt-04: 브라우저 렌더 · `receive_audit` · `run_deck_checks`는 지시로 금지(작업 중 «미리보기 서버 확인» 훅 안내가 떴으나 지시가 우선) — `N-19` 원인(옛 `revision.css` 캐시)과 라벨 좌표·`N-24` 줄 수는 **정적 추론·추정**이며 재측정으로만 확정된다(§0-A-1 반증 조건). `_verify/deck-audit.json`은 접촉 금지라 열지 않았고, 근거는 `tmp/…/BUILD3/run_deck_checks-r3·r4.log`와 `scripts/audit_render.js`·`audit_typography.js` 판정식이다. `note_build.py`는 출력이 `자료/`(허용 밖)라 제자리 실행하지 않고 tmp 출력으로 드리프트 0만 확인했다. `verify_deck_quality --strict`는 지시 목록 밖이라 실행하지 않았다.
- attempt-05: 브라우저 렌더 · `receive_audit` · `run_deck_checks`는 지시로 금지라 오케스트레이터 재측정 대상이다. `verify_deck_quality --strict`는 지시 목록 밖이라 실행하지 않았다(기본 모드만 실행 · §6-D). `_verify/` 접촉 금지 준수. `note_build.py` 전체 재실행은 §0-C 관측(소스·출력 드리프트) 때문에 의도적으로 하지 않았고, §3 문장만 출력 파일에 직접 추가했다 — 이 방식이 맞는지는 오케스트레이터 판정이 필요하다.
- attempt-06: 브라우저 렌더 · `receive_audit` · `run_deck_checks`는 지시로 금지라, 부록 헤더 «부록» 표시 · 부록 10장이 원본과 같게 렌더되는지 · V5-04 라벨과 V3-06 정의문 줄 수는 **정적 추정**이다(§8). `verify_deck_quality --strict`는 지시 목록 밖이라 실행하지 않았다. `deck.contract.json`은 편집 금지라 `verify_deck`의 계약 FAIL 3은 해소하지 못했다(§11-13).

## 산출물

`강의덱.초안/shell.html` · `part-01~06.html` · `revision.css` · `강의덱.html`(조립기 생성) · `강의덱_발표자노트.html` · `3주차_초안.md` · `deck.contract.json`(attempt-03 무변경) · `자료/images/s81_harness-horse-metaphor-v1-transparent.png` · `자료/3주차_콘텐츠_집필노트.md` · `조립_보고.md` · (attempt-03) `제작관리/보존매트릭스_3차시.csv` P1~P6·V3-06 `변경내용` · (attempt-06) 부록 10장은 `강의덱.초안/shell.html`의 THX 뒤
