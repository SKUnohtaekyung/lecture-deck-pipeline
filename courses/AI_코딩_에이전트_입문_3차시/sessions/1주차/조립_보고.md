# 조립 보고 — 1차시 (T40-D + T40-A 상당 · 단일 라이터)

> 기준 덱: **73장** · sha256 `b56c6562` (attempt-04 보존 문안 정정 재조립본 · 2026-09-11)
> 작성: 1차시 조립 담당 · 2026-09-10(attempt-01·02) / 2026-09-11(attempt-03 · 마무리 정정 · BUILD1-CSSFIX · attempt-04) · 커밋하지 않음
> 정본 입력: `제작관리/제작브리프.md` · `보존매트릭스_1차시.csv`(85행) · `매핑_1차시.md` v2 · 오케스트레이터 확정 판정(R-MAP1-02 12건) · `profile.md` · `슬라이드지침.md` · `강의덱.초안/README.md` · attempt-02: `제작관리/렌더비교_1차시.md` · attempt-03: 독립 검토 R-BUILD1 + 오케스트레이터 판정
> 모든 명령은 `CREATE_SLIDES_COURSE=AI_코딩_에이전트_입문_3차시` 아래에서 실행했다. **최신 수정은 §-3(attempt-04 · 보존 문안 정정)이고, CSS 잔해 복원은 §-2(BUILD1-CSSFIX), 나머지 최종 기준은 §-1(attempt-03)이다.** §0은 attempt-02 기록, §1~§11은 attempt-01 기록이며, 값이 바뀐 곳은 그때그때의 최신 절에 적었다.

## -3. attempt-04 — 보존 문안 정정 5건 (2026-09-11)

R-PRESERVE-1(범위 한정 PASS)에서 문안 결함 5건이 나왔고, 오케스트레이터가 모두 고치기로 판정했다. 수정은 shard 4파일(`part-01`·`part-03`·`part-04`·`shell.html`)의 **문구와 alt에만** 했고, 클래스·구조·CSS 변경은 0이다. 덱은 조립기로 다시 만들었다. 노트와 초안은 §-1과 같은 방식(직접 동기화)으로 맞췄다. `deck.contract.json`·`제작관리/`·2·3주차·원본은 건드리지 않았다. 수정 전 사본은 `tmp/…/BUILD1/A04_pre/`에 있다.

### -3-1. 건별 원본 대조 · 전→후

| # | 위치 | 원본(worktree shard) | 전 | 후 |
|---|---|---|---|---|
| 1 | `S28A` 목록 3항 | 글자가 잘리고 밀리지 않음 | 입력하면 점수가 오름 | **글자가 잘리고 밀리지 않음**(원문) |
| 1 | `S28A` 터미널 | …관심사 2~3개가 보이며,`<br>`글자가 잘 읽히면 첫 생성 성공입니다. | …단어와 입력칸이 보이며, 단어를 맞게 입력하면`<br>`다음 단어로 넘어가면 첫 생성 성공입니다.(비문) | …단어와 입력칸이 보이며,`<br>`**글자가 잘리거나 밀리지 않으면** 첫 생성 성공입니다. — 목록 3항과 같은 항목 · 줄 수 3 유지 |
| 1 | `S28A` 노트 48 · 초안 멘트 | (신규 노트) | …무엇을 하면 끝인지… 앞 장 «검증» 칸과 같은 내용입니다. | …**무엇을 확인하면** 끝인지… 앞 장 «검증» 칸과 **짝을 이룹니다**.(화면 문장 · 매트릭스 보존유보 «M6-05의 «검증» 칸과 짝»에 맞춤) |
| 2 | `S39` 체크 3항 | ✓ 글자와 배치가 깨지지 않음 | ✓ 입력하면 점수가 오름 | **✓ 글자와 배치가 깨지지 않음**(원문 · 3항 유지) |
| 2 | `S39` 목업 kicker | HELLO, I AM | SCORE 0(태그 «점수 0»과 중복) | **TYPE THIS WORD** — 매트릭스에 적힌 태그 «점수 0»과 노트 53의 «점수 0» 언급은 유지 |
| 3 | `12` 목업 | WORKSPACE / 현재 폴더를 여세요 | PROJECT / 프로젝트 폴더를 여세요 | 원문 복원 |
| 3 | `12` 02 설명 | …현재 작업공간 영역을 함께 찾습니다. | …현재 프로젝트 폴더 영역을 함께 찾습니다. | …**현재 작업공간 영역**을 함께 찾습니다. |
| 3 | `M6-04` 4단계(신규 장) | — | …현재 프로젝트 폴더 영역을 찾습니다. | …**현재 작업공간 영역**을 찾습니다.(바로 뒤 `12`와 같은 용어 · grep 0 조건) |
| 4 | `S42` img alt | 이번 주 한 파일과 다음 주 여러 파일 프로젝트를 비교하는 이미지 | 이번 차시 한 파일과 다음 차시 여러 파일 프로젝트를 비교하는 이미지 | **기준 파일 하나로 날짜별 뉴스 파일을 여러 개 만드는 2차시 뉴스 조사 Agent 흐름을 나타낸 이미지** |
| 5 | `S10B` 7 운영·개선 | 사용자의 반응과 문제를 살펴 다음 개선으로 이어 갑니다. | (뒤에) 써 본 사람의 반응을 보고 다시 고칩니다. | 추가 문장 삭제(원문) |

판단 근거:

- **3 — 복원**: 매트릭스 `VC-W1x-12`의 판정근거는 «브리프 §4 M2 · 원문에 Antigravity UI 명칭 없음 → 일반 표현 사용»뿐이다. UI 명칭을 모를 때 일반 표현을 쓰라는 근거일 뿐, «PROJECT»가 새 도구의 UI 명칭이라는 근거가 아니다(D-02 미결). «WORKSPACE/작업공간»도 도구 고유어가 아닌 일반 표현이고, `17` 제목과 `S36` must_keep이 같은 말을 쓴다. 그래서 «사유 없음»으로 판정했다. 목업의 라벨과 안내문은 매트릭스에서 한 절로 묶여 있어 함께 되돌렸다. 라벨만 되돌리면 원본에도 매트릭스에도 없는 조합이 된다.
- **5 — 출처**: 삭제한 문장은 원본 `W05` «개선» 칸의 정의문(`proc-def`)이다. 매트릭스가 흡수하라고 한 비유 7종에는 들지 않는다. 7종(여행 계획·플레이팅·손님 앞·주방·창고·거울 앞·매장)은 모두 남아 있어 §3 «M5 흡수» 서술은 그대로 유효하다.
- **4 — alt**: 그림 실물은 «문서가 든 폴더 하나 → 세 갈래 → 문서 세 장»이다(파일을 직접 열어 확인). 2주차 덱의 근거는 `NEWS_AGENT.md` 기준 파일 · «날짜별 Markdown 파일로 저장» · `news/2026-09-10.md` · 반복 실행이다. §-1 마무리 정정 ①(그림에 보이는 것만 묘사)과 충돌하지 않도록, 그림의 «하나→여럿» 구조를 2차시 실제 흐름에 대응시키는 데서 멈췄다. 화면 문구와 노트에는 같은 서술이 없어 alt만 고쳤다.
- **2 — kicker**: 중복을 하나로 줄이는 방법으로 태그 삭제(3→2칸) 대신 kicker 교체를 택했다. 태그 3칸 구조와 매트릭스·노트의 «점수 0»을 지키기 위해서다.

### -3-2. 재조립·검증

| 명령 | exit | 결과 |
|---|---:|---|
| `assemble_deck.py …/1주차/강의덱.초안` | 0 | slide sections **73** · part-divider 5 · `[PASS]` · sha256 `8f6bc2e4` → **`b56c6562`** |
| `diff` `A04_pre/강의덱.html` ↔ 새 덱 | — | 바뀐 줄 8(518 S10B · 650 M6-04 · 656 12 · 869~870 S28A · 917~918 S39 · 974 S42), 그 밖 0 |
| `verify_deck.py … --parts 5` | 0 | FAIL 0 · WARN 2 · PASS 65(같음). 다만 «`<br>` 직전 조사 종결 후보»가 4→**5건**이 됐다(`S28A: …보이며<br>` 추가). 린트 정규식 `(…\|이며\|거나)$`가 «보이-»+연결어미 «-며»를 «이며»로 읽은 것이다. **원본 worktree 덱에도 같은 후보가 있다**(`CREATE_SLIDES_COURSE=바이브코딩`으로 실행한 10건 중 `S28A: …보이며<br>`). 원본 구조로 되돌리면서 함께 돌아온 것이다 |
| `verify_deck_quality.py …`(기본) | 0 | FAIL 0 · WARN 6 · PASS 5(수정 전 같은 날 실측과 같음) |
| `verify_notes.py` | 0 | 판정 68 · 미판정 0 · 불일치 0 |
| `check_title_survival.py 1주차 --strict` | 0 | 강등 0 · 대응 없음 0 · 초안 73행 |
| `report_draft_sync.py 1 --strict` | 0 | 일치 62 · 제목불일치 11(같은 성격) · 초안에만 0 · 덱에만 0 |
| `verify_report_freshness.py 1주차 --strict` | 0 | 판정 1 · 미판정 0 · 위반 0(73장 · `b56c6562`) — 수정 전 같은 명령도 PASS(`8f6bc2e4`) |
| `CSSORPHAN/dropped.py <새 shard> <원본 shard>` | 0 | 새 1주차 **합계 0** · 원본 0 |
| 옛 문구 grep(덱·shard·노트·초안) | — | «입력하면(`<br>`\|공백)다음 단어로 넘어가면» 0 · «다음 차시 여러 파일 프로젝트» 0 · «프로젝트 폴더 영역» 0 · «써 본 사람의 반응» 0 · «SCORE 0» 0 · «PROJECT» 라벨 0 |
| 노트 · 초안 sha256 | — | 노트 `352627ea`→`a32aa335`(조립 전후 같음 — 조립기가 덮어쓰지 않음) · 초안 `a38cc6dc`→`ebd6a28b` |

### -3-3. 실행하지 못한 검증 · 범위 밖 관측

- `run_deck_checks`·`receive_audit`·브라우저 재측정은 위임 범위 밖이라 실행하지 않았다(`_verify/` 미접촉). 따라서 **렌더 수치가 바뀌었는지는 확인되지 않았다.** 재측정할 곳:
  - `S28A` — 터미널 2줄째가 짧아지고 3줄째가 길어졌다. 22px·가용 폭 약 635px에 약 500px로 추정해 줄 수 3 유지를 예상한다. `render_stretchY`·`render_lowFill` 등재 장이다.
  - `S39` — 체크 칸은 원본과 같은 CSS·같은 문구다. `render_below` 3 등재 장이다.
  - `S10B` — 7행이 1문장으로 줄었다.
  - `12`·`M6-04` — 문구가 짧아졌다.
- `verify_deck_quality --strict`·`verify_draft_quality`는 위임 목록에 없어 실행하지 않았다.
- `generated-guard` 훅이 노트 직접 편집에 관측 경고를 냈다. 이 덱은 조립기가 노트를 다시 만들지 않으므로(§-2-2 · 이번에도 sha 불변 확인) 기존 방식대로 직접 맞췄다.
- 원본 worktree 덱의 verify_deck은 exit 1(FAIL 3)이다. 다른 과목 계약 아래에서 돌린 비교용 실행이라 조사 종결 후보 목록만 인용하고 판정하지 않았다. 과목을 지정하지 않은 첫 실행은 `AmbiguousCourseError`로 멈췄다.
- `git status --short`는 전후 29줄로 차이가 없다. 백업 이후 바뀐 파일은 허용 목록뿐이다(원본 worktree 1주차·`courses/바이브코딩`·`제작관리` 0). 저장소 밖 스크래치는 0개다.

### -3-4. 매트릭스 반영 대상 · 결정요청

- **매트릭스(오케스트레이터 몫)**:
  - `VC-W1x-S28A` — 체크 3항의 3번째와 터미널 셋째 절을 «글자가 잘리고 밀리지 않음»/«글자가 잘리거나 밀리지 않으면»으로 바꾼다.
  - `VC-W1x-S39` — 3체크의 3번째를 원문 유지로 바꾸고, 목업에 kicker «TYPE THIS WORD»를 적는다.
  - `VC-W1x-12` — «WORKSPACE / 현재 폴더를 여세요»·«현재 작업공간 영역»을 원문 유지로 바꾼다(변경은 도구명·킥커만 남음).
  - `VC-W1x-S42` — alt 새 문구를 적는다.
  - `VC-W1x-S10B` — W05 «개선» 정의문은 흡수하지 않는다고 적는다.
  - M6-04 4단계 문구도 반영한다.
- **결정요청 1** — `S39` 제목 «타자 게임이 실제로 **동작하는지**»(M2 치환분)와 달리, 복원된 체크 3항(열림·보임·깨짐 없음)에는 동작 확인 항목이 없다. 동작 확인은 `M6-05`·`31`·`32`·`M6-07`이 맡는다. 제목을 원본 계열 «…보이는지»로 되돌릴지 정해야 한다.
- **결정요청 2** — `S39` kicker «TYPE THIS WORD»는 새로 넣은 문구다. 태그 «점수 0»을 지우는 쪽(3→2칸 · 노트 53 수정)을 원하면 바꾼다. 원형 표지 «0»도 점수로 읽힐 수 있는데, 지적 범위 밖이라 두었다.
- **결정요청 3** — `S28A`에서 «입력하면 점수가 오름»이 빠져 `M6-05` «검증» 칸(«…맞게 입력하면 점수가 오른다»)과의 짝이 느슨해졌다.

## -2. BUILD1-CSSFIX — 조립 잔해 CSS 복원 (2026-09-11)

오케스트레이터 실측 결과, shard에 선택자 머리 줄이 빠진 선언 조각이 남아 있었다. 최상위에 남은 `}`가 다음 `{`까지를 선택자로 먹어 **바로 뒤의 실제 규칙 4개가 무효**였다. 판정 도구는 `tmp/AI_코딩_에이전트_입문_3차시/CSSORPHAN/dropped.py`이고, 원본 worktree shard에서는 0건이라 조립 중에 생긴 A 결함이다. 원본과 대조해 보니 **제외 장 ID(`X00`~`X06`·`26`)나 옛 회차 어휘(`cover-week`)가 든 줄만 통째로 빠지고**, 여러 줄짜리 규칙의 이어지는 줄이 남아 있었다. 수정은 shard 2파일(`shell.html`·`revision.css`)에서만 했고, 덱은 조립기로 다시 만들었다. part 파일·`deck.contract.json`·발표자노트는 건드리지 않았다.

### -2-1. 조각별 처리 — 복원 0 · 삭제 5 · 미판정 0

| 위치(수정 전) | 무효화됐던 규칙 | 원본 대응(worktree shard 머리 줄) | 처리 | 근거(새 덱 `class` 사용) |
|---|---|---|---|---|
| `shell.html:26~27` | `[data-slide="S00"] .s00-hero` | `shell.html:25` `[data-slide="S00"] .cover-weeks{…` · `:27` `[data-slide="S00"] .cover-week{…` | **삭제** | 원본 머리 줄이 가리키는 클래스 `cover-week*`는 새 덱에서 0회다. 같은 요소는 §9-3 개명으로 `cover-tracks`/`cover-track`(S00 · 10회)이 됐고, 원본 6규칙(25~32)은 선언까지 같은 채로 `shell.html` 블록 ③(수정 전 265~275)에 이미 있다. 따라서 이 두 줄은 규칙을 옮기고 남은 잔해다. 원문대로 복원하면 적용 대상이 없는 규칙이 되고 옛 표기 게이트가 2줄 늘어나며, 개명해 복원하면 블록 ③과 중복된다 |
| `shell.html:40~41` | `.slide .work-step`(기본 경화 ① · 수정 전 73) | `shell.html:54` `[data-slide="X00"] .x-cond{…`(블록 주석 `:45`) | 삭제(블록 주석 포함) | 제외 장 X00 · `x-cond` 0 |
| `shell.html:43~53` | (위와 같은 프렐류드로 이어짐) | `shell.html:58·63·65·67·77·82·84·86·89` — 전부 `X01`~`X06` 한정 선택자(블록 주석 `:57`) | 삭제(블록 주석 포함) | 제외 장 X01~X06 · `x-list`·`x-verify`·`x-flow`·`x-extra` 0 |
| `revision.css:522~536` | `.privacy-lanes`(수정 전 538) | `revision.css:520` `.slide.request-four-slide[data-slide="26"] .request-four-image.asset-slot--support {` · `:530` `… .request-four-image > img {` | 삭제 | `data-slide="26"`·`request-four-*` 새 덱 0 · 원본 shard html에도 0(원본 `강의덱_배포.html`에만 있음) · `보존매트릭스_1차시.csv`에 `26` 행 없음 |
| `revision.css:1007~1012` | `.intro-ai-copy`(수정 전 1016) | `revision.css:1008` `.slide.request-four-slide[data-slide="26"] .request-four-image.asset-slot--support {`(주석 `:1007`) | 삭제(주석 포함) | 위와 같음 |

과정 표기 치환(M1) 때문에 지워진 선택자는 `cover-week` 2줄뿐이다. 이 경우 원문 복원 원칙보다 요소 개명(§9-3)이 우선했다. 요소가 이미 `cover-track` 규칙으로 스타일되고 있어서다.

### -2-2. 재조립·검증

| 명령 | exit | 결과 |
|---|---:|---|
| `python tmp/AI_코딩_에이전트_입문_3차시/CSSORPHAN/dropped.py <새 1주차 shard> <원본 worktree shard>` | 0 | 새 1주차 **합계 0**(수정 전 4) · 원본 합계 0 |
| `python scripts/assemble_deck.py "courses/AI_코딩_에이전트_입문_3차시/sessions/1주차/강의덱.초안"` | 0 | slide sections **73** · part-divider 5 · `[PASS]` · sha256 `2e69eab8` → **`8f6bc2e4`** |
| `diff` 조립 전 사본(`tmp/…/BUILD1/CSSFIX_pre/강의덱.html`) ↔ 새 덱 | — | 삭제 17줄 · 추가 0(`26,27d25` · `39,53d36`, 전부 head `<style>`) — `revision.css`는 `<link>`라 덱 diff에 나타나지 않는다 |
| `python scripts/verify_deck.py …/강의덱.html --parts 5` | 0 | FAIL 0 · WARN 2 · PASS 65(attempt-03과 같다) |
| 게이트 rg(옛 표기) · `cover-track` | — | 덱 파일 5줄(변화 없음) · 10(변화 없음) |
| 발표자노트 | — | 조립 절차(`검증-명령-지도.md` §1)에 재생성 단계가 없다 · sha256 `352627ea` 그대로 |

### -2-3. 화면 영향 예상 — 재측정 대상

다시 유효해진 규칙이 실제로 걸리는 장(새 덱 `class` 토큰 기준):

- **S00** — `.s00-hero`(absolute · right 0 · top 78 · 1100×570 · z-index 2)
- **S01A** — `.intro-ai-copy`(left 64 · top 116 · width 540)
- **M6-04 · S45 · S53 · S56** — `.slide .work-step`(font-size `--fs-body` · line-height 1.45). 행 높이가 늘어 바닥선이 달라질 수 있다

`.privacy-lanes`는 사용처가 0이라 화면에 영향이 없다. §0-5 `known_violations`와 §0-6 렌더 수치는 위 규칙이 무효인 상태에서 잰 값이다.

### -2-4. 실행하지 못한 검증 · 범위 밖 관측

- `run_deck_checks`·`receive_audit`·브라우저 재측정은 위임 범위 밖이다(오케스트레이터 몫). `verify_notes`·`verify_deck_quality`는 실행하지 않았다. 덱 diff가 head `<style>` 17줄 삭제뿐이라 슬라이드 본문과 노트가 바뀌지 않았기 때문이다.
- **기존 R-QC-14 2건**(편집 중 css-lint 훅이 보고) — `revision.css:280` `.scope-copy span { display:block…` · `:452` `.permission-rules b { display:block…`. 수정 전 사본(같은 줄)과 원본 worktree(277·449)에 이미 있던 것이라 이번 수정이 만든 것이 아니다. 조각 복원 범위 밖이라 고치지 않았다. 이 파일이 pre-commit css 검사 대상인지는 확인하지 않았다.
- 작업 중 다른 담당의 쓰기를 관측했다(손대지 않음): `2주차/`(초안·`part-01`·`revision.css`·노트) · `제작관리/상태장부.md`·`재개_인계.md`. `courses/바이브코딩/`·원본 worktree는 변경 0이다.

## -1. attempt-03 — 검토 지적 수정 (2026-09-11)

독립 검토 R-BUILD1과 오케스트레이터 판정에 따른 6항 조치. 전부 `강의덱.초안/`(shard) 수정 후 조립기로 재조립했다. 덱·`deck.contract.json`·`강의덱_발표자노트.html`·`1주차_초안.md`만 건드렸다. `run_deck_checks`·`receive_audit`·브라우저 재측정은 오케스트레이터 몫이라 실행하지 않았다.

### -1-1. 6항 조치

| # | 위치 | 전 | 후 |
|---|---|---|---|
| 1 | `part-02.html:4` P2 디바이더 부제 | `AI가 자신 있게 말해도 사실이 아닐 수 있습니다.<br>무엇을 의심하고 무엇을 확인할지 정합니다.`(2줄·바닥선 초과) | `AI가 자신 있게 말해도 사실이 아닐 수 있어, 의심하고 확인합니다.`(1줄·원본 P6 `part-05.html:29` 한 줄 패턴 대응) |
| 2 | `part-01.html:67` `09` | `is-focus`가 PERPLEXITY 카드에 있어 같은 장 «절대적으로 우수하기보다» 유보와 충돌 · CLAUDE·GEMINI·PERPLEXITY 설명문 3개가 CSV·원문01 어디에도 없음 | `is-focus` 제거(4종 중 어느 도구도 강조하지 않음 — 이 과정의 실사용 도구는 목록 밖 Google Antigravity라 원본의 «CODEX=실사용 도구라 강조» 논리가 그대로 옮겨지지 않는다) · 설명문 3개를 CSV가 준 카드 제목(`h3`) 문구를 그대로 문장으로 편 것으로 교체(Claude "긴 문서를 읽고 내용을 분석합니다." · Gemini "문서·이미지·파일 등 다양한 형식을 함께 다룹니다." · Perplexity "검색을 기반으로 필요한 정보를 찾습니다.") — 원본 CODEX 설명문("현재 폴더 안의 실제 파일을…")을 그대로 옮기지 않은 것은 Gemini가 실제로 로컬 파일을 편집하는 도구가 아니라 그 문장이 사실과 어긋나기 때문 |
| 3 | `part-02.html` `M6-03` | «같은 결과가 나오지 않습니다» | «같은 결과가 나오지 않을 수 있습니다»(덱·발표자노트·초안 3곳 동기화) |
| 4 | `shell.html:406-407` `05` img alt | «문제와 기능을 MVP로 만드는 2차시 흐름» · «배포와 개선의 3차시 흐름»(구 3주 커리큘럼 잔재) | 같은 슬라이드 화면 문구에 맞춰 «조사 기준을 파일로 정하고 Agent가 반복 실행하는 2차시 흐름» · «CSV를 검증해 대시보드와 분석 문서로 확장하는 3차시 흐름»(이미지 파일 자체는 무변경) |
| 5 | `part-03.html` `M6-04` `eval-steps viz-flow-v` | 베이스 클래스·a11y 속성 없음 | `.viz-flow` 클래스 추가 + `role="img"` + `aria-label="세로 절차 흐름 4단계: 1 설치하기 → 2 실행하기 → 3 로그인하기 → 4 기본 화면과 주요 영역 확인"`(`kit/charts/diagrams-process.md:119` 규격) — `.viz-flow`는 kit CSS에 규칙이 없어(문서의 "신규 CSS 요지" 표기뿐) 렌더 변화 없음. 레이아웃은 기존 `.eval-steps`(patterns.css:116, flex column)가 그대로 담당 |
| 6 | `shell.html:198` `.dp-badge`(16px) · `shell.html:258` `.stage-off`(15px) | 폰트 하한 점검 대상 | (정정 2026-09-11) 이전 절의 «`--fs-badge`(13px) 하한 이상» 근거는 잘못된 인용 — `--fs-badge` 토큰은 존재하지 않는다. 올바른 근거: `scripts/audit_typography.js:41·123·126`의 `label` 등급 하한 **14px**, 두 값(16px·15px) 모두 이를 충족하고 각각 24자 이하 짧은 라벨(pill 배지·보조 태그)이며 원본 정본 덱 유래다. **값은 변경 없음** · `kit/guide/토큰-치트시트.md:126` 「스케일 밖 값은 사유 주석 필수」에 따라 `shell.html:198·258`에 CSS 사유 주석을 신설(마무리 정정 항목 2) |

**마무리 정정(2026-09-11, 독립 검토 R-BUILD1-02 비치명 3건, 오케스트레이터 판정)**: ① `shell.html:406-407` `05` img alt를 「이미지에 실제로 보이는 것」만 중립 묘사로 재작성(그림이 2·3차시 내용을 보여준다는 주장 제거 — 항목 4는 attempt-03 당시 값으로 낡음) ② `shell.html:198·258` `.dp-badge`·`.stage-off`에 위 표 6번 근거를 CSS 사유 주석으로 신설(값 불변) ③ `제작관리/보존매트릭스_1차시.csv`(P6 행)·`제작관리/매핑_1차시.md:147`의 옛 P6 부제 2문장을 덱 현재 부제 «AI가 자신 있게 말해도 사실이 아닐 수 있어, 의심하고 확인합니다.»와 일치시켰다. 재조립 후 sha256 `c07a41ad`→`2e69eab8`.

### -1-2. `deck.contract.json` 정정

`known_violations.render_below`: count **26→25**, `slides`에서 `P2` 제거, reason의 «P1·P2·P4 각 1(pd-sub@679 — 원본 P1·P3·P7을 재번호)»를 «디바이더 P1·P4 ← 원본 P1·P3(동일 pd-sub@679)»로 정정(P2는 항목 1 수정으로 해소). 나머지 8개 `known_violations` 항목은 건드리지 않았다.

### -1-3. 재조립·검증

| 명령 | exit | 결과 |
|---|---:|---|
| `assemble_deck.py …/1주차/강의덱.초안` | 0 | 섹션 73 · 전환 5 · 「기본 경화 5종」 주석 잔존 확인 |
| `verify_deck.py … --parts 5` | 0 | FAIL 0 · WARN 2(변경 전과 동일 종류 — prompt_only 2 · `<br>` 조사 종결 4건, P2는 더 이상 해당 없음) · PASS 65 |
| `verify_deck_quality.py …`(기본 모드) | 0 | FAIL 0 · WARN 6 · PASS 5(attempt-02와 동일 분포 — 이번 6항이 범위 밖) |
| `verify_notes.py` | 0 | 판정 68 · 미판정 0 · 불일치 0(수정 전 M6-03 제목 불일치 1건 → 발표자노트 동기화로 해소) |
| `check_title_survival.py 1주차 --strict` | 0 | 제목자리 71 · 제목교체 2 · 강등 0 |
| `report_draft_sync.py 1 --strict` | 0 | 일치 62 · 제목불일치 11(전부 `(간지)`·`(표지)`·`(마무리)` 접두, attempt-02와 동일 성격) · 초안에만 0 · 덱에만 0(수정 전 M6-03 제목불일치 1건 → 초안 동기화로 해소) |
| 게이트 rg(옛 표기·about-me) | — | 덱 5줄(전부 head 「기본 경화 5종」 주석, attempt-02와 동일) · 노트 0 · `about-me` 0 |

### -1-4. 실행하지 못한 검증 · 이유

- `run_deck_checks 1주차`·`receive_audit`·브라우저 렌더 재측정 — 위임 범위 밖(오케스트레이터가 재측정 담당). `_verify/`에 쓰지 않았다.
- `verify_deck_quality.py --strict`·`verify_draft_quality.py` — attempt-02가 이미 등재한 기존 FAIL(§7)을 다시 재는 것이라 이번 6항과 무관해 실행하지 않았다.

## 0. attempt-02 — 렌더 실측 FAIL 수정 (2026-09-10)

입력은 `제작관리/렌더비교_1차시.md`의 A/B 분류다. 수정은 전부 shard(`강의덱.초안/shell.html`의 `kit-additions` 끝 「attempt-02」 블록 · `part-03`·`part-04`)에서 했고, 덱은 조립기로만 만들었다. 수정마다 같은 조건(1280×720 · dpr 2 · scale 1)으로 로컬 headless Chrome에서 `audit_all.js`·`audit_render.js`를 다시 돌렸다. 증거는 `tmp/…/BUILD1/A2_render_*.json`에 있고, `_verify/`에는 쓰지 않았다. 바뀐 11장은 스크린샷으로도 눈으로 확인했다.

### 0-1. A 항목별 조치

| 항목 | 원인(실측) | 조치 | 재측정 |
|---|---|---|---|
| `M6-04` 바닥선 3 | 4단 스택이 382px 자리에 436px로 넘쳤다(4번째 행 707) | 단계 이름과 설명을 한 줄에 나란히 두었다(330px 열 + 나머지). 행 안쪽 여백 8·간격 4, 설명 18→22px, 이름 21→24px | 바닥선 0 · 행 308~648 |
| `M6-05` 바닥선 3 · 껍데기 4 | 행마다 붙은 여분 여백 16px 때문에 마지막 행이 689까지 내려갔다. 좌측 «목적» 등 2자 라벨이 240×63 카드에 들어 있었다 | 행 여백 0·간격 18. 좌측을 `.card surface`에서 `.pill`로 바꿨다(레이아웃 스펙 built_on). pill로 바꾸자 우측 값 카드의 가로 늘림(72w/53x)이 드러나, 520px로 묶어 가운데에 두었다 | 바닥선 0 · 껍데기 0 · 가로늘림 0 |
| `M6-03` 가로·세로 늘림·채움률 | `flex:1` 격자 때문에 카드 높이 370px에 내용은 206px였다. 폭 567px에 가장 긴 줄은 약 326px였다 | 격자 높이를 내용에 맞추고 세로 가운데로 옮겼다. 카드는 2×500px로 가운데 | 0 · 0 · 0 |
| `M6-07` 가로·세로 늘림·채움률 | kit `.cc-side{align-self:stretch}`가 `flex:1` 격자를 따라 늘었다 | 격자 높이를 내용에 맞추고 세로 가운데로 옮겼다. 두 칸은 440px로 가운데, 목록 20→22px | 0 · 0 · 0 |
| `15` 겹침 1 | **원본 part `<style>` 이식 누락**(§0-2). `.folder-path-stage`에 규칙이 없어 본문이 y=0에 깔렸다 | 원본 규칙 9개를 원문 그대로 복구 | 겹침 0 · 본문 216~662 |
| `32` 바닥선 1 | 왼쪽 설명이 454px 자리에 485px였다(P@681) | 여백만 줄였다(padding-block 22→14 · h3 아래 18→10 · 목록 위 18→6 · 끝 문단 위 18→12). 글자 크기는 그대로 | 바닥선 0 · P 653 |
| `S3PE` 채움률 +1 | 짧은 «이렇게 쓰면» 칸이 긴 칸 높이로 늘어 채움률 22%였다 | `.eng-prompt .eng-bad{align-self:start}` | 채움률 0(`eng-concept` 세로늘림·빈슬롯은 B) |
| 오버플로 1 | 로컬 재측정으로 **`14`** 로 특정했다 — `task-shot drive-open-shot` scrollHeight 480 > clientHeight 460(캡처 464px가 444px 틀에 들어 있다) | **미수정 · B로 재분류(확인 요청)**. A 목록 밖 보존 슬라이드이고, 원본 계약 `render_ovf`가 같은 슬라이드 14(IMG@683)를 적시한다 | 오버플로 1 유지 |

### 0-2. 추가 발견 — 원본 part `<style>` 이식 누락 (A 목록 밖 · 복구함)

원본 `강의덱.초안/part-01·02·05.html` 머리에 있던 `<style data-part>` 블록이 첫 조립 때 통째로 빠졌다. 원본 part 규칙 99개 셀렉터를 새 CSS와 원문 대조했더니, 덱에 있는 슬라이드 중 **`A01` 4 · `15` 9 · `S53` 10 · `S57` 10 = 33규칙**이 없었다(나머지는 제외된 T01·T02·W01~W05·S10A 것). 원본 셸 `<style>`에서 빠진 것은 전부 제외 장(X00~X06)이나 개명한 `cover-week` 것이었다. 33규칙을 원문 그대로 셸에 옮겼다. 원본 레이아웃으로 되돌리는 복구이지 새 배치가 아니다. 복구 뒤 네 장의 결함형 렌더는 0이다. 서술문이 15~16px로 렌더되던 것이 22px로 돌아와 폰트하한 위반은 39→29로 줄었다(M6-04 설명 22px 포함). 15에서는 게이트 밖 지표인 껍데기 상자 1이 새로 잡혔는데, 요소는 특정하지 못했다.

### 0-3. S58 판정 — 캡처 제거 · «강사 시연 화면» 자리표시

덱 흐름을 따라가면 전부 `typing-game` 안이다: `S34`(`C:\VibeCoding` 안에 typing-game 생성 · 확인 주소 `C:\VibeCoding\typing-game\`) → `S35`(그 폴더 열기) → `S36`(목표 경로 C: › VibeCoding › typing-game) → `32`(터미널 제목 `C:\VibeCoding\typing-game`) → `S38`(`typing-game\` 바로 안) → `S39`(주소 `C:\VibeCoding\typing-game\index.html`). 본문이 흐름과 맞고 어긋난 것은 캡처다. 그래서 `win05` 캡처를 빼고 `.final-location-shot.demo-placeholder`(role=img · aria-label)로 바꿨다. 자리표시 문구는 «파일 탐색기에서 index.html 찾기 · 주소 표시줄이 `C:\VibeCoding\typing-game`인지 확인»이다. 같은 판정으로 노트 68번, 초안 `6-S58` 행, 집필노트 자산표를 맞췄고, 집필노트 §5의 D-05 항목을 지웠다. 부수효과: S58 바닥선 3→1(`P@672`만 남음). 덱 이미지 참조는 28→27이 되어 `자료/screenshot/win05_vibecoding-index-root.png`가 미참조로 남는다(허용 목록 밖이라 지우지 않았다). §11-1 D-05는 이것으로 해소됐다.

### 0-4. 품질 상시 FAIL 2건

| 규칙 | 출처 판정 | 조치 |
|---|---|---|
| **R-QC-08** 시각자료 46.3%(19/41) | 원본 85장(47.6%)과 같은 규칙이다. 이번에 더 낮아진 것은 캡처 제거와 M6 무시각 장 때문이다 | `run_checks`와 같은 레코드를 재현해 무시각 22장을 뽑았다. 그중 **이미 `role="img"`+aria-label로 선언된 코드 GUI 목업**이 viz 표지가 없어 집계되지 않던 4곳에 `data-viz`를 붙였다: `S34` folder-tree-gui · `S35` agent-app-gui · `31` completion-screen · `S38` project-tree-visual. kit에는 `[data-viz]` 스타일이 없어 렌더 수치는 무변화다. `M6-04`는 kit 카탈로그 flow-v(`kit/charts/diagrams-process.md` 세로 스텝+↓)라 `viz-flow-v`를 붙였다(선례: 바이브코딩 3주차 14곳). → **24/41 = 58.5% PASS**. ⚠️ 새 시각자료를 만든 것이 아니라 «있는 도해를 세게 한 것»이다(`verify_deck_quality.py` 임계 주석이 경고하는 효과). waiver를 원하면 속성 5곳만 되돌리면 된다 |
| **R-QC-18** 빈 슬롯 S3PE·S3PRV | 원본과 같은 규칙·위치다(prompt_only, 원본에서도 미생산) | **미수정**. S3PE 슬롯은 지워도 렌더가 같지만, S3PRV 슬롯은 `.privacy-principle`의 첫 격자 칸(190px)을 차지해 지우면 A 목록 밖 보존 슬라이드의 배치가 바뀐다. 한쪽만 지워도 임계가 0이라 FAIL은 남는다. §11-5 결정요청 유지 |

### 0-5. 계약 등재 — `known_violations` B 9유형 44건 (22장)

사유는 공통으로 «원본 정본 덱과 동일 ID·동일 유형 · 증거 `제작관리/렌더비교_1차시.md` · 2026-09-10 실측»이고, 항목마다 분해와 `slides` 목록을 붙였다. `warn_baseline`은 건드리지 않았다.

| waiver | count | 슬라이드 |
|---|---:|---|
| `render_below` | 26 | S01 · S01A×2 · S01B · P1 · P2 · P4 · 07×2 · S10C · 11×2 · 12×3 · 14×2 · S3DO · 31 · S39×3 · S42 · S58 · S59×2 |
| `render_lap` | 1 | S3PRV |
| `render_ovf` | 1 | 14 (A→B 재분류 · 확인 요청) |
| `render_slots` | 2 | S3PE · S3PRV |
| `render_occl` | 1 | S3PRV — 로컬 headless 재측정은 S00 1건을 더 잡았다(수정 전 기준선에서도 같아 환경 차이로 판단) |
| `render_wideEmpty` | 1 | S3PRV |
| `render_stretchX` | 3 | S01A · 31 · S36 |
| `render_stretchY` | 3 | S3PE · S28A · S42 |
| `render_lowFill` | 6 | S10D · S3PRV · S28A · 31 · S36×2 |

### 0-6. 재조립 · 정적 검증 (최종 덱 `aa6d6dee`)

| 명령 | exit | 결과 |
|---|---|---|
| `assemble_deck.py …/1주차/강의덱.초안` | 0 | 섹션 73 · 전환 5 · 「기본 경화 5종」 표지 1(누락 재발 없음) |
| `verify_deck.py … --parts 5` | 0 | FAIL 0 · WARN 2 · PASS 65(계약 등재 뒤 재실행해도 같음) |
| `verify_deck_quality.py … --strict` | 1 | FAIL 6: R-QC-01 S01A · R-QC-02 S50 · R-QC-16 12.2% · R-QC-17 20 · R-QC-18 · R-QC-19 S56. **R-QC-08은 PASS로 전환**(attempt-01 FAIL 7) |
| `verify_deck_quality.py …`(기본) | 1 | FAIL 1(R-QC-18 상시) · WARN 5 · PASS 5 |
| `verify_notes.py` | 0 | 판정 68 · 미판정 0 · 불일치 0 |
| `check_title_survival.py 1주차 --strict` | 0 | 제목자리 71 · 교체 2 · 강등 0 |
| `report_draft_sync.py 1 --strict` | 0 | 일치 62 · 제목불일치 11 · 초안에만 0 · 덱에만 0. `1주차`를 넘기면 `1주차주차`를 찾아 판정 없이 exit 0으로 끝난다 |
| `verify_draft_quality.py 1 --strict` | 1 | FAIL 3(R-QD-01·04·05 — attempt-01과 같고 이번 범위 밖). `1주차`를 넘기면 R-QD-00 경로 결함 |
| `verify_contract_waivers.py` | 0 | 계약 11건 · 위반 0 |
| 게이트 킥커 / 셸 / 옛 표기 | — | PART 1 9 · 2 5 · 3 12 · 4 14 · 5 13 · WEEK 1 0 / 1차시 8 / 덱 파일 5줄(head 주석) · `.deck` 영역 0 · 노트 0 · about-me 0 |
| 참조 무결성 | — | 로컬 참조 31 · 없음 0(+예상 슬롯 `data-expected-src` 2) · 자료 이미지 28 중 참조 27 · 미참조 1(win05) |

로컬 렌더 재측정(`tmp` 증거) — 바닥선 35→**26** · 겹침 2→**1** · 오버플로 1→1 · 빈슬롯 2→2 · 가로늘림 5→**3** · 세로늘림 5→**3** · 가로꽉채우고빔 1→1 · 채움률 9→**6** · 껍데기 7→**4** · 폰트하한 39→**29** · 자간 20 · 정렬충돌 3. 남은 결함은 슬라이드 단위로 §0-5 등재분과 전부 일치한다.

### 0-7. 실행하지 못한 검증 · 이유

- `receive_audit`와 `run_deck_checks 1주차`는 실행하지 않았다. 렌더 증거 재측정은 오케스트레이터 몫이고 `_verify/` 쓰기는 금지다. 지금 `_verify/1주차/deck-audit.json`은 수정 전 덱(`4519dc24`) 기준이라, 러너는 증거가 낡았다며 FAIL하는 것이 정상이다.
- 원본 worktree 덱의 로컬 재측정은 포기했다. headless 탐색이 커밋되지 않았다(about:blank에서 60초). 그래서 `14` 오버플로의 원본 대응 근거는 원본 계약 문구뿐이다.
- `15` 껍데기 상자 1은 요소를 특정하지 못했다(근사 프로브로 재현 안 됨 · 게이트 아님).

## 1. 최종 장수 · part 구성 — 73장 · PART 전환 5장

| 파일 | PART | 장수 | 슬라이드(새 `data-slide`) |
|---|---|---|---|
| `shell.html` 앞 | 오프닝 | 8 | S00 S01 S01A S01B S01C 05 S02 S03 |
| `part-01.html` | PART 1 · AI와 Coding Agent | 10 | P1 A01 06 07 08 **M6-01** 09 S10C S10D S10B |
| `part-02.html` | PART 2 · AI를 믿는 방법 | 7 | P2(←원본 P6) **M6-02** S3SY S3SYP **M6-03** S3HUM S3DO |
| `part-03.html` | PART 3 · 안전한 작업 환경 | 13 | P3(←원본 P2) 11 **M6-04** 12 17 14 15 20 21 23 24 S3PRV 19 |
| `part-04.html` | PART 4 · 첫 웹 결과물 만들기 | 15 | P4(←원본 P3) S3PE **M6-05** S3ITR 29 30 S34 S35 S36 S28A 31 32 **M6-06** S38 S39 |
| `part-05.html` | PART 5 · 고치고 다시 확인하기 | 14 | P5 S44 S45 S46 S47 S48 S49 S50 S52 S53 S54 S55 S56 S57 |
| `shell.html` 뒤 | 마무리 | 6 | S58 S59 **M6-07** S60 S42 S62 |

원본 재사용 66장(본편 42 + 실습안내 24) + M6 신규 7장 = 73. 원본 85장 추적률 100%(추적표 `자료/1주차_콘텐츠_집필노트.md` §1).

## 2. 시간표 — 앞 50 + 뒤 50 + 정리·질의 10 (휴식 10 별도 · 합 120)

매핑 §② 분 값을 그대로 옮기고, 각 50분 블록의 미배정 5분을 «블록 여유(복원)» 행으로 되돌렸다(정본 `1주차_초안.md` §0 시간표).

| 블록 | 슬라이드 | 실습 대기·따라하기·복구·질의 | 복원 여유 | 합 |
|---|---|---|---|---|
| 앞 | 39.0 | 6.0 | 5.0 | **50.0** |
| 뒤 | 31.0 | 14.0 | 5.0 | **50.0** |
| 정리·질의 | 6.3 | 3.7 | — | **10.0** |

## 3. M1~M6 적용 계수 — CSV 대비 누락 0

| 항목 | 값 |
|---|---|
| 덱에 오는 원본 행 | 66 |
| 변경유형이 있는 행 / 치환표 반영 행 / **누락** | 58 / 58 / **0** |
| 무수정 복사 | 8장 — S01A S01B S01C A01 06 07 S3DO S3PRV |
| 변경유형 분포(덱 반영분) | M1 16 · M1+M2 23 · M2 13 · M1+M4 4 · M5+M1 1 · M3+M5 1 |
| M5 흡수 | W01·W02·W03·W05 → S10B(비유 7종 + «이 과정에서는 다루지 않습니다»·«배포는 3차시 마지막 시연» 배지 2) |
| M6 신규 | 7장(kit 레이아웃 ID는 집필노트 §1 표) |
| 치환 규칙 | 201건. 빌더가 규칙마다 «정확히 1회 일치»를 강제하고, 어긋나면 조립을 중단한다 — 최종 조립 오류 0 |

## 4. R-MAP1-02 최종 조립 지시 12건

| # | 지시 | 결과 |
|---|---|---|
| ① | 킥커 33장 치환 · 셸 «1차시» | 게이트 수치는 §5 |
| ② ⑪ | 옛 표기 게이트(`…WEEK\|주차\|이번 주\|다음 주`) | 슬라이드 영역 **0** · 노트 **0** · 덱 파일 전체 5줄 = §5 사유. `X02`는 덱에 없음 |
| ③ ⑩ | `S34` 5곳 · 소재 치환 | `rg -c 'about-me' 강의덱.html` = **0** |
| ④ | 덱 `<title>` · 노트 `<title>`·`.pn-kicker`·`.pn-title` | 4곳 적용(«첫 웹 타자 게임» · «VIBECODING 1차시») |
| ⑤ | `S10B` «6 배포»에 W05 «매장에 내걸기» | 적용 + W계열 비유 7종 전부 흡수 |
| ⑥ ⑫ | 노트 `[미확인 · 강사 시연]` — 12·21·S35 | 3곳 적용 + **M6-04 1곳 추가**(같은 미확인 UI를 다루는 신규 장) + 머리 범례 1 |
| ⑦ | `win05` 재사용 · alt 정합 · 실측 판정 | 재사용. **이미지 파일을 직접 열어 확인**: 주소 표시줄이 `내 PC › 로컬 디스크 (C:) › VibeCoding`에서 끝나고 `index`가 그 바로 안에 있다(`typing-game` 없음) → 본문 경로 `C:\VibeCoding\typing-game\`과 **위치 불일치**. alt를 캡처 실내용으로 고치고, S58 노트에 불일치를 적었다. 최종 판정은 브라우저 실측 몫 |
| ⑧ | 캡처 6개 처리 | 제거 4(`20`·`21`·`23`·`24` → `.demo-placeholder` 텍스트 자리표시, alt 함께 삭제) · 유지 1(`win02`) · 재사용 1(`win05`). 덱의 캡처 참조 2종 |
| ⑨ | 디바이더 `pd-eyebrow` 숫자 하드코딩 금지 · 순서 P1→P6→P2→P3→P5 | `pd-eyebrow`는 «PART»만(JS가 `PART n / N` 생성) · 배치 순서 유지. **단 새 덱 `data-slide`는 P1~P5로 재번호** — §9-1 |
| 추가 | `30` «JavaScript는 필요할 때만» 교체 | «외부 라이브러리 없이 / 게임 동작은 같은 파일 안의 JavaScript로만» |
| 추가 | D-08 강제 메시지(1차시분) | 1 → `M6-02`·`S3SY` · 2 → `M6-07`·`24`·`S59` · 5 → `19`·`S36`. 계약 `must_keep` 11건 |

## 5. 게이트 3종 · 참조 무결성 · 노트 계수

| 게이트 | 명령 | 결과 |
|---|---|---|
| 킥커 분포 | `rg -o 's-team">(WEEK 1\|PART [0-9]+)' 강의덱.html \| sort \| uniq -c` | PART 1 **9** · PART 2 **5** · PART 3 **12** · PART 4 **14** · PART 5 **13** · WEEK 1 **0** · PART 6 **0** — 매핑 기대값과 일치 |
| 셸 킥커 | `rg -o 's-team">1차시' 강의덱.html \| wc -l` | **8**(기대 8) |
| 옛 표기(덱) | `rg -i -c 'codex\|about-me\|자기소개\|한 줄 소개\|관심사\|활동명\|WEEK\|주차\|이번 주\|다음 주'` | 파일 전체 **5줄**(58·59·60·90·140행). 전부 `<head>` 안 「기본 경화 5종」 **주석**의 «주차»다. 이 블록은 템플릿 원문을 유지해야 해서 주석을 고치지 않았다. `.deck` 슬라이드 영역만 세면 **0** |
| 옛 표기(노트) | 같은 명령 · `강의덱_발표자노트.html` | **0** |
| about-me | `rg -c 'about-me' 강의덱.html` | **0** |
| 참조 무결성 | 덱의 `src`·`href` 로컬 참조 파일 존재 확인(tmp 스크립트) | 실참조 **32 · 없음 0**. prompt_only 예상 슬롯 2(`s3pe_*`·`s3prv_*`) — 원본에서도 생산된 적 없는 파일 |
| 이미지 복사 | — | **28개**(`자료/images/` 26 · `자료/screenshot/` 2), 전부 덱이 참조 · 미참조 0. 이 중 3종은 파일명의 `week1~3`를 `s1~s3`로 바꿔 복사했다(그림은 동일) |
| 노트 | `verify_notes.py` | **68장분 · 항목 86개** · 68/68 일치. 매핑 기대 73장분 중 화면 번호가 없는 5장(S00·S01·S01B·S01C·S3DO)은 verify_notes가 노트 항목을 거부해, 그 전달 큐를 S01A·S3HUM 노트에 접어 넣었다 |

## 6. 검증 명령 · 종료코드 · 건수

| 명령 | exit | 결과 |
|---|---|---|
| `python scripts/assemble_deck.py "…/1주차/강의덱.초안"` | 0 | 섹션 73 · 전환 5 |
| `python scripts/verify_deck.py …/강의덱.html --parts 5` | **0** | FAIL 0 · WARN 2 · PASS 65 · 구도 다양성 18종/본문 64장 |
| `python scripts/verify_deck_quality.py …/강의덱.html --strict` | **1** | FAIL 7 · WARN 0 · PASS 4 · 판정 강등(사람검토) 3 |
| `python scripts/verify_notes.py …/강의덱.html …/강의덱_발표자노트.html` | **0** | 판정 68 · 미판정 0 · 불일치 0 |
| `python scripts/check_title_survival.py 1주차 --strict` | **0** | 제목자리 71 · 제목교체 2(S01B·S3DO 전면 전환 장) · 강등 0 |
| `python scripts/report_draft_sync.py 1 --strict` | **0** | 일치 62 · 제목불일치 11(`(표지)`·`(간지)`·`(마무리)` 접두) · 초안에만 0 · 덱에만 0 |
| `python scripts/verify_draft_quality.py 1 --strict` | **1** | FAIL 3 · PASS 7 · 0판정가드 미판정 0 |

`report_draft_sync.py`는 인자 `1주차`를 넣으면 `1주차주차`로 붙여 **구경로를 찾는다**(검증명령.md §2 «숫자만 넘긴다»와 같은 결함). 그래서 숫자 `1`로 실행했다.

### 원본 대비(같은 명령 · worktree 85장 덱)

| 명령 | 원본 | 새 덱 |
|---|---|---|
| verify_deck WARN «<br> 앞 조사 종결» | 10건 | 4건(남은 4건은 무수정 원문 S3ITR×2·32·S57) |
| verify_deck_quality --strict | FAIL 9 | FAIL 7 |
| verify_notes | 불일치 3 · FAIL | 불일치 0 · PASS |

## 7. 남은 FAIL · WARN과 사유

**verify_deck WARN 2** — ① prompt_only 슬롯 2(S3PE·S3PRV · 원본과 같다) ② `<br>` 앞 조사 종결 4건(모두 손대지 않은 원문).

**verify_deck_quality FAIL 7**

| 규칙 | 값 | 사유 |
|---|---|---|
| R-QC-01 | S01A 73자 | 무수정 원본 · 원본에서도 FAIL |
| R-QC-02 | S50 4블록 | 원본은 S49·S50 2장이었고 새 덱은 1장 |
| R-QC-08(상시) | 시각자료 46.3%(19/41) · 임계 55% | 원본 47.6%. 캡처 4장을 제거했고 M6 7장에는 이미지가 없다 |
| R-QC-16 | 박스 기반 12.2%(5/41) · 임계 10% | A01·S3ITR은 원본 · M6-01(7)·M6-05(8)·M6-06(5)은 내용이 칸 수를 정한다(유형 7 · 대응 4행 · 단계 5). 임계는 바이브코딩 1주차 0.056 · 2주차 0.226 실측에서 나왔다. `profile.md` §4 «표와 카드는 후순위»와도 어긋난다 — **결정요청** |
| R-QC-17 | 슬라이드 20 | 원본에서도 FAIL. 비율이 50→100%가 된 것은 다른 실습 장(X00)이 수강생참고로 빠져서다 |
| R-QC-18(상시) | 빈 슬롯 2 | 원본과 같다(prompt_only) |
| R-QC-19 | S56 | 원본과 같다 |

해소한 것: R-QC-15 — M6 신규 장에 붙였던 하단 결론 띠 4개를 제목 아래 리드로 올려 17.1% → **9.8%**. 원본에 있던 R-QC-03(T02)은 T02를 제외하면서 사라졌다.

**verify_draft_quality FAIL 3**

| 규칙 | 사유 |
|---|---|
| R-QD-01 | S02 47자 — 고정 도입 장(`s02-slide`)이고 원본 구조 그대로다. 1/60행(1.7%)으로 임계 도출 기준 W1 3.6%보다 낮다 |
| R-QD-04 | PART 2(7)·3(13)·4(15)·5(14)가 8~11 밖이다. 사유는 집필노트 §4에 적었다. `profile.md` §2 «회차당 장수 범위»가 «미정»이라 검사기가 기본값 8~11로 판정했다 |
| R-QD-05 | `1주차_콘텐츠_리뷰.html` 없음. 폴더에 있는 `콘텐츠_리뷰.html`(골격 사본)은 이름이 달라 인식되지 않는다. 허용 목록 밖이라 만들지 않았다 — **결정요청** |

## 8. 미판정(통과가 아니다)

- **R-COPY-01~03 3건** — 문체 임계가 `profile.md`에 없어(§2·§3 기준선이 전부 «미정 — 첫 완성 덱 실측 후 기록») 사람검토로 강등됐다. 검사기 문구는 «프로필 §3-G에 … 없음»이다. 검출값은 R-COPY-01 1건(S3SY 원문) · R-COPY-02 0건 · R-COPY-03 12건(원본 15건).
- **R-QC-12** — 개념KB가 없어 적용하지 않았다.
- **렌더 전부 대기** — 오버플로 · 겹침 · computed 22px · `S10B` 좌열 560px에서의 3줄 예산 · `09` 4행 · `.demo-placeholder` 4장 · M6 7장 배치 · `win05`와 본문 경로 대조 · D-06(09·05 이미지와 새 문안의 시각 충돌) · `revision.css` 22px 보정 3건이 렌더에서 같은지.
- **실행하지 않은 것(지시)** — `receive_audit` · `run_deck_checks` · 브라우저. 렌더 증거가 없으므로 첫 러너는 FAIL이 정상이다. 또 계약 `warn_baseline.static_gates`를 0으로 두었는데 verify_deck WARN이 현재 2라서, 러너 래칫이 이를 증가로 볼 수 있다 — **미확인 예상**이다.

## 9. 허용 범위 안에서 한 이탈 · 판단 (투명 보고)

1. **디바이더 ID 재번호** `P6→P2` · `P2→P3` · `P3→P4`. verify_deck의 «PART divider 번호 단조 증가»는 waiver 경로가 없는 하드 게이트다. 화면 문구와 배치 순서는 불변이고, 원본 대응은 집필노트 §1에 있다.
2. **`data-series="edit-template"`** 를 S44·S46·S49·S50에 붙였다. P6가 앞으로 옮겨 가면서 S49·S50·S52가 같은 구도로 3연속이 됐기 때문이다(waiver 없는 FAIL). 원본 S44 보존유보에 적힌 «이후 4장의 템플릿»을 명시한 것이다(원본 W01~W05의 `data-series="proc"`와 같은 계약).
3. **클래스 개명** `codex-*→agent-*` · `journey-week*→journey-track*` · `cover-week*→cover-track*`. 게이트 ②가 대소문자를 무시하므로 클래스명도 걸린다. 개명한 구도는 계약 `layout_families`에 원래 family로 등재했다.
4. `revision.css`의 22px 미만 `.s-body` 선언 3건을 실효값 22px로 맞췄다. 뒤쪽 일괄 블록이 이미 22px로 덮고 있어 렌더는 같을 것으로 예상하지만 브라우저 확인이 필요하다.
5. `S10B` 좌열을 440→560px로 넓혔다(비유 7종 흡수 예산). `09`는 목록을 3행에서 4행(`is-4col`)으로 바꿨다.
6. S02·32·S62에서 **내가 치환한 문장**의 `<br>` 위치만 조정했다.
7. **빌더 사고와 복구** — 골격 `shell.html`과 산출 경로가 같아, 두 번째 조립부터 「기본 경화 5종」 블록이 통째로 빠져 있었다. 발견한 뒤 `sessions/_template` 사본에서 복구했고, §5~§8은 복구 뒤 전량 재측정한 값이다.
8. **지침 3종을 늦게 읽었다** — `profile.md` · `슬라이드지침.md` · `강의덱.초안/README.md`를 조립 후반에야 대조했다. 그 결과 고친 것:
   - README 계약: 스타일 태그에 `id="kit-additions"` 부착 · 신규 박스 제목 셀렉터 2개(`.map-left > b` · `.demo-placeholder > b`)를 경화 ② 위계 목록에 **추가**(그래서 이 블록은 «템플릿 + 셀렉터 2개»다 — 추가 외 차이 없음을 확인) · `layout_families`에 `roadmap` 등재(선례: 바이브코딩 3주차)
   - 지침 색 의미·kit 스펙: M6-03의 두 실행 결과는 우열이 없는 중립 비교라 양쪽 accent-bar를 ink로(민트 제거)
   - 계약 `must_keep`의 S3HUM 설명을 «관통 문장»에서 «핵심 메시지(제작브리프 §3)»로 정정(과목 관통 문장은 `profile.md` §1의 다른 문장이다)
   - 초안과 집필노트의 프로필 서술을 «§3-G 없음»에서 «§2·§3 기준선 미정»으로 정정
   - 고치지 않고 결정요청으로 올린 것: §11-7(화면 출처 표시)
9. 초안 `1주차_초안.md`는 **작성 주체 = 스킬**이다. 조립 결과를 4열 표로 역기술했고, `#` 칸에 원본 ID를 병기했으며, S01B·S3DO는 `(간지)`로 표시했다.

## 10. 허용 밖 변경 · 충돌

- `git -c core.quotepath=false status --short`로 범위 밖을 대조했다. 세션 시작 스냅숏 27줄은 **전부 그대로**다. **새 줄 1개 `M .claude/launch.json`**(mtime 16:39:59)이 생겼는데, **이 담당이 쓴 파일이 아니다**(허용 목록 밖이고 빌더 경로에도 없다). 같은 시각대에 `제작관리/상태장부.md`(16:40)가 갱신된 것으로 보아 다른 담당의 변경으로 보인다. 되돌리지 않았다.
- 원본 mtime은 그대로다: worktree shard·노트·캡처 07-23 · 본체 캡처 07-26 · 본체 덱 08-03 · 템플릿 shell 08-03 · `profile.md`·`슬라이드지침.md` 09-08 · `보존매트릭스_1차시.csv` 09-10 11:30 · `매핑_1차시.md` 11:37(둘 다 이 작업 시작 전).
- `제작관리/`는 읽기만 했다. 작업 중 다른 담당의 쓰기가 관측됐지만(`매핑_2차시.md` 16:33 · `보존매트릭스_2차시.csv` 16:34 · `상태장부.md` 16:40) 이 담당이 쓴 파일과 겹치지 않는다.
- 임시 파일은 `tmp/AI_코딩_에이전트_입문_3차시/BUILD1/`에만 있고, 저장소 밖 스크래치는 **0개**다.

## 11. 결정요청

1. **D-05** — attempt-02에서 해소(§0-3: 흐름이 `typing-game` 안이라 캡처를 제거하고 자리표시로 교체). 번호는 §9-8의 «§11-7» 참조를 지키려고 남긴다.
2. **R-QC-16 임계**(바이브코딩 실측)를 이 과정에 적용할지 — 적용하지 않는다면 `known_violations` 등재를 결정해야 한다(이번에는 waiver 0으로 두었다). `profile.md` §4 «카드 후순위»와 M6-01·M6-05 카드형 구도의 충돌도 함께 판단해야 한다.
3. **R-QD-05** — `콘텐츠_리뷰.html`을 `1주차_콘텐츠_리뷰.html`로 둘지(허용 목록 밖).
4. **`profile.md` §2·§3 기준선 «미정»** — R-COPY 3건이 미판정이고, R-QD-04는 기본값으로 판정됐다. 이 덱을 첫 완성 덱 실측으로 기록할지 결정해야 한다.
5. **R-QC-18 prompt_only 2슬롯** — 이미지를 생산할지, 슬롯을 걷어낼지(걷어내면 S3PE·S3PRV 레이아웃이 달라진다).
6. **`warn_baseline.static_gates`** — 0으로 둘지, verify_deck WARN 2를 사유와 함께 등재할지.
7. **화면 출처 표시 충돌** — `슬라이드지침.md` «출처 표시는 화면이 아니라 강사 자료에서 관리»와 CSV 보존유보(S3SY `arXiv:2310.13548` 출처 줄 · S3HUM 인용 2건의 `cite` · 08 «Karpathy 2025.02» 표기 «필수 유지»)가 충돌한다. 행 단위 정본인 CSV를 따라 **화면에 남겼다**. 지침을 따르려면 세 장의 출처를 노트로 옮겨야 한다.
8. 매핑에서 넘어온 항목: **D-02** Antigravity UI 명칭 · **D-06** 09·05 이미지와 문안 정합 · **D-08** 강제 메시지 3 · **D-09** 부록 2장·수강생참고 8장 배포 형식.

## 산출물

`강의덱.초안/shell.html` · `part-01~05.html` · `revision.css` · `강의덱.html`(조립기 생성) · `강의덱_발표자노트.html` · `1주차_초안.md` · `deck.contract.json` · `자료/images/`(26) · `자료/screenshot/`(2) · `자료/1주차_콘텐츠_집필노트.md` · `조립_보고.md`
