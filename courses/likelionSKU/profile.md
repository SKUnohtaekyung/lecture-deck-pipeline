# 과목 프로필 — likelionSKU

> 스키마 정본은 [`입력양식/과목프로필템플릿.md`](../../입력양식/과목프로필템플릿.md). 이 파일은 그 인스턴스다.
> 여기 있는 값은 **이 과목에서만 참**이다. 스킬 본문으로 되돌려 넣지 않는다.
> 작성 2026-08-29 · 근거: `likelionSKU/세션/BM_수익모델/README.md` · `likelionSKU/세션/BM_수익모델/_컨텍스트_타겟과커리큘럼.md` · `likelionSKU/세션/바이브코딩/vibe coding_plan.md` · `likelionSKU/덱_템플릿킷/00_레이아웃-선택가이드.md` · `likelionSKU/.claude/skills/likelion-deck/references/deck-assembly.md` · 원본 덱 2개 실측(`BM_수익모델_강의덱.html`·`바이브코딩_강의덱.html`) · `likelionSKU/`는 참조용 원본이며 저장소 밖(gitignore) 자산이다 — 이 프로필이 그 값을 이 저장소 안으로 옮겨 적은 것이다.

## 1. 과목 기본

| 항목 | 값 |
|---|---|
| 과목명 | likelionSKU (SKU 멋쟁이사자처럼 UX/UI 트랙) |
| 대상 수강생 | 대학 1~3학년 비즈니스 비전공 아기사자 — 멋쟁이사자처럼(LIKELION) UX/UI 트랙 기획/디자인 파트원. 트랙 내에서 이미 기획·화면설계(역기획·페르소나·MVP·IA/플로우·화면설계·상태설계)를 누적 학습한 상태이나 비즈니스/재무 배경은 없음 전제 |
| 관통 문장 | (공란 — 두 세션을 관통해 반복되는 단일 문장을 원본에서 확인하지 못함. `likelionSKU/README.md`의 "한 벌의 템플릿킷으로, 매번 같은 퀄리티의 강의덱을."은 **제작 시스템 자체의 태그라인**이라 학생에게 반복 노출되는 수업 메시지가 아니므로 관통 문장으로 채택하지 않았다. 각 세션은 세션 고유 메시지만 갖는다 — BM: "우리가 만든 서비스, 이거 어떻게 돈 벌어요?"(README.md) / 바이브코딩: "좋은 결과는 좋은 요청에서 시작한다. 좋은 요청은 문제, 사용자, 구조, 기준, 제외 범위를 포함한다."(`vibe coding_plan.md`) — 둘 다 **세션 1개에 국한**돼 과목 전체의 관통 문장 자격은 없다) |
| 회차 단위 명칭 | 세션 |
| 회차 구조 | 1주차 = 1세션(세션 아래 "교시" 같은 하위 시간 단위 없음) · 세션 길이 60~90분 — BM_수익모델 60~80분(`세션/BM_수익모델/README.md` "시간 \| 60~80분 · 토론식") · 바이브코딩 90분(`세션/바이브코딩/vibe coding_plan.md` "형식: 90분 개별 실습"). 두 세션은 LIKELION 20주 커리큘럼상 각각 16주차·12주차에 배치된 독립 세션이며, 이 저장소 자체의 "주차" 묶음 구조(바이브코딩 과목의 6교시형)와는 다르다 |

> 커리큘럼 정본 경로는 아직 없다 — 이번 단계(B)는 원본 `likelionSKU/` 세션 문서(`_컨텍스트_타겟과커리큘럼.md`·`vibe coding_plan.md`)에서 값만 옮겨 적었을 뿐, `courses/likelionSKU/` 안에 별도 커리큘럼 기준안을 신설하지 않았다(이번 작업 범위 밖).

## 2. 규모 기준선

> ⚠️ 이 과목은 "회차=세션=주차"가 1:1이라(§1) 템플릿의 "회차당 장수"와 "한 주차 총 장수"가 같은 값을 가리킨다. 아래에서는 **계획 단계 값**과 **완성 덱 실측값**을 분리했다 — 조립 과정에서 표지·전환 슬라이드가 늘며 계획보다 실측이 커지는 경향은 바이브코딩 과목에서도 관찰된 패턴이다.

| 항목 | 값 | 출처(어느 실측인가) |
|---|---|---|
| 세션당 계획 장수 | 26~33장 (BM 계획 26장 · 바이브코딩 계획 33장) | BM: `세션/BM_수익모델/슬라이드/BM_수익모델_HTML덱_상세계획서.md:17` "슬라이드 수 \| **26장**(고정 3장 + 본문 23장)" · 바이브: `세션/바이브코딩/슬라이드_구성안.md:1` "바이브코딩 강의덱 — 슬라이드 구성안 (**33장 확정본**)"(`:4` "40장 → Part 3 압축·제작과정 컷·다크 클로징 제거 → **33장**") |
| 세션당 실측 장수(= 한 주차 총 장수) | 30~31장 (BM 30장 · 바이브코딩 31장) | **직접 실측(2026-08-29)** — `grep -oE 'class="slide( [^"]*)?"' <파일> \| wc -l`로 `<section class="slide …">` 요소 계수(태그·클래스 결합으로 이중 확인). `세션/BM_수익모델/BM_수익모델_강의덱.html` = **30** · `세션/바이브코딩/바이브코딩_강의덱.html` = **31** |

> **총 장수는 게이트로 쓰지 않는다.** 계획(26~33)과 실측(30~31)의 차이 자체가 "장수는 설명 단위 수의 결과"라는 원칙(R-PLAN-01, 바이브코딩 §2-B 참고)을 재확인한다 — 목표 장수를 먼저 정하고 채우는 방향으로 쓰지 않는다.

## 3. 밀도 기준선 *(공란 — WARN)*

이 과목에 대해 `verify_draft_quality.py`/`verify_deck_quality.py`류의 밀도 게이트가 아직 실행된 적이 없고, 원본 자료에도 슬라이드당 정보 단위·본문 글자수·노트:본문 비율을 직접 측정한 기록을 찾지 못했다. **출처를 특정할 수 없으므로 값을 비워 두고 WARN으로 시작한다** — 바이브코딩의 실측치(예: 슬라이드당 3~6개, 본문 글자수 하한 76자)를 빌려 쓰지 않는다.

| 항목 | 값 | 출처 |
|---|---|---|
| 슬라이드당 정보 단위 범위 | | |
| 본문 글자수 대리 신호 하한 | | |
| 노트:본문 비율 상한 | | |

## 4. 표현 취향 *(보편 원리 아님 — 이 과목 사용자의 선택)*

> ⚠️ 아래는 원본 `덱_템플릿킷/00_레이아웃-선택가이드.md`·`.claude/skills/likelion-deck/references/deck-assembly.md`에 **횡단 규칙으로 명문화**된 값이며, 원본 시스템 사용자가 이미 확정한 선호다. 레이아웃 동점 시 우선순위나 "이 슬라이드가 합격선"이라고 지목한 사용자 결정 기록은 원본에서 찾지 못해 공란으로 둔다.

| 항목 | 값 |
|---|---|
| 레이아웃 동점 시 우선순위 | (공란 — 원본에 명시적 동점 우선순위 서술 없음) |
| 기준 슬라이드(합격선 예시) | (공란 — 원본에 "이 슬라이드가 합격선"이라 지목한 기록 없음) |
| 그 밖의 확정된 선호 — 색은 문법 | **코발트**(`--blue`) = 강조/핵심/오늘의 대상, 한 슬라이드에서 **키워드 1~2개만**. **오렌지·레드** = 문제·오답·경고·주의 전용. 한 슬라이드에 **파랑+오렌지+레드를 동시에 강조하지 않는다** (`00_레이아웃-선택가이드.md` §2① · `deck-assembly.md` "두 횡단 규칙" 1) |
| 그 밖의 확정된 선호 — 제목은 결론 | 슬라이드 제목(`.s-title`)은 **주제명이 아니라 결론**을 1~2줄로. 예: "수익 모델"(X) → "수익 모델은 발명이 아니라 선택과 조합"(O) (`00_레이아웃-선택가이드.md` §3 리듬 규칙 · `deck-assembly.md` "eyebrow/제목 규칙") |
| 그 밖의 확정된 선호 — 레이아웃 리듬 | **같은 레이아웃 3연속 금지** — 설명형(center-v·tbl·card-grid)과 참여형(center-msg·flow·lean)을 번갈아 배치. 파트마다 `part-divider`로 진행 위치를 표시 (`00_레이아웃-선택가이드.md` §3) |
| 그 밖의 확정된 선호 — 가운데 커넥터 | 두 패널/단계를 이을 땐 **파란 화살표·라인**으로 관계를 명시: 흐름(→) · 포함(⊃) · 대비(≠) · 가정→검증(↓) (`00_레이아웃-선택가이드.md` §2② · `deck-assembly.md` "두 횡단 규칙" 2) |

## 5. 브랜드·자산

| 항목 | 값 |
|---|---|
| 워드마크 문자열 | `SKU LIKELION` |
| 팀 라벨 | `UX/UI Team` / `Official LIKELION at SKU` — 원본 마크업은 두 줄 조판: `<div class="s-team">UX/UI Team<br>Official LIKELION&nbsp;&nbsp;at&nbsp;&nbsp;SKU</div>` (출처: `deck-assembly.md` "슬라이드 골격") |
| 로고 경로 *(참조 기록만)* | 원본 `likelionSKU/덱_템플릿킷/img/logo.png` (84×84 PNG·RGBA·8275 bytes, 2026-08-29 확인). **바이너리 반입은 이번 범위 밖** — 실제 덱 제작 시점에 `courses/likelionSKU/자산/` 등으로 복사할지 판단한다. 원본 폴더는 gitignore 대상이라 그대로 경로 참조하면 끊긴다 |
| 스타터 경로 | `kit/starter/deck-template.html` · `kit/starter/presenter-notes-template.html` — 이 저장소의 공용 스타터(과목 전용 스타터는 아직 없음) |
| 테마 | `likelionSKU` |

> **테마는 포인터만 적는다.** 값(색·타이포 토큰)의 정본은 `kit/themes/likelionSKU/tokens.css`이고,
> 무엇이 테마마다 바뀌고 무엇이 전 테마 공통으로 고정되는지는 `kit/guide/테마-계약.md`가 정본이다.
> 과목과 테마는 N:1이다(`kit/guide/테마-계약.md` §3, G2). 이 문서 작성 시점에 `kit/themes/likelionSKU/tokens.css`는
> 아직 없거나 병행 작업으로 변할 수 있다 — 정본 갱신은 그 파일 쪽 작업이 담당하며 이 프로필은 포인터만 유지한다.

## 6. 도구·제품 맥락

실습에서 다루는 제품·도구:

- **Codex**(ChatGPT Go 기반) · **Google Antigravity** — 바이브코딩 세션. 출처: `세션/바이브코딩/vibe coding_plan.md` "도구: ChatGPT Go 기반 Codex, Google Antigravity"
- **린캔버스**(Lean Canvas) — BM 세션 팀 실습의 핵심 산출물. 출처: `세션/BM_수익모델/README.md` "형식 \| 3명씩 2팀 · 팀 실습(**린캔버스**) 중심"
- **BMC**(비즈니스모델캔버스) — BM 세션에서 함께 다루는 프레임워크. 출처: `세션/BM_수익모델/비즈니스모델_수익모델_정리본.md` 등 BM 자료 다수 파일에서 언급 확인(파일 목록은 아래 §8 rg 결과의 부산물로 확인했으며, 본문 인용은 생략)

스킬 본문의 예시가 이 값에 의존해서는 안 된다.

## 7. 규칙 소급 면제 등재

**해당 없음** — 신설 과목이라 아직 동결되거나 완성된 산출물이 없다. 빈 표 유지.

| 대상 | 면제 게이트 | 사유 | 등재일 |
|---|---|---|---|

## 8. 격리 검사용 리터럴

`scripts/verify_subject_isolation.py`가 이 목록을 읽어 `SKILL.md`·`kit/`·`references/phases/`·`skills/`에서 찾는다. 하나라도 나오면 FAIL이다. 아래 전부 **등재 전 `rg -F "<문자열>" SKILL.md kit/ references/phases/ skills/`로 0건을 확인**했다(2026-08-29).

- 아기사자
- 멋쟁이사자처럼
- UX/UI Team
- Official LIKELION at SKU
- 대학 1~3학년
- 비즈니스 비전공
- 26~33장
- 30~31장

### 검사에서 제외한 것과 사유

| 대상 | 사유 |
|---|---|
| `SKU LIKELION`(워드마크 전체 문자열) | **등재하지 않음.** `rg -F "SKU LIKELION" SKILL.md kit/ references/phases/ skills/` → `kit/styles/deck.css`에 1건 이미 존재("SKU LIKELION 강의덱 — 디자인 시스템 + 레이아웃 모듈"). 이 kit 파일 헤더 주석은 **이 저장소의 공용 레이아웃 CSS가 원래 `BM_수익모델_강의덱.html`에서 추출됐다는 이력 기록**이며, 새 과목 값 누출이 아니다. 등재하면 항상 FAIL하므로 워드마크는 §5에만 값으로 남기고 §8에는 올리지 않는다 |
| `린캔버스` | **등재하지 않음.** 같은 `rg` 방식으로 `kit/styles/deck.css`에 2건 존재(`.canvas-fill` 설명 주석, `.lean-slide` 도형 주석 "린캔버스 9칸"). `SKU LIKELION`과 같은 이유(공용 kit이 원본에서 추출된 이력) — 등재 시 항상 FAIL |
| `LIKELION` · `SKU`(단독 토큰) | **등재하지 않음.** 위 두 항목과 같은 줄에서 검출되는 데다, 단독으로는 너무 일반적인 토큰이라 무관한 문맥과 충돌할 위험이 크다 |
| `likelionSKU`(과목 폴더명) | **의도적 제외(작업 지시).** 테마 폴더명 `kit/themes/likelionSKU/`와 동일 문자열이라 "모든 출현이 운영 파라미터" 조건을 채우지 못한다. 등재하면 `tokens.css` 헤더·테마 계약 문서의 테마명 표기가 전부 FAIL로 오탐한다 |
| 세션 길이 리터럴(`60~80분`·`90분`·`60~90분`) | **등재하지 않음(2026-08-29 rg로는 0건이었으나 판단상 제외).** 바이브코딩 프로필이 `270분`을 "사고 이력에도 등장할 수 있는 문자열"이라며 제외한 것과 같은 이유 — 순수 시간 길이 숫자는 무관한 문맥(다른 과목·사고 이력 서술)에서도 우연히 재사용되기 쉬워 리터럴로서 신뢰도가 낮다고 판단했다 |
