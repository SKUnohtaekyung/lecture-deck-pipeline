# 3주차 조립 보고

> 작성: create-slides (조립자) · 2026-08-08 (7차 갱신 · 77장 · 단일 HTML + 발표자 모드 산출)
> 대상 초안: `3주차_초안.md` v5 (4시간 6PART)
> **소유권**: 초안 메타에 `작성 주체` 필드 없음 → R-OWN-03 보수적 기본값 = **사람 작성**.
> 제목·본문을 고치지 않고 이 파일로 보고한다(R-OWN-01·R-OWN-02).

## 진행 상태

**78장 전량 조립 완료** — 결정표 목표(78장)와 정확히 일치한다.

| 구간 | 장수 | 조립 | 정적 검증 | 브라우저 실측 |
|---|---:|---|---|---|
| 도입 S01~S05 | 5 | ✅ | ✅ | ✅ |
| PART 1 | 14 | ✅ | ✅ | ✅ |
| PART 2 | 9 | ✅ | ✅ | ✅ |
| PART 3 | 11 | ✅ | ✅ | ✅ |
| PART 4 | 13 | ✅ | ✅ | ✅ |
| PART 5 | 13 | ✅ | ✅ | ✅ |
| PART 6 | 13 | ✅ | ✅ | ✅ |
| **합계** | **78** | | | |

**남은 것은 화면 두 자리뿐이다** — V6-03 · V6-04의 Sites 실계정 캡처(결정요청 D16).
빈 `asset-slot`을 두면 R-QC-18 상시 FAIL이므로 슬롯이 아니라 `[실제 화면 확인 필요]`
문구만 두었고, 두 장 모두 **글과 안내도는 완성 상태**라 그대로 강의할 수 있다.

덱 밖 잔여: **발표자 노트 HTML** · 실제 UI 캡처(V3-03·V4-04·V4-05·V5-11은 CSS 목업으로 대체).

## 브라우저 전수 실측 (2026-08-08 5차 · `audit_all.js` · scale 1 강제 · 1500×940)

증거: `sessions/_verify/3주차/deck-audit.json` · 러너 `python scripts/run_deck_checks.py 3 --parts 6`

**심각 결함 0** — 바닥선 초과 0 · 슬라이드 이탈 0 · 요소 겹침 0 · 오버플로 0 ·
**어절 중간 줄바꿈 0** · 빈 `asset-slot` 0 · 껍데기 상자 0.

| 지표 | 1차(28장) | 3차(44장) | 4차(55장) | 5차(현재 · 78장) |
|---|---|---|---|---|
| 바닥선 초과 · 이탈 · 겹침 · 오버플로 | 0 | 0 | 0 | **0** |
| 어절 중간 줄바꿈 | 5건 | 0건 | 0건 | **0건** |
| 빈 `asset-slot` · 껍데기 상자 | 0 · 4 | 0 · 0 | 0 · 0 | **0 · 0** |
| **폰트 하한 위반** | 1 | 1 | 1 | **0** |
| 「모양은 시각물인데 시각자료 0」 | 6장 | 4장 | 2장 | **0장** |
| 잉크 중앙값 | 0.351 | 0.471 | 0.478 | **0.499** |
| 최저 잉크 | 0.18 | 0.241 | 0.241 | **0.241** (V3-09) |
| `verify_deck` | FAIL 1 | FAIL 1 | FAIL 1 | **FAIL 0 · WARN 0 · PASS 50** |
| `verify_deck_quality` WARN | — | 3 | 2 | **1** |
| `run_deck_checks 3 --parts 6` | FAIL | FAIL | FAIL | **PASS** |

러너가 처음으로 **RESULT | PASS**를 냈다 — 정적 게이트와 렌더 증거가 모두 통과했다.
남은 `verify_deck_quality` WARN 1건은 R-QC-02(의미 블록 4개)로 **초안 소유권상 조립에서
단위를 더할 수 없는** 항목이다(아래 「계층 문제 3」).

## 이번 라운드에 고친 것

### PART 1 교정 3건 (인수인계가 지정한 선결 조건)

1. **어절 중간 줄바꿈 5건 → 0** — 개별 장을 손보지 않고 shell에 `word-break:keep-all`을
   덱 전체 상속으로 걸었다(⑥ 블록). 1·2·3주차에 모두 나온 지적이라 슬라이드 문제가
   아니라 규칙 문제로 판정했다(R-FEEDBACK-01).
2. **V1-02에 `D-concentric` 부착** — 결정표대로 동심 포함도 SVG를 얹고 레이아웃을
   `w3-cont`(도해 + 두 칸 설명 2단)로 다시 짰다. `visualShapeNoVisual` 해소.
3. **잉크 점유율** — V1-02 0.18 → 0.46 · V1-08 0.18 → 0.39.

### ⛔ 근본 원인: shell CSS가 **존재하지 않는 토큰 6종**을 쓰고 있었다

`--blue-tint` · `--mint-tint` · `--coral-tint` · `--gray-100` · `--gray-500` · `--gray-800`은
kit 어디에도 정의돼 있지 않다. 그래서 **17곳의 카드 배경·글자색이 통째로 무효**가 되어
`background:var(--blue-tint)`가 투명으로 렌더됐다 — V1-02·V1-08의 잉크 0.18은
「정보가 적어서」가 아니라 **박스가 안 그려져서**였다.

- 실제 토큰으로 교체: `--blue-soft` · `--mint-soft` · `--coral-soft` · `--surface` · `--gray-400` · `--gray-700`.
- **정적 게이트도 렌더 감사도 이걸 못 잡는다** — 문법이 유효한 CSS이고, 투명 배경은
  이탈·겹침·오버플로가 아니다. 검사 스크립트를 `tmp/check_tokens.py`로 임시 작성해
  잡았다. 상시 검사로 승격할 후보다(§환류 참고).

### 되돌아와 있던 2주차 교정 복원 (⑦ 블록)

3주차 shell의 「기본 경화」 블록에 **킷 컴포넌트 폰트 하한이 빠져 있었다** —
`legibility.css`가 `.callout`을 19px로 되돌려 놓은 상태였다. 2주차가 얻어낸 교정이
템플릿으로 돌아오지 못한 그 현상이다. 함께 복원한 것:

- `.callout` · `.cc-side li` · `.dc-cap` · `.eval-steps .work-step .desc` → 22px
- `.pd-sub{top:594px}` · `.cover-presenter{bottom:54px}` · `.dc-stage{bottom:54px}`
  → **`S01 cover-presenter@674` 기존 결함이 이걸로 해소됐다**(인수인계 차단 요인 ③).
- `.s-full.fill > .grid-2/.grid-3{min-height:0}`

### PART 2 조립 (9장)

| ID | 정보모양 | 구도 | element |
|---|---|---|---|
| P2 | — | 고정 divider | 부제를 초안 문구 「AI를 쓰는 방식은 하나가 아닙니다.」로 |
| M2 | flow | `roadmap`(5스텝) | — |
| V2-01 | contrast | `w3-vs-2col` | 좌우 각 칸에 SVG(혼자 볼 때 새는 항목 ↔ 나눠 볼 때) |
| V2-02 | concept | `w3-deffig` | 세로 직선 흐름 SVG(요청 → 에이전트 하나 → 결과) |
| V2-03 | concept | `w3-stage` | 왕복 흐름 SVG(메인 ⇄ 서브) |
| V2-04 | concept | `w3-deffig` | 팬아웃 SVG(프로젝트 → HTML·CSS·JS·QA → 사람이 판정) |
| V2-05 | comparison | `w3-tri` | 3열 × 3행 비교표(통제권·장면·적합) |
| V2-06 | structure | `w3-stage` | 허브 SVG(지휘자 ↔ 4담당 · 담당끼리 연결선 없음) |
| V2-07 | numeric | `w3-chartsplit` | `C-column` 3막대(싱글 1배 / 멀티 최소 3배 / 최대 10배) |

- 구도 배치는 `deffig → stage → deffig → table → stage → chartsplit`으로, **같은 패밀리가
  연속으로 붙지 않게** 했다(R-LAYOUT-02). 같은 패밀리라도 element 축을 바꿨다
  (직선 → 왕복 → 팬아웃).
- V2-07의 수치는 **1급 [S-441]의 "3-10x more tokens"만** 썼다. 3급 단독 수치
  「4~220배」는 G1에 따라 화면에 올리지 않았다.
- 신규 구도 5종(`w3-cont`·`w3-decl`·`w3-stage`·`w3-tri`·`w3-chartsplit`)을
  `deck.contract.json`의 `layout_families`에 등재했다.

### PART 3 조립 (11장)

| ID | 정보모양 | 구도 | element |
|---|---|---|---|
| P3 | — | 고정 divider | 부제 「내 프로젝트를 넷으로 나눕니다.」 |
| M3 | flow | `roadmap`(5스텝 · 초안 6→5 묶음) | — |
| V3-01 | contrast | `w3-vs-2col` | SVG 2개(일반 4역할에서 백엔드·DB가 꺼짐 ↔ 파일 4개로 나눔) |
| V3-02 | mapping | `w3-mapslide` | 파일 → 담당 4행 매핑(담당 4색 고정) |
| V3-03 | mapping | `w3-screen-zones` | 예시 화면 CSS 목업 + 담당별 관점 4구역 |
| V3-04 | screen-operation | `w3-practice` | 실습 ④ + 학생 프롬프트 |
| V3-05 | structure | `w3-stage` | 담당 4명이 각자 파일 하나만 보는 SVG(시선 교차 없음) |
| V3-06 | contrast | `w3-vs-2col` | SVG 2개(두 손 충돌 ↔ 쓰기 한 명 · 읽기 여럿) |
| V3-07 | flow | `w3-stage` | 혼자용 규칙 → 팀용으로 두 줄 확장되는 SVG |
| V3-08 | screen-operation | `w3-practice` | 실습 ⑤ + 학생 프롬프트 |
| V3-09 | declaration | `w3-decl` | 완료 체크 2 |

- **담당 4색을 여기서 확정했다** — HTML=`--blue` · CSS=`--mint` · JS=`--coral` · QA=`--periwinkle`.
  초안 3-2의 「4색 고정(오늘 내내 같은 색)」 지시대로 PART 4~6에서 같은 값을 쓴다.
  shell의 `.w3-own-html/-css/-js/-qa`가 `--own`·`--own-on`·`--own-soft` 3변수를 넘긴다.
- **V3-03은 실제 캡처 대신 CSS 목업으로 만들었다** — 결정표는 `IMAGE_EXPLANATORY`(활동
  기록 도구 화면)로 판정했지만 캡처가 아직 없고, 빈 `asset-slot`을 남기면 R-QC-18
  상시 FAIL이다. 이 장은 **버튼 위치를 가르치는 장이 아니라** 「같은 화면인데 담당마다
  보는 것이 다르다」를 가르치는 장이라 개념 목업이 허용된다. 실제 캡처가 확보되면 교체 가능.

### ★ 누락 복구 — 실습 슬라이드에 학생 프롬프트가 통째로 빠져 있었다

초안의 🗣 줄은 「비유·멘트」 열에 있지만 **강사 멘트가 아니라 학생이 실제로 타이핑하는
문장**이다. 1차 조립에서 PART 1 실습 3장(V1-04·V1-06·V1-09) 전부 이 줄이 화면에
들어가지 않았다 — 그 상태로는 실습이 성립하지 않는다.

- `.w3-prompt`(검은 터미널 · 「이렇게 말하세요」 배지)를 신설해 **실습 장 안에 인라인**으로
  넣었다. 2주차는 별도 슬라이드로 뒀지만 3주차는 4시간에 실습이 15개라 장수를 두 배로
  늘릴 수 없다(A4R1 선례).
- 실측 효과: 실습 장 잉크 0.38 → **0.52** · 바닥선 초과 0 유지(deadBottom 10).
- **PART 4~6의 실습 10장에도 같은 처리가 필요하다.**

### PART 4 조립 (13장)

| ID | 정보모양 | 구도 | element |
|---|---|---|---|
| P4 | — | 고정 divider | 부제 「팀을 돌리고, 엇갈리면 사람이 판정합니다.」 |
| M4 | flow | `roadmap`(5스텝 · 초안 6→5 묶음) | — |
| V4-01 | contrast | `w3-vs-2col` | SVG 2개(2주차 사이클 ↔ 3주차 사이클 · **같은 모양, 사람만 넷**) |
| V4-02 | classification | `w3-req3slide` | 요청 3칸(③만 코랄 — 가장 자주 빠뜨리는 칸) |
| V4-03 | screen-operation | `w3-practice` | 실습 ⑥ + 학생 프롬프트 |
| V4-04 | mapping | `w3-screen-zones` | 같은 화면 · 세 담당이 본 곳 3색 |
| V4-05 | comparison | `w3-shotslide` | 두 시안 CSS 목업(큰 글씨 ↔ 배지) |
| V4-06 | classification | `w3-verdictslide` | 다수결 ✗ 배너 + 판정값 4칸 |
| V4-07 | screen-operation | `w3-practice w3-vtableslide` | 실습 ⑦ — 단계 3 + 판정표 **가로 2단** |
| V4-08 | screen-operation | `w3-practice` | 실습 ⑧ + 학생 프롬프트 |
| V4-09 | concept | `w3-anchorsplit` **(split 2/3)** | 익숙한 시선 ↔ 처음 보는 시선 대비 SVG |
| V4-10 | screen-operation | `w3-practice` | 실습 ⑨ + 학생 프롬프트 |
| V4-11 | checklist | `w3-qaslide` | 6항목 × 통과/실패/해당없음 표 |

- **V4-05도 CSS 목업이다** — 결정표는 「두 시안 실제 렌더」를 요구했는데, 이 장에서
  보여 줄 것은 *강사 예시 앱의 두 배치안*이라 목업이 오히려 정확하다(실제 제품 화면이 아니다).
- **V4-07은 세로로 쌓으면 안 들어간다.** 실습 3단계 + 판정표 4행 + 완료 띠를 세로로
  쌓으니 완료 띠가 **730px**까지 내려가 슬라이드를 벗어났다(바닥선 666). 폰트를 내리는
  대신 **단계와 표를 가로 2단으로** 돌려 해소했다 — 가로가 남는데 세로로 쌓아 넘치는 것은
  구도 선택의 문제지 폰트의 문제가 아니다.

### 게이트가 잡아 준 것 2건

- **R-QC-17(실습 완결성)** — V4-07을 처음엔 리드 문장 + 표로 만들어 「단계 요소 <2개」에
  걸렸다. 초안 3줄을 전부 `work-step`으로 되살려 해소했다(그 3줄이 원래 실습 절차다).
- **R-QC-19(격자 안 맨 텍스트) 22건** — 아래 「검출기 수정」 참고. **오탐이었다.**

### PART 5 · PART 6 · 도입 · 마무리 조립 (26장)

| ID | 정보모양 | 구도 | element |
|---|---|---|---|
| S01 | — | 고정 표지 | 초안 0-1 문구 |
| S02 | declaration | `w3-vow` | 지난주 두 칸 + 오늘 붙는 두 칸 밴드 |
| S03 | contrast | `w3-vs-2col` | 대화창 1 ↔ 담당 4 SVG |
| S04 | flow | `w3-stage` | 2주차 MVP → 오늘 하는 일 → 열리는 주소 |
| S05 | flow | 고정 아젠다 | 6정거장 **1열**(2열은 7개 이상의 탈출구) |
| V5-01 | containment | `w3-cont` | 동심 포함도(내 폴더 ✓ / 바깥 ?) |
| V5-02 | containment | `w3-layerslide` | **세로 계층 밴드** — 초안이 「나란한 카드 4개 금지」를 명시했다 |
| V5-03 | flow | `w3-stage` | Host → Client → Server + 3갈래 분기 |
| V5-04 | flow | `w3-stateslide` | 연결됨 / 승인 대기 / 범위 초과 |
| V5-05 | concept | `w3-deffig` | 저장소 안에 커밋이 쌓이는 타임라인 |
| V5-06 | flow | `w3-stage` | 확인 → 고르기 → 커밋(첫 칸 강조) |
| V5-07 · V5-10 | screen-operation | `w3-practice` | 실습 ⑩⑫ + 학생 프롬프트 |
| V5-08 | checklist | `w3-excludeslide` | 위험 파일 5종 제외 |
| V5-09 | comparison | `w3-msgcompare` | ✗3 → ✓1 커밋 메시지 |
| V5-11 | contrast | `w3-vs-2col` | 완료 답변 ↔ GitHub 커밋 목록 목업 |
| V6-01 | contrast | `w3-vs-2col` | 파일 목록 ↔ 열리는 URL 목업 |
| V6-02 | checklist | `w3-excludeslide` | 공개 전 4종 + 회수 불가 경계선 |
| **V6-03** | screen-operation | `w3-stage` | 안내도 + **[실제 화면 확인 필요]** |
| **V6-04** | screen-operation | `w3-practice` | 실습 ⑬ + **[실제 화면 확인 필요]** |
| V6-05 | flow | `w3-branchslide` | 성공 / 폴백 / 미완료 3갈래 |
| V6-06 | classification | `w3-quadslide` | 4칸 점검 |
| V6-07 · V6-09 | screen-operation | `w3-practice` | 실습 ⑭⑮ |
| V6-08 | flow | `w3-goldensplit` **(split 3/3)** | 발견 여럿 → 하나 → 3단 |
| V6-10 | flow | `w3-stage` | 3주 성장 타임라인 |
| END | — | 고정 마무리 | 3주 회고 + 계약 문구 「검증된 것만 완료로 봅니다」 |

- **도입이 3장에서 5장이 됐다.** 킷 템플릿은 표지·도입·아젠다 3장뿐인데 결정표가 5장을
  요구했다. 아젠다 슬라이드의 ID를 `S03` → `S05`로 옮기고(클래스 `s03-slide`는 킷 것이라
  그대로), 그 사이에 `S03`·`S04`를 새로 만들었다. `deck.contract.json`의 `intro`도
  다섯 개로 갱신했다 — 이 검사는 **문서 첫 N장의 ID 순서**를 본다.
- **마무리(END)를 채워 4라운드 내내 떠 있던 `verify_deck` FAIL 1이 해소됐다.**
- **split은 정확히 3장이다**(V1-07 · V4-09 · V6-08) — 결정표가 정한 상한 그대로이고
  서로 붙어 있지 않다.

### 마지막 라운드에서 게이트가 잡아 준 것

- **폰트 하한 5건** — 대부분 **킷 기본값이 하한 미만**인 자리였다(`.cover-lead` 18 ·
  `.s03-slide .an-intro` 21 · `.concept-lead` 21). 킷이 `.s03-slide .an-intro`(0,2,0)로
  거는 값을 `.an-intro`(0,1,0)로 덮으려다 조용히 무시됐다 — **명시도를 같은 형태로 맞춰야**
  먹는다. 나머지 둘(긴 eyebrow·긴 로드맵 desc)은 문구를 줄였다.
- **V6-04 바닥선 7px 초과** — 폰트를 내리지 않고 안내 박스 패딩만 조여 해소.
- **S02 하단 죽은 여백 154px** — `.s02-slide .s-body-wrap`에 `bottom:54px` + `space-evenly`.

## 중복 정리 — 78장 → 74장 (2026-08-08 · 사용자 지시 「과감하게 없애줘」)

전 78장의 제목·리드·본문을 뽑아 대조해 **같은 그림·같은 메시지가 두 번 나오는 곳** 4건을
잘라냈다. 실습 15개는 그대로다(잘린 4장은 전부 비실습 장).

| 삭제 | 제목 | 왜 중복인가 | 살린 문장 |
|---|---|---|---|
| `V3-03` | 담당별로 무엇을 보나 | **V4-04와 같은 장치·같은 메시지** — 같은 화면 위에 담당별 색 구역. 담당↔영역은 V3-02가 이미 세웠다 | — (V4-04가 대신함) |
| `V3-05` | 담당은 자기 구역만 봅니다 | **V2-06(오케스트레이터)과 같은 그림** — 「자기 악보만 / 옆 담당 모름」. 파일 매핑은 V3-02가 이미 했다 | 「요청창을 담당 수만큼 나눠서 엽니다」 → V4-03 실습 ⑥ 1단계 |
| `V3-09` | 이제 팀을 돌립니다 | **V1-12와 같은 서식·같은 기능**(체크 배지 + 다음 파트 예고). P4 전환 부제가 같은 말을 한다 | — (P4 부제가 대신함) |
| `V4-01` | 겁먹지 마세요 | S03·S04에 이은 **「혼자 → 넷」 대비의 세 번째 반복** | 「순서는 지난주와 똑같습니다 — 사람만 늘었습니다」 → M4 리드 |

**발표자 노트로 내린 것**: V3-05의 「한 번에 전부 물어보면 다시 싱글 에이전트로 돌아가는
것이라 나눈 의미가 없어집니다」 — 말로 하면 되는 경고라 화면에 장을 쓰지 않는다.

### 왜 이 4장인가 — 남긴 것과의 경계

- **V4-11(화면 QA 6항목)은 남겼다.** 처음엔 V4-10 실습 ⑨(정상값·다른 값·빈 값 3항목)와
  체크리스트가 겹친다고 봤는데, 다시 보니 **행위자가 다르다** — V4-10은 *QA 담당(에이전트)*
  에게 시키는 것이고 V4-11은 *내가 직접 화면을 보는* 마지막 확인이다. 이 과목의 관통 문장
  (「완료는 증거로」)이 서 있는 자리라 지우면 안 된다.
- **V3-06(단일 작성자) ↔ V4-08 실습 ⑧의 「단일 작성자입니다」**는 중복이 아니라 회상이다.
- **V4-02(요청 셋) ↔ V4-03(실습 ⑥)**, **V4-06(판정값 4) ↔ V4-07(실습 ⑦)**은
  가르치고-시키는 짝이라 그대로 둔다.

### 정리 후 실측 (74장)

| 지표 | 78장 | **74장** |
|---|---|---|
| 바닥선·이탈·겹침·오버플로·어절잘림·빈슬롯·껍데기상자 | 0 | **0** |
| 폰트 하한 위반 | 0 | **0** |
| 「모양은 시각물인데 시각자료 0」 | 0장 | **0장** |
| 잉크 최솟값 | 0.241 | **0.274** |
| 잉크 중앙값 | 0.499 | **0.499** |
| `verify_deck` | FAIL 0 WARN 0 | **FAIL 0 · WARN 1**(V2-06 `<br>` 앞 조사 — 다른 세션 수정분) |
| 러너 | PASS | **PASS** |

PART 3은 11 → 8장, PART 4는 13 → 12장이 됐다. 45분짜리 PART 4에서 1장,
35분짜리 PART 3에서 3장을 덜었으니 **진행 여유가 5~7분 늘었다**(장당 약 1.5~2분 기준).

## Sites 실화면 반영 + 신규 3장 (2026-08-08 · 사용자 제공 자료)

사용자가 Codex Sites 실화면 4장과 실사용 프롬프트를 줬다. **결정요청 D16이 해소됐다.**

### 확인된 것 / 아직 아닌 것

| 항목 | 상태 |
|---|---|
| 사이드바 진입(새 채팅 · 풀 리퀘스트 · **사이트** · 예약 · 플러그인) | ✅ 확인 |
| 사이트 목록(열 = 사이트 / 공유 대상 · 우상단 `만들기` · 행마다 `공유`) | ✅ 확인 |
| 주소 형식 `<슬러그>.<계정>.chatgpt.site` | ✅ 확인 |
| 새 사이트 기본 공유 대상 = 🔒`나만` | ✅ 확인 |
| 사이트 설정(이름 · URL · 도메인 · 맞춤 도메인 · 공유 `관리`) | ✅ 확인 |
| **공유 모달 — 「액세스 권한이 있는 사용자」 → `인터넷 사용자 누구나` → `게시`** | ✅ **확인 — D16 해소** |
| `만들기` 클릭 이후 화면 | ⏳ 미확인 |
| 기존 로컬 프로젝트를 사이트로 넘기는 방법 | ⏳ 미확인 |

⚠️ **개인정보는 화면에 넣지 않았다.** 제공 스크린샷의 계정명·이메일·실제 슬러그 대신
`내-사이트.내계정.chatgpt.site` 자리표시를 쓴다.

### 재작성 2장

- **V6-03 사이트 만들기** — `[실제 화면 확인 필요]` 박스를 걷어내고 **사이드바 + 사이트 목록
  목업**(`w3-codex`)으로 다시 그렸다. 실제 화면 배치를 따랐다 — 좌측 메뉴에서 `사이트`가
  선택된 상태, 우상단 `만들기`, 행에 주소와 🔒`나만`과 `공유`.
- **V6-04 실습 ⑬** — 절차를 **4단계**로 확정하고(만든 직후 「나만」 → 공유 → 관리 →
  「인터넷 사용자 누구나」 + 게시 → 다른 창에서 확인) 우측에 **공유 모달 목업**을 붙였다.
  초안은 3단계였으나 실화면이 「관리 → 범위 선택 → 게시」 두 단계를 요구해 갈랐다.

### 신규 3장

| ID | 위치 | 무엇 |
|---|---|---|
| **V3-10** 팀 구성 프롬프트 | V3-08(실습 ⑤) 뒤 | 사용자 실사용 프롬프트를 **사람 이름 → 담당 이름**으로 옮겨 전문 게재 |
| **V5-12** MCP | V5-02 뒤 | 정의 + **규격 없음(거미줄) ↔ 공통 규격(허브)** 대비 도해 |
| **V5-13** 프로토콜 | V5-12 뒤 | 「미리 맞춰 둔 약속」 — 우편 봉투 도해를 **직접 그렸다**(외부 이미지 미사용) |

- **프롬프트는 줄이지 않았다.** 학생이 그대로 복사해 쓰는 문장이라 요약하면 안 된다.
  한 컬럼 22px로는 9줄이 세로 예산이라(2주차 실측) **가로 2단**(`w3-bigprompt`)으로 나눴다.
  사람 이름 4인(수진·민호·지훈·도윤)은 v5 재설계 결정대로 **HTML·CSS·JS·QA 담당**으로 바꿨다.
- **V3-10에는 완료 기준 띠를 달지 않았다.** 실습 ⑤의 완성 기준은 V3-08에 이미 있어
  같은 실습에 두 번 붙는 셈이었고, 그 자리를 프롬프트 세로 예산에 줬다.
  ⚠️ 처음엔 달았다가 좌측 칸이 컨테이너를 40px 넘겨 **완료 띠와 겹쳤다**(실측 겹침 1건).

### 실측 (77장)

`run_deck_checks 3 --parts 6` → **RESULT PASS**. 바닥선·이탈·겹침·오버플로·어절잘림·
빈슬롯 **전부 0** · 폰트 하한 위반 **0** · 「모양은 시각물인데 시각자료 0」 **0장** ·
잉크 중앙값 0.495. 신규 3장 잉크: V3-10 0.47 · V5-12 0.60 · V5-13 0.86.

## 산출물 3종 — 단일 HTML · 발표자 노트 · 발표본 (2026-08-08)

| 파일 | 크기 | 무엇 |
|---|---:|---|
| `강의덱.html` | 0.3MB | **정본 생성물.** kit CSS를 상대경로로 링크한다 — 로컬 HTTP 서버 필요 |
| `강의덱_배포.html` | 3.2MB | **단일 HTML.** CSS·폰트·자산을 전부 인라인했다(외부 요청 0건) |
| `강의덱_발표자노트.html` | 0.1MB | 강사용 세로 문서. 💬 농담 · 👀 시연 큐 · 🗣 프롬프트 |
| `강의덱_발표.html` | 3.3MB | **발표본.** 배포본에 발표 런타임을 주입한 것. `P` 키로 발표자 창 |
| `강의덱_발표.meta.json` | — | 사이드카(buildHash · deckId · 지문) |

### 발표자 노트 — 초안 💬·👀·🗣에서 생성

`tmp` 스크립트로 초안 표의 「비유·멘트」 열을 파싱해 만들었다.
**75블록 · 88항목 · `verify_notes` MISMATCH 0 (OK 75/75).**

- 🖼️는 조립 지시라 노트에 넣지 않았다(강사가 볼 것이 아니다).
- ⚠️ **삭제된 4장의 멘트를 버리지 않고 옮겼다.** 중복 정리로 없앤 장에도 쓸 만한
  멘트가 있었다 — V3-03 → V4-04 · V3-05 → V4-03 · V3-09 → V3-08 · V4-01 → M4.
  슬라이드를 지울 때 노트까지 같이 사라지면 강사가 그 말을 영영 못 본다.
- 초안에 행이 없는 신규 3장(V3-10 · V5-12 · V5-13)은 손으로 적었다.

### 발표자 모드

`inject_presenter.py`로 완성된 배포본에 런타임을 주입했다. 이 스크립트는 조각에서
재생성하지 않고 **원문 바이트를 보존한 채 삽입만** 하며, 주입 전후 지문이 하나라도
어긋나면 파일을 쓰지 않는다(fail-closed).

`verify_presenter_deck.py` → **FAIL 0 · WARN 0 · PASS 19**. 확인된 것:
외부 src/href 0건 · 슬라이드 77장 동일 · slideId 시퀀스 동일 ·
**슬라이드별 innerHTML 해시 동일**(내용 무변경 증명) · 노트 75슬라이드 매핑 ·
`--pv-*` 토큰 라이트/다크 21쌍 · 인쇄 정책(발표 UI 제외 + 1장=1페이지).

- 진입: 리모컨 **「발표자 모드 (P)」** 버튼 또는 `P` 키 → 별도 창이 열린다.
- ⚠️ **팝업 창 자체는 이 세션에서 실행 확인을 못 했다.** 인앱 브라우저가 `window.open`을
  막는다. 런타임 구조·노트 배선·토큰은 위 19항목으로 검증했지만, **강의 전에 실제
  브라우저(Chrome/Edge)에서 `P`를 눌러 두 창이 뜨는지 한 번 확인**해야 한다.
  듀얼 모니터 환경에서의 창 배치도 그때 함께 본다.

## 조립 중 발견한 함정 2건 (다음 PART에서도 걸린다)

### 1. ⛔ `viz-*`는 자유 태그가 아니다 — 일부는 킷이 CSS를 소유한다

`kit/styles/patterns.css:54`의 `.viz-radial svg{width:900px;height:360px}` 때문에,
내 SVG에 `viz-radial`을 붙인 V2-04의 격자가 **590px / 900px로 무너졌다**(선언은 `1.06fr .94fr`).
좌측 본문이 214px 폭에 갇힌 상태였는데 **정적 게이트도 렌더 감사도 통과했다** —
이탈·겹침이 아니라 «비율만» 틀린 형태이기 때문이다.

- 킷이 소유하는 이름: `viz-radial` · `viz-concentric` · `viz-cycle` · `viz-gantt` ·
  `viz-split` · `viz-code` · `viz-tree` · `viz-flow-ph`.
- 킷 마크업(`.rad-*` 등)을 그대로 쓰는 게 아니면 **소유되지 않은 이름**을 쓴다
  (`viz-flow-h`/`-v` · `viz-matrix` · `viz-column` · `viz-containment` · `viz-hub`).

### 2. 종횡비가 있는 SVG는 격자 폭을 역으로 민다

`min-width:0`으로도 안 잡혔다. 도해 SVG는 `position:absolute; inset:0`으로 **흐름에서
빼내** 크기 되먹임을 끊었다(`.w3-deffig-fig` · `.w3-stage-fig` · `.w3-conc`).

## 검출기 수정 ② — `scripts/verify_deck_quality.py` R-QC-19 오탐

`work-step` 안 맨 텍스트를 찾는 로직이 **중첩 인라인 태그에서 오탐**을 냈다(실측 22건/8장).

- `<span class="desc">앞 <strong>강조</strong> 뒤</span>`에서, 「단순 태그 쌍」 제거 정규식의
  `[^<]*`는 `<`를 못 넘으므로 **desc 스팬이 한 번에 안 지워지고** 안쪽 `strong`만 지워진다.
  남은 「앞 뒤」가 맨 텍스트로 계수됐다 — 실제로는 desc 스팬 **안**이라 익명 격자 아이템이
  아니고, 이 검사가 잡으려는 결함이 아니다.
- 고침: 안쪽부터 **고정점에 도달할 때까지 반복 제거**한다. 진짜 맨 텍스트(호스트 직계)는
  그대로 잡힌다(단위 확인 완료).
- 실측: 1주차 1 → 0 · 2주차 0 → 0 · 3주차 22 → 0.
- ⚠️ **넓히려다 되돌린 기록**: 1주차의 1건은 «class 없는 `<div>` 래퍼»였는데 정규식이 첫
  `</div>`에서 잘려 마크업이 불균형해진 **부작용으로** 우연히 걸린 것이었다. 그걸 명시
  규칙으로 바꾸니 1주차 13건 · 2주차 24건으로 튀었고, 표본은 대부분 자식이 둘뿐인 정상
  2열 배치였다(익명 아이템이 col 2에 제대로 떨어져 결함이 아니다). **검사 범위를 넓힐 때는
  오탐률을 함께 재라**는 규칙대로 되돌렸다. 넓히려면 직계 자식 수까지 세어
  「3번째 이후가 col 1로 되감기는가」를 봐야 한다.

## 검출기 수정 ① — `scripts/audit_render.js`

두 검출기가 같은 슬라이드에 **반대 판정**을 내고 있었다.

- `gfx`를 `img/svg/canvas`로만 셌다. 킷의 `C-column`·`C-bar`는 `.code-chart` + `.cc-fill`
  즉 **div 스택**이라 그림 요소가 하나도 없다 → 카탈로그대로 만든 차트가
  「numeric인데 시각자료 0」으로 잡혔다. 정적 검사 R-QC-08은 `viz-*` 클래스로 세어
  같은 슬라이드를 통과시킨다.
- 고침: 정적 검사와 같은 표지(`viz-*` / `data-viz`)를 렌더 감사도 인정한다(중복 계수 없음).
- 부수 효과: 도해·차트 **안의** 무텍스트 도형(막대·노드)이 `hollow`로 잡히던 것도
  해소했다. 실측 `hollow` 4 → 0 · `hollowBadge` 63 → 28.

## 계층 문제 — 고치지 않고 보고 (R-OWN-02)

### 1. V1-07 제목과 본문의 계층이 어긋난다 (이월)

제목 「이제 처음으로 코드를 만집니다」는 전환 선언인데 본문은 마우스 오버 효과의 정의다.
조립에서는 제목을 그대로 두고 상단 전환 문장 / 하단 용어 정의로 시각 계층만 살렸다.
**문구 교정은 초안 소유자 판단이다.**

### 2. numeric 정보 모양이 78장 중 1장뿐이다 (이월)

차트를 얹을 수 있는 장이 V2-07 하나다. 수치로 보여 줄 소재(실습 소요 시간 분포,
확인 항목 통과율 등)가 초안에 더 있으면 화면이 눈에 띄게 좋아진다.

### 3. V1-02 · V1-12의 정보 단위가 4개로 하한(5)에 못 미친다 (이월)

초안 행의 본문이 4단위라 조립에서 임의로 단위를 더할 수 없다(R-OWN-01).

### 4. 로드맵 단계 묶기 — 2026-08-08 사용자 확정 「5단계로 묶기」

초안 `2-MAP` 본문은 「① 왜 나누나 → ② 싱글 → ③ 서브 → ④ 멀티 → ⑤ 셋을 나란히 비교
→ ⑥ 오케스트레이터 → ⑦ 나누면 무조건 좋을까」 7단계인데, **로드맵 템플릿이 스텝
6개 이상을 금지**한다(shell ⑤ 블록 · 1280px에서 칸당 140px로 판독이 무너진다).

②③④를 「세 가지 방식」 한 칸으로 묶어 5단계로 조립했고, **사용자가 이 방식을 확정**했다
(대안이던 「로드맵 2장 분할」·「템플릿 상한 7로 상향」은 채택되지 않았다).

- **M3에도 같은 처리를 적용했다** — 초안 6단계의 ②③(담당 넷 / 담당별로 무엇을 보나)을
  「담당 넷과 보는 것」 한 칸으로 묶었다.
- **M4 · M6이 남았다** — 결정표가 6스텝으로 적어 뒀으니 조립할 때 같은 방식으로 묶는다.
- 내용은 빠짐없이 들어가지만 **초안 문구 그대로가 아니다.** 로드맵은 그 파트의 안내
  화면이고 각 단계는 뒤따르는 본문 슬라이드가 온전히 다룬다.

## 미해결 — 남은 것

1. ✅ ~~Sites 실계정 캡처(D16)~~ — **2026-08-08 사용자 제공 실화면으로 해소.**
   남은 미확인 둘: **`만들기` 클릭 이후 화면**과 **기존 로컬 프로젝트를 사이트로 넘기는 방법**.
   둘 다 당일 시연으로 확인하면 되고, 지금 덱은 그 전제로 쓰여 있다.
2. ✅ ~~발표자 노트 HTML~~ — **생성 완료**(75블록 · 88항목 · MISMATCH 0).
   남은 것은 **실제 브라우저에서 발표자 창 팝업 실행 확인** 하나다.
3. **실제 UI 캡처 대체분 4장** — V3-03 · V4-04 · V4-05 · V5-11은 CSS 목업이다.
   전부 「강사 예시 앱의 배치」를 보여 주는 장이라 목업이 정당하지만, 실제 캡처가
   더 나은 장이면 교체할 수 있다.
4. ✅ ~~마무리(END)~~ ~~도입 5장~~ ~~M5·M6 로드맵~~ ~~PART 5·6 실습 프롬프트~~ — 전부 해소.

## 환류 후보 (R-FEEDBACK-01 · 승격 위치까지 적는다)

| # | 발견 | 승격 위치 후보 | 상태 |
|---|---|---|---|
| 1 | 미정의 CSS 토큰이 조용히 배경을 지운다 | `scripts/verify_deck.py`에 「var(--x) 미정의」 검사 신설 | **미승격** — 이번엔 `tmp/check_tokens.py`로 임시 처리 |
| 2 | `word-break:keep-all` 부재로 한글이 낱글자 단위로 끊긴다 | `kit/styles/deck.css` | **보류** — 1주차 동결·2주차 재측정 필요. 3주차 shell ⑥에만 적용 |
| 3 | 킷이 소유한 `viz-*` 이름 목록이 어디에도 없다 | `kit/charts/README.md` 또는 `카탈로그-규격.md` | **미승격** |
| 4 | 킷 컴포넌트 22px 하한이 주차마다 원상복귀한다 | `sessions/_template/강의덱.초안/shell.html` | **미승격** — 3주차 shell ⑦에만 적용 |
| 5 | `audit_render.js`가 CSS 차트를 시각자료로 못 셌다 | — | ✅ **적용 완료**(스크립트 직접 수정) |
| 6 | 초안 🗣(학생 프롬프트)가 조립에서 통째로 빠질 수 있다 | `scripts/check_title_survival.py`류의 「초안 🗣 생존」 검사 | **미승격** — 이번엔 초안 대조로 발견 |
| 7 | R-QC-19가 중첩 인라인 태그에서 오탐 | — | ✅ **적용 완료**(반복 제거로 고정점) |
| 8 | 킷 기본값이 22px 하한 미만인 자리가 계속 나온다(`.cover-lead`·`.an-intro`·`.concept-lead`) | `kit/styles/legibility.css` | **미승격** — 3주차 shell ⑦에만 적용 |
| 9 | 도입이 3장 고정이라 5장짜리 서사를 담지 못한다 | `sessions/_template/강의덱.초안/shell.html` | **미승격** — 3주차에서 S03·S04를 손으로 추가 |

---

## 2026-08-08 · PART 1 수정 12건 (사용자 지시) — 범위 밖으로 남긴 것

수정 대상은 `강의덱.초안/part-01.html` + `shell.html`의 PART 1 전용 CSS로 한정됐다.
아래는 그 과정에서 **발견했으나 지시 범위 밖이라 고치지 않은 것**이다(R-OWN-02).

| # | 발견 | 왜 안 고쳤나 | 고칠 때 함께 봐야 할 것 |
|---|---|---|---|
| 1 | **덱 안에서 백업 폴더 날짜가 두 개다** — V1-05 도해는 `백업_2026-08-08`, V1-06 프롬프트는 `백업_2026-08-07`. 초안 1-6도 `2026-08-07`이다 | 콘텐츠 계층 문제이고 어느 쪽이 정답인지는 강의 날짜가 정한다 | 초안 1-5·1-6과 V1-05·V1-06을 한 번에 맞춘다 |
| 2 | **완성 기준의 위치가 PART마다 다르다** — PART 1은 큰 제목 아래 기준 행(`.w3-crit`), PART 3~6은 여전히 하단 콜아웃(`.w3-goal` 12곳) | 지시가 PART 1 한정이었다 | `.w3-goal` → `.w3-pr-hd`+`.w3-crit` 이관. R-QC-15(하단 결론 콜아웃 비율)가 같이 내려간다 |
| 3 | **프롬프트 UI도 PART마다 다르다** — PART 1은 터미널 카드(`.w3-ask`), PART 3~6은 인라인 다크 박스(`.w3-prompt` 8곳) | 같은 이유 | `.w3-prompt`는 그대로 두고 `.w3-ask`로 하나씩 옮긴다. 두 CSS를 당분간 함께 유지해야 한다 |
| 4 | **장식성 좌측 세로선이 PART 1 밖에도 있다** — `.w3-hn`은 part-05도 쓴다(2곳), `.w3-gate-cell`도 2곳 | 전역으로 지우면 part-05 화면이 같이 바뀐다 | 지금은 `[data-slide="V1-01"]`·`[data-slide="V1-05"]`로 scoped. 전역화하려면 part-05를 함께 재측정 |
| 5 | `.roadmap`(M1~M6) 6장 전부 「박스 15개 · 시각자료 0」 | 지시가 M1의 **문구**였다 | 환류 후보 표 참고. 로드맵 템플릿 차원의 문제다 |

**해소된 것**: 위 환류 후보 표의 문제와 별개로, V1-02의 「하네스의 앞 두 칸」이라는
잘못된 개념 프레이밍을 제거했다 — 이 장은 하네스 5칸의 부분집합이 아니라
「맡기기 전 준비 게이트」다. 동심원(containment) 도해를 세로 흐름으로 교체했고
`deck.contract.json`에 `w3-gatefig: flow` · `w3-hoverfig: diagram`을 등재했다.

**검증**: `run_deck_checks.py 3 --assemble --parts 6` → **RESULT PASS**(4단계 전부).
브라우저 실측(scale=1 · 슬라이드 1280×720 확인) — 78장 기준 바닥선 초과 0 · 이탈 0 ·
겹침 0 · 어절 중간 줄바꿈 0 · 빈 슬롯 0 · 폰트 22px 하한 위반 0 · 콘솔 에러 0.
증거: `sessions/_verify/3주차/deck-audit.json`.

## 2026-08-08 · PART 2 수정 (사용자 지시) — 하단 박스 판정과 남긴 것

### 하단 한 줄 박스 전수 판정 (P2 · M2 · V2-01~07)

판정 기준은 사용자가 준 것이다 — 「순서·비교·구조·흐름을 설명하는 문장」은 본문/도해로
내리고, 「다음 장 연결 · 핵심 결론 · 안전 경고」처럼 독립 역할이 있을 때만 박스로 남긴다.
**문장은 하나도 버리지 않았다. 자리만 옮겼다**(R-OWN-01 · R-ANNEX-01).

| 슬라이드 | 레이아웃 | 하단 요소 | 판정 | 조치 |
|---|---|---|---|---|
| `P2` · `M2` | divider · roadmap | 없음 | — | 없음 |
| `V2-01` | compare2 | 회사 비유 callout | **비교를 설명** | `.s-lead`로 올려 «비교를 여는 전제»로 배치 |
| `V2-02` | definition | 없음 | — | 없음 |
| `V2-03` | diagram | 요리 비유 callout | **구조를 설명** | 도해 캡션 `.w3-stage-cap`(박스·fill 없음)으로 하강 |
| `V2-04` | definition | 없음 | — | 없음 |
| `V2-05` | table | 없음 | — | 없음 |
| `V2-06` | diagram | 지휘자 비유 + 청중 지목 | **핵심 결론** | 하단 박스 **유지**(사용자 지시와 일치) |
| `V2-07` | chart | `.w3-cs-note` | 차트 캡션 | 유지 — 차트에 붙은 캡션이라 정당 |

- **`hug-center`는 이제 PART 2에서 `V2-06` 한 곳뿐이다**(3곳 → 1곳).
- ⚠️ **V2-03과 V2-06은 같은 `w3-stage`인데 마무리 형태가 다르다.** 의도된 차이다 —
  V2-03의 문장은 도해를 말로 옮긴 것이고, V2-06의 문장은 도해와 무관하게 청중을
  직접 지목한다. DOM만 보면 「변주 실패」로 보일 수 있으니 이 기록을 먼저 봐라.

### 남은 WARN 1건 — 고치지 않았다 (사용자 지시가 우선)

`verify_deck` **WARN 1**: `<br> 직전 어절 조사 종결 후보 1건: ['V2-06: …자리는<br>']`

- 원인은 사용자가 지정한 줄바꿈이다 — 「오늘 그 자리는 / 여러분입니다.」는 조사 `는`으로
  앞줄이 끝난다. MEMORY 「수동 `<br>`은 조사·어미가 앞줄에 매달리지 않는 구·절 경계에서만」
  휴리스틱에 걸리지만, **정본 우선순위상 현재 사용자 지시가 이긴다**(SKILL.md ③).
- FAIL이 아니라 WARN이라 종료코드에는 영향이 없다(`RESULT PASS`).
- `known_violations`로 덮지 않았다 — 덮으면 신호가 사라진다. 판단은 사용자 몫으로 남긴다.

### V2-04 겹침의 실제 원인 (기록)

「네 결과가 동시에」 라벨이 **연결선 4개를 가로지르고 있었다.** 글자 상자 y 234~252 ·
수집 세로선 x=153·247이 y 224~256 → 실제 교차. 폰트는 건드리지 않고 도해를 재배치했다:
상단 12px 압축 → 수집 가로선을 y=236으로 → 라벨을 그 아래 전용 칩(y 248~290)으로 분리.
**칩 뒤로도 선이 지나지 않도록** 세로선을 칩 위(236→248)와 아래(290→304)로 끊었다.
실측: 라벨과 교차하는 path **0개** · 칩 내부 여백 위 8px / 아래 10px.

### 검증

`run_deck_checks.py 3 --assemble --parts 6` → **RESULT PASS**(4단계 전부 · exit 0).
`verify_deck` FAIL 0 · WARN 1(위 항목) · PASS 50.
브라우저 실측(활성 슬라이드 1280×720 · scale=1 · INVALID 없음) — 78장 기준
바닥선 초과 0 · 이탈 0 · 겹침 0 · 어절 중간 줄바꿈 0 · 빈 슬롯 0 · 폰트 하한 위반 0 · 콘솔 에러 0.
PART 2 9장 전부 `deadBottom 0`(선재 `V2-07` 74 제외 — 이번 범위 밖).
V2-06 마무리 박스: 3줄 · 중앙 정렬 · 행간 31.9px(=22×1.45) · 바닥 666(컨테이너 하한과 일치).

**부수 수정 1건**: `[data-slide="V2-06"] .w3-close`(0,2,0)의 `line-height`가 킷
`.deck .slide .callout`(0,3,0)에 져서 **조용히 무시되고 있었다**(선언 1.45 · 실제 1.6).
`.deck .slide[data-slide=…]`로 (0,4,0)를 만들어 살렸다 — shell ② 블록이 경고하는 함정이다.

---

## 2026-08-09 · 사용자 지정 수정 8건

전량 사용자가 화면을 보고 지목한 건이다. 초안 재해석이 아니라 **지시 이행**이다.

| # | 자리 | 무엇을 했나 |
|---|---|---|
| 1 | 13p `V1-06` | 백업 프롬프트를 사용자 문장으로 교체 — 상위 폴더 `VibeCoding`에 남기고 **작업은 원본에서** 계속한다는 방향을 명시 |
| 2 | 12p `V1-05` | 백업 폴더명을 `백업_2026-08-09`로 정합(13p와 날짜가 달랐다) |
| 3 | 20p `M2` | 「~를 ~를」 조사 반복 해소 |
| 4 | 22p `V2-02` · 24p `V2-04` | 요리 비유 한 줄 추가 + 좌측 칼럼 하단 여백 해소 |
| 5 | 32p `V3-06` | 「한 **명**」 → 「한 **담당**」 **4곳**(제목 · `.w3-def` · SVG 라벨 · 우측 `<h3>`) |
| 6 | `V3-06` 위치 | 실습③(`V3-04`) **앞**으로 이동 — 32p / 실습③은 33p |
| 7 | 29p `M3` | 로드맵 3·4단계 내용 스왑(6번 이동에 맞춤) + 「한 명씩」→「하나씩」 용어 통일 |
| 8 | 36p `V3-10` | 프롬프트 전문을 **사용자 원문 그대로** 교체 |

### ⚠️ 초안 규칙을 의도적으로 어긴 곳 — 1건

**`V3-10`(36p)에 사람 이름을 도입했다**(짱구 · 철수 · 훈이 · 맹구).
`3주차_초안.md`의 「용어 원칙 — 사람 이름은 쓰지 않는다」와 **정면으로 충돌한다.**

- **사용자 확정 사항이다.** 학생이 그대로 복사해 실행하는 프롬프트라 원문을 손대지 않는 쪽을 택했다.
- **범위는 이 한 장뿐이다.** 나머지 슬라이드는 전부 역할명(HTML/CSS/JS/QA 담당)을 유지한다.
- **부작용을 알고 넘긴다**: 학생이 이 프롬프트를 실행하면 만들어지는 실제 에이전트 이름(짱구·철수…)과
  다른 슬라이드의 화면 용어(HTML 담당·CSS 담당…)가 **서로 달라진다.** 강사가 말로 이어 줘야 한다.
- 같은 이유로 **`QA 엔지니어`** 표기도 덱 표준 `QA 담당`과 다르게 남겼다 — 사용자가 원문 유지로 확정했다.
  `V3-10` 안에서도 좌측은 「QA 엔지니어」, 우측·다른 장은 「담당」이라 **한 덱에 두 표기가 공존한다.**
- 이 판단의 근거는 shard `part-03.html`의 `V3-10` 주석에도 적어 뒀다(파일만 보고 「오류」로 되돌리지 않도록).

### 22 · 23 · 24p 비유 3연작 — 형태가 다른 이유

세 장에 같은 「주방」 비유를 이어 붙였는데 **붙는 자리가 다르다. 의도된 차이다.**

| 장 | 구도 | 비유가 붙는 자리 |
|---|---|---|
| 22p `V2-02` | 좌 텍스트 / 우 도해 | **좌측 칼럼 맨 아래** 스트립(`.w3-analogy` · `margin-top:auto`) |
| 23p `V2-03` | 중앙 도해 | **도해 캡션**(`.w3-stage-cap` · 2026-08-08에 하단 박스에서 하강시킨 것) |
| 24p `V2-04` | 좌 텍스트 / 우 도해 | 22p와 동일한 좌측 스트립 |

- 23p는 도해가 화면 중앙을 쓰므로 아래가 캡션 자리다. 22·24p는 우측이 도해라 아래에 캡션 자리가 없고,
  대신 **좌측 텍스트 칼럼의 빈 세로**가 문제였다. 같은 자리에 붙일 수가 없다.
- 22·24p를 **박스로 만들지 않았다.** 2026-08-08에 PART 2에서 걷어낸 하단 콜아웃을 되살리는 셈이 되고,
  MEMORY 「박스를 늘려 채우지 마라」에도 걸린다. 좌측 보더만 있는 스트립으로 끝냈다.
- **DOM만 보면 「변주 실패」로 오해할 수 있으니 이 표를 먼저 봐라.**

### 검증 — 실제 명령과 실제 출력

```
python scripts/assemble_deck.py "courses/바이브코딩/sessions/3주차/강의덱.초안"
  → parts merged 6 · slide sections 76 · [PASS] · exit 0
python scripts/run_deck_checks.py 3 --parts 6
  → [PASS] 정적 게이트 / [PASS] 내용 품질 / [PASS] 발표자 노트 / [PASS] 렌더·타이포 감사
     RESULT PASS · exit 0
python scripts/verify_notes.py … --baseline tmp/w3_notes_baseline.json
  → MISMATCH=0 OK=74/74 · REGRESSION=2 · exit 1
```

**`verify_notes` REGRESSION 2건은 의도한 변경이다 — 소실이 아니다.**
베이스라인이 `(pn-no, 제목)` 쌍을 키로 잡는데 32·33번을 **맞바꾸고 33번 제목을 고쳤으니**
양쪽 다 「없어진 슬라이드」로 잡힌다. 실제 지표는 그대로다 — **항목 총계 86 → 86**(한 문장도 안 버렸다),
덱↔노트 대조는 **MISMATCH 0 · OK 74/74**. 러너가 부르는 `verify_notes`(베이스라인 없음)는 PASS다.

브라우저 실측 — `python -m http.server 8799` · 활성 슬라이드 rect **1280×720 · `--scale`=1**(INVALID 아님).
`sessions/_verify/3주차/deck-audit.json`에 저장(76장):

- **바닥선 초과 0 · 이탈 0 · 겹침 0(abs 0) · 어절 잘림 0 · 빈 슬롯 0 · 폰트 하한 위반 0 · offenders 빈 배열.**
- 22p·24p `.w3-analogy` — `margin-top` 계산값 `auto`가 실제로 먹었고 **칼럼 바닥과 간격 0px**(맨 아래 부착 확인).
  `gap` 16px · 보더 `4px rgb(20,184,166)`(=`--mint`) · 22px · `rgb(58,61,69)`(=legibility `--gray-700`).
  ⚠️ 명시도 승격은 **필요 없었다** — `.w3-deffig-text > .w3-analogy`(0,2,0)로 충분했다.
- 36p `V3-10` — `.w3-bigprompt` 바닥 **666.0px 정확히 하한**(초과 아님) · 우측 칼럼 바닥 624.7 · overflow 0 ·
  폰트 22px **그대로**(줄이지 않았다). 좌측 6문단 / 우측 4문단.
- 32p `V3-06` SVG 「쓰기 한 담당」 — bbox x 181~279.7. 좌 원 여백 **63** · 우 원(cx=364) 여백 **62.3** ·
  박스 상단까지 10.7. **겹침 없음** → `font-size:21` 유지했다.
- 32p `V3-06` 우측 `<h3>「쓰는 건 한 담당입니다」` — 24px · **1줄 유지** · `scrollWidth-clientWidth=0`(넘침 없음).
  두 칼럼 폭 565px·바닥 534.2px로 **좌우 동일**(글자가 1자 늘어도 격자가 안 밀렸다). `.s-full` 바닥 666.
  ⚠️ 이 h3는 처음 지시 표에서 빠져 있었다 — 같은 장에 「한 명」이 남아 제목과 어긋나 있었고, 재검에서 잡아 고쳤다.
  V3-06 전수 재확인 결과 **사람 단위로 읽히는 「명」은 이제 이 장에 0건**이다(좌측 h3·하단 callout·`<p>` 포함).
  발표자 노트 pn-no 32 블록 멘트에도 「명」이 없어 손대지 않았다. pn-no 3 「"AI"는 한 명이 아닙니다」는 **사람 비유라 그대로 뒀다.**
- 페이지 번호 실측: 22 `V2-02` · 24 `V2-04` · 29 `M3` · **32 `V3-06`** · **33 `V3-04`(실습 ③)** · 36 `V3-10`.

~~**실행하지 못한 검증**: 스크린샷.~~ → 아래 「정정」에서 해소했다.

### ★ 정정 — 22p·24p는 위 측정이 통과했는데도 화면이 안 고쳐져 있었다

**「칼럼 바닥과 간격 0px」이라는 위 측정은 참이면서 아무것도 보증하지 않았다.**
브라우저 패널을 띄워 눈으로 보니 좌측 칼럼은 여전히 **457px에서 끝나고 아래 209px가 비어 있었다.**

- 원인: `.w3-deffig`의 기본이 `align-items:start`라 **텍스트 칼럼이 내용 높이로 잡힌다.**
  그러면 «칼럼 바닥 = 마지막 요소 바닥»이 항상 참이고, `margin-top:auto`는 흡수할 여유가 없어
  **계산값 `0px`로 조용히 무효**가 된다. 도해 칼럼만 132~666을 채우고 텍스트 칼럼은 132~457이었다.
- **이것이 MEMORY 「검출기의 0이 눈먼 범위 안의 0일 수 있다」의 교과서적 사례다.** 잰 값은 맞지만
  «무엇에 대한 0인가»가 틀렸다. 러너·정적 게이트·렌더 감사 **전부 PASS 상태에서** 그랬다.
- 고침: 두 장에 `align-items:stretch`를 주어 칼럼을 도해와 같은 534px로 만든 뒤,
  **본문 2문단과 비유에 `margin-top:auto`를 주어 균등 분산**했다. 제목·정의는 붙여 뒀다 —
  `justify-content:space-between`을 쓰면 제목↔정의가 71px로 벌어져 부제가 떨어져 나간다(세 안을 화면에서 비교해 골랐다).
- 결과 실측(`--scale`=1 · 슬라이드 1280×720): 두 장 모두 칼럼 **132~666**(도해와 동일).
  22p 제목 132~178 · 정의 196~229 · 본문 315~386 / 472~543 · 비유 629~666.
  24p 제목 132~178 · 정의 196~262 · 본문 350~385 / 473~509 · 비유 596~666.
  **제목 상단 132px 앵커는 유지**했다(사용자가 「대주제 위치는 괜찮다」고 확인한 값).
- **스크린샷 육안 확인 완료**(22p·24p). 탭을 fronted 상태로 만들어야 프레임이 합성된다.
- ⚠️ **덧붙여: 브라우저가 `강의덱.html`을 캐시해 재조립 결과가 반영되지 않았다.** V2-02는 새 CSS로,
  V2-04는 옛 CSS로 렌더되는 상태였고 하마터면 「선택자가 V2-04에만 안 먹는다」고 오진할 뻔했다.
  파일에는 두 규칙이 다 있었다. **재조립 후에는 `?v=N`으로 캐시를 깨고 다시 재라**(§9 기존 규칙).

**최종 게이트**: `run_deck_checks.py 3 --parts 6` → 4단계 전부 **PASS · exit 0**.
`verify_deck` **FAIL 0 · WARN 0 · PASS 50** — 이전 기록의 WARN 1건(`V2-06` `<br>` 앞 조사 종결)은
이 라운드 착수 시점에 **이미 없었다**(두 번째 `<br>`가 그전 미커밋 작업에서 빠졌다). 이 라운드가 없앤 것이 아니다.
렌더 감사 76장 — 바닥선 초과 0 · 이탈 0 · 겹침 0 · 어절 잘림 0 · 빈 슬롯 0 · 폰트 하한 위반 0.

---

## 2026-08-09 · PART 4·5 수정

### 실습 번호 — PART 4는 번호 없는 구간이 됐다

사용자 지시로 **PART 4의 실습 라벨(eyebrow)을 전부 뗐다.** 그 결과 뒤 번호를 당겨
최종 덱은 **①② (PART 1) · ③④ (PART 3) · PART 4 없음 · ⑤⑥⑦ (PART 5) · ⑧⑨⑩ (PART 6)** 이다.
**PART 4는 유일하게 번호 없는 실습 구간**이며, 이는 누락이 아니라 확정된 상태다 —
「⑤ 앞이 ④인데 PART 4에 번호가 없다」를 버그로 보고 되돌리지 마라.

### 48p(V4-11「화면 QA 6항목」) 삭제 경위

- 커리큘럼 §11 Computer Use 화면 QA의 **대체분**으로 넣었던 장이다.
- **삭제 사유는 내용이 아니라 형태다.** 「통과 / 실패 / 해당없음」 체크칸을 채우는 표라서
  참고 예시가 아니라 **반드시 채워야 하는 규정처럼 읽혔다.**
- 여섯 항목은 버리지 않고 **47p(V4-10 독립 QA 실행)에 본문 한 줄(`.w3-eg`)로 흡수**했다:
  「예를 들면 — 기존 흐름 · 새 흐름 · 다른 값 · 오류 상황 · 좁은 화면 · 글자 크기.
  **내 프로젝트에 있는 것만** 골라 봅니다.」
- ⚠️ **표·박스·체크칸으로 되돌리지 마라.** 되돌리면 삭제 사유가 그대로 되살아난다.
  어조도 「예를 들면 … 골라 봅니다」(참고 예시)를 유지한다.
- 죽은 CSS `.w3-qa6` 계열과 계약의 `w3-qaslide`·`w3-qa6` 등재는 덱 0건을 확인하고 제거했다.

### 52p(V5-02) 재편 — 4단 밴드 → 2층 구도

옛 구도(`.w3-layers` + `.l1~.l4`)를 걷어내고 `.w3-two-layer`로 바꾼 이유 세 가지:

1. **거짓 3단 중첩** — 들여쓰기가 「플러그인 > Skill > 커넥터 > MCP」라는, 실제로는 없는 계층을 그렸다.
2. **MCP가 가장 사소해 보였다** — 가장 아래·가장 좁은 밴드였는데 이 장의 핵심 개념이다.
3. **범주 혼재** — 「담는 단위(플러그인)」와 「잇는 규격(MCP)」이라는 성격이 다른 둘이 한 줄로 섞였다.

새 구도는 플러그인·MCP 두 블록을 **같은 폭·같은 패딩·같은 테두리 두께**로 두고,
플러그인이 담는 것(Skill·커넥터)만 그 안의 칩으로 내렸다.
초안의 「플러그인이 나머지를 담는다」는 두 블록 사이 bridge 한 줄
(**플러그인이 MCP까지 함께 담아 오기도 합니다**)로 살렸다.
⚠️ **두 블록의 대등함을 깨지 마라** — 그게 이 재편의 목적이다.
칩 글자를 20px로 낮추지도 마라 — Skill·커넥터 설명은 보조 캡션이 아니라 **초안 5-2행 본문 그 자체**다(22px 하한).
`is-proto`가 민트 fill이지만 글자는 `--ink`/`--mint-deep`만 쓴다(R-COLOR-02 · 흰 글자 금지).

### 57p(V5-05)로 내린 M5 사전 조건 문장

M5 리드에 있던 「계정과 저장소 연결은 **수업 전에 이미 끝나 있습니다.** 오늘은 확인만 합니다.」를
GitHub를 처음 꺼내는 57p로 내렸다(`.w3-pre`).
근거는 개념KB 필수 진술 **[C-GitHub저장소]** —
「연결·인증을 사전 조건으로 전제하며 연결 방법 자체는 다루지 않는다」의 **화면 근거**다.
지우면 그 진술이 발표자 노트에만 남는다.

### 「바깥」→「외부」 치환에서 의도적으로 제외한 2곳

둘 다 **외부 서비스가 아니라 도형의 방향·위치**를 가리키므로 「바깥」이 맞다.

| 자리 | 원문 | 왜 남겼나 |
|---|---|---|
| 15p `V1-08` | 그림자가 **바깥**으로 퍼지는 | 도형 바깥쪽 방향 |
| 51p `V5-01` aria-label | **바깥** 링 | 동심원의 바깥 링(위치) |

### 발표자 노트

- `pn-no` 48 블록(「화면 QA 6항목」)을 삭제하고 **49~76을 48~75로 1씩 당겼다**(1~47은 그대로).
- **멘트는 한 문장도 버리지 않았다.** 48블록의 시연 큐 1건
  (「휴대폰으로 열어 좁은 화면을 같이 확인시킨다 …」)을 **47p(독립 QA 실행)로 이동**했다.
  항목 총계 **86 → 86**(불변).
- 58p→57p 제목을 덱에 맞춰 `커밋(commit)은 「무엇을 올릴지 고르는 것」부터입니다`로 고쳤다.

### 검증 — 실제 명령과 실제 출력

```
python scripts/assemble_deck.py "courses/바이브코딩/sessions/3주차/강의덱.초안"
  → parts merged 6 · slide sections 75 · part-divider 6 · [PASS] · exit 0

python scripts/run_deck_checks.py 3 --parts 6
  → [PASS] 정적 게이트(verify_deck) · [PASS] 내용 품질 · [PASS] 발표자 노트
    [PASS] 렌더·타이포 감사 — 렌더 75장
    RESULT | PASS | 정적 게이트 + 렌더 증거 모두 확인 · exit 0

python scripts/verify_notes.py … --baseline tmp/w3_notes_base2.json
  → MISMATCH=0 OK=73/73 · 항목 총계 86 → 86
    REGRESSION=28은 전부 [슬라이드소실] 형태로, 48~75 재번호 때문에 baseline 키
    (pn-no+제목)가 어긋나 뜬 것이다. 항목 손실이 아니다(총계 불변·MISMATCH 0).
```

**슬라이드 76 → 75장.** 렌더 감사(1280×720 · `--scale`=1 실측 확인):
바닥선 초과 **0** · 이탈 0 · 겹침 0 · 어절 잘림 0 · 빈 슬롯 0 · **폰트 하한 위반 0**.

세로 예산이 빠듯했던 세 장 실측(슬라이드 좌표 · 바닥선 666):

| 장 | 마지막 요소 바닥 | 여유 | 비고 |
|---|---|---|---|
| 47p `V4-10` | 652 (`.w3-goal` 콜아웃) | 14 | `.w3-eg` 443~475 · 22px · **1줄** |
| 52p `V5-02` | 652 (콜아웃) | 14 | 2층 224~569 · 칩 22px · 블록 폭 둘 다 1152 |
| 57p `V5-05` | 666 (도해 칼럼) | 0 | `.w3-pre` 198~262 · 22px · 2줄 · 텍스트 칼럼 132~513 |

세 장 모두 **CSS를 추가로 조이지 않고 그대로 들어갔다**(패딩·gap 축소 불필요).
47p·52p·57p는 탭을 fronted 상태로 만들어 **스크린샷까지 육안 확인**했다.
재조립 뒤 `?v=N`으로 캐시를 깨고 쟀다(§9).

---

## 2026-08-09 (2차) · 1~76p 전면 수정

이 절은 **shard 담당분**(part-01·02·03·05·06 · `deck.contract.json` · 발표자노트)만 적는다.
`shell.html`·`part-04.html`은 메인이 같은 라운드에서 따로 처리했고 그 몫은 아래에 이어 적힌다.

### `.s-team` 훅 누락 — `PART n/N`과 진행 도트가 3주차에서만 안 나왔던 원인

스타터 JS는 `.s-head .s-team`을 **찾아서** `.s-part`로 바꿔치기한다. 3주차 본문 헤더에는
그 훅이 거의 없어(68개 중 67개 누락) `if (team)`이 거짓이 되고, **아무 일도 하지 않은 채 조용히 끝났다.**

이 유형이 정적 검증에 안 걸린 이유를 적어 둔다 — **HTML은 유효하고, CSS도 유효하고, JS도 예외를 던지지 않는다.**
빠진 것은 「있어야 할 빈 훅」이라 문법 검사·CSS 회귀·콘솔 에러 어디에도 흔적이 없다.
잡히는 자리는 **렌더 후 화면 측정**뿐이다. 앞으로 스타터가 「찾아서 바꿔치기하는」 훅은
「없으면 조용히 통과」가 기본 동작이라고 보고, **훅 개수 = 본문 섹션 수**를 셈으로 확인한다.

복원 결과 — 각 파일에서 «치환 수 = 본문 섹션 수»(전체 섹션 − part-divider 1)로 대조했다:

| 파일 | `<section class="slide` | part-divider | 본문 섹션 | `.s-team` |
|---|---|---|---|---|
| part-01 | 13 | 1 (P1) | 12 | **12** ✓ |
| part-02 | 9 | 1 (P2) | 8 | **8** ✓ |
| part-03 | 9 | 1 (P3) | 8 | **8** ✓ |
| part-05 | 15 | 1 (P5) | 14 | **14** ✓ |
| part-06 | 12 | 1 (P6) | 11 | **11** ✓ |

part-divider(P1~P6)에는 헤더 자체가 없다 — 파트 전환은 헤더 전체 제거가 규약이라 **넣지 않았다.**

### 완성 기준 삭제 — 담당분 10곳

사용자 지시로 덱 전체 14장에서 「완성 기준」을 없앴다. 지운 자리마다
`<!-- 2026-08-09 사용자 지시로 완성 기준 삭제 -->` 주석을 남겨 되살리지 못하게 했다.

| 파일 | 슬라이드 | 지운 요소 |
|---|---|---|
| part-01 | `V1-06` · `V1-09` | `<p class="w3-crit">` (제목 블록 안 한 줄) |
| part-03 | `V3-04` · `V3-08` | `<div class="callout blue hug-center w3-goal">` |
| part-05 | `V5-07` · `V5-09` · `V5-10` | 같은 `w3-goal` |
| part-06 | `V6-04` · `V6-07` · `V6-09` | 같은 `w3-goal` |

나머지 4곳(part-04)은 메인 담당이다. `.w3-crit`/`.w3-crit-k`/`.w3-goal` **CSS는 shell에 있어 손대지 않았다.**

### 리드 교체 2건 — 진행 메타와 칸 번호 참조 제거

- **`M2`** — 「이번 파트는 개념만 합니다. 실습은 PART 3부터입니다.」 → 「같은 일을 맡기는 방식이 **셋** 있습니다 — 언제 나누는 게 이득인지까지 봅니다.」
  옛 문장은 **수강생에게 필요 없는 진행 메타**(무엇만 한다/실습은 언제부터)였다. 로드맵 리드 자리는 그 파트를 한 문장으로 요약하는 자리라 그렇게 바꿨다.
- **`M6`** — 「②와 ③은 다른 단계입니다.」 → 「**만들었다고 열리는 것이 아닙니다** — 주소를 만들고, 열어 보고, 고쳐서 다시 내놓습니다.」
  「②와 ③」은 **로드맵 칸 번호를 가리키는 메타 표현**이라 화면만 보는 수강생에게 뜻이 통하지 않는다. 같은 내용을 칸 번호 없이 말했다.

### `V2-06` 도해 라벨 삭제 — 화면 문구만, aria-label은 유지

SVG 안 `<text x="946" y="150">담당끼리는 연결선이 없습니다</text>`를 지웠다.
바로 위 `.w3-def`가 이미 「옆 담당이 뭘 하는지 모릅니다」라고 같은 말을 하고 있어 **한 화면에서 두 번 말하는 중복**이었다.

`figure`의 `aria-label`에 있는 「담당끼리는 연결선이 없다」는 **그대로 뒀다** — 그것은 화면 문구가 아니라
**도해를 볼 수 없는 사람에게 도해의 구조를 서술하는 문장**이다. 화면에서 중복이라고 접근성 서술까지 지우면
스크린리더 사용자에게는 「연결선이 없다」는 정보 자체가 사라진다.

지운 뒤 좌표를 확인했다: 남은 요소는 허브(x 360~640 · y 18~94)와 담당 4상자(y 192~278)뿐이고,
지운 라벨이 있던 y=150 띠에는 허브→담당 연결선만 지난다. **겹치는 요소도, 비어 보이는 구멍도 없어 다른 요소의 y는 건드리지 않았다**(선·노드 배치 불변).

### `V5-03` Server 부제 — 「GitHub 같은 외부 서비스」 → 「지도·달력 서비스」

이 시점은 **54p**이고 GitHub는 **56p(`V5-05`)가 첫 등장**이다. 아직 안 배운 제품명이 도해에 먼저 나오고 있었다.
브랜드명 대신 일반명사로 두어 **강의 범위 밖 제품을 화면에 올리지 않는다**는 과목 지침(§2 범위)에 맞췄다.

글자 폭 실측 — 상자 `x=578 width=196`(내부 578~774), 텍스트 `text-anchor="middle" x=676 font-size=19`:

- 한글 8자(지·도·달·력·서·비·스 + …) × 19px = **152px**
- 가운뎃점 `·` ≈ 9.5px + 공백 1개 ≈ 5px
- 합계 **≈ 166px** → 중심 676 기준 **593~759**, 상자 내부 196px 안에 좌우 15px씩 여유. **줄이지 않고 그대로 들어간다.**

### `P6` 제목 — 「공개와 공유 범위」 → 「배포와 공유 범위」

부제도 「세상에 내놓습니다.」 → 「주소를 만들고, 열리는지 확인하고, 세상에 내놓습니다.」로 늘렸다.

`.pd-sub`가 2줄이 되면 바닥선 666을 넘는 알려진 결함(MEMORY)이 있어 **먼저 폭을 계산했다** — 킷 실측치는
`.pd-title{font-size:56px; max-width:1080px}` · `.pd-sub{font-size:21px; width:920px}`(`kit/styles/deck.css` §14):

| 요소 | 내용 | 계산 | 상자 | 판정 |
|---|---|---|---|---|
| `.pd-title` | 한글 7자 + 공백 1 | 7×56 ≈ **392px** | 1080px | 1줄 |
| `.pd-sub` | 한글 22자 + 쉼표 2 · 마침표 1 · 공백 5 | 22×21=462 + 약 59 ≈ **521px** | 920px | 1줄 |

⚠️ **덱 안에 두 용어가 공존한다.** 사용자가 지시한 것은 **P6 제목뿐**이고, 본문 슬라이드는 여전히 「공개」를 쓴다
(`V6-02` "공개하면 되돌리기 어렵습니다" · `V6-05` "공개가 안 되면" · `V6-07` "공개 URL 자가 점검" · `V6-09` "한 건 고치고 다시 공개" · `M6` 로드맵 3·4단계).
지시 범위를 넘겨 본문까지 일괄 치환하지 않았다 — **용어 통일 여부는 사용자 결정 사항으로 남긴다.**

### `deck.contract.json`

- `decks.강의덱.intro`(S01~S05) · `dividers`(6) **변화 없음**.
- `_notes.slide_id_prefix`에 마무리가 두 장이 된 것을 반영했다 — **END(회고) + THX(THANK YOU)**. 도입 표기도 실제와 맞춰 `S01~S03` → `S01~S05`로 고쳤다(`intro` 배열은 이미 5개였다).
- `layout_families`에 **`"w3-thx": "centered"`** 를 추가했다. 신규 구도를 등재하지 않으면 verify_deck이 「같은 구도 연속」을 **오탐**한다.

### 발표자노트

- `pn-no` 63 블록의 제목을 **「배포와 공유 범위」**로 덱과 맞췄다(`verify_notes`가 「pn-no + 제목」으로 대조한다).
- **이번 라운드에서 바뀐 다른 제목은 없다** — 리드·부제·도해 라벨만 바뀌었다. 전수 확인 후 나머지는 손대지 않았다.
- 새 THANK YOU 장(76p)은 **멘트가 없어 노트 블록을 만들지 않았다.**

### 못 한 것 — `V3-04`·`V3-08` 프롬프트 하강은 CSS가 필요하다

`V3-04`·`V3-08`은 완성 기준 띠가 빠지면서 `.w3-prompt`(인라인 다크 박스) 아래가 빈다.
프롬프트를 그 자리로 내려야 하는데, **`.w3-prompt`와 `.s-full.fill`의 세로 분배는 전부 shell CSS에 있다.**
마크업만으로는 안 되고, 인라인 `style`은 금지라 **손대지 않고 남겼다.**

**→ 메인이 shell에서 다음을 걸어야 한다:**

```css
[data-slide="V3-04"] .w3-prompt,
[data-slide="V3-08"] .w3-prompt{ margin-top:auto; }
```

(`.s-full.fill`이 flex column인 전제다. 아니면 대신 프롬프트 위 요소에 `margin-bottom:auto`를 준다.)

**→ 적용 완료.** 위 CSS를 그대로 걸었다. 실측: 33p·35p 프롬프트가 **572~666**(하단 부착).

---

### 메인 작업분 — `shell.html` · `part-04.html`

| 곳 | 한 일 |
|---|---|
| 1p `S01` | 터미널 문구 → 「담당을 나눠 검토하게 하고, **배포까지 진행합니다**」 |
| 5p `S05` | 목차 6번을 P6 제목과 맞춰 「배포와 공유 범위」로 (둘은 미러링 쌍이라 한쪽만 두면 어긋난다) |
| 6p `P1` | 제목 → 「지금을 지키고, 화면에 움직임을 더합니다」 |
| 38p `M4` | 리드 → 「요청 → 검토 → 판정 → 구현 → 확인. 순서는 그대로고, 검토하는 담당만 여럿입니다.」 |
| 39p `V4-02` | 하단 콜아웃 삭제 |
| 40·44·45·47p | 완성 기준 삭제(part-04 4곳) |
| 42p `V4-05` | 도해 재작성 + 칩 대비 수정 |
| 46p `V4-09` | 「둘 다 봅니다」 → 상자별 오른쪽 라벨 2개 |
| 75p `END` | 「팀명/부제」 자리표시자 삭제 · 하단 THANK YOU 문구 삭제 |
| 76p `THX` | **신설** — 킷 다크 `.closing` |

### ★ `PART n/N`이 3주차에만 안 나온 이유 (규명)

스타터 JS는 `.s-head .s-team`을 **찾아서** `.s-part`로 바꿔치기한다. 3주차 본문 헤더 68개 중
**67개에 그 훅이 없어** `if (team)`이 거짓이 되고 **아무 일도 일어나지 않았다.**
1주차 60/61 · 2주차 102/104 · 킷 스타터도 이 훅을 갖고 있다 — 3주차 조각만 빠뜨린 것이다.

- **정적 검증에 안 걸린다.** 없는 것을 못 찾은 것뿐이라 오류가 아니다. 브라우저에서 「우측 상단이 비어 있다」로만 드러난다.
- 훅 복원 후 실측: `.s-part` **63장 주입**(= 76 − 도입 5 − divider 6 − END − THX). 남은 `.s-team` 1개는 표지의 `WEEK 03 / 03`으로 의도된 것이다.
- **재발 방지**: 새 주차 조각을 만들 때 헤더에서 `.s-team`을 지우지 마라. 팀명 텍스트는 비워도 되지만 **빈 `<div class="s-team"></div>`는 남겨야** JS가 채운다.

### 22·24·52p — 앞 라운드 처방이 틀렸던 것을 정정

2026-08-09 1차에 「gap을 12→16px로 벌린다」로 처리했는데 **오답이었다.** 문장 사이를 균등하게
벌리자 붙어 있어야 할 정의·본문까지 갈라졌다(사용자 지적: 「간격만 넓히면 다야? 붙일 건 붙여야지」).

**올바른 처방**: gap은 원래대로 조이고, `.w3-def`와 **마지막 요소** 두 곳에만 `margin-top:auto`를 건다.
auto가 남는 세로를 균등 분배해 「제목 / 빈칸 / **정의+본문 한 덩어리** / 빈칸 / 마지막 줄」이 된다.
실측(V2-02): 제목 132~178 · 정의 305~338 · 본문 끝 504 · 비유 629~666.

### 42p 도해 — 무엇을 고쳤나

옛 판은 양쪽 다 「점선 구역 2개」라 **골격이 거의 같고** 차이가 작은 배지 하나에만 있었다. 셋을 바꿨다:
① 골격을 다르게(왼쪽=맨 위 큰 블록+기록 3줄 / 오른쪽=제목 줄 안 배지+기록 4줄)
② 다른 자리에 민트 「여기」 표식을 직접 찍음 ③ 시안마다 「오늘 5개를 어디에 놓았나」 한 줄.

- **「HTML 담당」이 안 보이던 것은 명시도 버그였다.** `.w3-own-chip`이 `color:var(--own-on)`(흰 글자)를
  선언하는데 `.w3-shot-head b`(0,1,1)가 `.w3-own-chip`(0,1,0)을 이겨 `--ink`로 덮여 **파랑 위 파랑**이었다.
  `b.w3-own-chip`(0,2,0)으로 올려 해소. 실측 `rgb(255,255,255)` on `rgb(29,78,216)`.
- ⚠️ **캡션을 더하자 겹침 2건 + 잘림 2건이 났고 렌더 감사가 잡았다.** 원인은 격자 행 기본값 `auto`(=min-content)라
  부모에 `flex:1 1 auto;min-height:0`이 있어도 행이 안 줄어든 것. `grid-template-rows:minmax(0,1fr)` +
  `.w3-shot{min-height:0}`으로 고쳤고, 목업이 필요 293 / 가용 252로 41px 모자란 것은
  **폰트가 아니라 내부 여백**을 조여 맞췄다(최종 필요 248 / 가용 252).

### 76p 신설에 딸려 온 계약 변경

`verify_deck`의 `closing_text`는 **마지막 슬라이드에서만** 문자열을 찾는다. 마지막 장이 END → THX로
바뀌면서 옛 값 「검증된 것만 완료로 봅니다」가 FAIL로 떴고, 계약을 `THANK YOU`로 옮겼다.
⚠️ **그 문장이 삭제된 것이 아니다** — END(75p) 하단에 그대로 있다. 검사 대상 장이 바뀐 것뿐이다.

- THANK YOU 96px > 표지 히어로 74px — 덱에서 가장 큰 볼드 타이포그래피라는 규칙을 지켰다(실측 확인).
- 배경 `rgb(16,30,82)`(=`--cover-bg`). 킷 `.closing`은 페이지 번호 주입 대상에서 제외돼 번호가 안 붙는다.

### 검증 — 실제 명령과 실제 출력

```
python scripts/assemble_deck.py … → slide sections 76 · divider 6 · [PASS] · exit 0
python scripts/run_deck_checks.py 3 --parts 6 → RESULT PASS · exit 0
  [PASS] 정적 게이트   FAIL 0 · WARN 0 · PASS 50
  [PASS] 내용 품질     FAIL 0 · WARN 2 · PASS 9
  [PASS] 발표자 노트   MISMATCH 0
  [PASS] 렌더·타이포   76장
```

브라우저 실측(활성 슬라이드 1280×720 · `--scale`=1 · `?v=N`으로 캐시 무효화):
**바닥선 초과 0 · 이탈 0 · 겹침 0 · 어절 잘림 0 · 오버플로 0 · 빈 슬롯 0 · 폰트 하한 위반 0 · offenders 빈 배열.**

**실행하지 못한 검증**: 브라우저 패널 폭이 402px에서 더 넓어지지 않아(`resize_window`가 반영되지 않음)
**픽셀 단위 육안 확인은 못 했다.** 대신 요소별로 `getBoundingClientRect`·`getComputedStyle`·텍스트를
직접 읽어 확인했다 — 42p 칩 색·「여기」 표식 2개·캡션 2줄, 46p 라벨 3개(「둘 다 봅니다」 소멸),
33·35p 프롬프트 572~666, 63p 부제 1줄(594~626), 75p 팀명 없음, 76p 96px·다크 배경.

### 남긴 것 (사용자 판단 사항)

- **「공개」와 「배포」가 덱 안에 공존한다.** 지시가 P6 제목 한정이었다. 본문은 여전히 「공개」다 —
  `V6-02`·`V6-05`·`V6-07`·`V6-09`·`M6` 로드맵 3·4단계. 목차(S05)만 제목과 짝이라 함께 바꿨다.
  `3주차_초안.md`·`강사대본`·`자료/*.md`에도 옛 표기가 남아 있다.
- **54p 「지도·달력 서비스」는 강의 범위 밖 도구다.** 브랜드명이 아닌 일반명사라 특정 제품을 화면에
  올리지는 않지만, 슬라이드 지침 §2의 「범위 밖 도구를 예시로 등장시키지 않는다」와는 경계에 있다.
- **배포본·발표본이 낡았다.** `강의덱_배포.html`·`강의덱_발표.html`은 76장 이전 상태다.

---

## 2026-08-09 (3차) · 실습 ④ 뒤 「생긴 파일」 3장 신설

> 이 절은 **발표자 노트 쪽 작업만** 적는다(덱 조립·검증은 별도로 이어 적는다).

### 노트 재정합 — `pn-no` 37↑ → +3

`part-03.html` 끝(옛 36p `V3-10` 뒤, `P4` 앞)에 3장이 들어가 덱이 76 → **79장**이 됐다.
`verify_notes`는 「`pn-no` + 제목」으로 덱을 가리키므로, 옛 37p 이후를 가리키던 노트가 전부 한 칸씩 밀린다.

- **`pn-no` 37~74 블록 38개를 40~77로 이동**(큰 번호부터 내려오며 치환 — 작은 번호부터 올리면 방금 바꾼 것을 다시 잡는다). 1~36은 손대지 않았다.
- **신설 블록 3개**를 `pn-no` 36 뒤에 삽입: 37 `V3-11` 실습 ④ 뒤, 폴더에 생긴 것 / 38 `V3-12` 새로 생긴 것들이 하는 일 / 39 `V3-13` TOML과 `.codex`, 두 가지만.
- 블록 **73 → 76**, `pn-item` **86 → 93**(신설 7건: 👀 2 · 💬 5). 기존 항목은 하나도 지우거나 옮기지 않았다.
- 라벨은 파일이 실제로 쓰는 세 문자열(`👀 시연·관찰 큐` · `💬 농담·애드리브` · `🗣 학생에게 줄 프롬프트`)만 쓰고, 질문 대응은 기존 `[안심 비트]` 표기 관행을 따라 본문 앞 `[질문 대응]` 굵은 머리말로 넣었다.

### 새 노트에 넣은 「강의 전 확인」 지시와 그 사유

37p 첫 항목을 **[⚠️ 강의 전 확인]** 으로 열었다 — 실습 ④ 프롬프트를 강의 전에 한 번 실행해 `.codex` 폴더 구조와 파일 이름을 **실제로 확인**하고, 다르면 이 장의 트리를 고치거나 슬라이드 대신 강사 화면을 띄우라는 지시다.

⚠️ **사유**: 초안이 「Custom Agent TOML 실습」을 뺀 이유가 «실제 유효 구조 HANDS-ON VALIDATION REQUIRED 미해소»였다. 그런데 **이번 3장은 그 경로·파일명을 화면에 확정 표기했다.** 검증되지 않은 값을 화면에 올린 상태이므로, 강사의 사전 실행이 유일한 안전장치다. 이 지시를 지우려면 먼저 실제 실행으로 구조를 확정해야 한다.

### 화면에 올리지 않고 노트로만 내린 3가지

| 내용 | 어디에 | 화면에 안 올린 이유 |
|---|---|---|
| 서브에이전트 실행 조건(사용자가 나눠 달라고 요청해야 켜진다) | 39p 💬 | PART 4가 병렬 검토를 본격적으로 다룬다 |
| QA를 읽기 전용으로 두는 이유(검사의 독립성) | 39p 💬 | 〃 |
| 역할 분리 ≠ 병렬 처리 | 38p 💬 | 〃 |

셋 다 **질문이 나오면 답할 것**이지, 화면에 미리 올릴 것이 아니다. 여기서 펼치면 PART 4가 김빠진다. 38p 항목에는 「길게 끌지 않는다」를 노트에 명시해 뒀다.

### ⚠️ 참고 자료의 정정 사항 — 되돌리지 말 것

첨부 문서 1은 **1번 답에서 「AGENTS.md·Skill 규칙이 있으면 알아서 켜진다」고 썼다가, 2번 답에서 스스로 정정했다**(「Codex는 서브에이전트를 자동 생성하지 않는다」).

**노트에 넣은 것은 정정된 2번 쪽**이다 — 「사용자가 나눠 달라고(병렬·위임) 요청했을 때 켜지고, 몇 명으로 어떻게 나눌지는 그다음에 Codex가 판단한다」. 다음 작업자가 같은 문서의 1번 답을 보고 「자동으로 켜진다」로 되돌리지 않도록 여기 남긴다. 노트에는 제품 동작이라 버전에 따라 바뀔 수 있다는 단서도 함께 달았다.

### 검증 — 실제 명령과 실제 결과

`verify_notes.py`의 파서(`SlideParser`·`NotesParser`·`front_match`·`count_items_per_slide`)를 그대로 불러 덱↔노트를 대조했다(`tmp/_pn_check.py`):

```
deck slides 79 | note blocks 76
notes range 2 .. 77
slides without notes: [1, 78, 79]  (표지 · END · THANK YOU)
pn-item total 93 | blocks 76
RESULT PASS (mismatches: 0)
```

번호 부분집합 · 단조증가 · 제목 전방일치 **불일치 0건**. 노트 없는 3장은 재정합 전과 같은 성격(표지·마무리 2장)이라 커버리지 손실이 아니다.

**실행하지 않은 검증**: `assemble_deck.py`·`run_deck_checks.py`는 이번 작업에서 돌리지 않았다 — 조립·전체 게이트는 별도 담당이다. 브라우저 렌더 감사도 이 작업 범위 밖이다.

## 2026-08-09 (4차) · PART 5 보강 + 지정 수정 8건

> 이 절은 **`part-01`·`part-04`·`part-06`·`deck.contract.json`·발표자 노트**만 적는다.
> 같은 라운드의 `shell.html`·`part-05.html`(PART 5 신설 2장 · 목차 · `END`)은 별도 담당이 적는다.

### `V4-06` 배너 교체 — 전제만 있고 결론이 없었다

| | 문장 |
|---|---|
| before | "셋 중 둘이 그러니까"는 근거가 아닙니다. **같은 자료를 봤으면 같은 답이 나오는 게 당연합니다.** |
| after | "셋 중 둘이 그러니까"는 근거가 아닙니다. **같은 AI가 비슷한 것을 보면 같이 틀리기도 합니다 — 겹친다고 맞는 게 아닙니다.** |

옛 문장의 결함은 둘이다.

1. **전제만 말하고 결론을 빠뜨렸다.** 「같은 답이 나오는 게 당연하다」까지만 말하고 「그러니 겹쳐도 맞는 게 아니다」를 안 했다. 수강생이 「그래서 뭐?」에서 끊긴다(사용자 지적: 이해가 안 된다).
2. **바로 앞 장과 정면으로 모순됐다.** `V4-04`는 「세 담당은 **서로 다른 것을** 봅니다」다. 두 장 건너 「같은 자료를 봤으면」이라고 전제하면 앞 장을 부정한다.

개념KB `C-결과충돌판정`의 필수 진술 ①은 「다수결이 무너지는 조건 = **상관된 오류**」이고, 이 배너는 같은 장 4카드 중 **「확인 불가」가 존재하는 이유**를 대는 자리다. 새 문장은 상관된 오류(같은 AI · 비슷한 입력 → 같이 틀림)를 직접 말해 그 역할을 되찾았다.

렌더 영향 없음 — `.w3-nomajority` 높이 **55px → 55px**(1줄 유지, 실측).

### `V4-09` — 제목만 바꾸고 용어는 남긴 이유

`독립 QA와 회귀` → **`고친 사람은 자기 실수를 못 봅니다`**.

⚠️ 아래 `.w3-def` 두 줄(**독립 QA** — 구현에 참여하지 않은 담당이 하는 확인 / **회귀** — 잘 되던 기존 기능이 다시 깨지는 것)은 **그대로 뒀다.** 초안 「용어 원칙」이 둘 다 화면 표준 용어로 지정했으므로 지우면 용어가 덱에서 사라진다. **제목이 뜻을 말하고 용어는 아래에서 소개하는 것이 이 덱의 기존 방식**이다(`V5-12` MCP · `V5-13` 프로토콜과 같은 형태) — 제목을 쉬운 말로 바꾸는 것과 용어를 없애는 것은 다른 일이다.

세로 증가 없음 — 새 제목 텍스트 폭 **477.2px < 컬럼 520px**이라 1줄 유지, 높이 46.4px 그대로(폰트 38px 미변경).

### `V6-10` 도해 겹침 — 실측 좌표 before/after

`viewBox 0 0 1000 300` 사용자 좌표계 · `getBBox()` 실측(뷰포트 축척과 무관).

| 요소 | before `y` | before bbox | after `y` | after bbox |
|---|---:|---|---:|---|
| 1주차 (년도) | 118 | 98.0~123.2 | **114** | 94.0~119.2 |
| 요청하는 사람 | 156 | 133.4~162.1 | **152** | 129.4~158.1 |
| 2주차 (년도) | 118 | 98.0~123.2 | **114** | 94.0~119.2 |
| 기획하는 사람 | 156 | 133.4~162.1 | **152** | 129.4~158.1 |
| 3주차 · 오늘 | 112 | 92.0~117.2 | **76** | 56.0~81.2 |
| 지휘하고 · 판정하고 | 150 | 127.4~156.1 | **114** | 91.4~120.1 |
| 공개를 결정하는 사람 | 180 | **157.4~186.1** | **152** | 129.4~158.1 |

- **결함**: 민트 원 상단이 `cy 200 − r 20 = 180`인데 「공개를 결정하는 사람」 bbox 하단이 **186.1** — **6.1px 파고들어 글자와 원이 맞닿았다.** 같은 그림의 1주차는 원 상단 184 · 글자 하단 162.1로 21.9px가 떠 있어 세 기둥의 여백이 제각각이기도 했다.
- **교정**: 세 기둥의 **역할명 마지막 줄을 전부 `y=152`로 통일**(bbox 129.4~158.1 · 셋이 동일). 3주차만 3줄이라 한 줄씩 위로 올렸고 줄간격은 1·2주차와 같은 **38**로 맞췄다(76 → 114 → 152).
- **결과**: 민트 원까지 baseline 기준 **28px** · bbox 기준 **21.9px** 확보 — 1주차 원래 여백과 정확히 같다. 자동 대조 결과 **텍스트↔원 겹침 0 · 같은 열 텍스트끼리 겹침 0**.
- 폰트 크기 · 아래 캡션(`y=252`) · 원(`cy=200`)은 건드리지 않았다. SVG 상단 여유도 남는다(최상단 bbox 56.0 > 0).

### `M6`만 「배포」로 바꿔 용어가 갈렸다 — 알고 남긴 상태

로드맵 `M6` 1단계 `공개 전 마지막 점검` → **`배포 전 마지막 점검`**(사용자 지정).

⚠️ **본문은 여전히 「공개」다.** `V6-02`(공개하면 되돌리기 어렵습니다) · `V6-05`(공개가 안 되면) · `V6-07` · `V6-09`는 손대지 않았다 — 전면 통일은 **사용자 미결**이라 지시 범위를 넘긴다. 따라서 지금 덱은 **로드맵은 「배포」, 본문은 「공개」로 용어가 갈린 상태**이며, 이것은 실수가 아니라 결정 대기 상태다. 통일 결정이 나오면 `V6-02`·`V6-05`·`V6-07`·`V6-09`와 `P6` 간지(「배포와 공유 범위」)까지 한 번에 본다.

### PART 1 이름 변경 — 하네스는 복습 한 장인데 제목이 주인공이었다

| | 제목 | 부제 |
|---|---|---|
| before | 시작 상태 보호와 하네스 | 손대기 전에, 지금을 지킵니다. |
| after | **백업과 첫 움직임** | **손대기 전에 지키고, 화면에 생기를 더합니다.** |

이 파트의 핵심은 **「백업」과 「첫 동적 화면」**이고 **하네스는 복습 한 장뿐**인데, 옛 제목이 하네스를 파트의 주인공으로 올려놨다(사용자 지적). 부제도 백업만 말해 뒤쪽 절반을 예고하지 못했다. 다른 간지가 전부 명사구(「담당 나누기와 팀 규칙」·「배포와 공유 범위」)라 결도 맞췄다.

실측: `.pd-title` 1줄(높이 66.1px) · `.pd-sub` **텍스트 폭 373.1px < 박스 920px**로 1줄(22px · 높이 31.9px). 넘침 없음.

### `V6-02` eyebrow 삭제 — 덱에서 「몇 번째」 표기가 0이 됐다

`<p class="s-eyebrow">오늘 세 번째 안전 점검</p>` 줄째 삭제. PART 5의 「오늘 두 번째」도 같은 라운드에 빠져 **덱 화면에서 회차를 세는 표기가 0건**이 된다. 회차 번호는 제작 쪽 메타이지 수강생에게 정보를 주지 않는다 — 되살리지 마라.

렌더 영향: 콘텐츠가 위로 당겨질 뿐이고 바닥선 초과·빈 칸 없음(브라우저 확인). ⚠️ **발표자 노트 본문의 「오늘 세 번 하는 점검의 첫 번째」·「오늘 두 번째 점검」·「PART 1·5에서 두 번 했다」는 그대로 뒀다** — 강사 멘트는 이번 지시 범위 밖이고, 「멘트 본문은 한 문장도 버리지 마라」가 함께 온 제약이었다. 화면에서 회차 표기를 완전히 없애기로 하면 노트 3곳도 함께 봐야 한다.

### `deck.contract.json` — 신설 구도 5개 등재

`layout_families`에 `w3-plugslide`:grid · `w3-plugmap`:containment · `w3-plug3`:grid · `w3-repo2slide`:compare2 · `w3-repo2`:compare2 를 추가(69 → **74**개). `_notes.layout_families`에 「2026-08-09(4차): PART 5 신설 2장분」 사유를 한 줄 남겼다. 등재하지 않으면 `verify_deck`이 미등록 구도를 「같은 구도 연속」으로 오탐한다. JSON 파싱 재검증 통과.

### 발표자 노트 재정합 — 79 → 81장

⚠️ **지시서의 「`pn-no` 59 이상 전부 +2」는 옛 59p 한 블록에만 어긋난다.** 신설 위치가 「옛 59p **앞**(`V5-14`)과 **뒤**(`V5-15`)」이므로 옛 59p 앞에는 삽입이 **한 장**뿐이다. 함께 온 최종 배치(`58p V5-04 · 59p V5-14 · 60p V5-05 · 61p V5-15 · 62p V5-06`)가 정본이라 보고 그쪽을 적용했다.

조립 전 `강의덱.html` 79장 실측으로 확인한 옛 번호: **58 `V5-04` / 59 `V5-05` / 60 `V5-06` / 61 `V5-07`**.

| 옛 `pn-no` | 새 `pn-no` |
|---|---|
| 2 ~ 58 | 그대로 |
| 59 (`V5-05`) | **60** (+1) |
| 60 ~ 77 | **62 ~ 79** (+2) |

- **신설 2블록**: 59 `V5-14`(GitHub도 붙여 쓰는 도구입니다 — 👀 1 · 💬 1) / 61 `V5-15`(저장소는 두 곳에 있습니다 — 💬 2, 둘째는 `[질문 대응]` 머리말).
- 라벨은 파일이 실제로 쓰는 세 문자열(`👀 시연·관찰 큐` · `💬 농담·애드리브` · `🗣 학생에게 줄 프롬프트`)만 썼다.
- **제목 동기화**: `P1` → 백업과 첫 움직임 / `V4-09` → 고친 사람은 자기 실수를 못 봅니다. **`END`는 노트 블록이 없어 바꿀 것이 없었다**(원래도 표지·`END`·`THX` 3장은 노트 미보유).
- 블록 **76 → 78** · `pn-item` **93 → 97**(👀 32→33 · 💬 50→53 · 🗣 11→11). 기존 멘트 본문 전량을 문자열 대조해 **소실 0건** 확인. 줄끝 CRLF 유지(917 → 945쌍).
- 새 번호 검증: 단조증가 · 중복 0 · 범위 2~79 · 미보유 `[1, 80, 81]`(표지 · `END` · `THANK YOU`) — 재정합 전과 같은 성격이라 커버리지 손실이 아니다.

### 검증 — 실행한 것과 실행하지 않은 것

**실행함** — 로컬 HTTP(`127.0.0.1:8931`)로 조립본을 띄워 1280×720에서 `getBoundingClientRect()`·`getBBox()` 실측(rect가 720 캔버스 좌표로 나오는 것을 `.s-pageno` bottom 698로 확인). `V6-10` 겹침 0 · `V4-09` 제목 1줄 · `P1` 부제 1줄 · `V4-06` 배너 높이 불변 · `V6-02` eyebrow 제거 후 레이아웃 정상. `deck.contract.json` JSON 파싱. 노트 번호·항목·본문 소실 자체 대조.

**실행하지 않음** — `assemble_deck.py`·`run_deck_checks.py`·`verify_notes.py`. 조립·전체 게이트는 별도 담당이고, **재조립 전에는 노트(81장 기준)와 덱(79장 기준)이 필연적으로 불일치**하므로 `verify_notes`는 재조립 뒤에 돌려야 의미가 있다. 위 실측도 **재조립 전 조립본 + JS로 새 값을 넣어 잰 것**이라, 조립 후 같은 자리에서 한 번 더 확인해야 한다.
