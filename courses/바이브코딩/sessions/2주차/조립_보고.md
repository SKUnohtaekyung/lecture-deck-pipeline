# 2주차 덱 조립 보고 — 수정요청서 2차 실행 (2026-08-01)

- **정본**: `2주차_슬라이드수정요청_2차.md` (12~77p)
- **결과**: 97장 → **98장** (삭제 4 · 신설 5 · 이동 1). `deck.contract.json` 갱신 완료.
- **실행 방식**: solo(하네스 미사용). 파트별 순차 편집 후 매 단계 브라우저 실측.

---

## 1. 실측 결과 (브라우저 전수 · `--scale=1` 강제 · 98장)

| 항목 | 값 |
| --- | --- |
| 바닥선(666px) 초과 | **0** |
| 슬라이드 이탈(y>720) | **0** |
| 좌우 안전영역 이탈 | **0** |
| 세로 무너짐(폭<36px 텍스트) | **0** |
| `—`로 완결 문장 잇는 구조(§0-1) | **0** (착수 시 26건) |
| 실습 프롬프트 블록 | **14개** |
| 덱 내 앱 UI·에디터 UI | **6개** |

정적 게이트: `verify_deck` **FAIL 0** · `verify_deck_quality` **FAIL 0** · unittest **77 OK**.

---

## 2. 실행 중 발견해 고친 결함 (수정요청서에 없던 것)

1. **`assemble_deck`이 CSS 주석 안 마크업 예시를 슬라이드로 셌다.**
   shell의 P0 주석에 쓴 `<section … data-slide="MN">` 예시가 유령 슬라이드 1장으로 잡혔다.
   → 주석 안 태그를 전부 비태그 표기로 바꿨다. 재발 방지 주석을 같은 자리에 남겼다.
2. **`.w2-ed-code > li`의 익명 격자 아이템 사고**(MEMORY의 R-QC-19 유형).
   마크다운 에디터에서 `<span>` 뒤의 맨 텍스트가 별도 격자 아이템이 되어 4줄이 133~207px로
   부풀고 슬라이드를 **1118px까지** 넘겼다. → 각 줄을 `<span class="ln">`으로 감쌌다.
3. **`.sub` 클래스명 충돌.** kit이 이미 `.cmp .sub{font-size:13px}`를 쓰고 있어
   §0-1 대체 줄을 `.sub`로 만들면 그 컴포넌트가 조용히 깨진다 → `.subline`으로 명명.
4. **R-QC-08 검출기가 새 시각자료를 못 봤다.**
   `viz-*` 클래스·`<canvas>`·`<img>`만 세는데, 이번에 만든 앱 UI·에디터·화면 흐름은
   HTML/CSS라 0으로 집계됐다. **경보를 해석으로 끄지 않고** 실제 시각자료 14곳에
   `viz-*` 클래스를 붙여 지표가 실제를 반영하게 했다(30.8% → 42.3%).

---

## 3. 남은 WARN (FAIL 아님 · 사람 판단 필요)

| 게이트 | 값 | 판단 |
| --- | --- | --- |
| R-QC-08 시각자료 비율 | 42.3% (임계 55%) | 착수 시 30.8%에서 개선. 임계 미달은 사실이며, 남은 설명 슬라이드에 도해를 더 넣어야 도달한다. **해소되지 않았음을 명시한다.** |
| R-QC-16 박스 4개 이상 | 19.2% (임계 10%) | 10장 중 **6장이 이번 범위 밖**(S02B·C4-15·C6-N2·A3F3 등 기존 장). 범위 내 신설 2장은 5개 → 4개로 줄였다. |
| R-QC-15 하단 콜아웃 | 23.1% (임계 12%) | §0-1 대체 규칙이 «부연을 별도 줄로»라 콜아웃이 늘었다. 규칙과 지표가 서로 당긴다 — 조정하려면 §0-1을 바꿔야 한다. |
| R-QC-03 근-빈 컨테이너 | 5장/19개 | C5-4(8개)는 색각 예시 박스라 의도된 것. 나머지는 확인 필요. |
| R-QC-17 실습 완결성 | 4/16장 | C4-11 + PART6 3장. C4-11은 아래 §4-1 참고. |

---

## 4. 보고 (R-OWN-02 — 고치지 않고 알린다)

### 4-1. 하네스 설계 실습(C4-11)과 실습 ⑩(A5F2)이 사실상 같은 실습이다

수정요청서는 C4-11을 «AGENTS.md 초안을 쓰게 한다»로 구체화하라고 했고 그대로 했다.
그 결과 **두 장이 같은 결과물(AGENTS.md)을 만든다**는 것이 화면에 드러났다.

- `C4-11` 하네스 설계 실습: 현황 보기 → AGENTS.md 초안 → 한 군데 고치기 → 저장
- `A5F2` 실습 AGENTS.md 6~10줄: 규칙 고르기 → 6~10줄 적기 → 적용 확인

합치거나 역할을 가르는 편이 낫다고 보지만, **수정요청서에 없는 판단이라 실행하지 않았다.**

### 4-2. 36p 우측 시각자료를 제거했다

"없으니만 못한 자료 — 계속 가져갈 거면 대폭 업그레이드"에 대해, 표 전환으로 폭이 필요해
**제거를 택했다**(§5-6의 두 선택지 중 하나). 되살리려면 알려 달라.

### 4-3. 실습 ⑥(C3-6)의 프롬프트는 다음 장(C3-N3)에 있다

§0-9는 실습 슬라이드 안에 넣으라고 했으나, 실습 ⑥은 §2-2 신설 장이 바로 뒤에서
«prd.md를 어떻게 넘기나 + 예시 프롬프트»를 통째로 다루므로 중복을 피해 그쪽에 뒀다.

---

## 5. 미결 (수정요청서 §5 그대로)

- **78~97p 미검토** — txt 다음 갱신분 대기.
- **§5-2** 신설 C3-N3의 폴더 이미지: 지금은 HTML 트리로 구현. 이미지가 필요하면 Codex 지시서에 추가.
- **§5-3 / §5-4** 25p 제목·35p 용어는 이번에 확정 문안으로 넣었다. 다르게 원하면 알려 달라.
  - 25p: "기능을 다 만들어도 불편이 그대로일 수 있습니다"
  - 35p: 재시도 분기 → "한 번 실패한 뒤" · 중복 실행 → "버튼을 두 번 눌렀을 때"
- **§5-5** 55p 분할은 하지 않았다(한 장에 들어갔다).


---

## 7. 수정요청 3차 실행 (2026-08-01, 같은 세션)

- **결과**: 98장 → **102장**. 프롬프트 전용 4장 신설.
- **실측(102장 전수)**: 바닥선 초과 0 · 이탈 0 · 좌우 이탈 0 · 세로 무너짐 0 · **이미지 슬롯 테두리·배경 잔존 0**
- 게이트: `verify_deck` FAIL 0 · `verify_deck_quality` FAIL 0 · unittest 77 OK

### 처리 목록

| 요청 | 대상 | 처리 |
| --- | --- | --- |
| 이미지 자리 «파란 구간 표시» 제거 | 전역 | `.img-slot-*`·`.asset-slot`의 테두리·배경·«이미지 예정» 자리표시를 전부 제거. 좌표·크기·본문 인셋은 유지 |
| 9p 레이아웃·줄글 개편 | `C1-2` | 가운데 이미지를 우측으로 보내고 좌측을 «정의 카드 2장 → 예시 칩 → 조립 한 줄» 3단으로 재구성 |
| 2p 파란 박스 | `S02` | 제목 아래 파란 `accent-bar` 삭제 |
| 38p 확인 | `M3` | 리드문의 `—` 구조 해소 |
| 40p 3단 박스 | `C1-13` | 카드 간격 24 → 30px · 패딩 확대 · 파일명을 pill로 승격해 위계 강화 |
| 42p 우측 그림 | `C3-2` | 430×488로 확대(약 1.4배) · 슬라이드 높이 중앙 정렬 · 좌측을 1열로 바꿔 간격 확보 |
| 47p 뒤 프롬프트 신설 | `C3-P5` | prd.md 5항목 요청 프롬프트 전용 장 |
| 48p 우측 재설계 | `C5-11` | 좌우 분할을 «트리 위 · 기준 아래» 세로 구성으로. «바뀝니다 / 안 바뀝니다» 대비 3칸 |
| 56p 박스 여백·용어 | `C4-12` | 중첩 3층 패딩 확대 · "바깥 층" → "하네스" |
| 58p 정렬 | `C4-4` | «끝난 뒤에 확인한다»를 오른쪽 끝으로 밀어 좌우 대칭 |
| 64p 우측 재제작 | `C4-8` | 표 → 세션 타임라인으로 되돌리고 «열림» 시점을 민트 블록으로 표시 · 높이 중앙 정렬 |
| 66p 화살표 제거 | `C4-9` | 하단 4단계 사이 꺾쇠 제거 |
| 68p 캡션 삭제 | `C4-10` | 차트 아래 «크기보다 방향만 봅니다» 캡션 삭제 |
| 70p 초록 박스 삭제 | `C4-11` | 하단 요약 박스 삭제 |
| 72p 뒤 프롬프트 신설 | `A5F2-P` | AGENTS.md 초안 요청 프롬프트 전용 장 |
| 85p 기호 제거 | `C5-9` | 제목 «design.md + 화면에 ≥2 적용» → «design.md 쓰고 화면에 두 가지 적용하기» |
| 87p 표현 정리 | `C5-13` | «이번 수업은 이 방식»·«이번 범위 밖» 삭제. 구분 방법만 남기고 쉬운 설명 추가 |
| 88p 파란 박스 | `C5-15` | 하단 결론 콜아웃 삭제 |
| 89·90p 뒤 프롬프트 신설 | `C5-16P`·`C5-17P` | 실습 ⑩·⑪ 프롬프트 전용 장 |
| PART6 인포그래픽 | `C6-N1`~`C6-N4` | 카드 4개 나열 → 관계가 보이는 인포그래픽(깔때기 / 기능→결과 + 범위 3칸 + 흐름 / 문서→에이전트→파일→첫작동 파이프라인 / 앞뒤 감싸기 + 규칙·권한 2층) |

### 함께 정리한 것

- **실습 장 안의 중복 터미널 제거**: 전용 프롬프트 장이 생긴 4개 실습(`C3-5`·`A5F2`·`C5-16`·`C5-17`)에서 본문 안 축소 터미널을 뺐다.
  중복 해소이자, `verify_deck`의 «같은 구도 4연속 금지» FAIL을 실제로 없앤 조치다(설명으로 넘기지 않았다).

### 남은 WARN

- **R-QC-08 시각자료 비율 48.2%** (임계 55%). 42.3% → 48.2%로 올랐지만 **여전히 미달이다.**


---

## 8. 시각자료 비율 해소 + PART6 인포그래픽화 (2026-08-01)

### R-QC-08 : 48.2% → **76.8% (PASS)**

두 갈래로 처리했다.

1. **미태그 도해 22곳에 `viz-*` 부여** — 깔때기(A1F1)·중첩 3층(C4-12)·계단(C4-15)·사분면(C4-7)·
   빙산(C5-1)·색각 대비(C5-4)·주소창 비교(C5-13) 등은 **이미 화면에 그려져 있던 도해**인데
   검출기가 `viz-*`/`img`/`canvas`만 세어 0으로 집계하고 있었다. 없던 것을 만든 게 아니라
   **있는 것을 세게 한 것**이다.
2. **PART6 참조 밴드를 실제 시각자료로 교체**(아래).

### PART6 참조 밴드 : 줄글 → 인포그래픽·차트

| 파트 | 이전(줄글) | 지금 |
| --- | --- | --- |
| PART1 | 한 줄 정의 틀·페인포인트 조건·실패 3줄 | **빈칸 문장 틀**(점선 슬롯) + **2축 사분면**(자주 걸리나 × 장면이 좁나, 적중 칸 민트) + **코랄 신호등 3점** |
| PART2 | MVP 정의·완료 기준·예외 3줄 | MVP 한 줄 + **완료 기준 미니 화면 목업**(입력칸·버튼·새 줄 하이라이트) + **예외 3칩**(코랄) |
| PART3 | 5항목·마크다운·넘기는 법 3줄 | **prd.md 문서 목업**(체크박스 5항목) + **마크다운 기호 그리드**(코드칩 6쌍) + **폴더 트리** |
| PART4 | 6칸·증거 4단계·갈림길 3줄 | **하네스 6배지**(파랑=전·초록=후) + **증거 강도 막대차트**(100/72/44/16%) + **갈림길 도해**(조건 → 목적지) |

각 파트의 핵심 정보는 그대로 유지했다 — 표현만 «읽는 것»에서 «보는 것»으로 바꿨다.

### 실측 (102장 전수)

바닥선 초과 0 · 이탈 0 · 좌우 이탈 0 · 세로 무너짐 0 · 이미지 슬롯 박스 잔존 0

| 게이트 | 결과 |
| --- | --- |
| `verify_deck --parts 6` | FAIL 0 · PASS 51 |
| `verify_deck_quality` | FAIL 0 · **WARN 5 → 4** |
| R-QC-08 시각자료 | **76.8% PASS** |
| R-QC-01 / R-QC-02 밀도 | **둘 다 PASS** (0/55장) |
| unittest | 77 OK |

### 남은 WARN 4

- R-QC-03 근-빈 컨테이너 5장 · R-QC-15 하단 콜아웃 21.4% · R-QC-16 박스 16.1% · R-QC-17 실습 완결성 4장.
- R-QC-16의 9장 중 **5장이 이번 범위 밖**(S02B·C1-12·C4-15·C5-4·A3F3).


---

## 9. 4차 수정 (2026-08-01)

**102장 → 107장.** 실습⑧ 프롬프트 전용 신설 1장 + PART6 파트당 2장 분리로 4장.

| 요청 | 처리 |
| --- | --- |
| PART6 파트당 2장 | `C6-N1~N4`(개념도) + `C6-S1~S4`(참조 시트). 마크다운 기호 그리드 제외 |
| 92p·74p 프롬프트 끊김 | 아래 «조용한 잘림» 참고 |
| 87p 프롬프트 신설 장 | `C5-9P` 신설. `.pf-band` 안에 중첩돼 y=841까지 넘치던 것도 해소 |
| 85p 프롬프트 → 86p 좌측 밑 | `C5-8` → `A4R1` 이동 |
| 20p 파란 박스 | 하단 결론 콜아웃 삭제 |
| 7p 풍부하게 | 리드 + 지난주/이번 주 각 3항목 |
| 3p 제목 | "여섯 교시가 첫 MVP로 이어집니다" → "오늘 하루로 첫 MVP까지 갑니다" |

### 조용한 잘림 — 측정기의 사각지대였다

`.terminal-dark`는 `overflow:hidden`인데 `.pw-term`을 `flex:1`로 늘려 둬서
**내용이 넘칠 때 마지막 줄들이 잘린 채 렌더**됐다. 실측 — C3-P5 **90px** ·
A5F2-P **56px** · C5-16P **56px**.

기존 측정기가 이걸 놓친 이유: **leaf 요소만 검사**했는데 `.terminal-copy`는
`<strong>` 자식이 있어 leaf가 아니었다. `overflow:hidden` 조상 밖으로 나간
텍스트를 잡는 검사를 추가했고, 지금은 **잘림 0**이다.

### 게이트 (107장 전수)

바닥선 초과 0 · 이탈 0 · 좌우 0 · **잘림 0** ·
`verify_deck` FAIL 0 · `verify_deck_quality` FAIL 0 · R-QC-08 **78.7% PASS** · unittest 77 OK

---

## 10. 하네스 시각 보강 — 배포본 직접 수정 (2026-08-03)

### 이번 배포본에 반영

- PART 5의 `C4-1` 뒤에 두 장을 추가했다 (`C4-N0`, `C4-H1`).
  - **같은 모델인데 왜 결과가 다를까**: 같은 AI라도 도구·권한·테스트·결과 회수 구조가 있느냐에 따라 작업 결과가 달라진다는 비교 이미지.
  - **힘센 말도 마구 없이는 목적지로 가지 못합니다**: 말=모델의 판단력, 마구·고삐=규칙·권한, 수레·도구=실행, 확인 문=테스트·피드백으로 읽는 기억용 비유 이미지.
- 이어지는 세 장의 역할을 다시 맞췄다.
  - `하네스란 무엇인가`: 판단 → 실행 연결 → 결과 회수의 짧은 정의.
  - `AI는 어떻게 실제 일을 하나`: 모델 판단 → 하네스 실행 → 작업 환경 변화 → 결과 확인의 순환.
  - `하네스, 사실 별거 아닙니다`: 범위·순서·증거·멈춤의 네 약속으로 회복.
- 두 새 장과 제목·내용이 바뀐 세 장의 발표자 노트를 보강하고, 이후 모든 노트 페이지 번호를 2만큼 이동했다.
- 화면 검토 후 80쪽은 한 장 그림을 좌·우 작업대 PNG로 분리해 확대 배치했다. 81쪽은 말·수레 그림을 왼쪽 하단으로 옮겨 페이지 번호와 분리하고, 오른쪽 상단에 재갈·굴레 클로즈업을 추가했다.

### 후속 수정 이력 (배포본 직접 수정 라운드, 2026-08-03)

- 2026-08-03 후속 수정: 배포본 80쪽의 기존 분할·잘림 이미지를 제거하고, 독립 생성한 두 작업대 PNG로 교체했다. `L-cs-central-contrast`의 좌우 동등 카드와 중앙 `≠` 커넥터를 사용하며, 각 설명문은 이미지 오른쪽 칼럼에 둔다. 82쪽 제목과 정의도 `하네스 엔지니어링이란?` 및 환경·도구·규칙·확인 구조를 설계하는 일로 명시했다.
- 2026-08-03 후속 수정: 80·81쪽의 축약 헤더를 공통 `VIBECODING · PART 5/6` 헤더 템플릿으로 교체했다. 81쪽은 우측 마구 이미지·설명·매핑 박스·결론 박스를 함께 위로 올리고, 아래 두 박스 사이에 명시적 여백을 뒀다. 도입 문구도 `하네스라는 이름의 비유`로 바꿔 80쪽의 정의와 연결한다.
- 2026-08-03 후속 수정: 80쪽은 `에이전트가 다음 행동을 고른다 → 하네스 엔지니어링이 그 선택을 실행·확인·다음 판단으로 잇는다`의 흐름으로 다시 썼다. 가운데 `≠`와 카드 배경을 제거하고, 오른쪽 장면을 `하네스 엔지니어링이 설계한 작업대`로 명시했다. 81쪽 본문 폭도 1152px 안전영역으로 고정했다.
- 2026-08-03 후속 수정: 81쪽의 말 비유 대응표를 단순 나열 대신 `말 → 마구·고삐 → 수레·도구 → 확인 문`의 인과 흐름으로 먼저 읽도록 보강했다. 이어서 판단하는 AI·규칙과 권한·실행 수단·테스트와 피드백의 대응을 제시해, 마구가 AI의 힘을 제어하는 장치가 아니라 목적지까지 이어 주는 연결 구조임을 드러냈다.
- 2026-08-03 후속 수정: 81쪽 우측 재갈·고삐 이미지와 설명을 12px 올렸다. 83쪽은 82쪽의 정의를 실제 반복 작업에 대입하는 `하네스가 실제 일을 잇는 방식`으로 바꾸고, 84·85쪽을 반복 안의 약속 → 프롬프트보다 바깥의 구조로 연결했다. 85쪽의 겹치던 보조 문구를 제거하고 전체를 위로 올렸다. 실습 ⑩과 예시 프롬프트는 88쪽 직후인 89·90쪽으로 옮겨, 규칙·자료·반복 절차를 작성하는 흐름으로 고쳤다.
- 2026-08-03 후속 수정: 83쪽의 페이지 번호 참조를 `방금 정의한`으로 바꿔 순서 이동에도 자연스럽게 유지되게 했다. 81쪽 우측 마구 이미지는 높이를 줄여 캡션·대응표와 분리했고, 85쪽의 프롬프트·컨텍스트·하네스 관계 문장은 두 줄로 분리했다.
- 2026-08-03 후속 수정: 83쪽에 도구를 `AI가 실제로 일을 하게 하는 손발`로 정의하고, 파일 수정·서버 실행·화면과 테스트 결과 확인이라는 입문자용 예시를 추가했다.

당시 이 라운드는 사용자 요청대로 `강의덱_배포.html`과 `강의덱_발표자노트.html`만 직접 수정했고, 정본 반영은 다음 라운드로 미뤄 뒀다 — 그 반영이 §11이다.

## 11. 정본 동기화 완료 (2026-08-04)

**배포본(`강의덱_배포.html`)이 승인된 최신 기준이라는 원칙으로, §10의 배포본 직접 수정분을 정본 shard로 이식했다.** 반대 방향(정본 → 배포본 덮어쓰기)이 아니다. 착수 전 `tmp/_sync_backup_2026-08-04/`에 배포본·발표자노트·part-05.html·shell.html·강의덱.html·deck.contract.json·이미지-에셋.json을 백업했다.

### 이식 내용

- **신설 2장**: `C4-N0`(80쪽·같은 모델도 환경에 따라 결과가 다른 대비) · `C4-H1`(81쪽·말 비유), 둘 다 `C4-1` 뒤.
- **재작성 4장**: `C4-2`(82쪽·`hn-reframe` 정의 카드 추가, 옛 hn-top/hn-formula는 마크업 보존 + `display:none`으로 화면에서만 내림) · `C4-16`(83쪽·3부품 모델 전면 교체, 클래스 `w2-harness`→`c4-tools-visual`) · `C4-N4`(84쪽 연결 문구) · `C4-12`(85쪽 연결 문구).
- **문구만 수정 1장**: `C4-N2`(88쪽 callout tip).
- **이동 2장**: `C4-11`(실습⑩) · `A5F2-P`(예시 프롬프트) — `C4-15` 뒤에서 `C4-N2` 뒤(89·90쪽)로.
- **이미지 3종 5개 배선**(전부 기존 승인 자산 재사용, 신규 생성 없음): `s80_unprepared/prepared-workbench-v3-tight` · `s81_horse-bridle-bit-v1` · `s81_harness-horse-metaphor-v1` · `s83-agent-tools-workflow-v1`.
- 갱신 파일: `강의덱.초안/part-05.html`(CSS+HTML) · `강의덱.html`(재조립, 110→112장) · `자료/이미지-에셋.json`(ASSET-122~126 등록, 스키마 검증 통과) · `deck.contract.json`(slides 112, `layout_families`에 `c4-tools-visual`→`toolflow` 등재). `shell.html`은 변경 불필요로 확인됐다 — 새 CSS 전부가 `[data-slide="..."]` 스코프 규칙이라 part-05 자체 style 블록(WAVE2 보정과 같은 자리)에 들어간다.
- 검증: 재조립 결과와 배포본을 슬라이드 단위로 정규화 비교(`src=`·주석·`data-image-role`류 부가 속성 제외)해 **9장 전부 내용 일치** 확인. `verify_deck --parts 6` FAIL 0(WARN 1은 착수 전과 동일한 기존 항목). `verify_deck_quality` FAIL 0. unittest 79 OK. 브라우저 렌더 감사(`audit_all.js`, 1400×900) 결과는 `sessions/_verify/2주차/deck-audit.json`.

### 발표본 재주입 (같은 날, 뒤늦게)

**위 동기화에서 `강의덱_발표.html`을 빠뜨렸다.** 정본·배포본·노트를 다 맞춰 놓고 발표본만 2026-08-03 파일(110장)로 남겨 두어, 사용자 지적으로 드러났다. 같은 날 재주입해 해소했다.

- 명령: `inject_presenter.py 강의덱_배포.html --notes 강의덱_발표자노트.html --deck-id vibecoding-week-2 --output 강의덱_발표.html --meta sessions/_verify/2주차/강의덱_발표.meta.json --force`
- 입력은 규정대로 **완성된 배포본**이다(`build_release.py`를 쓰지 않는다 — 조각에서 재생성하면 동결을 깬다). **deckId `vibecoding-week-2`는 그대로 유지**했다 — 바꾸면 강사가 적어 둔 메모가 유실된다.
- 결과: 45.5MB · **112장** · 주입 게이트 8개 전건 통과 · **노트 112/112 매핑** · 메모 항목 **128개**(joke 19 · hint 79 · demo 30) · 빈 메모 16장(원래부터 멘트가 없는 장 — 정상).
- 검증: `verify_presenter_deck` **FAIL 0 · WARN 0 · PASS 19**(슬라이드 불변 지문·slideId 시퀀스·innerHTML 해시·`<img>` 37개 전부 배포본과 동일). 브라우저 실측 — 슬라이드 112장 · 새 슬라이드 5종 존재 · **콘솔 에러 0** · **청중 화면에 멘트·아이콘 누출 0**. 새 슬라이드 멘트 7종은 사이드카 diff가 아니라 **원문 grep**으로 실재 확인했다(사이드카는 개수만 담아 문구 변경을 못 잡는다).
- `verify_deck`의 `.s-body 22px 미만` FAIL 1건은 **배포본·발표본의 정상 기준선**이다(kit CSS가 인라인되며 kit 원본 선언이 세션 CSS 검사 범위로 들어온다 · 실제 렌더는 shell override로 22px 이상). `R-META-01` 31건도 base64 인코딩 문자열 오탐이라 실제 화면 잔존은 0이다 — 둘 다 회귀가 아니다.
- **수동 체크리스트 8항목(10단계 §5)은 남아 있다.** 공식 실행 경로가 `file://` 더블클릭인데 인앱 브라우저는 그 보안 제약을 재현하지 못하므로, 발표자 창 분리·팝업 차단 동작·PDF 저장은 **실제 Windows Chrome·Edge에서 사람이 확인**해야 한다.

### ⚠️ 발견된 결함 — 다음 라운드로 남김 (배포본에도 있음, 이번 동기화가 만든 게 아님)

**`C4-H1`(81쪽)에 이미지-텍스트 겹침 2건.** 렌더 감사 실측(1280×720 덱 좌표 기준):
- `.s-title`(제목, y 150~196.4)과 `.hm-detail`의 재갈·굴레 클로즈업 이미지(y 126~326, x 786~1216)가 세로 46.4px·가로 430px 겹친다.
- `.hm-lead`(리드 문장, y 214.4~279.7, x 64~684)와 `.hm-figure`의 말·수레 히어로 이미지(y 238~606, x 42~802)가 세로 41.7px·가로 620px(리드 전체 폭) 겹친다.

두 이미지가 절대 위치(`position:absolute`)로 배치되면서 자연스러운 흐름의 제목·리드 문장과 겹치게 됐다. `verify_deck` 같은 정적 검사는 못 잡고 브라우저 렌더 감사(`audit_render.js`)만 잡는다 — 이번에 처음 측정됐다(구 정본은 이 슬라이드가 없어 비교 기준이 없었다). 절대 금지 규칙 R-IMG-04a(겹침 0) 위반이라 원칙적으로는 고쳐야 하지만, **배포본에도 동일하게 있는 결함이라 이번 동기화 범위에서 임의로 고치지 않았다** — 고치면 정본과 배포본이 다시 어긋난다. 다음 라운드에서 `.hm-detail`·`.hm-figure`의 위치·크기를 조정해 겹침을 없애고, 같은 수정을 배포본에도 반영해야 한다.

### 남은 선재 결함(이번 범위 밖, §「2026-08-03 2주차 발표본 산출」·MEMORY와 동일)

바닥선 초과 3장(C2-7·C5-11·C4-N2로 알려졌던 항목) — 이번 렌더 감사(`deadZones`/`thinnest` 지표)로는 바닥선 자체 초과는 잡히지 않았고 겹침 2건만 신규 확인됐다. `verify_notes`의 MISMATCH 2건(표지·THANK YOU, `[부분집합]`)은 노트 검사기의 구조적 특성이며 착수 전과 동일하다.
