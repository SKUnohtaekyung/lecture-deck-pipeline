# 규칙 정본 모순·게이트 구멍 3건 수정 계획

> **상태: 실행 완료 (2026-08-23).** §2 결정은 **(B) 취향 유지 + 강제 해제**로 확정됐고 Phase 1~4 전량 반영했다.
> 검증: 회귀 231 OK(신설 6) · 러너 1·2·3주차 exit 0 · WARN 래칫 전부 불변 · 정적 verify 4종 exit 0 · pre-commit exit 0.
> 결과 요약은 `.agents/agent-memory/create-slides/MEMORY.md` 「규칙 정본 모순·게이트 구멍 3건」에 등재했다.
> 남은 것은 커밋 승인뿐(스테이징 완료).
> 출처: `courses/바이브코딩/sessions/2주차/강의덱_실험_검토보고.html` §6 「덱이 아니라 저장소 쪽 발견 3건」
> 성격: 강의 산출물은 한 글자도 건드리지 않는다. 규칙 문서 2건 + 검사 스크립트 2건만 손댄다.

## 1. 현물 확인 결과 (2026-08-23 재대조 — 보고서 주장을 코드·문서에서 직접 확인)

| # | 주장 | 확인 | 실제 위치 |
|---|---|---|---|
| ① | 제목 규칙이 두 문서에서 정반대 | **사실** | `courses/바이브코딩/profile.md:92` ↔ `courses/바이브코딩/슬라이드지침.md:81` · `kit/guide/문체-원칙.md:71-72` |
| ② | 배포처가 낡음(Netlify) | **사실 · 범위가 보고서보다 넓다** | `슬라이드지침.md:45,51` + `커리큘럼_기준안.md:7,112,459,483,497,543` |
| ③ | 게이트 2종이 클래스명 의존으로 뚫림 | **사실 · 방향이 서로 다르다** | `verify_deck.py:1702-1725`(미탐) · `verify_deck_quality.py:403,441`(오탐) |

### ① 보고서가 놓친 구분 — 「슬라이드 제목」과 「스텝 제목」은 다르다

`profile.md`에는 제목 관련 항목이 **두 군데** 있고 성격이 정반대다.

- `profile.md:70` `덱_스텝제목_서술형_상한: 0` — **살아 있다.** 슬라이드 «안의» 단계 제목이 대상이고
  `verify_deck_quality.py` R-COPY-03이 집행한다. 근거는 사용자 원문(2주차 수정요청 §0-3). **건드리지 않는다.**
- `profile.md:92` §4 표 「확정된 선호 — 제목」 — 대상은 **슬라이드 제목(`.s-title`·h2)**.
  이쪽 검사는 2026-07-31에 폐기됐다(`문체-원칙.md:71-72` — 합격 덱 제목의 61%가 서술형).

→ 손댈 후보는 **`profile.md:92` 한 행뿐**이다. §3의 키를 함께 지우면 살아 있는 게이트가 죽는다.

### ② 실제 산출물은 이미 Sites로 갔다

`courses/바이브코딩/sessions/3주차/강의덱.html`에 "Netlify" **0회**, "Sites" 0회(한국어 표기).
3주차 초안은 GitHub + ChatGPT Sites로 설계돼 있다(`3주차_초안.md:41,45,49`).
2주차 계획서가 명시적으로 정정한다 — 「기존 자료에 남아 있는 Netlify 배포안은 이전 참고안이다」(`:41`).
→ **산출물이 아니라 규칙 문서만 낡았다.** 수정은 동결 위반이 아니다.

### ③ 두 게이트는 고장 방향이 반대다

- **`verify_deck.py:1715` — 미탐(false negative).** 22px 하한 루프가
  `if '.s-body' in _sel or '.s-lead' in _sel:` 로 셀렉터 «문자열»을 요구한다.
  본문 클래스 이름이 다르면 미달이 몇 건이든 **검사 대상 0 → PASS**.
  브라우저 감사(`audit_typography.js`)는 `getComputedStyle`로 재므로 정확했다.
- **`verify_deck_quality.py:403` — 오탐(false positive).** R-QC-15는 「하단 결론 콜아웃 **보유율 > 0.12면 플래그**」다(`:218`).
  `_FOOT_RE = /(?:^|\s)(?:[\w-]*-foot)(?:\s|$)/` 가 페이지번호 띠 `.s-foot`까지 콜아웃으로 세어 **보유율을 부풀린다**.
  kit 덱은 `.s-pageno`를 쓰기 때문에 지금까지 드러나지 않았다.

**공통 구조**: 둘 다 「클래스 이름을 바꾸면 게이트가 조용해진다」. 그리고 `AGENTS.md` 「무엇이 기계로 강제되는가」의
관측→집행 승격 규율은 **오탐률만** 재게 하고 **미탐은 규율에 없다.**

## 2. 선행 결정 (사용자) — 1건

**`profile.md:92` 「슬라이드 제목은 정식 용어로 둔다」를 어떻게 할 것인가.**

이 절은 스스로 *「보편 원리 아님 — 이 과목 사용자의 선택」*이라 밝히고 있고, 2026-07-28에 사용자가 확정한 취향이다.
2026-07-31에 폐기된 것은 **게이트**이지 **취향**이 아니다. 그래서 자동으로 지울 수 없다.

- **(A) 취향도 철회** — 행을 삭제한다. 서술형 제목을 자유롭게 쓴다.
- **(B) 취향은 유지, 강제는 안 함** *(추천)* — 행을 남기되 「2026-07-31 게이트 폐기. 강제하지 않는 선호이며
  실측상 합격 덱의 61%가 서술형 제목이다」를 단서로 붙인다. 문서 간 모순은 사라지고 사용자 선택은 보존된다.

## 3. 실행 단계

### Phase 1 — 규칙 문서 2건 (코드 변경 없음)
1. **①** 결정에 따라 `profile.md:92` 수정. `profile.md:70`·`:82`는 **불변**.
2. **②** `슬라이드지침.md:45,51`의 Netlify → ChatGPT Sites로 정정하고 정정 일자·근거(2주차 계획서 `:41`)를 남긴다.
3. **②** `커리큘럼_기준안.md` 6곳은 **본문을 고치지 않고 §머리에 정정 주석 1개**를 단다
   — 이 문서는 승인된 커리큘럼 기준이자 역사 기록이라 소급 재작성하지 않는다.
   *(대안: 전량 치환. 사용자가 원하면 전환.)*

### Phase 2 — 게이트 2건 (코드)
4. **③-오탐**: `verify_deck_quality.py`의 `has_conclusion_callout()`에서 페이지번호 띠 계열을 제외한다.
   `_FOOT_RE`를 좁히거나(`-footnote` 류만 인정) 제외 목록에 `s-foot`·`s-pageno`를 넣는다.
   기존 실측 기준선(1주차 0.083 · 2주차 0.340)이 **변하지 않는지** 재측정으로 확인 — kit 덱은 `.s-pageno`를 쓰므로 불변 예상.
5. **③-미탐**: `verify_deck.py`의 세션 CSS 22px 린트를 «클래스명 의존»에서 떼어낸다. 두 갈래:
   - (i) 정적 검사를 **내리고** 브라우저 감사(`audit_typography.js`)를 이 항목의 정본으로 삼는다 — 검토보고 권고.
   - (ii) 남기되 셀렉터 화이트리스트가 아니라 «슬라이드 본문 영역 안의 모든 텍스트 규칙»으로 판정 대상을 넓힌다.
   → **선행 확인**: `run_deck_checks.py`가 `audit_typography.js`를 항상 도는지. 항상 돌면 (i), 아니면 (ii).
   `verify_declared_vs_enforced.py`의 `.s-body` 결합(`:242-255`)은 **kit CSS ↔ 토큰 치트시트** 쌍이라 이 린트와 무관 —
   실행 시 재확인한다.
6. **미탐 규율 신설 판단**: `AGENTS.md` 「무엇이 기계로 강제되는가」에 「집행 승격 시 오탐률과 **미탐률**을 함께 잰다」를
   추가할지 결정. 규칙 추가는 값이 아니라 절차이므로 문서 1줄 + 회귀 없음.

### Phase 3 — 검증 (종료코드 확인)
```
python -m unittest tests.test_deck_pipeline tests.test_image_pipeline tests.test_quality_gates tests.test_deck_contract tests.test_declared_vs_enforced tests.test_rule_pointers tests.test_typography_rules tests.test_analyze_agent_usage tests.test_audit_context_budget tests.test_hook_guards
python scripts/run_deck_checks.py 1주차
python scripts/run_deck_checks.py 2주차
python scripts/run_deck_checks.py 3주차
python scripts/verify_declared_vs_enforced.py
python scripts/verify_subject_isolation.py
```
- 회귀는 **10모듈 전량**(지도 §6). 일부만 돌리면 나머지 게이트가 검증되지 않는다.
- Phase 2-4는 판정 로직 변경이므로 **회귀 케이스를 먼저 추가**한다(`tests/test_quality_gates.py` — `.s-foot`이 콜아웃으로 세이지 않음).
- Phase 2-5도 마찬가지로 「본문 클래스 이름이 달라도 22px 미달을 잡는다」 케이스를 추가한다 — **이 구멍에 회귀가 0개다.**

### Phase 4 — 등재·정리
7. `.agents/agent-memory/create-slides/MEMORY.md` `## 미해결` 갱신 — 8-19 오후 작업(실험덱·검토보고)이 아직 미등재다.
8. `git status --short`로 요청 범위 밖 변경 0 확인. `tmp/` 잔여물 확인.
9. 커밋(사용자 승인 후).

## 4. 위험과 경계

- **동결 위반 없음.** 1·2·3주차 산출물(덱·배포·발표·노트·자료)은 읽지도 고치지도 않는다. 손대는 것은 규칙 문서와 검사 스크립트뿐.
- **게이트 수정은 기준선을 흔들 수 있다.** Phase 2-4·2-5는 판정이 바뀌므로 세 주차 러너를 **전부** 다시 돌려
  수치 변동이 0인지, 변했다면 그것이 「진짜 결함이 이제 보이는 것」인지 확인한다. 변동을 계약 baseline에 조용히 묻지 않는다.
- **문서 값을 낮춰 모순을 없애지 않는다**(AGENTS.md). ①은 «규칙을 깎는» 수정이 아니라 «폐기된 규칙의 잔해를 치우는» 수정이다.
