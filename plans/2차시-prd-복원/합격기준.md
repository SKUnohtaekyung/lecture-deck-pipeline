# 합격 기준 — 2차시 prd.md 복원 · 순서 개편

> 계획 `PLAN.md`(v3) · 루프 최대 **2회** · 2026-09-11
> 이 표의 AC가 **전부 PASS**여야 완료다. 한 항목이라도 FAIL이면 다음 라운드에서 **그 AC만** 고친다. 2회 뒤에도 FAIL이 남으면 멈추고 잔여를 보고한다(완료로 적지 않는다).

## 0. 기준선 (편집 전 실측 · 커밋 `40d9ec0` 트리 · `tmp/w2base/`)

| 게이트 | 기준선 |
|---|---|
| `verify_deck.py --parts 8` | FAIL 0 · WARN 1 · PASS 67 · 미판정 1 |
| `verify_deck_quality.py --strict` | FAIL 4 · PASS 7(상시 FAIL 규칙 R-META-01·R-QC-08·R-QC-18은 FAIL 아님) |
| `verify_notes.py` | 판정 75 · 불일치 0 |
| `run_deck_checks.py 2주차` | PASS · 구조 WARN 1/1 · 품질 WARN 4/4 · 렌더 WARN 100/100 |
| 계약 `known_violations` | render_lap 2(C4-H1) · render_wideEmpty 2(C4-12·C4-N2) · render_stretchX 2(C4-12·C4-13) |

**기준선을 올려서 통과시키지 않는다** — `warn_baseline`·`known_violations` 수치 상향은 AC-G5 위반이다(AGENTS.md 「문서 쪽 값을 낮추지 마라」의 같은 원리).

## A. 내용 — 이번 대화의 사용자 결정과 일치하는가

| AC | 기준 | 판정 수단 |
|---|---|---|
| A1 | PART 4의 25~29쪽이 **PRD와 prd.md(약어 풀이·표준 없음·누가/누구에게/형식/언제 비교) → prd.md 정의 → 빈칸은 추측(5항목) → 실제 prd.md 예시(집중 타이머 5제목↔대상·핵심·흐름·제외·완료 짝) → 넘기는 법** 순서로 연속한다 | 순서 스크립트 + 검토자 |
| A2 | `C3-N4`(AGENTS.md란)가 `C4-5`·`C4-N5`·`C4-11`보다 **앞**, `C4-N2`가 `C4-11` **바로 앞**. PART 6에 AGENTS.md 정의·작성법 장이 **없다** | 순서 스크립트 |
| A3 | `N-08`이 `N-09` 바로 뒤·`N-10` 앞. PART 4에 NEWS_AGENT.md 장이 없다. `M1` 5단계에 «NEWS_AGENT.md 준비» 없음 | 순서 스크립트 + grep |
| A4 | 실습 ①(`C5-9`) 1단계가 **design.md 만들기**이고 방법 A(직접)·방법 B(화면 캡처 → AI Chat → Google Antigravity로 `design.md` 저장)가 둘 다 보인다. `N-24` 프롬프트가 **목적·입력·출력·검증 기준**으로 분해돼 있고 캡처 개인정보 확인이 있다 | 검토자 |
| A5 | `C4-16`·`N-16`·`C5-15`가 덱·노트·계약·초안에 없다. `C4-2`에 Loop 한 줄이 있고 `C4-N4` «방금 본 반복»이 성립한다 | grep + 검토자 |
| A6 | `N-07`(Graph)이 `N-19` 바로 뒤(PART 6 마지막)이고 첫 문장이 반복 실행과 이어진다. 헤더 PART 6 | 순서 스크립트 + 검토자 |
| A7 | «prd.md를 먼저 읽» 계열 문구가 PART 4(C3-N3·A3F3) **밖에 0건**. `C4-N4`·`C4-4`·`C4-N2`·`A5F2-P`는 design.md | grep |
| A8 | `실습 ⑤`·`검증 기록` **0건**. 실습 번호 ①~④가 순서대로 한 번씩. `M4`·`N-09`·`N-21` 문구가 삭제와 정합 | grep + 검토자 |
| A9 | 정리 PART(`C6-S4`·`C4-13`)에 `Skill` **0건**. 부록 `C4-8`은 유지 | grep |
| A10 | `C3-N3` 프롬프트가 기존 화면을 **고치는** 요청이다. must_keep(방법 B) 유지 | 검토자 |
| A11 | `C1-13` AGENTS.md 칸 «PART 5에서 직접 만듭니다» · `N-18` «작업 실행» · `C4-3` 리드 수정 | grep |
| A12 | 3·4·6쪽(S02B·S03·M1)의 구조는 유지(M1 5단계 산출물 문구만 변경) | diff |
| A13 | 이동한 모든 장의 헤더 `s-team` PART 라벨이 새 PART와 일치 | 스크립트 |
| A14 | 신설 장 문구가 입문자 기준(한 장에 한 결론 · 정식 용어 + 쉬운 풀이 · 긴 줄글 없음 — `슬라이드지침.md` 화면 원칙)을 지킨다 | 검토자 |

## B. 정합 — 산출물끼리 어긋나지 않는가

| AC | 기준 | 판정 수단 |
|---|---|---|
| B1 | 덱 `data-slide` 순서 = `PLAN.md` §1 표 = 계약 `sequences` = 초안 표 = 매핑 §② = 노트 `pn-slide` 순서 · 총 77 · divider 8 | 순서 스크립트 |
| B2 | 계약 `must_keep` 16항(N-16 삭제)이 전부 해당 장 화면에 있다 · `dark_terminal_slides`에 N-24 포함·삭제 장 없음 | 스크립트 |
| B3 | 매트릭스 CSV·집필노트 처분표에 이번 처분(삭제 3·이동 5·부활 C3-2·흡수 C3-N1)과 사유가 있다 · 신설 N-23·N-24가 매핑 §④와 집필노트 추적표(판단 5분류)에 있다 | 검토자 |
| B4 | 시간표 합: 앞 50분 ≤ 50.0 · 뒤 50분 ≤ 38.0(참가자 실작업 제외) · 실습 ① 참가자 실작업 3.0분 반영 | 검토자 |
| B5 | `ChatGPT`·`Codex` 신규 유입 0건(덱·노트) | grep |
| B6 | 노트 안 쪽 번호 참조가 새 번호와 맞다 | 검토자 |

## C. 화면 — 레이아웃이 깨지거나 겹치지 않는가 (**필수**)

| AC | 기준 | 판정 수단 |
|---|---|---|
| C1 | 브라우저 렌더 감사(1280×720 이상)에서 **요소 겹침 · 슬라이드(720) 이탈 · 바닥선(666) 초과 · 단어 중간 줄바꿈 · 빈 asset-slot**이 신설·이동·수정 장에서 **0**, 덱 전체에서 `known_violations` 등재분(C4-H1 lap 2 등) 외 **0** | `audit_all.js` → `deck-audit.json` |
| C2 | 신설·이동·수정 장 전부를 **스크린샷으로 직접 보고** 겹침·잘림·어긋남이 없음을 확인 | 메인 육안(스크린샷) |
| C3 | 폰트 하한·16px 추락: 신설·수정 장에서 **신규 폰트하한 위반 0** · 렌더 WARN 총계 ≤ 100 | `deck-audit.json` · 러너 래칫 |
| C4 | CSS 로드 정상(`document.styleSheets` ≥ 기준 · 한 번에 보이는 슬라이드 1장) | 브라우저 JS |

## D. 시각 자료 — SVG·도식을 기능적으로 쓰는가

| AC | 기준 | 판정 수단 |
|---|---|---|
| D1 | 신설 `N-23`·`N-24`와 개편 `C5-9`에 **정보를 설명하는 SVG 또는 도식**이 있고 `viz-*` 표식이 붙어 있다(`C3-2`는 기존 설명 이미지 s37 + 비교 구조) | grep + 검토자 |
| D2 | raw `#hex`·그라데이션 0(R-COLOR-01) · 민트·코랄 fill 위 흰 글자 0 · 장식용 SVG 아님 | verify_deck + 검토자 |
| D3 | 새 구도 클래스가 계약 `layout_families`에 등재 · 직전 장과 같은 패밀리 연속 없음(R-LAYOUT-02) | verify_deck |

## G. 게이트 — 종료코드

| AC | 기준 |
|---|---|
| G1 | `assemble_deck.py` exit 0 |
| G2 | `verify_deck.py --parts 8` **FAIL 0** · WARN ≤ 1 |
| G3 | `verify_deck_quality.py --strict` FAIL ≤ 4이고 **새로 FAIL로 올라온 규칙 ID 0**(R-QC-08 시각자료 비율 PASS 유지) |
| G4 | `verify_notes.py` 불일치 0 · 미판정 0 |
| G5 | `run_deck_checks.py 2주차` **exit 0** · 기준선 수치 상향 없음 |
| G6 | `git status --short`에 이 계획의 allowlist 밖 변경 0 · 저장소 밖 임시 파일 0 |

## 루프 규칙

1. **라운드 = 편집 → 조립·정적(G) → 렌더·스크린샷(C) → 검토자 AC 판정(A·B·D).**
2. 라운드 1 판정에서 FAIL AC가 0이면 종료. 있으면 라운드 2에서 **FAIL AC만** 해당 워커가 고치고, 메인이 G·C를 다시 돌리고, 검토자가 **FAIL였던 AC + 수정이 닿은 장의 회귀**를 다시 판정한다.
3. 라운드 2 뒤 남은 FAIL은 고치지 않고 `PROGRESS.md`·`MEMORY.md` `## 미해결`에 잔여로 적고 보고한다.
4. 검토자 판정은 `PASS / FAIL / 판정불가(사유)`로만 받는다. **판정불가는 PASS가 아니다.**
