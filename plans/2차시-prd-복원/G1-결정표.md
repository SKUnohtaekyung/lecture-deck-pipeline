# G1 결정표 — 신설·개편 4장 (W2)

> 2026-09-11 · 작성 W2(`ac3-writer`) · 근거 `PLAN.md` §2-2·§2-4(C5-9)·§6 D5·D7·D8 · `합격기준.md` A1·A4·A14·D1~D3
> SKILL ③-B: 제출은 무조건, 사용자 전권 위임(PLAN §0)이라 **제출 후 대기 없이 조립**한다.
> 패밀리 이름은 `verify_deck.py family_signature()` 판정값이다(직전·직후 칸도 같은 기준).

## 결정표

| 쪽 · ID | 정보 모양 · 형태 | 구도 패밀리(레이아웃) | 시각 element | 강조 배정 | 마무리 | 이미지 판정 | 핵심 한마디 | 직전 / 직후 패밀리 |
|---|---|---|---|---|---|---|---|---|
| 25 · `C3-2` | contrast · 병렬(기준 행 순서는 바꿔도 뜻 유지) | `prdvs`(신규 `n2-prdvs`) — `L-cs-central-contrast` 변형: 정의 한 줄 → 좌 PRD / 가운데 기준 척추 / 우 prd.md 5행 짝정렬 | 설명 이미지 s37을 두 열 머리 사이에 두어 `≠` 커넥터 역할(두꺼운 더미=PRD · 다섯 칸 파일=prd.md) | 결론: 제목 + 우측 머리 «AI가 읽는 기획서»(`hl-mint-mark`) / 근거: «만들기 전에 먼저»(`underline`) / 핵심어: «다섯 항목»(`text`) | 없음(표 끝에서 멈춤) | `IMAGE_EXPLANATORY` · `support` · **재사용**(ASSET-116, 신규 생성 0) | PRD는 팀이, prd.md는 AI가 읽는 기획서다 | `owner`(C1-13) / `full`(C3-N2) |
| 28 · `N-23` | mapping · 병렬(파일 줄 순서는 실제 파일 순서 고정) | `prdfile`(신규 `n2-prdfile`) — `L-td-mapping-rows`: 좌 파일 뷰 · 가운데 괄호 · 우 항목 라벨 | `E-code` 계열 파일 뷰(`viz-code`, `## 제목` 줄 강조 배경) + SVG 괄호·리더선 5개 | 결론: 제목 «다섯 항목»(`hl-mint-mark`) | 없음 | `NO_IMAGE` — 실제 파일 자체가 시각물 | 제목 다섯 개가 곧 다섯 항목이다 | `other`(C3-3) / `terminal`(C3-N3) |
| 35 · `N-24` | flow · 산문(단계 순서를 바꾸면 깨짐) | `mdreq`(신규 `n2-mdreq`) — `L-dc-flow-canvas` 상단 4단 흐름 + 하단 좌 터미널 / 우 노트 세로 3칸 | `D-flow-h`(HTML 정거장 4 + SVG 화살표 3, 사람/도구 색 구분) + 검정 터미널 프롬프트(`viz-prompt`) | 근거: 정거장 3 «한 줄은 직접»(`hl-mint-underline`) / 결론: 터미널 «한 줄은 제가 고친 뒤»(`strong` — 검정 터미널 안은 흰 글자 계약이라 민트 강조를 쓰지 않음) | 없음 | `NO_IMAGE` — 흐름 도식·프롬프트로 충분 | 캡처 → AI Chat 정리 → 내가 한 줄 수정 → Google Antigravity로 저장 | `practice`(C5-9) / `terminal`(C5-9P) ※ 아래 각주 1 |
| 34 · `C5-9` | flow · 산문 | `practice`(기존 `w2-practice` 유지 — 실습 계열 판정 R-QC-17 보존) — `L-vf-numbered-stack` 변형: 1단계에서 A/B로 갈라졌다가 2단계로 합류 | `D-flow-v`(work-step 3 + SVG 분기·합류·하강 연결선 3) | 결론: «화면을 직접 열어»(`hl-mint-mark`) / 근거: «판단이 담긴 문장으로»(`underline`) | 없음(3단계 안에 «문서만 만들고 끝 금지» 코랄 칩) | `NO_IMAGE` — 절차 도식이 본체 | design.md를 만들고, 한 줄은 내가 고치고, 화면 2곳에 실제로 적용한다 | `roadmap`(M4) / `terminal`(N-24) |

## 각주

1. **N-24 ↔ C5-9P 패밀리** — 계획(§3 계약 `dark_terminal_slides`(+N-24))과 `ac_check` B2가 N-24의 검정 터미널을 요구한다. `family_signature()`는 `terminal-dark`를 계약 등재 클래스보다 먼저 판정하므로 N-24·C5-9P는 둘 다 `terminal`로 읽힌다 → 연속 2(검사 한도 ≤2, FAIL 아님). 화면 골격은 다르게 잡았다: C5-9P = 전폭 터미널 + 하단 노트 3열 / N-24 = 상단 흐름 도식이 주인공 + 좁은 터미널 + 우측 노트 세로. `data-series` 우회는 쓰지 않는다(연작이 아님).
2. **동점 선택 근거** — 네 장 모두 프로필 §4 «차트·과정도·비교도가 주인공 → 글과 여백 → 카드» 순으로 도식을 먼저 골랐다. C3-2만 도식 대신 표형 짝정렬인 이유: 기준 5개 × 2문서라 위치 대응(행)이 가장 곧게 읽힌다(R-BOX-01 «표로 도망가지 마라»는 mapping 한정 — 여기는 contrast).
3. **R-TERM-01** — N-24 프롬프트는 같은 성격의 C5-9P·N-11·A5F2-P와 같은 `terminal-dark` 변형.
4. **실측 소재** — N-23 파일 줄은 `courses/바이브코딩/sessions/2주차/실습예제/mvp/timer_집중타이머/prd.md`(19줄)에서 `## 제목` 5개와 핵심 줄을 줄 순서 그대로 옮기고, 줄 폭 때문에 줄인 곳은 뜻을 보존했다(1절 «끝나면 알 수 있게», 3절 «집중 시간(분)·(줄어듦)», 4절 «일시정지·재개 → 일시정지 · 기록 통계 → 통계»). 빈 줄은 뺐다.
5. **도구명** — 원본 «에이전트(Codex)» → «Coding Agent», AI Chat·Google Antigravity로 치환(`ChatGPT`·`Codex` 신규 유입 0).

## 사전 점검 7항 (02-슬라이드맵)

| 항 | 결과 |
|---|---|
| 1 정보 모양 12종 기재 | ✓ 표 2열 · 마크업 `data-shape` 동일 값 |
| 2 역인덱스 열람(layouts·charts) | ✓ 두 파일 모두 열람 |
| 3 레이아웃·element 분리 | ✓ 표 3·4열 |
| 4 형태 판별 | ✓ 표 2열 |
| 5 강조 의미 배정 | ✓ 표 5열(장당 ≤3) |
| 6 직전과 구도·마무리 다름 | ✓ 표 9열 · 각주 1 |
| 7 이미지 4상태 | ✓ 표 7열 · `자료/이미지-에셋.json` 기록 |
