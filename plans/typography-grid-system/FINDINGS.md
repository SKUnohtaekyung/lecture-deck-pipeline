# 실행 중 발견 — 계획 범위 밖 / 사용자 판정 대기

> 이 계획(`PLAN.md`)을 실행하며 실측으로 드러났으나 **계획의 어느 단계도 처리 범위로 잡지 않은** 항목이다.
> 여기 적어 두지 않으면 다음 세션이 같은 것을 다시 발견하는 비용을 치른다.
> **이 계획의 단계에서 임의로 처리하지 않는다.**

| # | 발견 | 단계 | 성격 | 제안 |
|---|---|---|---|---|
| **A** | **자간 광학 보정 누락이 `.pd-title` 1종이 아니라 15종 / 36요소**다(2주차 6클래스 17요소 · 1주차 11클래스 19요소). 특히 `.cc-neq`@52px 5건은 `.cm-title`(52px, `-2.08px` 보정됨)과 같은 크기인데 보정만 없어 한 덱 안에서 52px 제목의 트래킹이 6배 갈린다 | S0 | 시스템 결함 | S6은 계획대로 `.pd-title`(간지 12장)만 처리. 나머지 14종은 **S8 소급 판정과 함께 사용자 결정** — 렌더가 바뀌는 슬라이드가 12장 → 30장 이상이 되어 「S6은 소규모」라는 안전 구조의 전제가 달라진다 |
| **B** | **`kit/styles/deck.css`의 `.concept-card`가 R-TYPE-01 위계 밖이다** — 제목 `h3` 18px · 설명 `p` **14.5px**. 「박스 제목 26 / 설명 20」과 어긋나고 14.5px는 캡션 티어(14px)와 같다. 마무리 슬라이드는 모든 덱이 쓴다 | S1 | 규칙↔코드 불일치 | **S5 판정: 손대지 않는다 · 사용자 결정으로 넘긴다.** `.concept-recap`은 §8-3이 「고정 구조 슬라이드 — 별도 좌표계」로 분류한 장이고, kit 공유라 값을 바꾸면 **1·2주차 마무리 장이 동시에 흔들린다**(S6의 「소규모」 전제 밖). 다만 14.5px 설명문은 캡션 티어와 같아 **학습자가 읽는 요약을 캡션 크기로 내보내는 상태**다 — 3주차 착수 전 처분 필요 |
| **C** | **`.cover-presenter`가 바닥선을 674px로 넘는다**(kit `deck.css` `bottom:46`). 템플릿 사본 렌더에서도 재현 — 즉 **새 주차는 첫 조립부터 바닥선 초과 1건을 안고 시작한다.** 1주차 17건 중 이 유형이 포함돼 있다 | S1 | 시스템 결함 | 계획 S6의 결함 교정 목록(`patterns.css` `bottom:44` 4종)과 **같은 유형인데 목록에 없다.** S6 착수 시 함께 처리할지 사용자 판정 |
| **D** | **`kit/guide/토큰-치트시트.md` R-TYPE-01이 지목하는 `.wk-title`·`.wk-desc`가 kit에 존재하지 않는다**(각 0건). 계획서 F14가 꼽은 `.proc-analogy`·`.proc-example`·`elements-code`와 **같은 유형이며 F14 목록에 빠져 있다.** `.desc`도 kit CSS에 정의가 없다(2주차 shell과 이제 템플릿에만 있다) | S1 | 문서가 없는 대상 지목 | **F14(S2)에 3건 추가.** 판정 방식은 F14와 동일 — [클래스를 만든다 / 지목을 지운다 / 실재 클래스로 바꾼다] |
| **D2** | **R-TYPE-01이 `.cd-out`을 「박스 안 설명문(20px/500)」 행에 넣었는데 실제로는 결론 라벨이다.** kit의 실제 마크업은 항상 `<b class="cd-out">`(= `.cd-leaf`의 직계 자식, **같은 노드**)이라 「박스 제목 `.cd-leaf > b`」 규칙과 같은 노드를 겨냥하고, 타입 셀렉터 한 칸 차이로 제목 규칙이 이겨 **설명 규칙이 죽는다.** kit 자신의 `.cd-out`은 19px/800 블루(결론 강조)다. 2주차 shell에도 같은 모순이 있다 | S1 | 규칙 오분류 | S1 템플릿에서는 두 셀렉터를 **뺐다**(모순을 이식하지 않기 위해). **S5의 R-TYPE-01 개정 때 `.cd-out`을 설명문 행에서 빼고 결론 라벨로 재분류** |
| **E** | **`sessions/README.md:54`가 폐기된 `/콘텐츠` 스킬을 파이프라인에 적고 있다**(`/리서치 → /콘텐츠 → /create-slides → /검토`). 실제 순서는 `/리서치 → /create-slides → /검토`다(2026-08-03 폐기). 같은 줄의 「집필노트 → `/콘텐츠`가 산출」도 낡았다 | S1 | 낡은 서술 | **S5에서 정정**(`sessions/README.md`는 S1·S5 공동 소유) |
| **F** | **`audit_render.js`의 「0」이 눈먼 범위 안의 0이다** — 2주차 바닥선 초과 0·겹침 0·이탈 0이 나왔으나, 검출기가 `<img>`/`<svg>`/`<figure>`를 바닥선 검사에서 빼고 겹침을 2단계까지만 본다(T10). MEMORY의 「2주차 선재 결함 바닥선 초과 3장」은 **현행 검출 범위 기준으로는** 해소 | S0 | 검출기 사각지대 | 계획대로 **S3에서 보수 후 재측정**해야 진짜 0인지 판정된다 |
| **H** | **1·2주차 발표자 노트가 둘 다 `verify_notes` FAIL 상태다.** 2주차 MISMATCH 2건(OK 108/110 · pn-no 94·95, 110 THANK YOU는 WARN) · 1주차도 FAIL. 내 변경 이전부터 있던 상태이며(2주차 노트 최종 수정 커밋 `2f4fd2c`), **아무도 이 검사를 돌리지 않아 드러나지 않았다** — 신설 러너가 처음 표면화시켰다 | S3 | 선재 결함 | 노트 수정은 이번 범위 밖. 3주차 착수 전 처분 필요. F4(노트 절차)의 직접 근거이기도 하다 |
| **K** | **차트 상단에 「지배값끼리 충돌」이 2건 있다** — 241px(8개) ↔ 236px(4개) 5px 차이 · 241px(8개) ↔ **242px(3개) 1px 차이.** 둘 다 「지배값」이라 근-미스 검사에 원리적으로 안 걸리던 것을, 독립 검증 지적으로 신설한 `dominantClashes`가 처음 잡았다. **같은 자리에 규격이 둘 있는 상태**이며 §6-3이 없애려는 문제의 가장 심한 형태다 | S3 | 실측 발견 | S4/S5의 세로 기준점 정의 때 「241로 통일할지 236·241·242 중 무엇을 남길지」 판정 필요 |
| **I** | **`check_title_survival.py`(F2)는 1주차에 적용되지 않는다.** 이 검사의 열쇠는 초안 행 번호 → 슬라이드 ID 대응(`1-2`→`C1-2`)인데, 1주차 초안의 번호는 `02`·`05`처럼 단순 일련번호라 대응이 성립하지 않는다. 그래서 1주차는 전 행이 텍스트 폴백으로 떨어지고, 이 검사는 그 경우 판정을 하지 않는다(대응미상) | S3 | 도구 한계 | 1주차를 검사 대상으로 삼으려면 번호 규약을 맞추거나 별칭 표가 필요하다. **없는 상태에서 「1주차 통과」라고 읽지 마라** |
| **J** | **R-QC-17(실습 완결성)은 결함형이 아니라 비율형처럼 움직인다.** 계획서 F12는 이를 「임계 0 결함형 4종」에 넣었으나 실측은 1주차 50.0%(1/2) · 2주차 **43.8%(7/16)**다. 승격하면 9건을 강등 등재해야 하고 그러면 게이트가 사실상 무력해진다 | S3 | 계획 전제 수정 | **승격 전에 「단계 요소 2개 미만」 판정의 타당도를 먼저 재검증**해야 한다. 상세: `F12-GATE-TRIAGE.md` |
| **G** | **`verify_deck_quality.py:106`의 R-QC-08 헤더 주석이 폐기된 수치를 서술한다** — "2주차 실측 0.208(11/53)"인데 현재 실측은 **0.646(42/65)**. 태깅 전 값이다 | S0 | 낡은 서술 | **F10(S2)에 1건 추가** |
