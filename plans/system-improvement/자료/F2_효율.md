# F2 — 재작업 루프 효율 분석 (전체 211커밋)

근거 명령: `git log --oneline | wc -l`(211) · `git -c core.quotepath=false log --name-only --format="" | sort | uniq -c | sort -rn`(파일별 수정 횟수) · `git log --oneline | grep -iE '^[a-f0-9]+ (fix|재수정|재생성|revert)'`(fix류) · `git log --oneline | grep -iE '배포본|발표본|재생성|build\('`(대형 산출물)

## ① 같은 파일이 수정된 커밋 횟수 — 상위 10

| 순위 | 파일 | 커밋수 |
|---|---|---|
| 1 | `.agents/agent-memory/create-slides/MEMORY.md` | 33 |
| 2 | `.agents/agent-memory/vibecoding-deck/MEMORY.md`(구 스킬명 시절 메모리 — create-slides로 개명 전) | 32 |
| 3 | `AGENTS.md` | 30 |
| 4 | `courses/바이브코딩/sessions/2주차/강의덱.html` | 29 |
| 5 | `scripts/verify_deck.py` | 27 |
| 6 | `SKILL.md`(루트) | 23 |
| 7 | `courses/바이브코딩/sessions/2주차/강의덱.초안/part-05.html` | 21 |
| 8 | `courses/바이브코딩/sessions/2주차/강의덱.초안/shell.html` | 20 |
| 8 | `courses/바이브코딩/sessions/2주차/강의덱.초안/part-03.html` | 20 |
| 8 | `courses/바이브코딩/sessions/2주차/deck.contract.json` | 20 |

1·2위 합쳐 65커밋(211건의 31%)이 「메모리 파일 갱신」에 쓰였다 — 규칙 정본을 계속 고쳐 나간 신호. 4·7·8위가 전부 2주차 산출물이라, 2주차가 단일 세션 중 가장 반복 수정이 몰린 대상이다(근거: `courses/바이브코딩/sessions/2주차/` 하위 파일이 상위 10 중 5개).

## ② fix/재수정 성격 커밋 비율과 연쇄

- 전체 211커밋 중 커밋 메시지가 `fix(...)`로 시작하는 것 35건 = **16.6%**.
- 대상별 분해: 35건 중 **14건(40%)이 `2주차` 관련**(`2f4fd2c`·`4572b90`·`c774fd6`·`a4cc0bf`·`ee06bc8`·`4d1b19a`·`3e5b08e`·`13b3215`·`bb2963f`·`052c222`·`41d107c`·`2366bf0`·`3f6694f`·`7b98241`) — 같은 대상(2주차 덱)에 대한 fix가 짧은 구간에 몰린 연쇄로 관측된다.
- 그중 눈에 띄는 근접 연쇄(같은 세션 내 연속 fix, `git log --oneline` 순서 기준):
  - `4572b90`(55p 빙산 잘림·치우침 수리) → `c774fd6`(58p 붕괴 수리 + 78p 재작성) — 렌더 사고가 슬라이드를 바꿔가며 연속 발생.
  - `4d1b19a`(박스 stretch 차단 — "문서만 고치고 안 고쳤던 집행기") — 커밋 메시지 자체가 이전에 규칙만 쓰고 집행 코드를 안 고쳐 재발했음을 명시(재작업 원인이 "선언↔집행 불일치"임을 자인).
  - `13b3215`(지표 정의 7종 중 2종 폐기) → `bb2963f`(실측이 게이트 임계 전제를 뒤집었다) — 검증 기준 자체를 만들고 나서 실측으로 뒤집는 재작업.
- `3주차`는 `71bb1b8`·`b4ffe3c` 2건만 fix로, 지정수정 성격 커밋(`33046cb`·`c7bca5a`·`a2de193`, feat 접두)까지 포함하면 재작업형 커밋이 총 5건 — 2주차보다 fix 밀도는 낮다(2주차 14건 vs 3주차 2건, feat 성격 지정수정까지 넓혀도 5건).

## ③ 대형 산출물(배포본·발표본) 재생성 커밋

`배포본|발표본|재생성|build(` 매치 10건 중 실제 "완제품 재생성"에 해당하는 커밋:

| 커밋 | 내용 |
|---|---|
| `a4fd011` | build(3주차): 배포본·발표본을 79장 현행에 맞춰 재생성 |
| `a2de193` | feat(3주차): … 배포본·발표본 동반 갱신(81장) |
| `c7bca5a` | feat(3주차): 단일 HTML·발표본 산출(4종째 갱신) |
| `dc598f4` | sync(2주차): 배포본 → 정본·발표본 동기화(110→112장) |
| `bd41359` | feat(presenter): 발표 런타임 kit 정본화 + 1주차 발표본 산출 |
| `8c07f2f` | build(deck): 1주차 오프라인 번들 재빌드 |
| `4fa8409` | 배포본 폰트 자립 + 단일파일 강제 게이트 신설 |

3주차만 보면 산출물 동반 재생성이 `a4fd011`·`a2de193`·`c7bca5a` 3회 발생했고, 슬라이드 수 변화(74→77→79→81장)가 있을 때마다 강의덱·배포본·발표본 3종을 동시에 재빌드한 패턴이다(각 커밋 메시지에 "산출물 3종을 같은 시점으로 맞췄다"가 반복 등장 — `a2de193`·`71bb1b8` 공통 문구).

## 핵심 수치 요약

- 전체 211커밋 중 fix류 35건(16.6%), 그중 40%가 2주차 한 세션에 집중.
- 파일별 수정 최다는 메모리·정본 문서(33·32·30회) — 코드/산출물보다 "규칙을 반복해서 고쳐 쓰는" 비용이 크다.
- 3주차는 지정수정 5커밋 동안 산출물 3종 동반 재생성이 3회 일어나 슬라이드 수가 늘 때마다 배포본·발표본을 다시 만든 고정비용이 있었다(74→81장, +7장에 재생성 3회).
