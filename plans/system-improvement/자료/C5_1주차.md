# C5 — 2주차 지적의 1주차 존재 스캔 (대조군)

대상: `courses/바이브코딩/sessions/1주차/강의덱.html`(275 data-slide 참조, `<section data-slide=` 75개), `강의덱.초안/part-01~05.html`, `강의덱_발표자노트.html`(81줄, 실질 내용 거의 없음 — 확인 대상 외).
방법: 파일 내 Grep 카운트/샘플로 현물 존재 여부만 판정. `개정이력`·조립_보고 등 이력 문서는 열지 않음(블라인드 규율 준수).

| 클러스터ID | 명제(2주차) | 판정 | 근거 파일:줄 | 비고 |
|---|---|---|---|---|
| B-605/606 | "~합니다" 종결 반복 패턴 금지 대상 | 존재함 | 강의덱.html 전역, `합니다.` 60회(정규식 카운트); part-01:20 part-02:5 part-03:10 part-04:7 part-05:8 | 1주차도 "~합니다." 종결이 지배적 문체. 2주차에서 지적된 것과 같은 반복 패턴이 1주차부터 있었음. |
| B-605(슬래시) | "N / M" 슬래시 이어붙이기 구조 금지 | 존재안함 | 강의덱.html에서 `X / Y` 패턴 74건 검색 → 표본 확인 결과 전부 URL 경로(`image/svg`)·파일 경로(`자료/images`)·폰트명 등 오탐, 문장연결형 슬래시 실질 0건 | 슬래시 문장구조 자체는 1주차에 없음. |
| B-618 | PART1~6 간지 캐치프레이즈(pd-sub) 삭제 대상 | 존재함 | 강의덱.html:331,632,728,1046,1198,1218 (`class="pd-sub"` 총 6건, PART 디바이더마다 1개씩); part-01~05.html 각 1건씩 분산 | 정확히 6개 PART 전부에 비유형 서브카피가 붙어있음. 2주차에서 "6개 전부 삭제" 대상으로 지목된 것과 동일 클래스·동일 패턴이 1주차 원본에 이미 존재. |
| B-626 | em dash 이어붙이기 구조 전역 금지 | 존재함 | 강의덱.html 전역 `—` 18회; part-01:7 part-03:3 part-04:1 part-05:1 | em dash 사용 자체가 1주차부터 있었음(단, "문장—부연" 구조인지는 개별 문맥 미검토 — 존재 자체만 확인). |
| B-627 | "우리 수업에서는" 류 표현 전역 삭제 대상 | 존재안함 | 강의덱.html + part-01~05.html 전체 `우리 수업에서는` 검색 0건 | 1주차엔 해당 표현 없음. |
| B-631 | 빈 이미지 슬롯 placeholder 스타일 처리 대상 | 대상소실 | 강의덱.html `data-image-state="ready"` 32건, `pending`/`placeholder` 상태 0건 | 1주차는 모든 이미지 슬롯이 실제 스크린샷으로 채워진 상태(ready)뿐 — 빈 슬롯이라는 대상 자체가 없음. |
| B-632 | 제목·번호 위 장식 아이콘 전역 금지 대상 | 존재안함 | 강의덱.html에서 `<h1-3>` 앞 이모지 장식 검색 → 매치는 SVG 브랜드 로고(1138,1147,1156 등)뿐, 이모지 장식 아이콘 없음 | 장식 이모지 아이콘 패턴 미발견. |
| B-635/654/655 | 실습 "완성"파란박스·"완료 확인"·"자주 막히는 지점"·"막히면" 블록 삭제 대상 | 존재안함 | "완료 확인"/"자주 막히는"/"막히면" 강의덱.html+part-01~05.html 전체 검색 0건. "완성"이라는 단어 자체는 9건(465,509,834,983,1225,1228행 등) 있으나 전부 본문 문장 내 서술("완성합니다","완성한 요리")이며 별도 파란 박스 컴포넌트 형태 아님 | 2주차가 지적한 전용 컴포넌트(완료확인/막히면 박스)는 1주차 구조에 없음 — 실습 슬라이드 UI 패턴 자체가 다름. |
| B-667 | 전 슬라이드 강조(밑줄·형광펜) 남용 재판정 대상 | 존재함 | `class="hl"` 4건 + `hl-mint-text` 55건 + `hl-blue-text/hl-coral-text` 19건, 강의덱.html 전역 총 78건의 색상 강조 스팬 | 슬라이드당 다건의 강조 스팬이 반복 사용됨(예: 465,509,983,1225행). "슬라이드당 가장 중요한 것 하나만" 기준과 비교하면 유사한 남용 소지가 1주차부터 있었음. |
| B-644 | raw hex 색상(토큰 외) 금지 하드게이트 | 존재안함 | `<style>` 블록 밖 hex 패턴 검색 → 매치 2건은 `&#9312;`/`&#9313;`(원문자 HTML 엔티티) 오탐, 실제 raw hex 없음 | 1주차는 색상을 토큰(`var(--mint)` 등)으로 일관 사용, raw hex 위반 없음. |

## 근거 채집 명령 요약
- `grep -o "합니다\." | wc -l`, `grep -o "—" | wc -l`, `grep -c "pd-sub"`, `grep -n "완료 확인\|자주 막히는\|막히면"`, `grep -o 'data-image-state="[^"]*"' | sort | uniq -c`, `grep -o 'class="hl"'/'hl-mint-text'/'hl-blue-text\|hl-coral-text'`, `awk '/<style/,/<\/style>/{next}{print}' | grep -oE '#[0-9a-fA-F]{3,6}'`
